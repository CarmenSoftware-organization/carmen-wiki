# macOS metadata
.DS_Store
**/.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# OS / Python (for .specs/verify_frontmatter.py)
__pycache__/
*.pyc

# Playwright MCP artifacts (browser snapshots, console logs, screenshots)
.playwright-mcp/
/*.png

# Sync-nav script — local env and pytest artifacts
.env
scripts/.env
.pytest_cache/
.venv/

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# .claude/settings.json IS committed — it carries the shared hooks and status line.
.claude/settings.local.json

# Obsidian's per-user vault settings — personal, not part of the wiki.
.obsidian/
