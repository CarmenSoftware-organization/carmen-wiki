# Full Wiki Snapshot Re-sync — Progress Log (2026-07-15)

Branch: `docs/resync-2026-07-15`. Spec: `.specs/2026-07-15-full-wiki-snapshot-resync-design.md`.
Approach B: snapshot-verify every page against current source. Last sync: 2026-06-25 (`207d842`).

## Inventory book (source: ../carmen-inventory-frontend-react, backend-v2, bruno, e2e, micro-report/micro-data)

| Module | Pages | Status | Claims fixed | New pages | Screenshot routes flagged |
|--------|-------|--------|--------------|-----------|---------------------------|
| purchase-request | 16 | EDITED (6 pages) | Fixed doc-lifecycle wording on the module landing page (was `Draft→Submitted→Under Review→Approved/Rejected/Sent Back`, actual `enum_purchase_request_doc_status = {draft,in_progress,voided,approved,completed}` — no separate submitted/rejected states, reject converges on voided). Rewrote 03-user-flow-purchaser.md and 04-test-scenarios-purchaser.md: Purchaser is the `enum_stage_role=purchase` stage inside the PR's own approval chain (edit vendor/price/discount/tax + Auto Allocate + bulk Approve/Reject/Send-for-Review/Split, matches 304-pr-purchaser-journey.spec.ts) — the previous "Convert-to-PO workbench" (per-line vendor deviation %, convert-qty, Allocate Vendor dialog at conversion time) does not exist; real Convert-to-PO is a separate 2-step whole-PR dialog in the Purchase Order module (`po-from-pr-dialog.tsx`) grouping by vendor+delivery_date+currency (not vendor+currency). Rewrote 03-user-flow-procurement-manager.md and 04-test-scenarios-procurement-manager.md: stripped a fabricated "Vendor Allocation Rules / Stuck PR Oversight" configuration surface with no matching route/component/endpoint — PM is just the escalated `approve`-role stage using the identical Approver UI. Updated 04-test-scenarios.md personas/cross-persona table (X-PR-01, X-PR-04, X-PR-05, X-PR-07) to match. | (none) | (see purchase-order row; PO dialog screens flagged there) |
| purchase-order | 18 | EDITED (18/18 pages + landing) | Landing + 01-data-model + 02-business-rules + 03-user-flow: added the 3rd creation path (`po_type = pricelist`, 4-step from-price-list wizard) missing from every enumeration; fixed the PR-to-PO grouping key everywhere from `(vendor, currency)` to `(vendor, delivery_date, currency)` (`buildPoGroupKey`); relocated "Convert-to-PO" from a fictitious PR-module "workbench" to the real 2-step whole-PR dialog inside the PO module (`po-from-pr-dialog.tsx`), matching the fact already established for purchase-request. Corrected send-back/reject/void semantics module-wide: `/review` (send-back) does NOT change `po_status` (only resets `workflow_current_stage`, stays `in_progress`) — was wrongly documented as `in_progress → draft`; `/reject` sets `po_status = voided` directly and terminally (only from `in_progress`, by whoever holds the current stage) — was wrongly documented as returning to `draft` or requiring the Procurement Manager; there is no separate "Void" action reachable from `draft`/`sent`/`partial` — those use Cancel/Close (both → `closed`). Removed an invented "high-value approval threshold" / pricelist-deviation-percentage routing mechanism (zero code hits for `threshold` anywhere in the backend or frontend) from `PO_AUTH_004`, `PO_XMOD_006`, and every persona/test page that referenced it. **Two persona axes were substantially rewritten as confirmed-fabricated:** 03-user-flow-procurement-manager.md + 04-test-scenarios-procurement-manager.md (stripped an entire "configurational surface" — vendor-ranking, grouping-rule, unit-conversion, pricelist-tolerance, and threshold editors, plus bulk void/close — no matching route/component, cut from ~29 to ~12 test scenarios); 03-user-flow-audit-config.md + 04-test-scenarios-audit-config.md (stripped a "Procurement Activity Queries" audit workspace and a PO-specific "Configuration workspace" with numbering/RBAC/PR-to-PO-rule/delegation editors — same fabrication pattern the purchase-request pass had flagged as "deferred," now confirmed absent via repo-wide search rather than deferred again). **A third axis — Finance — was found to not exist as a distinct persona at all:** 03-user-flow-finance.md + 04-test-scenarios-finance.md rewritten as correction pages, and three-way-match (PO ↔ GRN ↔ invoice) / AP-posting references corrected across 01a, 02-business-rules (`PO_POST_008`/`PO_POST_009`/`PO_XMOD_007`), 03-user-flow-vendor.md, 03-user-flow-receiver.md, 04-test-scenarios-vendor.md, and 04-test-scenarios-receiver.md — zero hits anywhere in frontend/backend for `three-way`, `vendor_invoice`, `tb_invoice`; the previously-cited e2e spec `403-po-finance-ap-match.spec.ts` does not exist on disk. (Reviewer follow-up: 5 residual unqualified instances survived the first commit adjacent to fixed text — 02-business-rules frontmatter description + §5 intro + §7 backend bullet, landing-page §5/§7 bullets, 01a Overview, the receiver mermaid `Finance three-way match → AP liability` edge, and a stale `(vendor, currency)` grouping bullet in 01-data-model §3/§5 — all swept in the follow-up commit, EN+TH, with a clean grep afterward: every remaining "three-way" hit sits inside negation/correction framing.) The one real e2e fixture (`fc@blueledgers.com`) is documented elsewhere as the Procurement Manager approver, not a separate Finance role. Segregation-of-duties (`PO_AUTH_010`, buyer ≠ GRN poster) also has no matching code and is now marked unconfirmed everywhere it appeared. `PO_VAL_004` / `01-data-model.md` §4 fixed to list all 3 `po_type` values (was missing `pricelist`). Added missing `credit-note.md` link + corrected sub-page count (17→18) on the landing page. TH mirrored in full (see TH mirror notes in the module report). — **CORRECTED IN FOLLOW-UP (2026-07-29):** the "zero code hits for `threshold`" finding above was itself wrong. The original search covered only the literal word `threshold`; the live mechanism uses different vocabulary — `tb_workflow.data.routing_rules`, evaluated by `evaluateCondition`/`findNextStep` in `workflows.navagation.service.ts` on every submit/approve, routing on `total_amount` (Σ line `total_price`, `po-workflow.mapper.ts`), `department`, or `category`, configured from the generic **Routing** tab of `/system-admin/workflow` (shared by PR/PO/SR, not PO-specific — the orchestrator's own docstring says "used across PR, PO, and SR"). Amount-threshold routing is confirmed live behavior, not fabricated: a workflow *may* define these rules (workflow-level config, not a fixed platform rule); `category` rules silently never match (no mapper emits a document-level `category` field, source comment `workflows.navagation.service.ts` ~L455-456). `PO_AUTH_004` and `PO_XMOD_006` in `02-business-rules.md` corrected accordingly (the pricelist-deviation-*percentage* routing claim in `PO_XMOD_006` remains unconfirmed — no such condition field exists, only `total_amount`/`department`/`category`); every persona/test page that had inherited the wrong "no amount threshold" framing was corrected in the same pass (`purchase-order.md`, `03-user-flow.md`, `03-user-flow-procurement-manager.md`, `03-user-flow-purchaser.md`, `03-user-flow-audit-config.md`, `04-test-scenarios-procurement-manager.md`, `04-test-scenarios-purchaser.md`), EN+TH. Delegation and segregation-of-duties (`PO_AUTH_010`) remain correctly unconfirmed — this correction only concerns amount-threshold routing. | — | `purchase-order/from-price-list` (no curated screenshot exists for the 4-step from-price-list wizard or the po-from-pr-dialog; only `index.png`/`detail.png`/`new.png` exist, which predate the 3-path creation model — flagging for Task 6) |
| good-receive-note | 13 | EDITED (13/13 sub-pages + landing) | **Mutation-timing correction (headline finding):** `GoodReceivedNoteLogic.save()` (`draft → saved`) — not `commit()` (`saved → committed`) — is the actual posting event: it creates `tb_inventory_transaction` rows, writes FIFO/average cost layers, and advances the source PO's `received_qty`/`po_status`. `commit()` only flips `doc_status` and locks the document; no further inventory/PO/GL effect found. Every page previously described commit as the posting event and save as inert — reversed module-wide (landing, 01-data-model, 02-business-rules §5, 03-user-flow*, 04-test-scenarios*). **Schema fixes:** `is_consignment`/`is_cash` booleans no longer exist on `tb_good_received_note` — replaced by `post_type enum_good_received_note_post_type` (`ap`/`consignment`/`cash`, default `ap`, no branching logic found anywhere); `signature_image_url` renamed to `signature_file_token`; added missing `post_type` enum to §4. **Fabrication removals (mirrors purchase-order's confirmed pattern):** three-way match, AP/GL journal posting, "GRN Clearing" accrual — zero hits for `journal`/`ledger`/`three-way`/`vendor_invoice`/`tb_invoice` repo-wide; `accepted_qty` field (a per-line "quality inspection" quantity distinct from `received_qty`) — zero hits anywhere in schema or code, removed from every page including a residual instance found in 01a-data-model-comments.md; batch-commit and end-of-period auto-commit — no batch endpoint or scheduled job found; Finance persona and GRN "configuration console" (lot-format editor, RBAC panel, integration endpoints) — no matching route/component, `enum_stage_role` has no `finance` member; `03-user-flow-finance.md`, `04-test-scenarios-finance.md`, `03-user-flow-audit-config.md`, `04-test-scenarios-audit-config.md` rewritten as correction pages (mirrors PO module's established pattern). **Corrected, not removed (real code, wrong claims):** void (`/void` → `voidGrnById`) has no server-side `doc_status` precondition beyond "not already voided" and does not reverse inventory/PO effects — contradicts prior "committed cannot be voided" / "elevated co-authorisation reversal" claims (frontend hides Void once `committed`, client-side only); multi-PO consolidation groups by vendor+currency only (not "+terms"); segregation-of-duties (Receiver ≠ Purchaser) has no matching `buyer_id` guard — marked unconfirmed (mirrors PO's `PO_AUTH_010`); lot-number generation is real but a fixed format (`RC{YY}{MM}{4-digit seq}`), not configurable as claimed; extra-cost allocation is stored with a distribution-mode tag but no code splits it across lines or feeds it into cost-layer creation — marked unconfirmed. TH mirrored in full across all 14 files (landing + 13 sub-pages), including 2 full-page rewrites (Finance, Audit/Config) and 1 full-page rewrite each for Receiver/Purchaser user-flow + test-scenarios. | — | None flagged — all corrections are documentation-only (wiki text vs. already-correct UI); no visible UI change was found to prompt a screenshot recapture this pass. |
| store-requisition | 16 | EDITED (16/16 sub-pages + landing) | **Lifecycle correction (headline finding):** `enum_doc_status = {draft, in_progress, completed, cancelled, voided}`; the real terminal path is `draft → in_progress → completed`, with `voided` (not `cancelled`) as the only reachable cancellation — `StoreRequisitionService.reject()` sets `doc_status = voided` unconditionally and unconditionally-available to whoever holds the current workflow stage (not an Inventory-Controller/Sysadmin-only "admin void"); `cancelled` is enum-defined but never assigned by any current service method. Every page previously described a distinct requester-withdrawal path and an all-lines-rejected auto-cancel path both landing on `cancelled` — both removed/corrected module-wide (landing, 01-data-model §4/§5, 02-business-rules §5, 03-user-flow*, 04-test-scenarios*). **Approve/Issue are the same mechanism:** `useApproveStoreRequisition` and `useIssueStoreRequisition` both call the identical `POST .../approve` endpoint (`useSrAction("approve")`), differing only by a `stage_role` tag — there is no distinct "commit" action; the document completes when that call fires at the final workflow stage (`workflow_next_stage === '-'`, `executeTransferOnComplete()`). `computeSrAction()` confirms approve+reject can mix in one `/approve` call but review cannot mix with either — fixed wherever pages described a 4-way split (approve/trim/reject/send-back) in one action. **Lot assignment is fully automatic FIFO** (`getAvailableFifoLots()`/`consumeFifoLots()` in `inventory-transaction.service.ts`) — no lot-selection UI exists anywhere in the SR frontend; removed "store-keeper manually picks lots" from every page that claimed it (data-model §5 item 12, business-rules `SR_VAL_012`/`SR_XMOD_002`, fulfiller user-flow + test-scenarios). **Fabrication removals (mirrors PR/PO/GRN's confirmed pattern):** segregation-of-duties (Requester≠Approver, Approver≠Fulfiller) — zero `requestor_id`/`approved_by_id` cross-check found, marked unconfirmed everywhere (was previously asserted as enforced, including a fictitious "SoD-relaxation threshold"); GL/journal-entry posting — zero hits for `journal`/`ledger` anywhere in this module (data-model §5 item 7, business-rules `SR_POST_007`); closed-period commit block (`SR_VAL_014`) — zero hits for `period` in the SR service/logic/DTOs; multi-tier value-threshold escalation, approval delegation, SLA time-out escalation, and a budget-module integration on the Approver page — zero hits for `threshold`/`delegat`/a budget route (test annotations in `701-sr.spec.ts` describing budget warnings and a "Delegate Approvals" button were found to be test-plan aspirational, not backend-confirmed). **Two persona axes rewritten as confirmed-fabricated correction stubs** (mirrors PO/GRN's Finance-persona precedent): `03-user-flow-receiver.md` + `04-test-scenarios-receiver.md` — no "Receiver" role, discrepancy-flag mechanism, or escalation route found anywhere (`enum_stage_role` has no `receiver` member); `03-user-flow-audit-config.md` + `04-test-scenarios-audit-config.md` — no admin-void console, GL-verification queue, RBAC/threshold config, or Auditor-exclusive route found distinct from the ordinary whole-document reject and the shared `tb_workflow`/`workflow_history`. **Schema corrections:** header `tb_store_requisition` confirmed to have both `from_location_id/code/name` AND `to_location_id/code/name` plus `sr_type enum_sr_type` and `doc_version Int` (not Decimal) — carmen/docs's `movement.source`/`movement.destination`/free-string movement type were stale; comment tables `tb_store_requisition_comment`/`tb_store_requisition_detail_comment` were missing a real `doc_version Int` field from `01a-data-model-comments.md`'s field tables — added. **Stock Replenishment finding:** the `/store-operation/stock-replenishment` screen exists but is **mock-data-driven** (`hooks/use-stock-replenishment.ts` returns a hardcoded fixture with a Thai TODO "switch to the real API when the backend is ready"); no replenishment endpoint exists in `carmen-turborepo-backend-v2` and no cron code exists in `micro-cronjobs` (zero hits for `replenish`/`min_qty`/`par_qty`) — corrected the page's opening claim and the landing page's §7 bullet, which both asserted the cron-generation as live. **Corrected, not removed (real code, wrong claims):** bulk approve/reject/review is real but scoped to selecting multiple *lines within one SR* (`sr-item-fields.tsx`), not multiple SR *documents* from the list (the list's only real bulk action is Export Selected, `TC-SR-060003`) — a fabricated "bulk-approve multiple SRs" edge case on the Approver test-scenarios page was corrected. **Residual sweeps (honest tally):** my own first sweep caught two survivors from the inherited edit (`03-user-flow-approver.md`'s At-a-Glance line + "Workflow position" mermaid still had a live `cancelled` node; `03-user-flow-fulfiller.md`'s frontmatter `description:` still said "selects lots, and commits the SR"), but **review then found 3 more residuals that first sweep missed** — (a) `03-user-flow.md`'s At-a-Glance box still said "Completed (with Cancelled / Voided branches)" and listed Receiver/Audit-Config as plain personas, plus its §1 note still said "awaiting receiver acknowledgement"; (b) `03-user-flow-requester.md` §2 step 10 (EN only) still asserted the Receiver logs receipt + discrepancy escalation; (c) `01-data-model.md`'s `completed` enum bullet still said "journal entries written" — all now fixed EN+TH in the follow-up commit, along with a stock-replenishment cross-reference tightened from "ledger writes `store_requisition` events" to the confirmed `tb_inventory_transaction` wording, and a further self-found survivor in `02-business-rules.md` §5.1 (the "Variant A auto-complete / Variants B-C explicit Complete" note plus a "3 variants produce different GL effects" phrase — both contradicted the confirmed same-endpoint completion mechanism and the unconfirmed-GL finding). Post-fix re-sweep of the whole module EN+TH for `cancelled`/`Receiver`/`journal` shows every remaining hit inside enum-enumeration or negation/correction framing. TH mirrored in full across all 17 files (landing + 16 sub-pages) via 4 parallel translation passes, including 4 full-stub rewrites (Receiver ×2, Audit/Config ×2) — each pass independently re-verified frontmatter/dateCreated and reported zero drift. — **CORRECTED IN FOLLOW-UP (2026-07-29):** the "zero hits for `threshold`" finding above (`SR_XMOD_008`, and the Approver/audit-config/test-scenario pages that inherited it) was wrong for the amount-routing half of the claim — that search covered only the literal word `threshold`. `tb_workflow.data.routing_rules` is real and generic (shared with PR/PO), routing SR stage advance on `total_amount` — computed by `sr-workflow.mapper.ts` as Σ (`approved_qty`/`requested_qty`) `× current_average_cost` per line, since SR detail rows carry no price column of their own — evaluated by `evaluateCondition`/`findNextStep` in `workflows.navagation.service.ts` on every submit/approve. Corrected in `02-business-rules.md` (`SR_XMOD_008`) and every page that had inherited the wrong "zero hits" framing: `03-user-flow-approver.md`, `03-user-flow-audit-config.md`, `04-test-scenarios.md`, `04-test-scenarios-approver.md` (APR-HP-06 rewritten from a removed scenario into a confirmed worked example; APR-EDGE-05 un-removed), `04-test-scenarios-audit-config.md`, EN+TH. The SoD-relaxation-threshold claim (a *different* concept — a claimed tenant config that relaxes Requester≠Approver/Approver≠Fulfiller enforcement below a value) remains correctly unconfirmed and untouched, as does approval delegation (`delegat` — zero hits, confirmed absent). | — | None flagged — all corrections are documentation-only; no visible UI change was found that would require a screenshot recapture (the stock-replenishment screen's mock-data status is not visually distinguishable from a real API-backed one). |
| inventory | 14 | EDITED (14/14 sub-pages + landing) | **Module-scope correction (headline):** this wiki module's real UI surface is exactly two screens — the read-only Transaction Log (`/inventory-management/transaction`) and Period End (`/inventory-management/period-end` + `/review`); the previously-documented stock-in/stock-out authoring flow, approval queues, and config/audit consoles do not exist here. **Backdating (headline):** movements are never rejected for a closed-period date — `resolveCurrentPeriod` stamps every new movement into the current open period regardless of document date (module-wide "backdated postings rejected per INV_VAL_008" claims reversed); closed-period *value* is protected separately (`isLotPeriodClosed` → CN Amount books current-period `diff_amount` instead of re-pricing). **Costing method (headline):** not per-product — `tb_business_unit.calculation_method` (`average`\|`fifo`, default `average`, platform schema) applies BU-wide via `getCalculationMethod(bu_code)`; no per-product `costing_method` column exists and no drain-guard on change. **Direct/consignment corrected:** `direct` receipts write the normal inbound layer PLUS an automatic offsetting `issue` layer (`createDirectExpenseOut`, `ISS-…` lot, excluded from the average) — not "no cost-layer row"; `consignment` has NO distinct code path (grouped with `inventory` everywhere incl. period-end/count filters) — memo-receipt/dual-COGS-AP narrative removed. **Period close corrected:** gates are `validatePeriodEnd` counts (PR/PO/SR `in_progress` mid-workflow; GRN not in `{draft, committed, voided}` — draft GRNs do NOT block; CN not in `{draft, completed, cancelled, voided}`; required-location physical counts complete — spot check is NOT a gate); close runs under `SELECT … FOR UPDATE` with re-validation (`Period already closed` on race), zeroes lots via `CLOSE-{YYMM}-{seq}` rows and re-creates them via `OPEN-…` rows in the auto-provisioned next period; `tb_period_snapshot` is written ONLY on the average-method path (`processAverageClose`) — FIFO tenants never populate it; no lock/reopen endpoint exists in this module (`locked` is set by the separate period service behind system-config/period, and `findCurrent` treats `locked` as current). **Fabrication removals (same classes as PR/PO/GRN/SR):** GL/journal postings (every `Dr/Cr` effect in `INV_POST_*`, the `tb_jv_header`/`tb_jv_detail` bridge paragraph — the JV tables exist but have zero non-schema references), threshold-based Store-Keeper→Controller→Finance approval tiers (zero `threshold` hits), Finance persona (both pages rewritten as correction pages), Audit/Config workspaces (lot-recall trace tool, snapshot-reconciliation query, config console with impact previews/dual-write — both pages rewritten as correction pages), SoD write-off rule (`INV_AUTH_010` unconfirmed), inventory-to-GL reconciliation (`INV_XMOD_008` removed). **Also corrected:** on-hand derivation is the plain `Σ in_qty − Σ out_qty` over ALL layers (`getLocationBalance`) — no snapshot anchor (close/open carry-over keeps the plain sum correct); GRN posts on **save** not commit (`INV_XMOD_001`, enum bullet); `accepted_qty` residual removed; negative-balance error is the literal `Insufficient stock. Requested: <X>, Available: <Y>` and is NOT enforced on credit-note consumption (books `diff_amount` instead); lot numbers are system-generated (`RC{YY}{MM}{seq}` etc.), no lot-entry UI; `doc_version` added to all 3 transaction-table field tables; frontend quirks documented (PC ref-type pill has no matching enum value; review card `is_complete = count > 0 && …` treats empty modules as incomplete while the backend gate passes them). E2E citations fixed: `720-stock-issue.spec.ts` navigates SR issue views (not manual stock-out); `900-period-end.spec.ts` close-workflow describes are "Feature pending". TH mirrored in full across all 15 files (5 parallel translation passes; 11 full rewrites + 4 diff-mirrors). | — | None flagged — all corrections are documentation-only; the two curated screenshots (`transaction.png`, `period-end.png`) depict screens whose current UI was verified against source without finding visible drift. |
| inventory-adjustment | 14 | EDITED (14/14 pages + landing) | **Headline finding: creation is posting, there is no draft/approval stage.** `StockInService.create()` / `StockOutService.create()` build the header object as `{ ...clientData, doc_status: enum_doc_status.completed }` — the explicit `completed` always wins over whatever the client sent — and call `executeAdjustmentIn`/`executeAdjustmentOut` in the same DB transaction. The frontend's Save (sets `doc_status:"draft"`) and Submit (sets `"completed"`) buttons both call the identical `create()` mutation, so both produce an immediately-posted, immutable document; `draft`, `in_progress`, and `cancelled` are valid `enum_doc_status` values never assigned by this module's own code. **Consequent dead-code finding:** `isReadOnly` is true whenever `doc_status` is `voided` or `completed`, so it is therefore always true for any persisted document, so the Edit button (`isView` true and `isReadOnly` false) never renders, Edit mode is never entered, and the Void button (`isEdit` true and `isReadOnly` false) is consequently unreachable too — even though `voidStockIn`/`voidStockOut` are real, working backend endpoints, reachable only via a direct API call. The list view's row-menu **Delete** action renders unconditionally (no `deleteDenied`/`deletePermission` passed to `actionColumn`) but always fails server-side with `"Cannot delete a completed Stock In — inventory has already been adjusted"` since server-side delete also requires `doc_status === draft`. **Void also soft-deletes:** `voidStockIn`/`voidStockOut` set `deleted_at` alongside `doc_status = voided` in the same update, and since list/detail both filter `deleted_at: null`, a voided document disappears entirely rather than showing a "Voided" badge (unlike GRN/PO/SR). **Cost handling differs by direction (real, verified):** Stock-In's `cost_per_unit` is user-editable (pre-filled from `useProductCostByLocationQty`, a current-average suggestion) and is what the ledger actually uses to build the new cost layer; Stock-Out's `cost_per_unit` column is hidden from the grid entirely and `create()` never writes `cost_per_unit`/`total_cost` onto `tb_stock_out_detail` (both stay `0`) — the real cost is resolved automatically by FIFO/Average consumption at write time. **No lot UI anywhere** — lot identity (`ADI-`/`ADO-` prefixes) is entirely system-generated on the inventory-transaction side; no lot-picker, expiry field, or lot-override control exists in `ia-item-fields.tsx`/`ia-item-table.tsx`. **Client-only enforcement gaps (real code, not previously documented):** reason-direction match (`tb_adjustment_type.type` vs document tree) is filtered by the picker but not re-checked server-side; location-type restriction (Inventory/Consignment only) is UI-only, `validateAndEnrichHeader` only checks the location exists; date-within-period is a Zod-only check with no backend equivalent found. **Fabrication removals (largest yet — the entire lifecycle, not just personas):** the previous version of every page in this module described a threshold-gated approval chain (Store Keeper auto-approve → Inventory Controller → Finance), GL journal postings with a `tb_adjustment_type.info.glAccount` mapping, `requiresDocument`/`requiresQualityCheck` reason flags, a segregation-of-duties rule, and a lot-selection sub-form with FIFO-override — a repo-wide search found zero `threshold` hits scoped to this module, zero `journal`/`ledger` hits, and no lot-entry UI anywhere; `03-user-flow-finance.md`/`04-test-scenarios-finance.md` and `03-user-flow-audit-config.md`/`04-test-scenarios-audit-config.md` rewritten as correction pages (mirrors the established PR/PO/GRN/SR/inventory pattern — `enum_stage_role` has no `finance` member). Store Keeper and Inventory Controller pages kept but rewritten to reflect they are the same undifferentiated screen (both gate on the single generic `inventory_management.view` permission; the CRUD-shaped `inventory_management.stock_in.*`/`.stock_out.*` permission keys exist but gate two *different* Store Operations nav entries — Stock Replenishment and Wastage Reporting — not this module's own screens). **Wastage Reporting is not a Stock-Out variant:** the sub-page wholesale mis-described it as "a `tb_stock_out` row with a wastage-flavoured `adjustment_type`" — it is actually a wholly separate, mock-data-only frontend feature (`types/wastage-reporting.ts`, own `pending`/`approved`/`rejected` status enum, own `wr_no` numbering) with a literal Thai TODO comment in `hooks/use-wastage-report.ts` ("switch to the real API when the backend is ready") and no backend model/service/controller anywhere — rewritten in full. TH mirrored in full across all 14 files (landing + 13 sub-pages). | — | None flagged — all corrections are documentation-only; the 3 curated screenshots (`index.png`, `detail.png`, `new.png`) depict screens whose current UI was verified against source without finding visible drift; `wastage-reporting.png` likewise unchanged (the mock-data status is not visually distinguishable from a real API-backed screen). |
| physical-count | 10 | EDITED (10/10 sub-pages + landing) | **Whole-module rewrite — the previous draft was written before the real UI existed and described a fabricated three-tier document/persona model with zero code backing.** Real flow (`pc-component.tsx` list → `pc-entry-component.tsx` entry → `pc-review-component.tsx` review) is a single continuous journey gated by one CRUD permission (`inventory_management.physical_count`) — no Count Lead/Counter/Approver/Auditor/Sysadmin split, no zone assignment (`tb_user_location` not referenced anywhere in this module), no tolerance threshold or recount mechanism, and no location transaction-lock (GRN/SR services have no physical-count check). `enum_physical_count_type` ("frozen vs live" in the old draft) is actually a per-**location** admin flag (`master-data/location`'s Count/Not-Count toggle) driving the period-end required-locations gate — the document-level field of the same name is never set by `create()`. **Headline finding (resolves the inventory-adjustment pass's flagged-unverified item, progress-log line 108):** `submit()` creates `tb_stock_in`/`tb_stock_out` directly at `doc_status = completed` for the variance rollup, but never imports or calls `InventoryTransactionService.executeAdjustmentIn`/`executeAdjustmentOut` — no `tb_inventory_transaction` row is written, `adjustment_type_id` stays `null` (no reason code), and no `info`/structured field links the new stock-in/out back to the source count (only a shared description string). `on_hand_qty` is not a creation-time snapshot — it stays `0` until **Submit for Review** (`reviewItems()`) recomputes it live from the current ledger balance; only **Save** stamps `counted_at`, so an all-typed-no-save path to Submit for Review can leave a line's `counted_at` null and block the final Submit (`PHC_VAL_004`) — flagged as a read-from-source edge case, not independently confirmed. A confirmed gap: no code path anywhere sets `tb_physical_count_period.status` to `counting` (the state `create()` requires) — `findCurrent()`'s auto-provision leaves it at `draft` forever absent a direct API call. `physical-count/new` and `physical-count/:id` (`pc-form.tsx`, editing a `department_id`-only field on a type commented `// Legacy type (used by old form)`) are orphaned pre-refactor routes unreachable from any real navigation. `03-user-flow-audit-config.md`/`04-test-scenarios-audit-config.md` rewritten as correction pages (mirrors the established PR/PO/GRN/SR/inventory/inventory-adjustment pattern). `02-business-rules.md` § 5.1 rewritten as a Live-Code-vs-planning-document table against the E2E repo's `tx-08-physical-stocktake.md` (a BRD-style design note describing a `FINALIZED`/GL-posted status, a location lock, and "its own transaction type — not Stock In/Out" that the live code contradicts by literally creating Stock In/Out). TH mirrored in full across all 11 files (landing + 10 sub-pages), including the 2 correction-page rewrites. | — | `physical-count/:id/entry` and `physical-count/:id/review` — curated screenshots (`entry.png`, `review.png`) already exist on disk (`assets/screenshots/inventory/physical-count/`) but are not yet embedded on the landing page (which still shows only `index.png`/`detail.png`); `detail.png`/`new.png` depict the orphaned legacy `/:id`/`/new` form, not the real entry/review flow — flagging the route→curated-embed remap for Task 6. |
| spot-check | 10 | EDITED (10/10 sub-pages + landing) | **Whole-module rewrite — the previous draft described a fabricated three-persona workflow with zero code backing, mirroring the physical-count pass's own headline finding.** Real flow (`sc-component.tsx` list → `sc-form.tsx` create, always in "add" mode → `sc-entry-component.tsx` entry → `sc-review-component.tsx` review) is a single continuous journey gated by one CRUD permission (`inventory_management.spot_check`) — no Inventory-Controller/Counter/Auditor/Sysadmin split, no counter assignment (`tb_user_location` not referenced), no variance-tolerance threshold, and no recount mechanism anywhere in the frontend or backend. **Headline finding (simpler than physical-count, not just unposted): `submit()` does not create `tb_stock_in`/`tb_stock_out` at all** — its own doc-comment states directly "Does not create stock-in/stock-out — any follow-up adjustments are user-driven"; a repo-wide search for `stock_in`/`stock_out`/`executeAdjustment`/`journal`/`ledger` inside this module returned zero hits. The final submit's only effect is `doc_status = completed` + `end_date` stamp; there is no linkage of any kind (not even a shared description string, unlike physical-count) connecting a spot check to any correction a user separately makes in inventory-adjustment. **New finding class (not seen in prior modules): dead view/edit/delete code.** `ScForm`'s `isView`/`isEdit` branches and the `useUpdateSpotCheck`/`useDeleteSpotCheck` hooks its Edit/Delete buttons call are unreachable — `ScForm` is only ever rendered from the create screen (`spot-check-by-location-content.tsx`), always without an entity, and `spot-check/:id` always renders the entry/counting screen instead. The module's own manual test-case catalog (`docs/test-cases/760-spot-check.md`, 32 cases, no automated Playwright spec exists) itself contains two scenarios (`TC-SPC-040001`, `TC-SPC-050001`) describing this unreachable view/edit/delete screen — flagged as a catalog/routing mismatch rather than corrected in the catalog itself (out of scope to edit). **Other real findings, not previously documented:** `on_hand_qty` is snapshotted twice — once at creation (live stock at sample time, unlike physical-count's `0`-seed) and again at Submit-for-Review (fresh live recompute) — so the figure compared on the review screen can differ from the one at creation if other movement occurred; `doc_status` can jump `pending → completed` directly, skipping `in_progress` entirely, if a user never triggers a mid-count Save (only `saveItems()` performs that transition, not `reviewItems()` or `submit()`); `reviewItems()` has no `completed`/`void` guard at all, so reopening a finished spot check from the History tab (whose click handler routes to the entry screen regardless of status) and clicking through to Submit-for-Review again silently overwrites `on_hand_qty`/`actual_qty`/`diff_qty`/`counted_at` on every detail row — only the terminal `submit()` call itself is blocked on a second attempt; Reset (`POST .../reset`) sets `doc_status = void` unconditionally but does **not** clear `tb_spot_check_detail` rows, contradicting the Bruno collection's own doc comment ("clears all recorded actual quantities and resets... to draft state"); `tb_spot_check_comment` (header-level comments) has a real Prisma model and backend controller but zero frontend hooks reference it (only detail-level `tb_spot_check_detail_comment` is wired up); the frontend's `SpotCheckStatus` type union includes dead `"voided"`/`"cancelled"` values with no schema backing (only 4 real values: `pending`/`in_progress`/`void`/`completed`). `03-user-flow-audit-config.md`/`04-test-scenarios-audit-config.md` rewritten as correction pages (mirrors the established PR/PO/GRN/SR/inventory/inventory-adjustment/physical-count pattern). `02-business-rules.md` § 5.1 rewritten as a Live-Code-vs-planning-document table against the E2E repo's `tx-10-spot-check.md` — whose core "variance posting is pending, not yet implemented" claim is confirmed accurate (unlike physical-count's planning doc, which was contradicted on its central posting claim), but whose 6-value status lifecycle (`draft`/`on-hold`/`cancelled`) and 5 check-types don't match the live 4-value enum/3-value method. Confirms and resolves the flagged-unverified cross-reference from `../carmen/docs`-era divergence item 5 (`consignment` grouped with `inventory` in spot-check filters, per the inventory module's own pass) — `findCurrentByLocation` groups by `location_type ∈ {inventory, consignment}` with no distinct branch. TH mirrored in full across all 11 files (landing + 10 sub-pages), including the 2 correction-page rewrites. | — | None flagged — all corrections are documentation-only; no visible UI change was found (the two curated screenshots, `index.png`/`review.png`, depict screens whose current UI was verified against source without finding visible drift; `detail.png` exists on disk but was already not embedded on the landing page before this pass and remains so — likely depicts the same list/create screens as `index.png`, not a distinct route). |
| product | 11 | EDITED (10/11 sub-pages + landing) | **Headline finding — resolves the inventory iteration's flagged-unconfirmed `INV_AUTH_004`:** every persona/business-rules page in this module wrongly claimed a separate "Inventory Controller" persona edits `tb_product_location`'s per-location policy (min/max/par/reorder qty), citing `INV_AUTH_004`. Direct read of `pd-location-tab.tsx` (the Product module's own "Locations" tab, part of the same `ProductForm` used for every other product field) shows the numeric fields are fully editable `<Input>` controls gated only by `isDisabled = mode === "view" || isPending` — no distinct field-level permission, no separate Inventory-Controller screen anywhere in the product or inventory codebases. This also matches the inventory module's own already-hedged finding (`INV_AUTH_004` marked `*(unconfirmed)*` there, progress-log line 102) that no route/component in that module edits this table. Corrected module-wide (`PRD_AUTH_008`, both `03-user-flow*` and `04-test-scenarios*` pages, ~10 files) — location-policy editing is Product Administrator authority, full stop. **Second finding — dated, confirmed frontend change since last sync:** as of 2026-07-14 (`f2b5832`, "lock code field, show auto-generated placeholder") the Product create/edit form's `code` field is unconditionally disabled and stripped from the create payload — the backend assigns a running-number code; the API itself still accepts a client-supplied `code` (bulk import / direct API). An identical change landed for Category/Sub-category/Item-group one day earlier (2026-07-13, `1e6703d`, "server-assigned category code"). Corrected `PRD_VAL_001`/`PRD_VAL_008`, the Product Administrator's primary-flow step 3, `category.md`'s Common Tasks table (button label also corrected `New` → `Add Category`; "Add sub-category"/"Add item group" buttons corrected to the real generic hover "Add child" action per `tree-node.tsx`), and `04-test-scenarios-product-admin.md` (`PA-HP-01`, `PA-HP-03` tab-name `Location Assignment`→`Locations`, `PA-HP-05`, `PA-VAL-01`). **Third finding — internal self-contradiction fixed:** `tb_product.doc_version` (added 2026-06-12 across 103 tenant tables, confirmed via Bruno's `PATCH /products/:id` body) is a real optimistic-concurrency counter — already correctly documented as `PRD_AUTH_013` in `02-business-rules.md` — but was missing from `01-data-model.md`'s own `tb_product` field table (added), and `04-test-scenarios-product-admin.md`'s `PA-EDGE-01` directly contradicted `PRD_AUTH_013` by asserting "no version field... last-write-wins" (rewritten to the correct `409 Conflict` behavior). **Fourth finding — two new schema entities since last sync, previously undocumented:** `tb_product_eco_label` / `tb_product_master_eco_label` (added 2026-06-01; real, independently-saving frontend section on the product detail view, `pd-eco-label-section.tsx` + `pd-eco-label-dialog.tsx`) and `tb_product_account_code_mapping` (added 2026-06-22; real on the API — `account_codes` add/update/remove on the product/category/sub-category/item-group create-update Bruno requests, `enum_account_type = {inventory, cogs, revenue, purchase}`, nearest-first cascade identical to the existing tax-profile/deviation-limit inheritance pattern — but a repo-wide frontend search found **no UI surface** for it). Both added to `01-data-model.md` as new §2.10/§2.11, cross-referenced from `02-business-rules.md` §7 as flagged-for-a-future-pass (not assigned placeholder rule IDs). **Other fixes:** rule count corrected from "approximately 65" to the exact 63 (18+10+13+10+12); `04-test-scenarios-product-admin.md`'s own At-a-Glance line cited a `100-product.spec.ts` that does not exist on disk (self-contradicting its own §6, which already said so correctly) — false citation removed; landing page's At-a-Glance "Key entities" list fixed (`tb_product_unit_conversion` → `tb_unit_conversion`, the real table name; `tb_product_variant` → `tb_product_eco_label`, since no variant table exists per the already-documented divergence) and its Product Variant key-concept bullet reworded to match; `category.md` was missing from the landing page's §7 sub-page list (11 sub-pages existed on disk but only 10 were linked) — added. TH mirrored in full across all 10 edited sub-pages + landing (4 parallel translation passes, each independently verified frontmatter/dateCreated and reported zero drift on re-check). | — | `product/new`, `product/:id` (code field now disabled/auto-generated — the existing `new.png`/`detail.png` screenshots, if they show an editable code input, are stale; `new.png` exists on disk but is not currently embedded on any wiki page) and `product-management/category` (`category.png`, embedded on `category.md` — category code field is now also disabled/auto-generated, and the create button is confirmed "Add Category" not "New"); no screenshot exists yet for the new Eco Label section on the product detail view. |
| recipe | 18 | EDITED (19/19: landing + 18 sub-pages) | **Headline: the module's documented operating model is almost entirely design-stage.** Verified against `micro-business/src/master/recipe*` + gateway `config_recipes`/`config_recipe-*`, Bruno `config/recipe*`, `routes/operation-plan/*`, and the error catalog: what exists is header-level recipe CRUD (`/api/config/{bu}/recipes`, `doc_version` optimistic lock, code-uniqueness `RECIPE_ALREADY_EXISTS`, category/cuisine existence checks that do NOT require `is_active`), a free 3-state status dropdown whose only backend effect is `published_at`/`archived_at` stamping (no publish gate of any kind; all transitions in all directions legal, incl. `ARCHIVED → PUBLISHED`), sub-recipe delete guard (`RECIPE_USED_AS_SUB_RECIPE`), prep-step + step-image REST endpoints (no UI in the recipe form), recipe image gallery (multipart full-sync), client-side-only cost calculator, and 4 real master-data screens. **No code exists for:** publish completeness gates (`REC_VAL_015`–`018`), ingredient-line persistence (grid is explicitly "Preview only — ingredients are not yet persisted" per `messages/en.json`; zero write path for `tb_recipe_ingredient`), `tb_recipe_version`/`tb_recipe_pricing_history` (schema-only, no writers), yield-variant editing, sub-recipe cost cascade, `recipe:*` RBAC (only frontend-only `operation_plan.view` placeholder, admin-gated group), menu items/POS, theoretical consumption, variance, cost-drift events, recipe→SR auto-create, clone, scaling calculator, labor_rate config. All 5 persona 03/04 page-pairs + both hubs got implementation-status callouts; landing + 01 + 02 got inline corrections. **Formula corrections (real code, wrong doc):** `actual_food_cost_percentage` = `total_ingredient_cost / selling_price × 100` (not `cost_per_portion / selling_price`); `labor_cost_percentage`/`overhead_percentage` are overwritten by the calc hook as price-relative %, not the 30/20 share-of-cost defaults; `labor_cost`/`overhead_cost`/`total_ingredient_cost` are manually-typed form fields, not derived. **Schema corrections:** `tb_recipe` has no `images` JSON column (real: `tb_recipe_image` child table) and no `course_type`/`total_time`; prep-step images live in `tb_recipe_preparation_step_image` (not a JSON column); prep-step table HAS 2 indexes (wiki said none); `doc_version` rows added to entity tables; stale Prisma line-number citations refreshed (5543–6073); scenario-14 claim "tb_recipe has no doc_version" corrected (it does, and it IS the concurrency mechanism). **E2E corrections:** module's only spec is `121-recipe-equipment-category.spec.ts`; recurring claim that `701-sr.spec.ts` covers a recipe-driven SR auto-create path (via `info.recipe_id`) removed everywhere — no `recipe_id` exists in SR code. **Duplicate-screen finding:** `/operation-plan/equipment-category` (nav-linked) and `/operation-plan/recipe-equipment-category` (routed, not nav-linked, but the E2E target) are two independent component trees hitting the same endpoint/table. **Master-data fixes:** category cycle-check is self-parent-only (no deep-cycle detection), reparent doesn't cascade `level`, `code` uniqueness case-insensitive, `name` not unique; equipment `available_qty<=total_qty` and maintenance-date ordering NOT validated (checked DTOs+service); no `category_name` rename fan-out (refresh only on equipment save); no maintenance-overdue badge. Also removed a leftover LLM self-talk artifact ("wait, this is wrong — let me redo") from 02-business-rules §3.1 worked example. Landing §7 was missing links to the 4 real master-data sub-pages — added. | (none — corrections only; a possible future `operation-plan` dashboard page noted in Route gaps) | None — all changes documentation-only; recipe list/detail screens unchanged since last capture (export/print buttons still disabled placeholders) |
| vendor-pricelist | 14 | EDITED (15/15: landing + 14 sub-pages) | **Headline: the module's documented operating model was almost entirely design-stage — the same recipe-module pattern, but with a new twist: a confirmed half-built feature.** Verified against `routes/vendor-management/*` + `routes/external/pl/*`, `micro-business/src/master/{price-list,price-list-template,request-for-pricing,check-price-list}`, gateway `application/{pricelists,pricelist-templates,request-for-pricings}`, Prisma, and e2e `150-vendor`/`159-pl`/`160-pl-template` specs. **What exists:** four independent CRUD screens (Vendor, Price List, Price List Template, Request for Pricing) + a `/vendor-management` KPI dashboard; `doc_version` optimistic locks; running-number `pricelist_no` generation; CSV import (grouped upsert on `pricelist_no`) + Excel up/download; a real `price-compare` endpoint (`is_preferred desc, price asc` — the actual "preferred vendor" mechanism); `active-vendors`/`active/:vendor_id/:date` lookups; RFQ `create()` that mints all per-vendor `pricelist_url_token`s + JWTs in one call; template `PATCH :id/status` (bare write, zero validation); portal `POST /api/check-pricelist/:url_token` that auto-creates a zero-priced draft pricelist from the template. **Confirmed gap (new finding class — half-built feature, not just unbuilt design):** the external portal's Save/Submit buttons call `PATCH`/`POST .../pricelist-external/:token[/submit]` — repo-wide search finds NO matching backend route; vendors can view the auto-created draft but cannot persist edits or submit; unlike stock-replenishment/wastage there is no TODO comment flagging it. **No code exists for:** the 6-phase campaign workflow, quality score/validation engine (`VPL_CALC_006`, `VPL_XMOD_009` removed), campaign/invitation derived state machines (`paused`/`cancelled`/`pending`/`in-progress`/etc. — nothing reads `start_date`/`end_date`/`submitted_at` to compute state; only real signal is `has_submitted: !!pricelist_id`), Manager high-value threshold + Finance-Manager multi-currency co-signoff, portal-token expiration/IP-allowlist/session-limit/revocation, reminder/escalation jobs (`reminder_days` stored but inert; nothing in micro-cronjobs), invitation email dispatch (token generated, nothing sends it), automatic `system` comments (no service ever writes a comment row — all comment tables are manual-CRUD only), MOQ non-increasing price check (`VPL_VAL_020`), active-pricelist immutability (`VPL_VAL_025`), status-transition state machine (`VPL_VAL_024`), segregation of duties, late-submission rejection, template-immutability-after-invitation. **Persona corrections:** Finance + Audit/Config 03/04 pages rewritten as correction pages (8th consecutive module with this pattern); Purchasing Manager folded into Purchaser (nothing for a Manager tier to gate). 02-business-rules rewritten with Confirmed/Design-target status per rule (`VPL_AUTH_*`/`VPL_POST_*` families dropped); 03-user-flow* rewritten around the 4 real screens; 04-test-scenarios* rebuilt on real specs. **E2E claim corrected:** "no dedicated spec exists" was false — `150-vendor.spec.ts` (875 lines), `159-pl.spec.ts`, `160-pl-template.spec.ts` all exist; `159-pl`'s Duplicate/Mark-as-Expired sections reference actions absent from the real list component (loose `.catch()` assertions) — flagged as aspirational. **Other fixes:** tax math (`tax_amt`/`price`) computed client-side only, backend persists verbatim; `effective_from/to` past-date checks confirmed real (`PRICE_LIST_FROM_IN_PAST` etc.); RFQ date-range check confirmed (`RFP_INVALID_DATE_RANGE`); soft-delete forces `status = inactive`; stale Prisma line citations 4039-4176 → 4405/4438/4473/4511; `submitted_at`/`info` field descriptions corrected (no writers); divergence-table items 2/4/6/7/11 corrected from "app-layer derives/enforces" to "not implemented anywhere". TH mirrored in full across all 15 files (4 parallel passes, each verified frontmatter + zero dateCreated drift). | — | None flagged — all corrections are documentation-only; the curated `index.png` (Price List list screen) and `request-price-list*.png` depict screens verified against source without visible drift; the external portal `/pl/:url_token` has no curated screenshot and (given the confirmed Save/Submit gap) should not get one until the backend half lands. |
| master-data | 14 | EDITED (14/14 pages + landing) | **Systemic finding (headline, applies module-wide): none of the 12 catalogue services in this module enforce a delete/reference guard.** Direct reads of `delete()` in `adjustment-type`, `unit`, `department`, `location`, `delivery-point`, `currency`, `tax-profile`, `credit-term`, `extra-cost-type`, `credit-note-reason`, `vendor`, and `vendor_business_type` services all show the identical shape: an unconditional soft-delete (`is_active: false` + `deleted_at`) with **zero** FK/reference check beforehand. Every "Cannot delete — referenced by X" validation row across all 12 pages was fabricated/unconfirmed and corrected to state the reference guard is absent; the same treatment applied to sibling claims found not to be backed by code: `adjustment-type`'s "type cannot be changed after first use" (confirmed absent — `update()` spreads the client payload with no guard), `location`'s "cannot change location_type after first movement" (confirmed absent) and "posts to inventory asset GL / department expense" (no GL/journal code exists anywhere — corrected to describe the real inventory-transaction-layer behavior, matching the `inventory` module's own already-established finding), `currency`'s "ISO code not found" / "exchange rate must be > 0" / "cannot inactivate — set as BU default" (all four confirmed absent from `currency.service.ts` and its Zod DTOs), `tax-profile`'s "rate must be >= 0", and `credit-term`'s "value must be >= 0" (both DTOs declare the field as a plain unconstrained `z.number()`). **Cross-module residual fixed (flagged by the physical-count pass, progress-log line 117):** `adjustment-type.md`'s §1/§7 wrongly listed physical-count and spot-check as consumers of this table — corrected to state physical-count's variance rollup leaves `adjustment_type_id` `null` and spot-check never creates a stock-in/out row at all; the landing page's Cross-Module-Dependencies bullets for both modules were fixed to match. **New confirmed-broken-feature finding (exchange-rate, mirrors the vendor-pricelist external-portal precedent):** the Exchange Rate list's "Update" bulk-sync button (`useExternalExchangeRates` in `use-exchange-rate.ts`) calls `GET /api/exchange-rate?base=<code>`, a legacy Next.js API route with no equivalent anywhere in the current NestJS gateway — the hook's own code comments confirm this is a known-broken carry-over ("was a Next route — move to backend... when the config module migrates") that degrades to an "endpoint is not available" error on static hosting; no `micro-cronjobs` FX job exists either (zero repo-wide hits). Also corrected: no "Refresh FX" action exists anywhere (PO/PR/GRN all just auto-populate the rate field from `tb_currency.exchange_rate` on currency selection, then allow free editing). **extra-cost-type:** confirmed (mirroring the good-receive-note module's own already-unconfirmed flag) that the GRN form's allocation-mode picker only offers `by_qty`/`by_value` (never `manual`, despite the enum permitting it) and that every cost line always has a plain manually-typed `amount` — no code anywhere computes or splits an amount by quantity or value proportion; rewrote the allocation-formula claims as design-intent rather than confirmed behavior. **vendor-business-type — schema correction (largest single-page fix):** the prior page asserted `tb_vendor` holds a `business_type_id` FK column pinning one "primary" type alongside the `business_type` JSON array. A direct read of the current tenant schema's `tb_vendor` model found **no such column and no `@relation` to `tb_vendor_business_type` at all** — the only link is the loose, unenforced `business_type` JSON array (no FK, no `onDelete` behavior, referential integrity is frontend-only); also fixed `doc_version`'s type (was documented as `Decimal`, is actually `Int`) and the Prisma source citation (was pointing at a stale `../carmen/docs/prisma-schema/schema.prisma` copy instead of the canonical backend-v2 tenant schema). **Systemic omission fixed:** `doc_version` (added 2026-06-12 across 103 tenant tables) was missing from the §5 field table on 9 of the 14 pages (`unit` ×2 tables, `department`, `location`, `delivery-point`, `currency`, `exchange-rate`, `tax-profile`, `adjustment-type`, `credit-note-reason`, `vendor`, `business-unit`) despite some already documenting the resulting optimistic-lock behavior in §6 — added everywhere, and fixed `exchange-rate`'s and `business-unit`'s corresponding business-rules text. **Stale frontend/Prisma citations fixed module-wide:** every page's §8 "Frontend" reference pointed at a non-existent repo (`../carmen-turborepo-frontend/apps/web/app/(app)/configuration/<entity>/` — confirmed no such sibling directory exists) — corrected to the real `../carmen-inventory-frontend-react/routes/config/<entity>/` (or `routes/vendor-management/vendor/` for vendor.md); Prisma line-number citations were stale across the board (schema growth since original authoring shifted every model) — recomputed and corrected for all 12 tenant-schema tables + 2 platform-schema tables cited. **Landing-page fixes:** §1 Overview wrongly listed "pricelist templates" as living in this module (it's `vendor-pricelist`'s `price-list-template` route) — removed and replaced with a new scope note (added to §1) documenting that the real `/config/*` route tree has exactly 14 leaf screens, two of which (`certification`, `eco`) have no wiki page yet (a pre-existing, already-logged 2026-06-18 gap — not authored this pass), while two of this module's 14 wiki pages (`vendor`, `business-unit`) document entities whose real UI lives outside `/config/*` — kept as legitimate cross-references, not relocated (structural/IA question, out of this pass's scope). `business-unit.md` (a cross-book reference page for the Platform-owned `tb_business_unit`) got a lighter-touch pass: frontend behavior for the calculation-method field was checked directly (`carmen-platform`'s `CalculationSettingsSection.tsx` — a plain always-editable `<select>` with no guard) and the mid-period-lock/BU-delete/currency-inactivation claims were downgraded to "unconfirmed" (backend microservice not traced) rather than fully verified absent, since the microservice internals sit outside this iteration's primary `carmen-inventory-frontend-react`/`carmen-turborepo-backend-v2` scope. TH mirrored in full across all 14 sub-pages + landing (same-session, verified frontmatter + zero `dateCreated` drift). | — | None flagged — all corrections are documentation-only (wrong/unconfirmed validation claims, stale citations, a schema-model correction); the one behavior-adjacent finding (exchange-rate's broken external-sync button) doesn't change what any screenshot would show — the button and its resulting error state aren't newly-appeared UI, just newly-documented as broken. |
| system-config | 11 | EDITED (11/11 sub-pages + landing) | **Headline finding: 4 of 11 sub-pages document concepts with no reachable Sysadmin screen at all, and a 5th (`document`) had its entire backing table wrong.** `dimension.md` and `menu.md` — `tb_dimension`/`tb_dimension_display_in` and `tb_menu` are schema-only: zero non-schema code references anywhere in `carmen-turborepo-backend-v2` (the one `dimension` hit is `dimension-comment.service.ts`, a comment thread on rows nothing ever creates); no `/system-admin/dimension` or `/system-admin/menu` route exists in `router.tsx`; the `dimension` JSONB column is real on ~65 tenant tables but zero reads/writes were found anywhere (checked the PR module in depth). Both pages rewritten with "Implementation status" correction sections (recipe-module precedent) — kept as documented design intent, corrected to state plainly that nothing implements them; the real app-shell nav is a static frontend constant (`landing-types.ts`'s `CHAPTERS`), not `tb_menu`-driven. `application-config.md` — `tb_application_config` is real and actively used (`report_email` SMTP key, confirmed via `app-config.service.ts`), but there is no general "Application Settings" browse/edit screen (no route in `router.tsx`); it's consumed key-by-key by config-email and a workflow signature-candidates screen only. `query-dataset.md` — the backend `sql-query` service (execute/save/listDbObjects/getDbObjectDefinition/dropDbObject) is real and closely matches prior documentation, but **no frontend route, component, or hook exists anywhere in `carmen-inventory-frontend-react`** (the previously-cited `routes/system-admin/query-dataset/page.tsx`, `use-sql-query.ts`, and frontend `sql-validator.ts` mirror do not exist); removed the page's embedded screenshot since no screen produced it. **Second headline (safety-relevant, confirmed via direct code read): the SQL Workbench `execute` endpoint is NOT read-only** — `validateSqlSafety(sql_text, { allowMultiple: true, allowDangerous: true })` skips the forbidden-keyword blocklist entirely and allows multi-statement DDL/DML (the controller's own Swagger description says "Runs any SQL... SELECT, DML... and DDL... one or more statements"); only the separate `save` path keeps the restrictive SELECT/WITH-or-CREATE-only validation this page previously (and wrongly) attributed to `execute`. **Third headline: permission gating is real for exactly one route, not three.** Only `POST .../sql-query/execute` carries `@RequirePlatformPermission('sql_workbench.manage')`; `save`, list, get-definition, and drop have **no permission guard at all** beyond basic auth — the previously-documented `sql-query.execute`/`.save`/`.drop` three-App-ID triad does not exist. The identical no-guard finding was independently confirmed on `config_app-config.controller.ts` (backing both `application-config.md` and `config-email.md`): only `KeycloakGuard` (authentication) applies, no `AppIdGuard`/`RequirePlatformPermission` — the documented "App ID `app-config.upsert`" gate for Email Configuration is unenforced; any authenticated caller with a valid `x-app-id` can read/write SMTP credentials today. **`document.md` — wrong backing table, corrected module-wide:** the page described `tb_attachment` (tenant schema) as the file-metadata mirror, but `tb_attachment` has **zero non-schema code references anywhere** — a dead table. The real registry is `tb_file_tag`, in a wholly separate `prisma-shared-schema-file` (`CARMEN_FILE`) database, written by the `micro-file` microservice via **MinIO** object storage (`minioClient.putObject`/`removeObject`/`presignedGetObject`), not the generic "S3-compatible + tb_attachment" story previously told. Also corrected: the 10 MB cap and MIME allow-list are **frontend-only** — `files.service.ts`'s `uploadFile()` has no server-side size or MIME/extension check at all (the claimed "defence-in-depth MIME sniffing server-side" was not found). AppId guards (`documents.upload/.list/.get/.download/.info/.presignedUrl/.delete`) were independently confirmed real and correctly named. This resolved a direct internal contradiction with `doc-version.md` §5, which described `tb_attachment.doc_version` as an active "re-render counter" — also false (zero code touches it); both pages corrected to describe it as inert schema on a dead table. **`period.md` and `running-code.md` — fabricated admin-screen UX, real underlying data models:** `period-component.tsx`/`period-dialog.tsx` is a plain generic CRUD list + edit dialog where status (`open`/`closed`/`locked`) is just one of several plain fields in the same dialog as fiscal_year/dates — there is no distinct Close/Lock/Reopen button, no audit-reason prompt, and no Snapshot tab or period-detail component anywhere in the directory (all fabricated in the prior version). `running-code-dialog.tsx` is a free-text `type` field plus a raw JSON `config` textarea with only a pretty-print "Format" button — no per-segment (A/B/C) width editor, no drag-reorder, no live "preview next three numbers" field (all fabricated); the underlying `config` JSONB pattern language (`date('yyyyMM')`, `running(5,'0')`, `format` assembly) is itself real and confirmed via `running-code.helper.ts` + its own unit tests, so only the §2 admin-screen description was rewritten, not §5's data model. **Cross-module correction (landing page + all affected modules' own already-completed passes untouched, but the system-config landing's own dependency list was wrong):** `enum_workflow_type` has exactly three members — `purchase_request`, `store_requisition`, `purchase_order` — confirmed against the Prisma schema; GRN, inventory-adjustment, physical-count, spot-check, and vendor-pricelist have no `workflow_type` enum member and cannot attach a `tb_workflow` row at all, contradicting the landing page's own "optional approval" language for all five — corrected. **Systemic citation fix:** every page in the module cited a nonexistent `../carmen-turborepo-frontend/apps/web/app/(app)/configuration/<x>/` repo (already-established-absent per the master-data pass, not yet swept into this module) — corrected to real `../carmen-inventory-frontend-react/routes/system-admin/<x>/` paths (or "no frontend exists" for dimension/menu/query-dataset), and the fabricated Next.js-style `page.tsx` + `_components/x.tsx` citation pattern (doesn't match this codebase's flat `<name>.route.tsx` + `<name>-component.tsx` convention) was corrected on config-email.md and dashboard-dataset.md. Stale Prisma line-number citations recomputed for `tb_dimension`/`tb_dimension_display_in`, `tb_menu`, `tb_application_config`/`tb_application_user_config`, `tb_config_running_code`, `tb_period`/`tb_period_snapshot`, and `tb_workflow`. `doc_version` field rows added to `application-config.md`'s §5 tables (present on `tb_application_config`/`tb_application_user_config` but previously omitted, mirroring the master-data pass's own established omission pattern). TH mirrored in full across all 12 files (landing + 11 sub-pages), including 2 substantially-rewritten correction pages (dimension, menu). | — | `system-config/query-dataset` — the previously-embedded `query-dataset.png` screenshot has been removed from the page since no frontend screen exists to have produced it (flagging the stale asset itself for Task 6 to investigate/retire); no other screenshot changes — the 5 real screens' curated screenshots (`period.png`, `running-code.png`, `config-email.png`, `document.png`, `workflow.png`+`workflow-detail.png`) were checked against source and found to still match current UI. |
| access-control | 6 | EDITED (6/6 sub-pages + landing) | **Two schema-drift corrections resolved module-wide:** `enum_platform_role` no longer exists — dropped 2026-06-10 (`06d8a921` "remove legacy platform_role column and API surface", confirmed in git log + current Prisma schema) and replaced by a fully relational, cluster-scoped system (`tb_platform_role` + `tb_platform_role_tb_permission` + `tb_user_tb_platform_role` + its own `tb_platform_permission` catalogue) that is Carmen Platform admin territory, not this `tb_user` record — fixed on `access-control.md`, `application-role.md`, `user.md` (data-model table + Edge Cases). `tb_password` externalization was previously attributed to a generic "external identity provider"; direct read of `auth.service.ts` confirms it's specifically **Keycloak** (`changePassword` proxies to the Keycloak Account API) — fixed on `user.md`'s task table and Edge Cases. **`business-unit-user.md` — headline finding: `tb_temp_bu_user` is schema-only, no code path anywhere reads or writes it** (repo-wide search of `carmen-turborepo-backend-v2` confirms zero non-schema references); the real invite flow (`POST /api/auth/invite-user`) is email-only and never touches BU or role — the prior page described a live BU-scoped invitation staging flow that doesn't exist. Also confirmed and documented `PermissionGuard`'s literal "God-mode bypass" code comment for `tb_user_tb_business_unit.role = admin`, and that there is no BU-membership admin CRUD screen anywhere in `carmen-inventory-frontend-react` (only self-service BU-switch/profile views). **`department-user.md` — two invariants previously documented as enforced are not:** ordinary (non-HOD) membership is *effectively* single-department per user (driven by `findByUserId`'s `findFirst` + a frontend picker filter, not a DB constraint), and single-HOD-per-department is *not enforced anywhere* in `departments.service.ts` — the HOD-add path only guards against a duplicate row for the same user+department, never clears a different user's existing HOD row. Also fixed `doc_version`'s Prisma type (was documented `Decimal`, is actually `Int`) and the `user_id` FK claim (not a declared Prisma `@relation`, cross-schema pattern). **`permission.md` — largest rewrite: comment threads and approval-workflow actions are gated by a wholly separate mechanism, not by `tb_permission`.** They carry `@UseGuards(AppIdGuard)` checking `x-app-id` against a client-application allowlist (`tb_application`/`tb_application_api`) — unrelated to the calling user's RBAC grants; corrected the App ID table (dropped a fabricated bare `create` comment action; `storeRequisition.save` → `.submit`, the real API name) and confirmed via `PermissionGuard`/`AppIdGuard` source that routes without a `@Permission` decorator have no RBAC check at all. **`user-location.md`/`application-role.md`/others** — corrected stale UI descriptions (tab-based pickers that don't exist; real screens are two-panel Transfer controls on `/system-admin/user/:id` and `/system-admin/role`), stale Prisma line-number citations, the dead `../carmen-turborepo-frontend` repo path (module-wide, replaced with `../carmen-inventory-frontend-react`), and added missing `doc_version` rows to 6 data-model tables across the module. TH mirrored in full across all 7 files (landing + 6 sub-pages); found and completed 4 sub-pages (`business-unit-user`, `department-user`, `permission`, `user-location`) left un-mirrored from an interrupted prior session, plus one partially-mirrored page (`application-role.md` — only §1 had been translated). | (none) | None flagged — all corrections are documentation-only; routes cited (`role`, `role/:id`, `role/new`, `user`, `user-activity`, `user/:id`, `department`, `department/:id`, `profile`, `profile/setting`) all exist in the route dump with no visible-UI drift found against the curated `application-role.png`/`application-role-detail.png`/`user.png`/`user-detail.png` screenshots. |
| dashboard | 10 | EDITED (10 pages) | Discovered the entire prior module model was fabricated/obsolete: `/dashboard` is **one route, one component** (`dashboard-component.tsx` — greeting + a personal "Saved Widgets" drag-and-drop grid of dataset-backed KPI/pie cards via `LookupDataset`), not six named domain sub-pages plus my-pending/my-approval companion widgets. Router history shows the six domain files (`dashboard-{main,pr,po,grn,inventory,sr}.tsx`) and the two widget files (`dashboard-{my-pending,my-approval}.tsx`) were **never wired into `routes/router.tsx`** even before the latest cleanup — commit `03891e3d` ("refactor(dashboard): convert to idiomatic structure, drop dead demo code", 2026-06-27) deleted all 8 files + `mock/` (19 files, 5,468 deletions), its own message confirming "no importers anywhere." Rewrote the landing page and all 8 named/companion sub-pages with an "Implementation status" callout + corrected At-a-Glance (no route, "Removed"/"never mounted") while preserving each page's tile/table content as clearly-labeled historical reference (file count held at 9/9 per constraints). Settled the system-config-iteration forward-flag in `widget-workspace.md`: corrected the "Naming note" from fabricated `tb_widget_dashboard`/`tb_widget_dashboard_item`/`tb_widget_default_layout` to the real `tb_dashboard_personal_widget` (tenant schema ~6205) + `tb_dashboard_bu_widget` (~6185, separate BU-scoped feature powering per-module landing dashboards under Procurement/Inventory-management/etc., out of this module's scope); also fixed stale `_components/`/`page.tsx` file-path citations (flattened by the same cleanup commit) and corrected the widget shape/type table (only `kpi`/`pie` are ever created by this page's picker, never `bar`, despite 9 declared `enum_dashboard_widget_type` values). `my-approval.md` additionally had a **false "Live" status claim** (widget was dead code even before deletion) — corrected and cross-linked to the real live equivalent, [purchase-request/my-approval](/en/inventory/purchase-request/my-approval) (`/procurement/approval`). | (none) | `/screenshots/dashboard/{main,pr,po,grn,inventory,sr}.png` depict demo screens that never had a live route — flag for removal/replacement in Task 6; `/screenshots/dashboard/index.png` should be re-verified against the actual current single-page layout (greeting + Saved Widgets grid) |
| reporting-audit | 8 | EDITED (8/8 sub-pages + landing) | **Both forward-flagged pages fully rewritten (widget.md, attachment.md — see Discrepancy log for the settling entries).** **Headline: neither the on-demand report "Run", every "Print" button, nor any scheduled fire currently writes a `tb_report_job` row** — all three resolve through the synchronous viewer endpoint (`POST .../reports/viewer`); only the unreachable legacy `generate-async` path writes jobs, and zero frontend callers of it were found. `reporting-audit/history.md` corrected to document this as a confirmed structural gap (the table/screen/endpoint are all real, just never populated) rather than "every report run lands here." **`tb_report_schedule` (tenant) confirmed dead** (zero code references) — real schedule store is a generic `Cronjob` table in a separate `micro-cronjobs` Postgres schema (`job_type="report"`); `reporting-audit/schedule.md` rewritten to describe the real create/list/delete-only screen (no detail page, cron builder, edit, pause toggle, test-run, or typed recipients — all previously documented, all confirmed absent). **`tb_user_login_session` (platform) confirmed dead** — this system authenticates via Keycloak JWTs with no local session table; `reporting-audit/user-activity.md` rewritten to show it's a filtered client-side view over the same `tb_activity` table as `reporting-audit/activity.md`, not a two-table join (real writer: `AuthService.logAuthActivity()`, login/logout only, scoped to the user's default BU only; no confirmed failed-login capture or page-view logging). **`tb_broadcast_notification` + `tb_user_broadcast_action` (platform) discovered live and previously undocumented** — replaces the old per-recipient `tb_notification` fan-out for system/BU broadcasts; `tb_news` corrected for real `business_unit_ids` BU-scoping and a `draft`/`published`/`archived` lifecycle (was claimed to have neither). `reporting-audit/activity.md` corrected: the claimed per-document "Activity drawer" doesn't exist anywhere — only the platform-wide `/system-admin/activity-log` screen does. `doc_version` (2026-06-12 rollout) added to `tb_activity`/`tb_notification`/`tb_message_format`/`tb_news` field tables where missing; stale Prisma line citations recomputed across all 8 sub-pages + landing. Landing page's At-a-Glance, §1 Overview, §3 Entity List (now 6 rows for 6 real entities + 2 read-only view pages = 8 sub-pages), §4 Cross-Module Dependencies, and §5 References all corrected to match. TH mirrored in full across all 9 files (landing + 8 sub-pages). | — | None flagged — all corrections are documentation-only; no visible UI change was found (the module's screens — report list, schedules list, history list — were verified against source without finding drift; the confirmed-empty history list and create/list/delete-only schedule screen are pre-existing UI states, not new changes this pass). |
| costing | 11 | EDITED (11/11 sub-pages + landing) | **Same fabrication pattern as every other module, plus a novel twist: the "Test_case/System_Process/*.md" citations used throughout `02-business-rules.md`/`03-user-flow.md` are a real planning-stage document set (`../carmen-inventory-frontend-e2e/docs/persona-doc/System Process/*.md` — INDEX.md, proc-03-cost-calculation.md, tx-01..11-*.md) cited under a wrong, non-existent path with fabricated capture dates and treated as code-verified fact when it is actually a requirements-gathering doc whose own P4 table records "User confirmed \<date>" answers, not observed behaviour.** Confirmed via code + that doc's own text: (1) **no per-product/per-category costing method** — `tb_business_unit.calculation_method` (platform schema, `average`\|`fifo`, default `average`) is a single BU-wide value; `InventoryTransactionService.getCalculationMethod(bu_code)` applies it uniformly, no mixing. (2) **no GL/journal-posting code anywhere** — the planning doc's own "What This Process Does NOT Do" section states "does not generate GL journal entries"; the wiki nonetheless added `Dr AP/Cr Inventory`, `Dr Department Expense/Cr AP`, `Dr COGS/Cr AP` postings and a "sub-ledger ↔ GL reconciliation" mechanism throughout landing §3, 02-business-rules §§3/4/5/6 (`COST_XMOD_009`), and all three persona pages — all removed. (3) **`COST_VAL_009` (block `calculation_method` change with non-zero on-hand) does not exist** — the planning doc records the lock as stakeholder-confirmed design intent (P4 Q1), but a repo-wide search of `platform_business-units.controller.ts`'s write path found no on-hand check, no drain precondition, no rejection path (matches the identical finding already made on `inventory/01-data-model.md` §5 item 4 in the 2026-07-15 inventory-module pass). (4) **average-only `tb_period_snapshot`** — FIFO-method business units carry residual lots forward as `close_period`/`open_period` cost-layer rows and never write a snapshot row at all (`processCloseTransactions` branches on `calculation_method`); this was previously implied to happen for both methods. (5) **`JournalEntry`/`AverageCostTracking` were fabricated table names** in the landing page's At-a-Glance box — no such Prisma models exist. **All three persona pages (Finance, Inventory Controller, Auditor) rewritten as correction pages** — no Finance/Auditor role exists (`enum_stage_role = {create,approve,purchase,issue,view_only}`, no finance/auditor member); [inventory-adjustment](/en/inventory/inventory-adjustment) §1 (already verified) shows `tb_stock_in`/`tb_stock_out` post unconditionally on creation with no approval queue, so the "Inventory Controller reviews a cost-pick preview" narrative has no reachable pending state to review. `01-data-model.md` was already largely accurate (kept, light touch: fixed a now-resolved cross-reference to `inventory/01-data-model.md` §5 item 4, fixed a false "adjustment_type_id drives GL routing" reference). `calculation-methods.md` kept as-is (established wiki convention: generic industry-pattern reference page, cross-referenced by `01-data-model.md` §5's divergence catalogue) except a stray trailing "TEST" artifact removed and its At-a-Glance/lock claim corrected to match the no-guard finding. `04-test-scenarios.md` rewritten from an 18-cross-persona/84-per-persona structure to 11 engine-level scenarios with no persona column; E2E mapping corrected after reading `900-period-end.spec.ts` directly — several of its describe blocks (Close workflow, Close action, Detail page) are explicitly marked `Feature pending`/`Backend only` in the spec file itself, so the FIFO/Average rollforward scenarios are not actually E2E-covered as the previous draft implied. TH mirrored in full across all 12 files (landing + 11 sub-pages), same corrections translated, identifiers/routes verbatim. | (none) | (none — `assets/screenshots/inventory/costing/` is empty and no page embeds a screenshot; the real period-end screens are already covered by the `inventory` module's own screenshot set) |
| templates | 2 | EDITED (2/2 sub-pages + landing) | **Headline: the two templates in this module are not the same mechanism, despite the landing page previously asserting one uniform "seed-only, deep-clone, hard-delete-blocked-if-used" model for both — verified against `purchase-request-template.service.ts`/`price-list-template.service.ts` (both real, direct-Prisma data layers) plus the frontend forms and both E2E spec files.** **PR template:** "Create PR from Template" is a pure client-side pre-fill (`new-purchase-request-content.tsx` fetches the full template list, matches by `?template_id=` in the browser, `getDefaultValues(purchaseRequest, template)` in `pr-form-schema.ts` merges fields into the new-PR form) — there is no backend clone endpoint and no `created_from_template_id` (or equivalent) column anywhere in `tb_purchase_request`; a template-sourced PR is indistinguishable from one created from scratch. `delete()` is an unconditional hard delete with zero usage guard (contradicts the previously-documented "hard-delete blocked once cloned"). Lifecycle is a single boolean `is_active` (default `true` on create) — there is no "draft" state; the previously-documented 3-state `draft`/`active`/`inactive` model derived from `is_active`+usage has no code backing. `getDefaultValues` also hardcodes the pre-filled PR line's `exchange_rate` to `1` and drops `currency_code`/`exchange_rate_date` — it does not "re-resolve FX at clone time" as previously claimed. The line-item grid (`prt-item-table.tsx`) exposes only location/product/unit/qty/currency — tax, discount, FOC quantity, and `dimension` are accepted by the DTO but have no UI field, so every line saved through the app has them at zero/empty. **Price List template:** the previous page never mentioned the module's actual payload — a **Products** section of per-product MOQ tiers (`tb_pricelist_template_detail.order_unit_obj` JSONB array), confirmed via `plt-form.tsx`/`plt-form-products-section.tsx` and the real `messages/en.json` copy ("Products in this template", "Add tier", "Remove tier"). Conversely, `reminder_days`/`send_reminders`/`escalation_after_days` — the fields the previous page built its entire "Common Tasks" and "Validation" sections around — are real DB columns accepted by `create()`/`update()` if posted directly, but have **no UI field anywhere in `plt-form.tsx`**, no server-side validation, and no background job (`micro-cronjobs` repo-wide search: zero hits for "reminder"/"escalat"). Clone is confirmed **removed**, not merely undocumented: `160-pl-template.spec.ts` has a dedicated "Pricelist Template — Clone (removed)" suite asserting zero clone affordances in list/detail/edit for every role. `remove()` is an unconditional **soft** delete (`status = inactive` + `deleted_at`, no usage guard) — the inverse of PR template's hard delete, and the inverse of the previously-documented "cannot hard-delete once issued." The DB-level `@@unique([name, deleted_at])` constraint was previously described as "application-enforced; no explicit `@@unique`" — it exists and is additionally re-checked in application code (redundant, not absent). Unlike PR templates, pricelist templates keep a **live, persisted FK** from every RFQ round issued against them (`tb_request_for_pricing.pricelist_template_id`) — the opposite asymmetry from PR templates' total lack of a link, which the landing page's old "uniform seed-only mechanic" framing obscured. Both E2E spec files (`310-pr-template.spec.ts`, `160-pl-template.spec.ts`) contain `describe` blocks / step annotations for features absent from current code (PR template "Clone" and "Set as Default" — no `is_default` field exists anywhere in schema; pricelist template's `TC-PT-030001` step list narrates a multi-MOQ switch, lead-time switch, max-items field, and reminder/escalation controls that the test body never interacts with) — flagged as aspirational/stale test documentation, not confirmed behavior. TH mirrored in full across all 3 files (landing + 2 sub-pages). | (none) | `/vendor-management/price-list-template` and `/procurement/purchase-request-template` (list views) — both gained sortable Created/Updated columns + an audit-cell "Updated" line on the card view, confirmed via commits `ddbee36`/`fda188a`/`737a5cd`/`ab4f27d` (2026-07-24, after this module's existing screenshots were captured); columns are hidden by default (`columnVisibility: { created_at: false, updated_at: false }`), so default-view screenshots may still be accurate — flagging for Task 6 to verify against the curated `purchase-request.png`/`price-list.png`. |

## Platform book (source: ../carmen-platform)

| Module | Pages | Status | Claims fixed | New pages | Screenshot routes flagged |
|--------|-------|--------|--------------|-----------|---------------------------|
| clusters | 3 | pending | | | |
| business-units | 2 | pending | | | |
| users | 3 | pending | | | |
| applications | 3 | pending | | | |
| rbac | 3 | pending | | | |
| report-templates | 4 | pending | | | |
| print-template-mapping | 3 | pending | | | |
| news | 3 | pending | | | |
| broadcasts | 3 | pending | | | |
| changelog (page) | 1 | pending | | | |
| profile (page) | 1 | pending | | | |

## Root/landing pages

| Page | Status |
|------|--------|
| en/home.md + th/home.md | VERIFIED — two-card descriptions still accurate, both book links resolve, no changes needed |
| en/inventory.md + th/inventory.md | EDITED — dead-link check clean (all 18 module links + Credit Note sub-page link resolve, module list complete vs. filesystem); Credit Note row's "AP reversal" claim removed (repeated a claim the purchase-order snapshot pass treats as unconfirmed — see discrepancy log) |
| en/platform.md + th/platform.md | VERIFIED (light check per brief — Task 4 covers platform modules next) — dead-link check clean, 11/11 entries (9 module dirs + changelog + profile) present, no changes needed |

## Discrepancy log

(claims found in wiki with no source backing — one bullet each: page, claim, what source actually says)

- `en/th/inventory/purchase-request/03-user-flow-purchaser.md` + `04-test-scenarios-purchaser.md` (pre-fix): described a per-line "Convert-to-PO workbench" with a pricelist-deviation-tolerance indicator, per-line "convert quantity" field, and a per-line "Allocate Vendor dialog" invoked at conversion time. The actual conversion UI (`carmen-inventory-frontend-react/routes/procurement/purchase-order/po-from-pr-dialog.tsx`, confirmed against `POST .../purchase-orders/group-pr` and `POST .../purchase-orders/confirm-pr` Bruno specs) is a 2-step dialog that selects whole approved PRs and groups them server-side by `(vendor, delivery_date, currency)` — no per-line controls exist. Fixed in this pass.
- `en/th/inventory/purchase-request/03-user-flow-procurement-manager.md` + `04-test-scenarios-procurement-manager.md` (pre-fix): described a "Vendor Allocation Rules" configuration screen (scoring weights, per-vendor priority overrides) and a "Stuck PR Oversight" bulk-action view owned by the Procurement Manager. No matching route, frontend component, or backend endpoint found in `carmen-inventory-frontend-react` or `carmen-turborepo-backend-v2`, and no such route appears in `.specs/resync-2026-07-15-routes-inventory.txt`. Fixed in this pass — PM is documented as the escalated `approve`-role stage using the identical Approver UI.
- `en/th/inventory/purchase-request.md` (pre-fix): At-a-Glance and Overview described the PR lifecycle as `Draft→Submitted→Under Review→Approved/Rejected/Sent Back`. Prisma's `enum_purchase_request_doc_status` (confirmed in `carmen-turborepo-backend-v2/packages/prisma-shared-schema-tenant/prisma/schema.prisma`) and the frontend `PR_STATUS` enum (post commit `278fc91`, 2026-07-07: "trim PR document status to the 5 real backend values") only have `{draft, in_progress, voided, approved, completed}` — there is no distinct `submitted`/`under_review`/`rejected` document status; "Submitted" maps to `in_progress`, and reject converges on `voided`. Fixed in this pass. (Note: `01-data-model.md` and `02-business-rules.md` already documented this correctly — only the module landing page's prose was stale.)
- `en/th/inventory/purchase-request/03-user-flow-purchaser.md` + `04-test-scenarios-purchaser.md` PUR-PERM-04 (pre-fix, and briefly in the first pass of this resync): implied the Convert-to-PO dialog is gated by purchase-order create permissions and that a Requestor/HOD would be denied. Source shows **no enforcement**: Bruno docs for `POST .../purchase-orders/group-pr` and `POST .../purchase-orders/confirm-pr` both list `Permissions: None`; the backend controller methods have no permission/role guard; the frontend dialog chain (`po-create-dialog.tsx` → `po-from-pr-dialog.tsx`) has no `hasPermission` check, and the `PERMISSIONS` catalog defines `procurement.purchase_order` as `viewOnly()` (no `.create` key, unreferenced by the dialog). E2E `TC-PR-410004` ("No Permission to Convert PR", `301-pr.spec.ts`) exists but does not assert denial — an absent button passes via `expect(true).toBe(true)`. Pages corrected to state that access is gated only by reaching the Purchase Order module UI; denial behavior should be treated as unimplemented until a guard lands.
- **Unverified / deferred** — `03-user-flow-audit-config.md` and `04-test-scenarios-audit-config.md` describe a dedicated "PR Activity Queries" audit workspace and a "Configuration workspace" (PR Workflow Settings, PR Type Defaults, Delegation Rules, Tax Codes, Currency Rates as distinct pages) with per-scenario preview panels and forecasts. No matching routes were found in `routes-inventory.txt` or the frontend during this pass, but this heavily overlaps the **system-config** module's scope (generic workflow / running-code / tax-profile / currency screens do exist elsewhere in the product, just not under these PR-specific names) — recommend re-verifying against the actual system-config screens when that module's iteration runs, rather than concluding fabrication from this iteration alone. Not rewritten in this pass.
- `en/th/inventory/purchase-order/01-data-model.md` §4 + `02-business-rules.md` `PO_VAL_004` (pre-fix): `enum_purchase_order_type` documented with only 2 values (`manual`, `purchase_request`). Prisma schema (`prisma-shared-schema-tenant/prisma/schema.prisma` lines ~152-156) and the frontend `PO_TYPE` enum (`types/purchase-order.ts`) both have 3 values — `manual`, `purchase_request`, `pricelist` — matching a whole 4-step "from-price-list" creation wizard (`routes/procurement/purchase-order/from-price-list/`) that was entirely unmentioned across every page in the module. Fixed in this pass.
- `en/th/inventory/purchase-order/03-user-flow-purchaser.md` (and 6+ other pages) (pre-fix): described PR-to-PO conversion as a "Convert-to-PO workbench" reached from the **Purchase Request** module's Approved-PRs queue, with per-line ticking of PR lines/quantities and a per-line pricelist-deviation-tolerance indicator, grouped by `(vendor_id, currency_id)` only. Actual mechanism (`purchase-order.service.ts` `buildPoGroupKey`, `groupPrForPo`, `confirmPrToPo`; frontend `po-from-pr-dialog.tsx`) is a 2-step dialog **inside the Purchase Order module** operating on whole selected PRs (no per-line controls), grouping server-side by `(vendor_id, delivery_date, currency_id)`. This exactly mirrors the fact already established in the purchase-request module's own resync pass — the PO module's pages had simply not been updated to match. Fixed in this pass across all affected pages.
- `en/th/inventory/purchase-order/02-business-rules.md` `PO_POST_005` (pre-fix, "Reject `in_progress → draft`") and the module-wide "send-back returns the PO to draft" narrative: source shows the `/review` endpoint (`purchase-order.service.ts` `review()`, `workflow-orchestrator.service.ts` `buildReviewWorkflow()`) never sets `po_status` — it only resets `workflow_current_stage`/`workflow_previous_stage`; the PO stays `in_progress`. Separately, the actual "Reject" endpoint (`/reject`) sets `po_status = enum_purchase_order_doc_status.voided` **directly and unconditionally** — a terminal transition, not a return to `draft`, and does not touch `is_active` (also previously misdocumented). Fixed across `02-business-rules.md`, `03-user-flow.md`, and all 6 persona flow pages + 6 corresponding test-scenario pages.
- `en/th/inventory/purchase-order/02-business-rules.md` `PO_AUTH_007`/`PO_POST_010` (pre-fix, "Void PO — Procurement Manager only — allowed from any of draft/in_progress/sent/partial"): no "void" endpoint exists at all in `purchase-order.controller.ts`/`.service.ts`. `voided` is reachable **only** from `in_progress` via `/reject`, performed by whoever holds the current workflow stage (not role-exclusive). Ending a PO from `draft`/`sent`/`partial` uses `/cancel` (`{draft,in_progress,sent}→closed`) or `/close` (`{sent,partial,in_progress}→closed`) — both terminate at `closed`, never `voided`. Fixed across all pages that referenced Manager-exclusive "Void."
- `en/th/inventory/purchase-order/02-business-rules.md` `PO_AUTH_004` (pre-fix, "Procurement Manager approves at a high-value stage when `total_amount` exceeds a tenant threshold"), `PO_XMOD_006` ("deviations above tolerance route to a high-value stage"), and the entire "Procurement Manager configurational surface" (vendor ranking & allocation, Convert-to-PO grouping-rule editor, unit-conversion editor, pricelist-tolerance editor, threshold editor) in `03-user-flow-procurement-manager.md` + `04-test-scenarios-procurement-manager.md`: a repo-wide search of `carmen-turborepo-backend-v2` and `carmen-inventory-frontend-react` for `threshold` found zero relevant hits (only unrelated matches like rate-limiting and low-stock counts); `enum_stage_role` has no amount-aware member; no vendor-ranking, grouping-rule-editor, or bulk-void/close route or component was found. Approval is confirmed to be purely stage-based (`isFinalApproval = workflow_next_stage === '-'` in `purchase-order.logic.ts`). Rewrote both pages, cutting `04-test-scenarios-procurement-manager.md` from ~29 to ~12 scenarios (all fabricated config/bulk scenarios removed, not replaced with new fabrications).
- `en/th/inventory/purchase-order/03-user-flow-finance.md` + `04-test-scenarios-finance.md` (pre-fix, entire pages): described a Finance Manager pre-transmission sign-off stage and a Finance Officer three-way-match (PO ↔ GRN ↔ vendor invoice) / AP-liability-posting flow with specific GL account entries, purchase-price-variance handling, and FX-adjustment postings, citing a test spec `403-po-finance-ap-match.spec.ts`. That spec file **does not exist** in `../carmen-inventory-frontend-e2e/tests/` (checked directly), and a repo-wide search for `three-way`, `vendor_invoice`, `VendorInvoice`, `tb_invoice` returned zero hits in both frontend and backend. `enum_stage_role` has no `finance` member. The only e2e approver fixture (`fc@blueledgers.com`) is independently documented in `04-test-scenarios.md` as the "Procurement Manager (FC Approver)" persona, i.e. current fixtures treat FC and PM as the same actor. Both pages rewritten as short correction notices rather than reconstructed with new unverified mechanics; all cross-references to `PO_POST_008`/`PO_POST_009` (three-way match) and `PO_XMOD_007` (AP/three-way match) in `01a-data-model-comments.md` and `02-business-rules.md` marked "not implemented"; three-way-match/AP references also stripped from `03-user-flow-vendor.md`, `03-user-flow-receiver.md`, `04-test-scenarios-vendor.md`, `04-test-scenarios-receiver.md`. The one real AP-adjacent feature, Credit Note (`tb_credit_note`, real Prisma line numbers and routes, confirmed genuine), is distinct from a three-way match and untouched apart from one softened cross-reference.
- `en/th/inventory/purchase-order/03-user-flow-audit-config.md` + `04-test-scenarios-audit-config.md` (pre-fix, entire pages): described a dedicated "Procurement Activity Queries" audit workspace (query templates, export-approval workflow, case-file notes) and a PO-specific "Configuration workspace" (PO Numbering & Templates, PO Workflow Settings with an approval-delegation-window manager, RBAC & Roles, Integration Settings, PR-to-PO Rules) with ~30 test scenarios. No route or component matching either workspace was found. This is the same narrative pattern the purchase-request module's own resync pass flagged as "unverified/deferred, re-verify against system-config" — this pass's broader, repo-wide searches (not scoped to any one module) found nothing supporting either workspace anywhere in the product, so it is corrected here as confirmed-absent rather than deferred again. Both pages rewritten as short correction pages pointing to the real, generic system-config module for workflow-stage and numbering configuration.
- `en/th/inventory/purchase-order/02-business-rules.md` / `03-user-flow-receiver.md` / `04-test-scenarios-receiver.md` `PO_AUTH_010` (pre-fix, "segregation of duties: GRN poster ≠ PO buyer/transmitter, enforced at GRN creation"): a repo-wide search of both `good-received-note.service.ts` and `purchase-order.service.ts` for `buyer_id` cross-checks or `segregation` found no matching code. Marked unconfirmed everywhere it appeared rather than presented as an enforced rule.
- **Unverified (not fixed, flagged only)** — several pages (e.g. `03-user-flow-purchaser.md`, `04-test-scenarios-purchaser.md`) describe a per-line pricelist-deviation-tolerance indicator and band (e.g. `±5%`) on the PO create/edit form. This could not be directly confirmed or refuted against the current PO form components (`po-form.tsx`, `po-item-fields.tsx`) within this pass's time budget — left as unverified rather than asserted as fact; softened language added at each occurrence found.
- **Unverified (not fixed, flagged only)** — the Close (`/close`) and Cancel (`/cancel`) endpoints' Bruno sample request bodies are both `{}` (empty), yet nearly every persona/test page describes "reason text required" for Close specifically. Whether a reason is captured via a separate comment-creation call outside the close/cancel request body was not confirmed this pass; treat "reason required" claims for Close/Cancel as unverified.
- `en/th/inventory/good-receive-note.md` + every sub-page (pre-fix): the entire module described `commit()` (`saved → committed`) as "the single event that mutates the world" (inventory increment, cost-layer write, PO advance, GL/AP posting), with `save()` (`draft → saved`) as an inert review-only step. Direct reads of `good-received-note.logic.ts` show the opposite: `save()` calls `createInventoryTransactions()` (writes `tb_inventory_transaction` + FIFO/average cost layers) and `updatePurchaseOrderReceiving()` (advances the source PO's `received_qty`/`po_status`) in one transaction; `commit()` only updates `doc_status`/`doc_version` with no further side effect found. Fixed across all 14 EN + 14 TH pages this pass.
- `en/th/inventory/good-receive-note/01-data-model.md` (pre-fix): documented `is_consignment Boolean?` and `is_cash Boolean?` on `tb_good_received_note`. Neither field exists in the current Prisma schema (`packages/prisma-shared-schema-tenant/prisma/schema.prisma`) — both were replaced by `post_type enum_good_received_note_post_type? @default(ap)` (`ap`/`consignment`/`cash`), which itself has no branching logic anywhere in the backend. Also `signature_image_url` → actual column is `signature_file_token`. Fixed in this pass.
- `en/th/inventory/good-receive-note.md` + `03-user-flow-receiver.md` + `04-test-scenarios-receiver.md` (pre-fix): described a per-line `accepted_qty` field distinct from `received_qty` implementing a "quality inspection" gate (accept/reject/partially-accept), with the gap `received_qty − accepted_qty` tracked as a quality-rejection variance. A repo-wide search of the Prisma schema and both frontend and backend source for `accepted_qty` returned zero hits — the field does not exist. A residual instance also survived in `01a-data-model-comments.md`'s prose ("used during inspection to record acceptance/rejection notes") — caught and fixed in the same pass's residual sweep.
- `en/th/inventory/good-receive-note.md` + `02-business-rules.md` (`GRN_POST_006`–`GRN_POST_009`, `GRN_XMOD_007`) + `03-user-flow-finance.md` + `04-test-scenarios-finance.md` (pre-fix, entire Finance-persona pages): described a three-way match (PO ↔ GRN ↔ vendor invoice), an AP-journal posting (`Dr GRN Clearing / Cr AP-Trade`), and a Finance Manager period-close/GRN-Clearing-aging workflow. A repo-wide search of `carmen-turborepo-backend-v2` and `carmen-inventory-frontend-react` for `journal`, `ledger`, `accounts_payable`, `three-way`, `vendor_invoice`, `VendorInvoice`, and `tb_invoice` returned zero hits; `enum_stage_role` has no `finance` member. Mirrors the identical, already-confirmed finding from the `purchase-order` module's own resync pass. `03-user-flow-finance.md` and `04-test-scenarios-finance.md` rewritten as correction pages this pass.
- `en/th/inventory/good-receive-note/03-user-flow-audit-config.md` + `04-test-scenarios-audit-config.md` (pre-fix, entire pages): described a dedicated GRN "configuration console" (lot-number-format editor, RBAC role/approval-threshold panel, tax/currency/reason-code maintenance, PO/Inventory/Finance/Vendor integration-endpoint cutover with dual-write) and an Auditor lot-recall-trace tool with Controller/DPO sensitive-field-export approval. No matching route, component, or endpoint found — same fabrication pattern already confirmed absent for the PR/PO modules' "config workbench". Rewritten as correction pages this pass, pointing to the real generic `system-config`/`master-data`/`access-control` modules for what is actually configurable.
- `en/th/inventory/good-receive-note/02-business-rules.md` `GRN_POST_010` (pre-fix, "voiding a committed GRN is not allowed... post-commit reversal requires Inventory Manager + Finance co-authorisation"): the actual `/void` endpoint (`GoodReceivedNoteService.voidGrnById`) has no `doc_status` precondition at all beyond "not already voided" — it does not check for `committed`, and does not reverse any inventory transaction, cost layer, or PO advance. The frontend hides the Void button once `doc_status = committed` (`canEdit = !isCommitted && !isVoid` in `grn-header.tsx`), which is a client-side-only restriction, not a server-enforced rule. Fixed across `02-business-rules.md`, `03-user-flow.md`, `03-user-flow-receiver.md`.
- `en/th/inventory/good-receive-note.md` (pre-fix, "multi-PO consolidation is supported when vendor, currency, and terms agree"): confirmed in `grn-po-wizard-dialog.tsx` that the multi-PO create wizard groups by vendor then rejects a selection spanning more than one `currency_id` — no check on credit terms was found. "and terms" dropped.
- `en/th/inventory/good-receive-note/02-business-rules.md` `GRN_AUTH_010` (pre-fix, "segregation of duties: Receiver ≠ Purchaser, enforced at commit"): a repo-wide search of `good-received-note.service.ts` and `good-received-note.logic.ts` for a `buyer_id` cross-check found nothing. Marked unconfirmed everywhere it appeared, mirroring the identical treatment already given to the PO module's `PO_AUTH_010`.
- **Unverified (not fixed, flagged only)** — extra-cost allocation (`tb_extra_cost`, `enum_allocate_extra_cost_type` with `manual`/`by_value`/`by_qty`) is real, but no code was found that actually splits an extra-cost amount across GRN lines or feeds the result into FIFO/average cost-layer creation (`inventory-transaction.service.ts`'s cost-layer code reads only the detail_item's own `base_net_amount`). The GRN_CALC_009–012 allocation formulas and the "Last Cost" feed described in `02-business-rules.md` §3 are left in place as a design-intent illustration with an unconfirmed flag added, rather than asserted as verified live behavior.
- **Unverified (not fixed, flagged only)** — lot-number auto-generation is confirmed real (`inventory-transaction.service.ts`, format `RC{YY}{MM}{4-digit sequence}`), but the prior "configurable format (date components, item identifiers, sequential suffix)" wording could not be confirmed; the generator appears to use one fixed pattern. Softened to note the format is fixed, not confirmed configurable.
- **Cross-module residual (flagged, not fixed — out of this iteration's scope)**: `en/inventory/purchase-order/04-test-scenarios-receiver.md` (PO module, RCV-HP-01 row) still references `accepted_qty` in a GRN-side scenario description — the same fabricated field identified and removed in this pass. Left untouched since it belongs to the `purchase-order` module's own pages, but flagged here for that module's next pass. — **resolved in cleanup pass (2026-07-29):** removed every `accepted_qty` reference across `en/th/inventory/purchase-order/03-user-flow-receiver.md`, `04-test-scenarios-receiver.md`, and `03-user-flow-vendor.md` (RCV-HP-01–06, RCV-VAL-04 dropped as no-longer-applicable, RCV-EDGE-02/04), reframed consistent with the GRN module's established finding (received quantities post as-is via GRN `save()`; no accept/reject quantity field exists). EN+TH parity maintained.
- `en/th/inventory/store-requisition.md` + every sub-page (pre-fix, entire module): described a 6-state carmen/docs-style lifecycle (`Draft → Submitted → UnderReview → Approved/PartiallyApproved → InProcess → Fulfilled → Completed`, plus `Rejected`/`Voided` as separate terminal states) and asserted `cancelled` as a real, reachable status via requester-withdrawal or all-lines-rejected auto-cancel. The canonical Prisma `enum_doc_status` (confirmed in `carmen-turborepo-backend-v2/packages/prisma-shared-schema-tenant/prisma/schema.prisma`) has exactly 5 values (`draft, in_progress, completed, cancelled, voided`), and a direct read of `store-requisition.service.ts` shows `cancelled` is never assigned by any method — the only mutation to it is a query-filter exclusion, never a write. Fixed module-wide.
- `en/th/inventory/store-requisition/01-data-model.md` + `03-user-flow-fulfiller.md` + `04-test-scenarios-fulfiller.md` (pre-fix): described the Fulfiller/Store Keeper opening a "lot sub-form" to manually select which lot(s) to release at issue time, including multi-lot splitting and a rotation-policy choice (FIFO-by-expiry vs FIFO-by-receipt). A repo-wide search of `routes/store-operation/store-requisition/` for `lot_no`/`lotNo`/`LotSelect` returned zero hits; `inventory-transaction.service.ts`'s `createFifoConsumption()` / `getAvailableFifoLots()` / `consumeFifoLots()` assign lots automatically. Fixed in this pass.
- `en/th/inventory/store-requisition/02-business-rules.md` `SR_AUTH_011`/`SR_AUTH_012` (pre-fix, "segregation of duties: Requester ≠ Approver" and "Approver ≠ Fulfiller, enforced at commit, relaxable below a tenant value threshold"): a repo-wide search of `store-requisition.service.ts` and `store-requisition.logic.ts` for a `requestor_id`/`approved_by_id` cross-check found nothing, and a search for `threshold` found zero hits anywhere in this module. Marked unconfirmed everywhere it appeared, mirroring the identical treatment already given to PO's `PO_AUTH_010` and GRN's `GRN_AUTH_010`.
- `en/th/inventory/store-requisition/02-business-rules.md` `SR_POST_007` + `01-data-model.md` §5 item 7 (pre-fix): described SR issuance generating Dr/Cr journal entries (destination cost-centre expense or inventory account debited, source inventory account credited). A repo-wide search of `carmen-turborepo-backend-v2` for `journal`/`ledger` against this module and its `tb_inventory_transaction` dependency returned zero hits — mirrors the identical, already-confirmed finding from the `purchase-order` and `good-receive-note` modules' own resync passes. Fixed in this pass.
- `en/th/inventory/store-requisition/03-user-flow-receiver.md` + `04-test-scenarios-receiver.md` (pre-fix, entire pages): described a destination-outlet "Receiver" persona acknowledging physical receipt against `issued_qty`/lot data and raising formal "discrepancy" comment-type events that escalate to an Inventory Controller. A repo-wide search of both `carmen-turborepo-backend-v2` and `carmen-inventory-frontend-react` for `receiver`/`discrepancy` returned zero hits inside the SR module; `enum_stage_role` (`create, approve, purchase, issue, view_only`) has no `receiver` member. Both pages rewritten as correction stubs this pass.
- `en/th/inventory/store-requisition/03-user-flow-audit-config.md` + `04-test-scenarios-audit-config.md` (pre-fix, entire pages): described an Inventory Controller variance-dashboard/pre-commit-admin-void console, a Finance closed-period gate with journal-entry verification, a Sysadmin workflow/RBAC/SoD-relaxation-threshold configuration console, and an Auditor read-only signature-trace tool. `store-requisition.service.ts` has no `void`/`admin-void` method distinct from the ordinary whole-document `reject()`; no `journal`/`ledger`/`period` code exists; no `threshold`/`delegat` hits exist anywhere in this module or the workflow orchestrator. This is the same fabrication pattern already confirmed absent for PR/PO/GRN's audit-config pages. Both pages rewritten as correction stubs this pass.
- `en/th/inventory/store-requisition/04-test-scenarios-approver.md` APR-EDGE-07 (pre-fix, "Approver selects multiple SRs in the list and bulk-approves"): the list's only confirmed bulk action is **Export Selected** (`sr-component.tsx`'s bulk-actions button, `TC-SR-060003`). A real bulk-approve/reject/review action does exist, but only *within* one SR's line table (`sr-item-fields.tsx`'s `handleBulkApprove`/`handleBulkReject`/`handleBulkReview`, applying to multiple selected lines on one document) — not across multiple SR documents. Corrected in this pass; added APR-EDGE-08 to document the real within-document mechanism.
- **Unverified (not fixed, flagged only)** — `701-sr.spec.ts`'s test-plan annotations (not the executed test code) describe several behaviors not confirmed elsewhere in source: a budget-exceeded warning at approval (`TC-SR-070003`), a 50-character-minimum rejection reason (`TC-SR-110002` — the actual reason `<textarea>` has `maxLength={256}` and no minimum found), a "Partially Rejected" status distinct from `enum_doc_status`'s 5 values (`TC-SR-110004`), and an approval-delegation flow (`TC-SR-060005` — the test code only checks a `delegateApprovalsButton` locator exists, `.count() > 0`, without asserting delegation actually occurs). Flagged as illustrative test-plan copy in the affected pages' references rather than removed outright, since the describe-block/TC-ID structure itself is real and should stay mapped.

- `en/th/inventory/inventory/01-data-model.md` § 1 (pre-fix): asserted GL postings from inventory movements land in `tb_jv_header`/`tb_jv_detail` "recorded by the posting engine". The JV models exist in the tenant Prisma schema, but the only non-schema reference anywhere in `carmen-turborepo-backend-v2` is an unrelated doc-type→table string map in `workflows.service.ts`; no inventory/GRN/SR/period-end code writes JV rows. Corrected to the no-GL finding (mirrors PO/GRN/SR). Every `GL: Dr …/Cr …` effect in `02-business-rules.md` § 5 and all worked-example journal entries stripped alongside.
- `en/th/inventory/inventory/02-business-rules.md` `INV_VAL_008` + module-wide (pre-fix): "backdated postings into a closed/locked period are rejected with `Cannot post into period <YYMM>…`". Actual code: `InventoryTransactionService.resolveCurrentPeriod()` stamps every new movement into the current open (or locked) period regardless of document date — backdating is re-dated, never rejected; no such error string exists. Closed-period value is protected separately: `isLotPeriodClosed` makes a Credit Note Amount against a closed-period lot book a current-period `diff_amount` instead of re-pricing. Fixed across 01/02/03*/04*/transaction/period-end pages.
- `en/th/inventory/inventory/*` (pre-fix, module-wide): costing method documented as per-product (`tb_product.costing_method`, FIFO vs WEIGHTED_AVERAGE, with a "blocked while on-hand non-zero" drain rule). `tb_product` has no such column; the method is `tb_business_unit.calculation_method enum_calculation_method {average, fifo}` (default `average`) in the **platform** schema, read BU-wide by `getCalculationMethod(bu_code)`; no drain-guard exists. Fixed everywhere (divergence item 4, `INV_CALC_005/006`, `INV_XMOD_009`, persona pages).
- `en/th/inventory/inventory/02-business-rules.md` `INV_POST_003`/`INV_POST_004`/`INV_POST_005` + `INV_VAL_009`/`INV_VAL_010` + landing + 01 § 5 items 7–8 (pre-fix): direct-location receipts documented as writing "no cost-layer row" and consignment as a memo-only register with COGS/AP-on-consumption. Actual code (`createFifoTransaction`/`createAverageTransaction` + `createDirectExpenseOut`): a `direct` receipt writes the normal inbound layer PLUS an automatic offsetting outbound `issue` layer at the same cost (net zero, excluded from the average); `consignment` has no distinct branch anywhere — it is grouped with `inventory` in every filter that touches it (period-end validate/review, physical-count-period, spot-check, SR from-location). Fixed module-wide.
- `en/th/inventory/inventory/02-business-rules.md` § 4 (pre-fix, `INV_AUTH_001`–`INV_AUTH_006`, `INV_AUTH_009`, `INV_AUTH_010`): a threshold-gated Store-Keeper→Inventory-Controller→Finance→Finance-Manager approval chain over `tb_stock_in`/`tb_stock_out`, an Auditor read-scope role, and an SoD write-off rule. Zero `threshold` hits in the inventory/stock-in/stock-out/period-end services; `enum_stage_role` has no `finance` member; stock-in/stock-out authoring belongs to the inventory-adjustment module's routes, not this module's; no SoD cross-check code. Authorization table replaced with the two real permission keys (`inventory_management.view`, `inventory_management.period_end.view/.execute`) plus unconfirmed-markers; the four persona pages (Finance ×2, Audit/Config ×2) rewritten as correction pages and Store Keeper / Inventory Controller pages rewritten to the real read-only-ledger and period-end-close flows.
- `en/th/inventory/inventory/period-end.md` (pre-fix): prerequisite checklist required every Spot Check completed, every GRN `completed`, adjustments `posted`, credit notes `posted` "driving COST_CALC_005 FX revaluation", plus Snapshot tab, GL handoff, Lock and Reopen buttons (Finance-only). Actual gates (`period-end.validate.ts`): PR/PO/SR `in_progress` with `workflow_next_stage ≠ '-'` block; GRN blocks only when NOT in `{draft, committed, voided}` (a draft GRN does not block); CN blocks only when NOT in `{draft, completed, cancelled, voided}`; required-location physical counts must be `completed`; **spot check is not a gate**. No Snapshot tab / GL handoff / Lock / Reopen exists — the screens are a current-period card + closed-period history and a review page with one Close action. Page rewritten.
- `en/th/inventory/inventory/period-end.md` + `01-data-model.md` § 2.7 / § 5 item 5 + `02-business-rules.md` `INV_POST_009` (pre-fix): period close writes `tb_period_snapshot` rows per `(period, location, product, lot)`. Actual: `tb_period_snapshot.create` exists **only** in `period-end.close-average.helper.ts` — the average-method close writes one row per `(product, location)` bucket; the FIFO close (`period-end.close-transaction.helper.ts`) writes lot carry-over transactions only and never populates the snapshot table. Fixed everywhere.
- `en/th/inventory/inventory/01-data-model.md` § 1/§ 3 + `02-business-rules.md` `INV_CALC_004` (pre-fix): on-hand derived as `last_snapshot.closing_qty + Σ(in−out) since the snapshot`. Actual (`getLocationBalance`): the plain `Σ in_qty − Σ out_qty` over ALL non-deleted layers, no snapshot anchor or date cutoff — the close/open carry-over rows (CLOSE zeroes each lot, OPEN re-adds it) are what keep the plain sum correct across periods. Fixed.
- `en/th/inventory/inventory/transaction.md` (pre-fix): referenced a cached `InventoryStatus.QuantityOnHand` the ledger must reconcile to (no such cache exists), claimed "the ledger NEVER receives a row whose period is closed — fix at source" as a rejection (it's re-dating), and mapped period close to `eop_out + close_period` / open to `eop_in + open_period` (close/open write `close_period`/`open_period` only; `eop_in`/`eop_out` are separate `enum_adjustment_type` variants surfaced via the inventory-adjustments API). Also documented GRN posting at `draft -> completed` (actual: save, `draft → saved`). Fixed; frontend quirk added (PC ref-type pill's `physical_count` value has no `enum_inventory_doc_type` member, so the filter can never match).
- `en/th/inventory/inventory/04-test-scenarios*.md` (pre-fix): cited `720-stock-issue.spec.ts` as covering manual stock-out/FIFO-spanning/negative-balance scenarios — the spec actually navigates the **store-requisition** issue views (list/detail/search/print); and treated `900-period-end.spec.ts` as covering close-workflow scenarios — its detail/close describes are explicitly marked "Feature pending" (placeholder assertions), only the list-page tests are runnable. E2E mapping tables corrected.
- **Unverified / deferred** — `tb_product_location` policy editing (min/max/par/reorder): the table is real but no route/component in this module (or found elsewhere this pass) edits it; `INV_AUTH_004` marked unconfirmed. Re-check when the product / master-data iterations run.
- **Frontend/backend mismatch (flagged as quirk, not fixed in code)** — `pe-review.tsx` computes `is_complete = count > 0 && completeCount === count`, so a module with zero documents in the period renders incomplete and disables Close, while the backend `validatePeriodEnd` counts zero blockers and would allow the close; likewise the review card counts a `draft` GRN against `GRN_COMPLETE = {committed, voided}` while the backend pass-list also accepts `draft`. Documented on period-end.md § 3 and 04-test-scenarios-inventory-controller IC-EDGE-01/02.
- `en/th/inventory/inventory-adjustment.md` + every sub-page (pre-fix, entire module): described a threshold-gated Store-Keeper→Inventory-Controller→Finance approval chain (`draft → in_progress → completed`, auto-approve below a dollar threshold, Controller/Finance approval above it), GL journal postings resolved from `tb_adjustment_type.info.glAccount`, `requiresDocument`/`requiresQualityCheck` reason-code flags, a segregation-of-duties rule (Store Keeper ≠ GRN receiver), and a lot-selection sub-form with FIFO-override for expiry write-offs. Direct reads of `stock-in.service.ts`/`stock-out.service.ts` show `create()` always writes `doc_status: enum_doc_status.completed` (the explicit value is the last key in the object literal, so it wins over the client's `doc_status`) and calls the ledger's `executeAdjustmentIn`/`executeAdjustmentOut` in the same transaction — there is no `in_progress` stage, no threshold check (zero `threshold` hits repo-wide scoped to this module), and no GL/journal code anywhere (zero `journal`/`ledger` hits). `tb_adjustment_type` has only `code`/`name`/`type`/`description`/`is_active`/`note` — no GL-account, document-required, or quality-check field exists on the schema or in `info`/`dimension` JSON usage anywhere. Fixed module-wide; this is the largest fabrication found across all modules resynced so far (the entire lifecycle, not just persona sub-pages).
- `en/th/inventory/inventory-adjustment.md` §1 + `03-user-flow*.md` (pre-fix, new finding this pass): because every persisted document has `doc_status = completed`, the detail screen's `isReadOnly` flag (`doc_status === 'voided' || 'completed'`) is always true, so the Edit button (`isView && !isReadOnly`) never renders and the Void button (which requires `isEdit`, only reachable via Edit) is consequently unreachable through the UI for any real document — confirmed by tracing `ia-form-hero.tsx`'s render conditions and finding `useVoidInventoryAdjustment`/`useUpdateInventoryAdjustment` are called nowhere else in the codebase. The list view's row-menu Delete action renders unconditionally (`actionColumn` called with no `deleteDenied`/`deletePermission`) but always fails server-side (`STOCK_IN_COMPLETED_NO_DELETE`/`STOCK_OUT_COMPLETED_NO_DELETE`, 400) since every document is `completed`. `voidStockIn`/`voidStockOut` additionally set `deleted_at` alongside `doc_status = voided` in the same update, so a voided document (reachable only via direct API call) disappears from list/detail entirely rather than showing a badge, unlike GRN/PO/SR's void pattern. Not previously documented at all (the prior version asserted a fully functional in-app approve/reject/void workflow). Added to `01-data-model.md`, `02-business-rules.md`, `03-user-flow*.md`, `04-test-scenarios*.md`.
- `en/th/inventory/inventory-adjustment/01-data-model.md` §2.5 + `02-business-rules.md` `ADJ_VAL_007`/`ADJ_CALC_003` (pre-fix): described `cost_per_unit`/`total_cost` on `tb_stock_out_detail` as a "preview picked by the costing engine at post time" implying some value is persisted. Direct read of `stock-out.service.ts`'s `create()` detail-insert shows `cost_per_unit`/`total_cost` are never included in the Prisma `create` data at all for stock-out — both columns remain at their schema default of `0` for every stock-out detail row; the frontend's line-item grid hides the `cost_per_unit` column entirely for Stock-Out (`ia-item-table.tsx`'s `visibleDataColumns` filter). Fixed.
- `en/th/inventory/inventory-adjustment/wastage-reporting.md` (pre-fix, entire page): described Wastage Reporting as "not a separate document type — every wastage entry is a `tb_stock_out` row whose `adjustment_type_id` resolves to a wastage-flavoured reason." Direct reads of `types/wastage-reporting.ts` and `hooks/use-wastage-report.ts` show this is a wholly separate frontend feature with its own `WastageReport` type (`wr_no`, `status: pending|approved|rejected` — not `enum_doc_status`, `reason` as a free-text string not an `adjustment_type_id` FK, `items[]` with `unit_id`/`unit_cost`/`loss_value` — no `cost_per_unit` naming, no lot reference) and — more importantly — the entire feature is mock-data-driven: every hook (`useWastageReport`, `useCreateWastageReport`, etc.) reads from/writes to an in-memory `wrMockData` fixture array with a simulated delay, and `use-wastage-report.ts` carries the literal comment `// ── TODO: เปลี่ยนเป็น API จริงเมื่อ backend พร้อม ──` ("switch to the real API when the backend is ready"). No `tb_wastage_report`-style Prisma model, service, or controller exists anywhere in `carmen-turborepo-backend-v2`. This mirrors the identical mock-data-only finding already confirmed for Store Requisition's Stock Replenishment screen in that module's own resync pass. Page rewritten in full.
- **Unverified (not fixed, flagged only)** — whether [physical-count](/en/inventory/physical-count)'s `PhysicalCountService.submit()` (which creates `tb_stock_in`/`tb_stock_out` rows directly at `doc_status = completed` with no `adjustment_type_id`, for count-variance rollup) also calls the ledger's `executeAdjustmentIn`/`executeAdjustmentOut` the way this module's own `create()` does was not traced in this pass — the count-driven rows' detail-creation code was read only far enough to confirm the header/detail insert and the absence of a reason code; whether an inventory-transaction write follows elsewhere in that same method was not confirmed. Flagged for re-verification when the `physical-count` module's own iteration runs.
- **Unverified (not fixed, flagged only)** — the "Insufficient stock" balance guard in `InventoryTransactionService.createAverageConsumption()` was confirmed to reject Stock-Out qty exceeding on-hand for Average-costed products; the equivalent behavior for FIFO-costed products (`createFifoConsumption()`, which delegates to `consumeFifoLots()`) was not independently traced this pass and should be treated as unconfirmed rather than assumed identical.
- **Unverified (not fixed, flagged only)** — `voidStockOut()`'s on-hand-sufficiency check before reversing was not found to have an explicit guard equivalent to `voidStockIn()`'s pre-loop balance comparison in the code read this pass; flagged as an asymmetry between the two void paths rather than asserted as either present or absent.
- **Resolves line 108's flagged-unverified item** — `en/th/inventory/physical-count.md` + every sub-page (pre-fix, entire module): described a three-tier `tb_physical_count_period` → `tb_physical_count` → `tb_physical_count_detail` exercise with a Count Lead/Counter/Approver-Finance/Auditor/Sysadmin persona split, zone-based counter assignment, tolerance-threshold-driven recount, a frozen-vs-live location transaction lock, and a variance rollup that posts with `info.countId`/`info.countPeriodId` linkage and a `COUNT_OVERAGE`/`COUNT_SHORTAGE` reason code "through the same posting workflow as an ad-hoc adjustment." Direct reads of `physical-count.service.ts` show: `submit()` never calls `InventoryTransactionService.executeAdjustmentIn`/`executeAdjustmentOut` (confirming line 108's flagged question — no ledger write occurs), never sets `adjustment_type_id` (stays `null`) or any `info` field on the created `tb_stock_in`/`tb_stock_out` headers/details; `create()` sets `status: in_progress` directly (`pending` is not reachable); zero `tolerance`/`recount`/`zone` hits exist anywhere in this module's frontend or backend; `enum_stage_role` has no distinguishing member and only one permission key (`inventory_management.physical_count`) gates the whole module. Fixed module-wide, EN+TH; `03-user-flow-audit-config.md`/`04-test-scenarios-audit-config.md` rewritten as correction pages.
- `en/th/inventory/physical-count/01-data-model.md` + `physical-count.md` § 3 (pre-fix): described `enum_physical_count_type` (`yes`/`no`) as a per-document frozen-vs-live counting-mode toggle chosen at sheet generation, with `yes` (frozen) blocking GRN/SR/adjustment postings at the location for the count window. The Prisma schema shows this enum is declared on **both** `tb_location` (default `no`, the real field — a per-location "is this location required for the period-end count" admin flag edited on `master-data/location`'s own form, confirmed identical to that module's own already-correct documentation) and `tb_physical_count` (default `yes`, but never set by `create()`, so it never varies in practice); no GRN/SR/stock-in/stock-out service anywhere checks for an in-progress count before posting. Fixed module-wide.
- `en/th/inventory/physical-count/01-data-model.md` + `02-business-rules.md` (pre-fix): documented `on_hand_qty` as a snapshot captured at count-sheet creation (frozen mode) or per-line at count time (live mode). Actual: `create()` seeds every line's `on_hand_qty = 0`; the only code that ever recomputes it is `reviewItems()` (the "Submit for Review" action), which sums `tb_inventory_transaction_detail.qty` live with no date cut-off, for every line, at the moment the counter submits for review — not a value frozen earlier. `save()` (the mid-count "Save" action) recomputes `diff_qty` against whatever `on_hand_qty` is currently stored, which is usually still `0` before the first review. Fixed module-wide.
- `en/th/inventory/physical-count/02-business-rules.md` `PHC_VAL_004` (pre-fix): submit-blocking check described as `product_counted == product_total`. Actual (`physical-count.service.ts` `submit()`): the check is `details.filter(d => d.counted_at == null)`, and only `save()` — not `reviewItems()` — stamps `counted_at`. Fixed, with a flagged (not independently confirmed) edge case: a line typed and carried straight to Submit for Review without an intervening Save can reach the review screen with a real `actual_qty` but a null `counted_at`, and would then block the final Submit.
- `en/th/inventory/physical-count.md` + `01-data-model.md` (pre-fix, not previously documented at all): `tb_physical_count_period.status` must be `counting` before `create()` will accept a new count document (`PHC_VAL_001`), but a repo-wide search found no code path anywhere — frontend or backend — that ever sets a period to `counting`; `physical-count-period.service.ts`'s `findCurrent()` (what the list screen calls) auto-provisions a missing period at `draft`, and the Bruno sample `POST /physical-count-periods` body itself sends `"status": null` (falls back to `draft`). Flagged as a confirmed gap rather than an asserted defect, since a pre-seeded tenant could hold periods already at `counting`. Added to both pages, EN+TH.
- `en/th/inventory/physical-count.md` (pre-fix, not previously documented): `physical-count/new` and `physical-count/:id` are wired into `router.tsx` but render `pc-form.tsx`, a create/edit form whose only field is `department_id` on a type explicitly commented `// Legacy type (used by old form)` in `types/physical-count.ts`; no button in the list screen links to `/new`, and the real create path (`pc-component.tsx`'s `handleAction`) never navigates to `/:id`. Documented as orphaned pre-refactor routes on the landing page and in `03-user-flow.md`.
- **Cross-module residual (flagged, not fixed — out of this iteration's scope)**: `en/th/inventory/master-data/adjustment-type.md` (both locales) still lists physical-count as a consumer of `tb_adjustment_type` ("variance write-on / write-off posts against an adjustment type"). This pass's finding directly contradicts that: the physical-count rollup never sets `adjustment_type_id` on the `tb_stock_in`/`tb_stock_out` headers it creates (stays `null`). Left untouched since it belongs to the `master-data` module's own pages; flagged here for that module's next pass.
- `en/th/inventory/spot-check.md` + every sub-page (pre-fix, entire module): described a three-persona workflow (Inventory Controller / Counter / Auditor+Sysadmin) with counter assignment via `tb_user_location`, a variance-tolerance-threshold-driven recount mechanism, and an automatic rollup of the variance into `tb_stock_in`/`tb_stock_out` (reason codes `SPOT_CHECK_OVERAGE`/`SPOT_CHECK_SHORTAGE`) with GL posting, mirroring the exact fabrication pattern already confirmed for physical-count. Direct reads of `spot-check.service.ts` show `submit()` does nothing but set `doc_status = completed` and stamp `end_date` — its own doc-comment states "Does not create stock-in/stock-out — any follow-up adjustments are user-driven"; a repo-wide search for `stock_in`/`stock_out`/`executeAdjustment`/`journal`/`ledger`/`threshold`/`tolerance`/`recount` scoped to this module returned zero hits. This is a simpler (less automated) mechanism than physical-count's own rollup, which at least creates raw unposted stock-in/out rows — spot check creates nothing at all beyond its own status change. Fixed module-wide, EN+TH; `03-user-flow-audit-config.md`/`04-test-scenarios-audit-config.md` rewritten as correction pages.
- `en/th/inventory/spot-check.md` + `01-data-model.md` (pre-fix, not previously documented): `sc-form.tsx`'s view/edit/delete code paths (`isView`/`isEdit` branches, Edit/Delete buttons, `useUpdateSpotCheck`/`useDeleteSpotCheck` hooks) are unreachable through any real navigation — `ScForm` is instantiated only from the create screen (`spot-check-by-location-content.tsx`), always without a `spotCheck` entity (always "add" mode); `spot-check/:id` (the only other plausible route) always renders `sc-entry-component.tsx` instead. The module's own manual test-case catalog (`docs/test-cases/760-spot-check.md`) contains two scenarios (`TC-SPC-040001`, `TC-SPC-050001`) describing exactly this unreachable view/edit/delete flow — flagged as a catalog/routing mismatch in `04-test-scenarios.md` § 5 rather than corrected in the catalog file itself (out of this pass's scope; the catalog lives in the E2E repo).
- `en/th/inventory/spot-check/01-data-model.md` (pre-fix, not previously documented): `on_hand_qty` on `tb_spot_check_detail` was described as a single book snapshot taken at check time. Actual: `create()` seeds it from live stock at sample time (a real snapshot, unlike physical-count's `0`-seed), but `reviewItems()` (Submit for Review) overwrites it again with a freshly-computed live figure — two snapshots, not one, and the second can differ from the first if other inventory movement occurred at that location in between. Fixed.
- `en/th/inventory/spot-check/02-business-rules.md` (pre-fix, not previously documented): no rule captured that `doc_status` can transition `pending → completed` directly, skipping `in_progress` entirely, since only `saveItems()` (not `reviewItems()` or `submit()`) performs the `pending → in_progress` transition — a user who never triggers a mid-count Save never sees `in_progress` at all. Added as `SPC_VAL_008`'s note and to the state-machine diagrams in `03-user-flow.md`.
- `en/th/inventory/spot-check/02-business-rules.md` `SPC_POST_004` (pre-fix, not previously documented): `reviewItems()` (the Submit-for-Review endpoint) has no `completed`/`void` status guard at all, unlike `saveItems()` and `submit()` which both reject on those statuses. Reopening a finished or voided spot check from the list's History tab (whose click handler routes to the entry screen regardless of `doc_status`) and clicking through to Submit for Review again will silently overwrite every detail row's `on_hand_qty`/`actual_qty`/`diff_qty`/`counted_at` — only the terminal `submit()` call is actually blocked on a repeat attempt. Confirmed gap, not exercised by an automated test; documented as a real, direct reading of the code.
- `en/th/inventory/spot-check.md` (pre-fix): Reset was described per the Bruno collection's own doc comment ("clears all recorded actual quantities and resets the spot check to draft state"). Direct read of `reset()` in `spot-check.service.ts` shows it only sets `doc_status = void` — it does not touch any `tb_spot_check_detail` row, and the target status is `void` (terminal alongside `completed`), not `draft` (which isn't even a value on `enum_spot_check_status`). Fixed; the Bruno doc-comment discrepancy is noted informationally, code is the ground truth used.
- **Unverified / deferred** — `tb_spot_check_comment` (header-level comments) has a real Prisma model and backend controller, but zero frontend hooks in `carmen-inventory-frontend-react` reference it (`use-spot-check-comments.ts` contains only a `TODO` placeholder for the header-level endpoints); only detail-level `tb_spot_check_detail_comment` is wired into the entry screen's notes dialog. Documented as schema-only in `01-data-model.md`; not a fabrication removal since the earlier draft had already (correctly, if incidentally) described spot-check's two comment tables without claiming the header one was in active use.
- **Resolves the inventory iteration's flagged-unverified item (progress-log line 102)** — `en/th/inventory/product/02-business-rules.md` `PRD_AUTH_008` + every `03-user-flow*`/`04-test-scenarios*` page in the product module (pre-fix): claimed a separate "Inventory Controller" persona owns editing `tb_product_location`'s per-location policy (min/max/re_order/par qty), citing `INV_AUTH_004`. Direct read of `pd-location-tab.tsx` (`carmen-inventory-frontend-react/routes/product-management/product/`, part of the same `ProductForm` used for every other product field) shows these are plain editable `<Input>` controls gated only by `isDisabled = mode === "view" || isPending` (`pd-form.tsx`) — no distinct field-level permission, and no separate Inventory-Controller route/component exists anywhere in the product or inventory codebases. The inventory module's own `02-business-rules.md` already independently hedges `INV_AUTH_004` as `*(unconfirmed)*` for the identical reason. Fixed across ~10 files this pass; `en/inventory/inventory/02-business-rules.md`'s own `INV_AUTH_004` row itself was left untouched (out of this iteration's scope — belongs to the `inventory` module) but the finding above should let that module's next pass resolve it directly from "unconfirmed" to "confirmed: no such edit surface, product module's Locations tab is the only one."
- `en/th/inventory/product/01-data-model.md` (pre-fix, not previously documented): `tb_product.code` and `tb_product_category`/`tb_product_sub_category`/`tb_product_item_group.code` were documented as user-typed at create, with server-side uniqueness validation (`PRD_VAL_001`/`PRD_VAL_008`) triggering on manual duplicate entry. Two dated frontend commits confirm this is stale: `carmen-inventory-frontend-react` `f2b5832` (2026-07-14, "feat(product): lock code field, show auto-generated placeholder") disables the product code field unconditionally and strips it from the create payload; `1e6703d` (2026-07-13, "feat: server-assigned category code (auto-generated)") does the identical thing for all three category-tree levels via `category-form.tsx`'s `stripAutoCode`. The backend (`product-category.service.ts`) still accepts a client-supplied code when one is sent (`isAutoCode = !data.code`), so the validation rules remain live for bulk-import/direct-API paths, just unreachable through the interactive forms. Fixed across `01-data-model.md`, `02-business-rules.md`, `03-user-flow-product-admin.md`, `category.md`, and `04-test-scenarios-product-admin.md`.
- `en/th/inventory/product/01-data-model.md` §2.1 (pre-fix): `tb_product`'s field table omitted `doc_version`, even though the schema declares it as literally the first field (`doc_version Int @default(0) @db.Integer`, added 2026-06-12 per `git log -S` across "103 tenant tables lacking it") and `02-business-rules.md`'s own `PRD_AUTH_013` already correctly documents the resulting optimistic-lock behavior (`409 Conflict` on `doc_version` mismatch, confirmed against the Bruno `PATCH /products/:id` sample body). Field added to the data-model table. Separately, `04-test-scenarios-product-admin.md`'s `PA-EDGE-01` directly contradicted `PRD_AUTH_013` within the same module ("no version field... last-write-wins") — rewritten to the confirmed `409 Conflict` behavior.
- `en/th/inventory/product/01-data-model.md` (pre-fix, not previously documented — two new entities): `tb_product_eco_label` / `tb_product_master_eco_label` (added 2026-06-01 per `git log -S`) is a real feature with its own frontend section (`pd-eco-label-section.tsx` + `pd-eco-label-dialog.tsx`, independent CRUD that fires its own API calls outside the product form's Save button) — added as new §2.10. `tb_product_account_code_mapping` (added 2026-06-22 per `git log -S`) is real on the API — `account_codes: { add/update/remove }` is a documented field on the Bruno create/update requests for `config/products`, `config/product-category`, `config/product-sub-category`, `config/product-item-group`, resolved via a nearest-first inheritance cascade (`resolveEffectiveAccountCodes` in `account-code.helper.ts`) identical in shape to the already-documented tax-profile/deviation-limit cascade — but a repo-wide search of `carmen-inventory-frontend-react` found zero references to `account_codes`/`accountCodeMapping` in any component or hook. Added as new §2.11, flagged explicitly as backend/API-only pending a UI surface.
- `en/th/inventory/product/04-test-scenarios-product-admin.md` (pre-fix): the page's own At-a-Glance line claimed E2E coverage "maps to `100-product.spec.ts`, `101-product-category.spec.ts`" — but `100-product.spec.ts` does not exist anywhere in `../carmen-inventory-frontend-e2e/tests/` (confirmed by direct directory listing: only `101-product-category.spec.ts` exists for this module), and the same page's own § 6 References already correctly said so ("there is no direct `100-product.spec.ts`") — an internal self-contradiction. Fixed by removing the false citation from the At-a-Glance line.
- `en/th/inventory/product.md` (pre-fix): At-a-Glance "Key entities/tables" listed `tb_product_unit_conversion` (no such table — the real table is `tb_unit_conversion`, already correctly named throughout `01-data-model.md`) and `tb_product_variant` (no such table exists — `01-data-model.md` § 5 item 1 already documents this as a confirmed divergence: variants are either extra `tb_product` rows or JSON keys in `tb_product.info`). Fixed the entity list and reworded the "Product Variant" key-concept bullet to match the already-correct data-model finding. Also: `category.md` (a real, existing 11th sub-page) was not linked from the landing page's § 7 "Pages in This Module" list (only 10 of 11 sub-pages were linked) — added.
- `en/th/inventory/recipe/**` (module-wide, pre-fix): the recipe module was documented as a fully-realised cost-engineering platform — publish gates, versioning (`tb_recipe_version`), pricing history, per-role RBAC (`recipe:create/edit/publish/archive/edit-cost/approve-menu-link/read-history`), ingredient-line persistence with two-unit conversion, sub-recipe cost cascade, theoretical consumption, menu-item linkage, recipe→SR auto-create, cost-drift events, variance dashboards, clone, scaling calculator, and a `labor_rate` tenant config. Direct read of `micro-business/src/master/recipe/recipe.service.ts` (521 lines — plain CRUD + status timestamps + sub-recipe delete guard), the gateway DTOs, Bruno `config/recipe*`, `constant/permissions.ts` (`operation_plan.view` is an explicit frontend-only placeholder), and `routes/operation-plan/recipe/*` shows none of that machinery exists; the ingredient grid even carries an on-screen banner "Preview only — ingredients are not yet persisted with the recipe". All 19 pages corrected with §1.1-style enforced-rule tables and implementation-status callouts rather than page deletion (rule IDs retained as the design/requirement catalogue).
- `en/th/inventory/recipe/02-business-rules.md` `REC_CALC_009` (pre-fix): documented `actual_food_cost_percentage = cost_per_portion / selling_price × 100`. The only implementation (`use-recipe-cost-calc.ts`) computes `total_ingredient_cost / selling_price × 100` — excludes labor/overhead and ignores yield division. Both formulas now documented side-by-side with the divergence called out (worked example shows 66.15% design vs 46.79% implemented for the same inputs).
- `en/th/inventory/recipe/02-business-rules.md` §3.1 (pre-fix): the worked example contained leftover generation self-talk verbatim in the published page ("= 66.15% ... wait, this is wrong` — let me redo:"). Replaced with a clean design-vs-implemented comparison.
- `en/th/inventory/recipe/01-data-model.md` (pre-fix): `tb_recipe.images Json @default("[]")` and `tb_recipe_preparation_step.images Json` documented as columns — neither exists; images are first-class child tables `tb_recipe_image` / `tb_recipe_preparation_step_image` (`file_token`/`sort_order`/`is_primary`, own REST endpoints + multipart gallery sync). Prep-step "Indexes: none declared" was also wrong (`@@index([recipe_id, deleted_at])`, `@@index([recipe_id, sequence_no])`). `doc_version` was missing from every recipe entity field table. New §2.10 documents the two image tables.
- `en/th/inventory/recipe/04-test-scenarios.md` scenario 14 (pre-fix): claimed "the recipe schema does not have a `doc_version` column on `tb_recipe`" and speculated about `updated_at`-based concurrency. `tb_recipe.doc_version` is literally the model's first field and the update/patch DTOs require it (`where: { id, doc_version }`) — this is the one cross-persona scenario that is fully implemented/testable today. Corrected.
- `en/th/inventory/recipe/**` E2E claims (pre-fix): four pages claimed `701-sr.spec.ts` provides adjacent coverage of a recipe-driven SR auto-create path via an `info.recipe_id` back-reference; no `recipe_id` reference exists anywhere in the SR services (re-confirmed this pass — consistent with the store-requisition iteration's finding), and the module's real (and only) spec, `121-recipe-equipment-category.spec.ts`, was cited nowhere. Fixed in all four + both hub pages; one page's own §6 references self-contradicted its At-a-Glance on this.
- `en/th/inventory/recipe/equipment-category.md` (pre-fix, hedged "verify wiring" now resolved): the `equipment-category` vs `recipe-equipment-category` route duplication is confirmed real — two independent component/type/hook trees (`routes/operation-plan/equipment-category/` nav-linked via `constant/module-list.ts`; `routes/operation-plan/recipe-equipment-category/` routed in `router.tsx` but absent from the nav) resolving to the identical REST endpoint (`API_ENDPOINTS.EQUIPMENT_CATEGORIES` and `RECIPE_EQUIPMENT_CATEGORIES` are the same URL). The only E2E spec targets the non-nav-linked duplicate. Rename fan-out of `tb_recipe_equipment.category_name` confirmed absent (no such code in `recipe-equipment-category.service.ts`); `category_name` refreshes only when the equipment row itself is saved.
- `en/th/inventory/recipe/equipment.md` (pre-fix): documented app-enforced rules `available_qty <= total_qty` and `next_maintenance_date >= last_maintenance_date`, plus a maintenance-"overdue" badge — none exists (checked `recipe-equipment.service.ts`, both gateway zod DTOs, and grep for overdue in the equipment components). Replaced with explicit not-enforced notes.
- `en/th/inventory/recipe/category.md` (pre-fix): documented a tree UI with drag-reparenting, descendant `level` recascade, deep cycle detection, sibling-name uniqueness, and active-parent validation. Actual (`recipe-category.service.ts` + `recipe-category-component.tsx`): flat DataGrid + parent dropdown; `level = parent.level + 1` on the moved row only; only direct self-parenting rejected (`RECIPE_CATEGORY_CANNOT_BE_OWN_PARENT`) — deep cycles NOT detected; `code` unique case-insensitive, `name` not checked at all; parent must merely be non-deleted (`is_active` unchecked). Fixed.
- `en/th/inventory/vendor-pricelist/**` (pre-fix, module-wide): every business-rules/user-flow/test-scenarios page described a 6-phase campaign workflow (template → campaign launch → invitation email → portal submission with auto-save/recall → validation engine + quality score → threshold-routed approval) synthesised from `../carmen/docs/vendor-pricelist-management/design.md`. Direct reads of `price-list.service.ts`, `price-list-template.service.ts`, `request-for-pricing.service.ts`, and `check-price-list.service.ts` show none of it exists: no distinct submit/approve/reject endpoints (status is a plain field on the ordinary update), no quality score (zero `quality_score` hits), no validation engine beyond frontend Zod + a few explicit date checks, no campaign/invitation derived statuses (nothing reads `start_date`/`end_date`/`submitted_at` to compute state), no reminder/dispatch jobs, no automatic `system` comments anywhere. Rewritten module-wide with Confirmed/Design-target labels per rule.
- `en/th/inventory/vendor-pricelist/03-user-flow-vendor.md` + portal pages (pre-fix): described a working vendor portal with currency picker, 3 submission methods, ~30s auto-save, recall-and-edit, and submit-confirmation flows. **Confirmed half-built feature (new finding class):** the frontend portal (`routes/external/pl/`, `hooks/use-price-list-external.ts`) wires Save to `PATCH /api/external/api/pricelist-external/:url_token` and Submit to `POST .../submit`, but a repo-wide search of `carmen-turborepo-backend-v2` finds no route matching `pricelist-external` — only `POST /api/check-pricelist/:url_token` (auto-create/return a zero-priced draft from the template) exists and works. Vendors can view but not persist. Unlike stock-replenishment/wastage-reporting (mock-data with Thai TODO comments), nothing in the frontend flags this as pending.
- `en/th/inventory/vendor-pricelist/02-business-rules.md` `VPL_AUTH_004`/`VPL_AUTH_005`/`VPL_AUTH_010`/`VPL_AUTH_014`/`VPL_AUTH_015` (pre-fix): Manager-only high-value approval threshold, Finance-Manager multi-currency co-signoff, segregation-of-duties, and token revocation — zero `threshold` hits scoped to this module (mirrors the PO module's identical confirmed finding), no `finance` member on `enum_stage_role`, no endpoint ever nulls `pricelist_url_token`. `03/04-finance` and `03/04-audit-config` rewritten as correction pages (8th consecutive module).
- `en/th/inventory/vendor-pricelist/04-test-scenarios.md` (pre-fix): claimed "No dedicated Playwright spec exists for the vendor-pricelist module today; pricelist E2E is a roadmap item." False — `150-vendor.spec.ts` (~875 lines), `159-pl.spec.ts`, and `160-pl-template.spec.ts` exist in `../carmen-inventory-frontend-e2e/tests/`, covering Vendor/Price List/Price List Template CRUD. (Only Request for Pricing and the external portal genuinely lack specs.) Conversely, `159-pl.spec.ts`'s Duplicate and Mark-as-Expired sections describe row actions that don't exist in the real list component (`use-pl-table.tsx` has only Edit + Delete) with loose best-effort assertions — flagged in the wiki as aspirational scaffolding, not corrected in the e2e repo (out of scope).
- `en/th/inventory/vendor-pricelist/01a-data-model-comments.md` + `01-data-model.md` divergence items 6/7/11 (pre-fix): claimed portal/email telemetry is "written to `tb_request_for_pricing_detail_comment` as `system` entries," quality score "lives in `info` JSON," and token policy is "enforced by the application's portal middleware." No service in the module ever creates a comment row, reads/writes `info`, or checks token expiry/IP/session — all three corrected to design-target/not-implemented.
- `en/th/inventory/vendor-pricelist/request-price-list.md` (pre-fix): "Send/resend invitation" as a separate idempotent action (all tokens are minted in the single `create()` call — no send step, and no email dispatch code at all), late-submission rejection after `end_date` (the portal check call never reads `end_date`), template-immutability post-dispatch (`update()` accepts a new `pricelist_template_id` freely), vendor-must-be-active check (absent), and stale Prisma line citations (4039-4176 → actual 4405/4438/4473/4511). All corrected.
- `en/th/inventory/master-data/*.md` (pre-fix, 12 of 14 pages — the largest single class of fabricated claims found in this module): every catalogue entity's "Validation & Errors" table and "Business Rules" §6 asserted a server-enforced "Cannot delete — referenced by X" guard blocking soft-delete of an in-use row. Direct reads of the corresponding `delete()` method in `adjustment-type.service.ts`, `units.service.ts`, `departments.service.ts`, `locations.service.ts`, `delivery-point.service.ts`, `currency.service.ts`, `tax_profile.service.ts`, `credit_term.service.ts`, `extra_cost_type.service.ts`, `credit-note-reason.service.ts` (lives under `procurement/`, not `master/`), `vendors.service.ts`, and `vendor_business_type.service.ts` show the identical pattern in all 12: an unconditional soft-delete (`is_active: false` + `deleted_at`) with zero FK/reference check beforehand. Corrected on all 12 pages, EN+TH.
- `en/th/inventory/master-data/adjustment-type.md` `update()` (pre-fix): "Type cannot be changed" after first use, described as a rejected error. `adjustment-type.service.ts`'s `update()` spreads the client payload directly into the Prisma update with only a code-uniqueness check — no guard on `type`. Fixed.
- `en/th/inventory/master-data/location.md` (pre-fix): "Cannot change location_type after first movement" and "location_type is sticky once used — journal entries would be corrupted." `locations.service.ts`'s `update()` accepts a new `location_type` freely with no prior-movement check; separately, "posts to inventory asset GL" / "posts straight to department expense" restated the already-confirmed-absent GL/journal-posting system (established by the `inventory` module's own resync pass) — corrected to describe the real inventory-transaction-layer behavior (`direct` writes an offsetting layer, `consignment` has no distinct code path). Fixed.
- `en/th/inventory/master-data/currency.md` (pre-fix): "ISO code not found", "exchange rate must be > 0", "cannot inactivate — set as BU default", and "cannot delete — referenced by documents/pricelists" all asserted as enforced server errors. `currency.service.ts`'s `create()`/`update()`/`delete()` and the `CurrencyCreateSchema`/`CurrencyUpdateSchema` Zod DTOs contain none of these checks — no cross-schema ISO lookup, no positive-value check, no BU-default check, no reference check. Fixed.
- `en/th/inventory/master-data/exchange-rate.md` (pre-fix, largest single-page correction in this module): "Exchange rate must be > 0", "effective date too far in future (configurable horizon)", "currency must be active", and "period is closed" all asserted as server-enforced validation. `ExchangeRateCreateSchema`/`ExchangeRateUpdateSchema` (`exchange-rate.dto.ts`) validate only that `currency_id` resolves to an existing row (no `is_active` filter) — no other checks exist in `exchange-rate.service.ts`. Also: "Refresh FX on a draft PO — PR and GRN do NOT have this action" — a repo-wide search for "Refresh FX" found zero hits; PR/PO/GRN all identically auto-populate the rate field from `tb_currency.exchange_rate` when the currency is (re)selected, then leave it freely editable — there is no distinct action. **New confirmed-broken-feature finding:** the list screen's "Update" bulk-sync button (`useExternalExchangeRates` hook) calls `GET /api/exchange-rate?base=<code>`, a legacy Next.js API route with no equivalent in the current NestJS gateway (only the CRUD `api/config/:bu_code/exchange-rates` controller exists) — the hook's own code comments confirm this is a known carry-over that degrades to an "endpoint is not available" error on static hosting. No `micro-cronjobs` FX job exists either (zero repo-wide hits for `exchange`/`fx`/`currency`). Fixed.
- `en/th/inventory/master-data/extra-cost-type.md` (pre-fix): described a `manual` allocation mode as one of three user-selectable options, with server-side validation of "missing allocation amount," "allocated sum doesn't equal parent" (rounding-tolerance reconciliation), and a posted-GRN edit lock specific to extra costs. Direct reads of `grn-extra-cost-fields.tsx` show the mode picker only offers `by_qty`/`by_value` (never `manual`, though the enum/DTO permit it) and every cost line always has an independent, manually-typed `amount` field with no wiring to the selected mode; `good-received-note.service.ts`'s extra-cost create/update paths have no sum-reconciliation check and no extra-cost-specific edit lock (mirrors the good-receive-note module's own already-unconfirmed flag on this exact point). Fixed.
- `en/th/inventory/master-data/vendor-business-type.md` §5.2 (pre-fix): described `tb_vendor` as holding two references to this entity — a `business_type_id` FK (a single "primary" type) alongside the `business_type` JSON array. A direct read of the current tenant schema's `tb_vendor` model found **no `business_type_id` column and no `@relation` to `tb_vendor_business_type` at all** — the only link is the `business_type` JSON array, a loose, app-enforced-only reference with no FK and no `onDelete` behavior. Also fixed: `doc_version`'s documented type (`Decimal @db.Decimal`, actually `Int`) and the Prisma source citation (was `../carmen/docs/prisma-schema/schema.prisma`, a stale/non-canonical copy — corrected to the real `carmen-turborepo-backend-v2` tenant schema). Fixed.
- **Resolves the physical-count pass's flagged cross-module residual (progress-log line 117)** — `en/th/inventory/master-data/adjustment-type.md` §1/§7 (pre-fix): listed physical-count and spot-check as consumers of this table ("variance write-on/write-off posts against an adjustment type" / "spot-check variances follow the same posting path"). Confirmed: physical-count's variance rollup never sets `adjustment_type_id` (stays `null`); spot-check never creates a `tb_stock_in`/`tb_stock_out` row at all. Fixed on `adjustment-type.md` and the landing page's Cross-Module-Dependencies bullets for both modules.
- `en/th/inventory/master-data.md` §1 (pre-fix): listed "pricelist templates" as living in the master-data module. Pricelist templates are owned by the `vendor-pricelist` module's `vendor-management/price-list-template` route, not `/config/*`. Fixed; a new scope note documents the confirmed 14-screen `/config/*` route tree and the two structural mismatches (`vendor`/`business-unit` documented here despite living outside `/config/*`; `certification`/`eco` real `/config/*` screens with no wiki page — a pre-existing, already-logged 2026-06-18 gap, not authored this pass).
- **Systemic omission (9 of 14 pages)** — `doc_version` (added 2026-06-12 across 103 tenant tables) was missing from the §5 Data Model field table on `unit.md` (both `tb_unit` and `tb_unit_conversion`), `department.md`, `location.md`, `delivery-point.md`, `currency.md`, `exchange-rate.md`, `tax-profile.md`, `adjustment-type.md`, `credit-note-reason.md`, `vendor.md`, and `business-unit.md`, despite some already documenting the resulting optimistic-lock behavior in §6 prose. Added everywhere; `exchange-rate.md`'s and `business-unit.md`'s business-rules text updated to match.
- **Stale citations module-wide** — every one of the 12 tenant-schema-backed pages' §8 "Frontend" reference pointed at `../carmen-turborepo-frontend/apps/web/app/(app)/configuration/<entity>/`, a repo/path that does not exist (confirmed: no `carmen-turborepo-frontend` sibling directory; the real frontend is `carmen-inventory-frontend-react`). Corrected to `../carmen-inventory-frontend-react/routes/config/<entity>/` (or `routes/vendor-management/vendor/` for vendor.md). Prisma line-number citations were also stale on every page (schema growth since original 2026-05-16 authoring shifted every model's line range) — recomputed against the current schema and corrected across all 12 tenant tables + 2 platform tables cited.
- `en/th/inventory/system-config/dimension.md` (pre-fix): described a full Sysadmin CRUD screen ("System Config → Dimensions → New", a display-in matrix editor, a `lookup` value-catalogue curator) for `tb_dimension`/`tb_dimension_display_in`. No route exists in `routes/router.tsx`, no `dimension.service.ts`/`.controller.ts` exists in `carmen-turborepo-backend-v2`, and the only non-schema reference to `tb_dimension` is a comment sub-feature (`dimension-comment.service.ts`) that assumes rows already exist by some unobserved means. Rewritten as a correction page (recipe-module precedent).
- `en/th/inventory/system-config/menu.md` (pre-fix): described `tb_menu` as the live navigation registry the app shell reads at boot/login. `tb_menu` has **zero references anywhere** outside its own Prisma model and migration SQL — no service, no controller, nothing reads it. The real navigation is a static, code-defined tree (`landing-types.ts`'s `CHAPTERS` for System Admin; an equivalent static structure for the main sidebar). Rewritten as a correction page.
- `en/th/inventory/system-config/application-config.md` (pre-fix): described a general "System Config → Application Settings" screen for browsing/editing any tenant config key, and an App ID `app-config.upsert` gating access. No such screen exists in `routes/router.tsx`; the real consumers are `use-app-config.ts`'s `useAppConfigByKey`/`useUpsertAppConfig`, called only by name-specific features (config-email's `report_email` key, workflow's signature-candidates screen). Separately, `config_app-config.controller.ts` carries no `AppIdGuard`/`RequirePlatformPermission` at all — only `KeycloakGuard`. Both claims corrected.
- `en/th/inventory/system-config/query-dataset.md` (pre-fix): cited a specific frontend implementation (`routes/system-admin/query-dataset/page.tsx`, `_components/query-dataset-component.tsx`, `hooks/use-sql-query.ts`, a frontend `sql-validator.ts` mirror) and described the `execute` endpoint as read-only (`SELECT`/`WITH`/`SHOW`/`EXPLAIN`/`DESCRIBE` only, single-statement) gated by three App IDs (`sql-query.execute`/`.save`/`.drop`). None of the cited frontend files exist anywhere in `carmen-inventory-frontend-react` (confirmed by repo-wide search). Direct read of `SqlQueryService.execute()` shows `validateSqlSafety(sql_text, { allowMultiple: true, allowDangerous: true })` — it allows any statement type including DDL/DML and multiple statements; the controller's own Swagger description confirms this ("Runs any SQL... one or more statements"). Only one route (`execute`) carries a permission guard (`sql_workbench.manage`); `save`/list/get-definition/drop have none. All four claims corrected.
- `en/th/inventory/system-config/document.md` §5 (pre-fix): documented `tb_attachment` (tenant schema) as "the file metadata mirror" backing this module, with a 10 MB cap and MIME allow-list "enforced client-side *and* server-side" / "`FILE_SERVICE` performs defence-in-depth MIME sniffing server-side." `tb_attachment` has zero non-schema code references anywhere in `carmen-turborepo-backend-v2` — the real registry is `tb_file_tag` in a separate `prisma-shared-schema-file` database, written by the `micro-file` microservice via MinIO; `files.service.ts`'s `uploadFile()` has no server-side size or MIME check at all. Both claims corrected.
- `en/th/inventory/system-config/doc-version.md` §5 (pre-fix): described `tb_attachment.doc_version` as an active "re-render counter... incremented each time the owning module re-renders the file, and it retains older versions for audit." Zero code anywhere increments, reads, or otherwise touches this field — `tb_attachment` itself is a dead table (see the `document.md` finding above). Corrected to state the field is inert schema, not a working mechanism of either kind.
- `en/th/inventory/system-config/period.md` §2/§4 (pre-fix): described dedicated "Close", "Lock", and "Reopen" actions (the latter requiring an audit reason and writing `tb_period_comment`) and a "Snapshot" detail tab. The real `/system-admin/period` screen (`period-component.tsx` + `period-dialog.tsx`) is a plain CRUD list + edit dialog where status is one `<Select>` field among several, submitted identically to any other field edit; no period-detail component, confirmation prompt, or snapshot viewer exists anywhere in the directory. Corrected.
- `en/th/inventory/system-config/running-code.md` §2 (pre-fix): described an "inline segment editor + preview" with per-segment (`A`/`B`/`C`) width controls and a live "preview next three numbers" field. The real `running-code-dialog.tsx` is a free-text `type` input plus a raw JSON `config` textarea with only a pretty-print "Format" button — no segment UI or preview exists. The underlying `config` JSONB pattern language itself (§5) is real and confirmed via `running-code.helper.ts`'s own unit tests — only the admin-screen UX description was fabricated. Corrected.
- `en/th/inventory/system-config.md` §4 Cross-Module Dependencies (pre-fix): described GRN, inventory-adjustment, physical-count, spot-check, and vendor-pricelist as each having an "optional" or gated workflow attachment via `tb_workflow`. `enum_workflow_type` has exactly three members (`purchase_request`, `store_requisition`, `purchase_order`) confirmed against the Prisma schema — none of those five modules can attach a workflow at all. Corrected module-wide on the landing page; each affected module's own already-completed pass is unchanged by this fix (out of scope to re-open).
- **FORWARD-FLAG for the reporting-audit + dashboard iterations (widget tables):** `tb_widget_workspace`, `tb_widget_dashboard`, `tb_widget_dashboard_item`, and `tb_widget_default_layout` **do not exist in any Prisma schema** (tenant/platform/file — zero `model tb_widget_*` declarations) nor in micro-data/micro-report. The real widget tables are `tb_dashboard_bu_widget` (tenant schema line ~6185; `dataset_id`+`widget_type`+`order_index`+`params`, BU-scoped) and `tb_dashboard_personal_widget` (line ~6205; same + `user_id`), served by `micro-cluster`'s `dashboard-bu-widget.service.ts`/`dashboard-personal-widget.service.ts` and micro-data's `model/dashboard.go` — widget rows store a `dataset_id` reference into the code-registered catalog, **not** saved SQL text. Also stale: `tb_report_job` is now line ~6101 and `tb_report_schedule` ~6135 (old cites ~5652/~5685 point at recipe/equipment fields). Fixed this pass in `system-config/query-dataset.md` + `dashboard-dataset.md` (EN+TH). Still wrong and needing the same correction in their own iterations: `reporting-audit/widget.md` (documents `tb_widget_workspace` in its At-a-Glance, §1, §2 task table, §5.3 field table, and §8 line cites, plus the nonexistent `tb_widget_dashboard`/`tb_widget_default_layout`) and `dashboard/widget-workspace.md` (its "Naming note" contrasts the two nonexistent table families as if both were real).
- **FORWARD-FLAG for the reporting-audit iteration:** `en/th/inventory/reporting-audit/attachment.md` still describes `tb_attachment` as "the shared binary metadata catalogue" for every module's file uploads, with a `doc_version` "re-render counter... incremented on re-render; older versions retained for audit" and Prisma citation "lines ~4427-4449". All three claims were disproven in this system-config pass (see the `document.md` and `doc-version.md` bullets above): `tb_attachment` has **zero non-schema code references anywhere** in `carmen-turborepo-backend-v2` — a dead table; the real file registry is `tb_file_tag` in the separate `prisma-shared-schema-file` (`CARMEN_FILE`) database, written by the `micro-file` microservice over **MinIO**; nothing increments or reads `tb_attachment.doc_version`; and the current `tb_attachment` model sits at lines ~4797-4819. `system-config/document.md` + `doc-version.md` no longer cross-link to `reporting-audit/attachment` for this reason. attachment.md needs the same correction when the reporting-audit module iteration runs.
- **SETTLES the purchase-request pass's deferred item (flagged in that module's row and re-flagged for this iteration):** `en/th/inventory/purchase-request/03-user-flow-audit-config.md` + `04-test-scenarios-audit-config.md` describe a sidebar "**Audit** workspace → **PR Activity Queries**" (query-builder with audit templates like "All PRs voided in period", a per-query audit log of who ran what) and a sidebar "**Configuration** workspace" with distinct pages — **PR Workflow Settings** (stage editor + Threshold Rules panel + preview forecasting affected PRs + `effective_from` versioning + configuration audit log), **PR Type Defaults**, **Delegation Rules**, **Tax Codes**, **Currency Rates** — plus a "PR → Activity Log tab" per-document entry point. **Verdict after reading every `/system-admin` route in `router.tsx` and every screen component in `routes/system-admin/`: NO PR-specific audit or config surface exists.** Mapping of each concept to what actually exists: "PR Activity Queries" → nearest is the generic `/system-admin/activity-log` (`activity-log-component.tsx`: a plain list with `action`/`entity_type`/`user` multi-select filters — no query builder, no templates, no PR scoping, no query-of-queries audit log; `/system-admin/user-activity` is the sibling per-user view); the "PR detail → Activity Log tab" does not exist either (no activity-named file in `routes/procurement/purchase-request/` — the PR form has a comment sheet only). "PR Workflow Settings" → the generic `/system-admin/workflow` editor (real: stages, SLA, assigned users, hide_fields for `workflow_type = purchase_request` rows — but no Threshold Rules panel, no preview/forecast, no `effective_from` versioning, no configuration audit log; threshold + delegation mechanisms were already confirmed absent repo-wide in the PO and SR passes). "PR Type Defaults" → no equivalent anywhere. "Delegation Rules" → no equivalent anywhere. "Tax Codes" → the generic `/config/tax-profile` master-data screen (flat single `tax_rate`, no effective-dated rate rows). "Currency Rates" → the generic `/config/currency` + `/config/exchange-rate` master-data screens (no `effective_from` versioning; see the master-data pass's broken-FX-sync finding). "Users & Roles" → the real `/system-admin/user` + `/system-admin/role` screens (access-control module). **Follow-up needed:** rewrite the two PR audit-config pages as correction pages (the established pattern already applied to that module's Purchaser/PM personas and to every subsequent module's audit-config axis) with the generic-screen mapping above. — **resolved in cleanup pass (2026-07-29):** `en/th/inventory/purchase-request/03-user-flow-audit-config.md` + `04-test-scenarios-audit-config.md` rewritten as correction pages using the generic-screen mapping above (`/system-admin/activity-log`, `/system-admin/workflow`, `/config/tax-profile`, `/config/currency` + `/config/exchange-rate`, `/system-admin/user` + `/system-admin/role`), mirroring the `purchase-order` module's own already-corrected audit-config pattern. Spot-verified all six routes still exist in `router.tsx` and each screen's shape (flat activity-log filter list, generic workflow stage editor with no threshold/delegation code, flat `tax_rate`, no `effective_from` on currency/exchange-rate) before writing. EN+TH parity maintained. **New finding surfaced during verification (not fixed, out of this cleanup pass's scope):** `PR_AUTH_005` (amount thresholds) and `PR_AUTH_006` (delegation) in `02-business-rules.md` are themselves unconfirmed — a repo-wide search for `threshold`/`delegat` in both frontend and backend found no amount-threshold-routing or approval-delegation-window mechanism anywhere, generic or PR-specific (consistent with the PO/SR passes' identical findings already noted above). These rule IDs are still cited as live behavior, unhedged, in `02-business-rules.md` itself and in `03-user-flow-procurement-manager.md`, `04-test-scenarios-procurement-manager.md`, the module landing page (`purchase-request.md`), the cross-persona overview (`04-test-scenarios.md` X-PR-01/X-PR-05/X-PR-08 and its Audit/Config summary line), and — confirmed in review — the module's own persona-index page, `en/th/inventory/purchase-request/03-user-flow.md:69` (Persona Index entry: `"Audit / Config ... System Administrator (workflow stage configuration, threshold setup, delegation rules)"` in both locales, identical claim, not previously enumerated). The two rewritten audit-config pages flag both rule IDs as unconfirmed and do not rely on them; a follow-up pass is needed to correct the remaining PR-module pages that still assert threshold/delegation as confirmed live behavior. — **resolved (2026-07-29, follow-up pass):** Independently re-verified before editing rather than trusting the prior finding at face value, and the prior finding turned out to be half-wrong. `PR_AUTH_005` (amount thresholds) is **not** absent — the earlier searches only grepped for the literal words `threshold`/`delegat` and missed the actual implementation, which uses different terminology entirely: the `/system-admin/workflow` screen has a **Routing** tab (`wf-routing.tsx`, tab id `routing` in `wf-detail.tsx`) that lets any workflow define `data.routing_rules` — a condition (`total_amount`/`department`/`category` compared via `eq`/`gt`/`lt`/`gte`/`lte`/`between`) plus an action (`NEXT_STAGE`/`SKIP_STAGE`) — evaluated live by `evaluateCondition`/`findNextStep` in `apps/micro-business/src/master/workflows/workflows.navagation.service.ts`, called from `WorkflowOrchestratorService.buildSubmitWorkflow`/`buildApproveWorkflow` on every submit and approve action. For PRs specifically, `pr-workflow.mapper.ts` (`prToWorkflowDocument`) emits `total_amount` as the sum of `purchase_request_detail[].total_price` into `navigation_request_data`, so a routing rule keyed on `total_amount` is live, tested (`pr-workflow.mapper.spec.ts` asserts `total_amount` emission), and generic to PR/PO/SR workflows alike — confirmed by reading the component, the constants file (`wf-routing-constants.ts`), the navigator service, the orchestrator, and the PR mapper directly, not by keyword grep. `PR_AUTH_006` (delegation) **is** absent, confirmed by both the original keyword grep and a second, broader grep for `reassign`/`proxy`/`substitute`/`backup approv`/`out.?of.?office`/`acting.?approv`/`deputy` across both repos (excluding `dist/`, generic Prisma/JS "delegate" patterns, and SQL keyword matches) — no user-to-user approval hand-off mechanism exists anywhere; `assigned_users` on a stage is a static list, not a delegation window. **Fixed:** every `PR_AUTH_005`/`PR_AUTH_006` citation across the module (EN+TH, 11 file pairs: `purchase-request.md`, `02-business-rules.md`, `03-user-flow.md`, `03-user-flow-approver.md`, `03-user-flow-procurement-manager.md`, `03-user-flow-audit-config.md`, `04-test-scenarios.md`, `04-test-scenarios-approver.md`, `04-test-scenarios-procurement-manager.md`, `04-test-scenarios-requestor.md`, `04-test-scenarios-audit-config.md`) — threshold claims left as confirmed live behavior (with the Routing-tab citation added where useful), delegation claims hedged as "(unconfirmed — no delegation code found)" per the module's established correction style, including six Approver-page scenario rows (`APP-HP-08`, `APP-PERM-07`, `APP-EDGE-04` marked not-executable; `APP-VAL-08`, `APP-EDGE-03` reworded to drop the delegation premise) and the `PR_AUTH_001`/`PR_VAL_003` "delegated authority" carve-outs. The two audit-config pages rewritten in the prior cleanup pass had themselves asserted "no amount-threshold routing mechanism... found" — that overcorrection was fixed too (split into a confirmed-threshold / unconfirmed-delegation distinction, permission-matrix row split accordingly). Frontmatter `date` bumped, `dateCreated` untouched, drift check clean, EN/TH parity maintained (22 files total). — **Small addition (2026-07-29, PO+SR follow-up pass):** `PR_AUTH_005`'s condition-field list (`total_amount`/`department`/`category`) was missing the nuance that `category`-based rules silently never match — no PR/PO/SR mapper emits a document-level `category` field into `navigation_request_data`, and a missing condition field evaluates to `false` rather than erroring (source comment, `workflows.navagation.service.ts` ~L455-456). Added to `02-business-rules.md` `PR_AUTH_005` and, since they also enumerate the condition fields, to `03-user-flow-audit-config.md` and `04-test-scenarios-approver.md` (APP-PERM-07) — EN+TH, 3 file pairs. Also noted the engine's undocumented `in`/`not_eq` operators (beyond the Routing-tab UI's `eq`/`gt`/`lt`/`gte`/`lte`/`between`) in the same `PR_AUTH_005` entry.
- **SETTLES the dashboard-iteration forward-flag** (raised in the row above; corrected a second time after reviewer catch — see next bullet): `en/th/inventory/dashboard/widget-workspace.md`'s "Naming note" contrasted `tb_widget_dashboard`/`tb_widget_dashboard_item`/`tb_widget_default_layout` against `tb_widget_workspace` as if all four were real, currently-existing backend tables. Confirmed via `carmen-turborepo-backend-v2` Prisma schemas and migrations: **all four are gone or never existed** — `tb_widget_dashboard`, `tb_widget_dashboard_item`, and `tb_widget_default_layout` never appear in any Prisma schema (tenant/platform/file); `tb_widget_workspace` *did* briefly exist (created by migration `20260512180928_widget_system_replace_dashboard`, tenant schema) but was dropped by migration `20260521040013_remove_widget_system` (`DROP TABLE IF EXISTS "tb_widget_workspace" CASCADE`, along with `tb_widget_dashboard`/`tb_widget_dashboard_item`/`tb_widget_default_layout`/`tb_widget_comment` in the same migration) — zero references remain in any current `schema.prisma` or `.ts`/`.go` source, and no data-explorer/saved-query feature exists to have used it. The real, currently-live tables are `tb_dashboard_personal_widget` (tenant schema ~line 6205 — also duplicated in the platform schema ~line 934, but the live `micro-cluster` `DashboardPersonalWidgetService` currently reads/writes via the **platform** `PRISMA_SYSTEM` client despite a tenant-schema code comment claiming the table was "relocated... to live with the BU's own data" — the migration to a tenant-scoped client does not appear complete; noting both facts rather than picking one) and `tb_dashboard_bu_widget` (~6185/~911, BU-scoped, powers the *separate*, out-of-module per-module landing dashboards under Procurement/Inventory-management/Vendor-management/Product-management/Operation-plan/Config — confirmed via `procurement-dashboard.tsx` → `useProcurementWidgets` → `GET api/:bu_code/dashboard-widgets/bu`). Fixed in this pass; dropped the stale `reporting-audit/widget` cross-link since that page documents this same fabricated table family and needs its own correction pass.
- **Reviewer catch on the above (2026-07-16, second pass):** the first fix still mis-stated `tb_widget_workspace` as "a distinct, separate concept (per-user saved data-explorer queries)... see reporting-audit/widget" — i.e. re-asserted it as a real, currently-existing table with a real feature behind it, contradicting the "none of the three exist" framing next to it. Verified both migrations directly (see above): the table was real for 9 days (2026-05-12 to 2026-05-21) then dropped with the rest of the widget subsystem; no data-explorer/saved-query feature exists anywhere in the codebase. Rewrote the Naming note (EN+TH) to state all four tables are gone/never-existed and removed the `reporting-audit/widget` cross-link for this specific claim.
- **En/th/inventory/dashboard/{main,pr,po,grn,inventory,sr,my-pending,my-approval}.md (pre-fix, all 8, both locales):** described a sidebar group of six named per-domain dashboard pages (`/dashboard/pr`, `/dashboard/po`, `/dashboard/grn`, `/dashboard/inventory`, `/dashboard/sr`, `/dashboard/main`) plus two "companion widget" sections (`my-pending`, `my-approval`) allegedly rendered on the live `/dashboard` page — the latter two pages went further and claimed outright **"Status: Live"**. None of the eight ever had a router entry: `routes/router.tsx` has registered exactly one `dashboard` path since before the most recent history, always resolving to the same greeting + "Saved Widgets" component seen today. The demo files existed on disk as dead, unrouted code until commit `03891e3d` (`carmen-inventory-frontend-react`, "refactor(dashboard): convert to idiomatic structure, drop dead demo code", 2026-06-27) deleted all 8 plus their `mock/` fixtures, with a commit message stating outright they had "no importers anywhere." Fixed in this pass — all 8 pages (EN+TH) rewritten with an "Implementation status" callout and corrected At-a-Glance; original tile/table content kept as explicitly-labeled historical reference (no page deleted, per the 9/9 file-count constraint).
- **Residual echo found outside this module's scope (not fixed here):** `en/th/inventory/access-control/permission.md` (`my-approve.findAll` row) still links to `[dashboard/my-approval]` as "the cross-module approval inbox" — that page is now flagged historical/never-live in this pass. Access-control is a separate, already-iterated module in this resync; leaving the fix to whoever re-touches that file rather than editing out-of-scope.
- **SETTLES the reporting-audit forward-flag on widget tables (line 166):** `reporting-audit/widget.md` (EN+TH) documented `tb_widget_workspace`/`tb_widget_dashboard`/`tb_widget_dashboard_item`/`tb_widget_default_layout` throughout its At-a-Glance, §1, §2 task table, §5 field tables, and §8 citations. Re-verified per the established facts (all four gone-or-never-existed; real tables `tb_dashboard_bu_widget` line ~6178 and `tb_dashboard_personal_widget` line ~6198) and rewrote the page in full. **New finding this pass, not previously logged:** the widget CRUD host has moved *again* since the dashboard-iteration note (line 169) was written — `micro-data/controller/dashboard_controller.go`'s own doc comment now states its BU + personal widget CRUD handlers are called "instead of micro-business (datasets) / micro-cluster (widgets)", and `apps/micro-cluster/src/cluster/dashboard-widget/` no longer has live source (only stale `coverage/` HTML remnants) — while the backend-gateway's `DashboardPersonalWidgetsService`/`DashboardBuWidgetsService` are confirmed thin HTTP proxies to micro-data (`fetch` to `DATASET_SERVICE_HOST`), not TCP calls to micro-cluster. This means `dashboard/widget-workspace.md`'s own §8 reference ("served by `micro-cluster`'s `DashboardPersonalWidgetService`/`DashboardPersonalWidgetController` (TCP `dashboard-personal-widget.*`)") is now stale and needs a follow-up correction to micro-data — flagging rather than editing since that file's module (dashboard) already completed its iteration (commit `50e072a`). `reporting-audit/widget.md` itself was written fresh against the current (micro-data) facts, so no forward-flag remains for this page.
- **SETTLES the reporting-audit forward-flag on `tb_attachment` (line 167):** `reporting-audit/attachment.md` (EN+TH) re-verified against the already-established `document.md`/`doc-version.md` findings — `tb_attachment` has zero non-schema code references (confirmed again this pass at its current line ~4790), the real registry is `tb_file_tag` (`prisma-shared-schema-file`, `CARMEN_FILE`) written by `micro-file`'s `files.service.ts` over MinIO. Rewrote the page in full to match `document.md`'s established framing while keeping its distinct §1.1 comment-attachment-shape content (retargeted from "tb_attachment" wording to the real `tb_file_tag`-backed mechanism).
- **New finding this pass — `tb_report_schedule` (tenant) is dead, same pattern as `tb_attachment`:** a repo-wide search found zero code references to `tb_report_schedule` anywhere in `carmen-turborepo-backend-v2/apps` outside its own Prisma declaration. `reports.service.ts`'s own code comment confirms the real backing: "micro-report no longer owns schedules; they live in micro-cronjob as rows in the Cronjob table with `job_type="report"` and `source_service="micro-report"`" — a **fourth, separate database** (`"CRONJOBS"."Cronjob"`, micro-cronjobs' own Postgres schema), not the tenant or platform Prisma schema. `reporting-audit/schedule.md` (EN+TH) rewritten in full: the real screen is create/list/delete only (no detail page, no cron-expression builder, no edit, no pause toggle, no test-run, no typed email/user/sftp recipients — confirmed absent by reading `schedule-component.tsx`/`create-schedule-dialog.tsx`); delivery is always `format: "viewer_url"` from the current create-dialog (hardcoded), so the "file" (PDF/Excel/CSV render) delivery path in the executor is confirmed unreachable from any UI.
- **New finding this pass — the report generation pipeline never populates `tb_report_job` under any currently-reachable path:** the report list's "Run" action (`POST .../reports/viewer`) and every module's "Print" button (`lib/print-document.ts`) both resolve straight to a rendered viewer URL via the `viewReport` handler — neither calls `generate-async` (the only endpoint that writes `tb_report_job`). Scheduled fires also never populate it, since delivery is always `viewer_url` (see the schedule finding above) and the executor's `executeViewerURL()` branch never touches the job table either — only the unreachable legacy `executeFile()` branch does. A repo-wide frontend search confirmed zero callers of `generate-async`/`job-status` anywhere. `reporting-audit/report.md` and `reporting-audit/history.md` (EN+TH) both rewritten with Implementation status callouts documenting this as a confirmed structural gap (the table, screen, and endpoint are all real and correctly wired — nothing currently writes to the table). This is the same "confirmed gap / half-built feature" pattern already established for vendor-pricelist's external portal and store-requisition's stock-replenishment mock.
- **New finding this pass — `tb_user_login_session` (platform) is dead, same pattern; the real user-activity signal is a filtered projection over `reporting-audit/activity`'s own table, not a two-table join:** zero non-schema code references found for `tb_user_login_session`. This system authenticates via Keycloak-issued JWTs (`KEYCLOAK_SERVICE` client proxy in `micro-business`'s `AuthService`) — there is no local session table at all. The confirmed real writer is `AuthService.logAuthActivity()`, called from `login()` (success only — failure branches return before any write, so failed-login capture is unconfirmed/absent) and `logout()`, writing a `tb_activity` row with `entity_type: 'auth'` scoped to the user's **default business unit only** (skipped entirely if none is set). A repo-wide search of `logTenantEvent`/`logEvents` callers found no `action: 'view'` writer anywhere (product/recipe image events and auth login/logout are the only callers) — sensitive-page-view logging does not exist. `reporting-audit/user-activity.md` (EN+TH) rewritten: confirmed the real screen (`user-activity-component.tsx`) hard-sets `entity_type = "auth"` on the *same* `ACTIVITY_LOGS` endpoint as `reporting-audit/activity.md`'s own screen — it is a client-side filtered view, not a separate table or API.
- **New finding this pass — `tb_broadcast_notification` + `tb_user_broadcast_action` (platform) are real, live, and were entirely undocumented; `tb_news` has real BU scoping and a publish lifecycle, opposite of what was claimed:** the Prisma doc-comment on `model tb_broadcast_notification` (`prisma-shared-schema-platform/schema.prisma` line ~372) states the broadcast tables explicitly "replace the previous fan-out-on-write pattern that inserted one `tb_notification` row per recipient" — `tb_notification` itself remains real for personal/workflow-driven per-recipient notifications (including scheduled-report delivery, see the schedule finding above). Separately, `tb_news.business_unit_ids` (JSONB array; empty = global) and `status` (`enum_news_status`: `draft` default / `published` / `archived`, with `published_at` auto-stamped and enforced by `findPublicAll()`/`findPublicOne()`) directly contradict the previous "no BU scoping... visible immediately" claim. `reporting-audit/notification.md` (EN+TH) rewritten with an Implementation status callout adding both new tables and correcting the `tb_news` model.
- **Also fixed this pass (module-wide, both locales):** `reporting-audit/activity.md` — the previously-claimed "Activity drawer" embedded on every document detail page does not exist anywhere in the frontend (`useActivityLog`/`ACTIVITY_LOGS` is referenced only by `/system-admin/activity-log`'s own component); corrected to describe the real platform-wide list screen and its actual filters (5-value action multi-select, ~13-value entity-type multi-select, actor filter, export, print). `doc_version` (added 2026-06-12) was missing from `tb_activity`'s and `tb_notification`'s/`tb_message_format`'s field tables — added. Stale Prisma line citations across all 8 sub-pages + the landing page recomputed against current schema line numbers (e.g. `tb_report_job` now line ~6094, not the old ~5652; `tb_report_template` line ~731, not ~589; `tb_notification` line ~332, not ~267).
- `en/th/inventory/costing/02-business-rules.md` + `03-user-flow.md` (pre-fix, throughout both, both locales): cited a document set at `Test_case/System_Process/INDEX.md` / `proc-03-cost-calculation.md` / `tx-01-grn.md` etc. with specific fabricated version numbers and "capture dates" (e.g. "version 1.1.0, capture date 2026-04-26"), treating its P4 "Open Questions / Confirmed Answers" table as code-verified fact. That exact path does not exist anywhere in any reference repo — but a real document set with the same filenames exists at `../carmen-inventory-frontend-e2e/docs/persona-doc/System Process/*.md`. Read directly: it is a **requirements-gathering / planning-stage document** whose P4 answers are logged as `"User confirmed <date>"` from a stakeholder session, not observed system behaviour, and its own § "What This Process Does NOT Do" explicitly states "Does not generate GL journal entries (separate accounting integration — TBC)" — directly contradicting the GL posting mechanics the wiki built on top of it. Fixed in this pass: citations repointed to the real path, reframed as design-intent (not code-verified), GL claims removed module-wide.
- `en/th/inventory/costing/02-business-rules.md` `COST_VAL_009`/`COST_AUTH_001` + `03-user-flow.md` §2.2 + all three persona pages (pre-fix): claimed a code-enforced guard blocking `tb_business_unit.calculation_method` change on a business unit with non-zero on-hand ("drain stock or run elevated migration"). The planning doc records this as a stakeholder-confirmed *design intent* (P4 Q1), not an implemented rule; a repo-wide search of `platform_business-units.controller.ts` and every write path to `calculation_method` found no on-hand check, no drain precondition, no rejection path anywhere — matches the identical finding already made independently on `inventory/01-data-model.md` §5 item 4 in the 2026-07-15 inventory-module resync pass ("No 'block change with non-zero on-hand' guard was found either; changing the BU setting is a platform-level admin action outside this module"). Fixed in this pass — documented as a real, currently-unguarded gap rather than an enforced rule.
- `en/th/inventory/costing.md` (pre-fix, At-a-Glance box): listed `AverageCostTracking` and `JournalEntry` as "Key entities/tables." Neither exists as a Prisma model in either the tenant or platform schema (`prisma-shared-schema-tenant`/`prisma-shared-schema-platform`) — confirmed via `grep -n "^model "` across both schema files. Fixed in this pass.
- `en/th/inventory/costing/03-user-flow-inventory-controller.md` + `04-test-scenarios-inventory-controller.md` (pre-fix, entire pages): described a 5-step "cost-aware adjustment approval flow" where the Inventory Controller reviews a FIFO/Average cost-pick preview on `tb_stock_in`/`tb_stock_out` documents before approving them. `inventory-adjustment`'s own already-verified resync pass established that `StockInService.create()`/`StockOutService.create()` write `doc_status = completed` unconditionally in the same call that posts to the ledger, regardless of which button the client sends — there is no reachable draft state and therefore nothing left in a reviewable pending state for any approver to act on ("there is no approval action for this persona to perform, since nothing is ever left in a reviewable pending state"). Both pages rewritten — `03-user-flow-inventory-controller.md` as a correction page, `04-test-scenarios-inventory-controller.md` scenarios redistributed to the module-level engine-scenario table.
- `en/th/inventory/templates/purchase-request.md` (pre-fix, §2/§4/§6): described "Create PR from Template" as a backend deep-clone writing a fresh `tb_purchase_request` and asserted historical PRs "still reference `created_from_template_id`." No such column exists anywhere in `tb_purchase_request` (checked the full model in `prisma-shared-schema-tenant/prisma/schema.prisma`), and the actual mechanism (`new-purchase-request-content.tsx` + `pr-form-schema.ts`'s `getDefaultValues`) is a pure client-side form pre-fill with no backend awareness of "instantiated from a template" at all. Fixed in this pass.
- `en/th/inventory/templates/purchase-request.md` (pre-fix, §5.1/§6): asserted a 3-state `draft`/`active`/`inactive` lifecycle derived from `is_active` + "used at least once." `tb_purchase_request_template` has no `status` field and no usage-tracking column; `is_active` is a single boolean rendered as a plain two-way `StatusSwitch`, defaulting to `true` on create. No code anywhere derives a third state. Fixed in this pass.
- `en/th/inventory/templates/purchase-request.md` (pre-fix, §2/§3): claimed hard-delete is blocked once a PR has been cloned from the template ("Hard-delete blocked — template in use"). `purchase-request-template.service.ts`'s `delete()` is an unconditional hard delete (`tx.tb_purchase_request_template.delete()`) with no usage check of any kind. Fixed in this pass.
- `en/th/inventory/templates/price-list.md` (pre-fix, §2/§4): described a UI "Clone" action for creating a template variant. A repo-wide search of the price-list-template frontend found zero references to "clone," and `../carmen-inventory-frontend-e2e/tests/160-pl-template.spec.ts` has a dedicated "Pricelist Template — Clone (removed)" suite explicitly asserting no clone button or menu item exists in list/detail/edit for any role. Fixed in this pass.
- `en/th/inventory/templates/price-list.md` (pre-fix, entire page): built its Common Tasks, Validation, and Business Rules sections around `reminder_days`/`send_reminders`/`escalation_after_days` as user-editable fields with enforced validation (sort order, positivity) and a background-job-driven "reminder execution" mechanic, while never mentioning the module's actual per-product MOQ-tier "Products" section at all. Direct read of `plt-form.tsx`/`plt-form-schema.ts` shows the create/edit form has no field for any of the three reminder/escalation columns (real DB columns, silently accepted by `create()`/`update()` if posted directly but read by no UI and no `micro-cronjobs` job), while the Products section (`plt-form-products-section.tsx`, `tb_pricelist_template_detail.order_unit_obj`) is the form's largest and most-used section. Rewritten in full this pass, including new §5.2/§5.3 data-model tables for the previously-undocumented detail/comment entities.
- `en/th/inventory/templates/price-list.md` (pre-fix, §6): stated `name` uniqueness is "application-enforced; no explicit `@@unique`" and that a hard-delete is blocked once a template has issued RFQ rounds. Schema shows a real `@@unique([name, deleted_at])` DB constraint (redundantly re-checked in app code, not "instead of" a DB constraint), and `price-list-template.service.ts`'s `remove()` is an unconditional soft delete with no usage guard at all — there is no hard-delete path to be blocked in the first place. Fixed in this pass.
- **Found during Task 3 (root/landing verification), not fixed — out of Task 3's file scope:** `en/th/inventory/purchase-order/credit-note.md` still asserts live AP/GL posting as fact ("Always emits an AP debit memo," "posts variance to write-off GL account," "posts the delta to FX gain/loss," "AP always posts") even though its own commit (`1f73d3e`) only touched 4 lines (added an "unverified — three-way-match" hedge to one Common-Tasks row) and every sibling module's confirmed finding is "no journal-entry or general-ledger posting anywhere in the backend, zero hits for `journal`/`ledger` repo-wide" (costing.md §1/§4, good-receive-note.md, inventory.md, store-requisition.md). The parent `purchase-order.md` landing page (corrected) already treats AP/invoice-matching as unconfirmed/design-intent, not live behavior, and its own Credit Note link (§ "Related Modules") uses neutral wording with no AP claim. `en/inventory.md`/`th/inventory.md`'s Credit Note row was brought in line with the parent module's corrected framing in this pass, but `credit-note.md` itself (§1 "What & Who", Common Tasks, Edge Cases) still needs the same AP/GL-posting sweep the rest of the purchase-order module already got. Recommend a follow-up pass on this one sub-page. **RESOLVED (follow-up pass, 2026-07-29):** direct reads of `credit-note.logic.ts`, `credit-note.service.ts`, and `inventory-transaction.service.ts` (`executeCreditNoteQty`/`executeCreditNoteAmount` → `createFifoCreditNoteQty`/`createAverageCreditNoteQty`/`createFifoCreditNoteAmount`/`createAverageCreditNoteAmount`) confirm zero hits for `journal`/`ledger`/`gl`/`debit_memo`/`write_off`/`fx`/`gain_loss`/AP-posting anywhere in the credit-note backend — `submit()` only calls the inventory-transaction service. Real behavior: `quantity_return` consumes the GRN's own FIFO lots first, then other available lots for the same product+location; any cost variance (or the full CN cost when no stock is available at all — no negative-inventory guard) is recorded as a plain `diff_amount` column on `tb_inventory_transaction_cost_layer`, never a GL/write-off posting. `amount_discount` re-prices the referenced GRN lot's remaining on-hand (or books the whole amount as `diff_amount` if the lot is period-closed or fully consumed). `exchange_rate` is a plain snapshot field auto-populated once from the GRN's or currency master's rate at edit time (`cn-general-fields.tsx`) and freely editable — no dynamic re-resolution keyed on `cn_date`, no FX gain/loss computation anywhere (matches the master-data-module finding for PO/PR/GRN). Also found and fixed two adjacent fabrications in the same sweep: a "raise a corrective journal voucher" validation action (`tb_jv_header` has zero non-schema references anywhere in the backend) and a "Rate not in history" / `tb_exchange_rate`-lookup validation message (zero repo-wide hits). Also corrected a wrong citation: `COST_CALC_005` is "credit-note-amount lot revaluation," not "FX revaluation" as this page previously cited it. Swept and fixed every instance across frontmatter description, At-a-Glance, §1, Common Tasks, Validation & Errors, Edge Cases, §5.1 data-model field descriptions, §6 workflow rules, and §7 cross-references — EN + TH mirrored, remaining `AP`/`GL`/`FX`/`journal`/`write-off` hits are all inside negation/correction framing (verified by grep).

## Route gaps

(source routes with no wiki page — carried decisions in Task 5)

- carmen-platform: `/sql-workbench` (App.tsx:312) and `*` catch-all exist in App.tsx but are absent from SITEMAP.md — SITEMAP drift; route is captured in routes-platform.txt
- carmen-inventory: `/operation-plan` index route renders `operation-dashboard.tsx` (an Operation Plan hub/dashboard screen) — no wiki page covers it; the recipe landing only documents the 5 sub-module screens. Decide in Task 5 whether it warrants a page or a landing-page mention.
- carmen-inventory: `/vendor-management` index route renders `vendor-dashboard.tsx` (a KPI widget hub — active vendors, active/expiring pricelists, RFQ counts) — no wiki page covers it; the vendor-pricelist landing now mentions it in prose but there is no dedicated page. Same shape as the `/operation-plan` gap above; decide both together in Task 5.
- carmen-inventory: `/pl/:url_token` (external vendor portal) has no dedicated wiki page of its own — it is documented inside `vendor-pricelist/03-user-flow-vendor.md` including its confirmed Save/Submit backend gap; probably sufficient, but noting for Task 5 completeness.
- carmen-inventory: `/system-admin/company-profile` and `/system-admin/default-setting` (+ the `/system-admin/business-setting` redirect to company-profile) are real, routed screens (BU profile fields; BU-level operational config for PR/SI/PO) with no wiki page in `system-config/` — module-map-adjacent to `master-data/business-unit.md` (which already documents `tb_business_unit` cross-book) but these two screens edit different field groups on the same table. Decide in Task 5 whether they get their own page(s) or a cross-reference from `business-unit.md` / the system-config landing page.
- carmen-inventory: `/system-admin/notification-template` (+`/new`, `/:id`) and `/system-admin/activity-log` are real, routed screens explicitly named in this module's own module-map row ("system-admin (period, running-code, workflow, notification, activity-log)") but have no wiki page under `system-config/` (11 pages exist; neither of these is one of them). Decide in Task 5 whether new pages are warranted.
- carmen-inventory: `/system-admin/sql-query` equivalent does not exist as a route at all (see Discrepancy log — `query-dataset.md` cited a nonexistent frontend implementation) — the backend capability is real; note for Task 5 whether the wiki should keep documenting it as backend-only or wait for a UI.

## Deferred

(anything postponed, with reason — e.g. backend down for screenshots)
