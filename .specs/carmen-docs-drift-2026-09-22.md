# carmen/docs drift register — 2026-09-22

`../carmen/docs/` has had **no commits since 2026-04-27** (`56fa3432`). Per the wiki's
own rule (implementation beats concept docs), the Inventory book re-sync of
2026-09-22 overwrote every claim below with what the source repos do at HEAD
(`carmen-turborepo-backend-v2` `ef4d6f08f`, `carmen-inventory-frontend-react`
`0713cbc9`). This file records what the concept docs still say so someone can
fix them at source; the wiki itself does not cite them as current truth.

Owner decision 2026-09-22 (Q11 b): record here, never edit `carmen/docs`.

| carmen/docs path | Stale claim | What HEAD does | Wiki page that corrects it |
|---|---|---|---|
| `app/inventory-management/inventory-transactions/BR-inventory-transactions.md` (lines 21, 31, 143, 162, 214) | Inventory transactions post automatically to the GL (variance, return postings) | No inventory→GL code path exists: `grep -rn -E "gl[-_]posting\|gl[-_]jv\|tb_gl_" apps/micro-business/src/{inventory,procurement}` → 0 hits; the only JV sources written today are `manual`, `reversal`, `closing`, template | [GL posting](/en/inventory/general-ledger/gl-posting) §2 |
| `inventory-management/period-end-process.md` (lines 135–136) | Period end includes "GL posting verification" and "period end journal entries" | Inventory period-end reads/writes `tb_inventory_period*` only (`period-end.close-transaction.helper.ts:80,195,210`); `tb_gl_period` is a separate 13-period calendar with no FK to it | [GL posting](/en/inventory/general-ledger/gl-posting) §3, [Period End](/en/inventory/inventory/period-end) |
| `store-requisitions/SR-API-JournalEntry-Endpoints.md` | SR journal-entry endpoints | No such endpoints exist in the gateway or Bruno | [Store Requisition](/en/inventory/store-requisition) |
| `purchase-request-management/purchase-request-ba.md` (`PR_BDG_001–005`, `softCommitmentPR/PO`, lines 77–81, 238) | Budget check and soft commitment on PR submit | No budget code path exists — only `budget_code?: string` on the DTO and a sample reject message | [PR business rules](/en/inventory/purchase-request/02-business-rules) |
| `purchase-request-management/data-models.md` (lines 163–172) | `PRType` enum / `pr_type` column | No `pr_type` column on `tb_purchase_request` | [PR data model](/en/inventory/purchase-request/01-data-model) |
| `purchase-request-management/PR-User-Experience.md` | Approval delegation; administrative void | Neither exists — `voided` is written only by reject (`purchase-request.service.ts:2201`); draft delete is a soft delete by owner/super-admin | [PR user flow](/en/inventory/purchase-request/03-user-flow) |
| `business-analysis/my-approvals-ba.md` | Per-module approval queue | Queue is the view `sys_v_my_pending` behind `GET /api/my-pending` (2026-09-16): PR/PO/SR only, includes the caller's own drafts, default `doc_date:asc` | [My Approval](/en/inventory/purchase-request/my-approval) |
| `product-management/PROD-PRD.md` (§3.1.1, FR-PRD-006..008; §3.1.6) | Product bulk import with templates/dry-run; barcode generation | No importer or barcode generator exists — export only (`hooks/use-product.ts:37`) | [Product](/en/inventory/product) |
| `product-management/PROD-API-Endpoints-Import-Export.md` | Import + export endpoints | Only export exists | [Product](/en/inventory/product) |
| GRN extra-cost docs (allocation mode `manual`) | `manual` distributes the extra cost | `allocateExtraCost` (`good-received-note.extra-cost.ts`): `by_qty` = equal share per line, `by_value` = weighted by `stock_qty`, `manual` allocates nothing | [Extra Cost Type](/en/inventory/master-data/extra-cost-type) |
| `workflow-permissions-system.md` (notification section) | Notification fan-out keyed on message formats / `type` strings (`tb_message_format`) | `tb_message_format` is dropped; notifications use the `NotifyInput` envelope (`doc_type` + `event`, `scope`) via `POST /api/internal/notifications` (2026-08-10/11) | [Notification](/en/inventory/reporting-audit/notification) |
| `purchase-order-management/purchase-order-module.md` (lines 220–229) | `Draft → Sent`, `Sent → Voided: Void`, `FullyReceived` | `in_progress → approved → sent_or_print` (final approval lands on `approved`; `sent_or_print` only via `POST :id/send-email` or `:id/mark-sent`, migrations `20260914080000/80100`); cancel/close → `closed`; terminal receipt state is `completed` | [PO business rules](/en/inventory/purchase-order/02-business-rules) |
| `cn/CN-Business-Requirements.md` | Credit note has an approval workflow, posts an AP debit memo, can be voided within an open period | CN goes `draft → completed` on Submit in one transaction; no workflow, no AP write, no void route; validation: one product per GRN once, discount/tax ≤ line value and ≤ GRN pro-rated share | [Credit Note](/en/inventory/purchase-order/credit-note) |
| `store-requisitions/SR-Technical-Specification.md` | Approver/issuer quantity caps (`issued ≤ approved ≤ requested`), manual lot selection, user-chosen movement type | None enforced: DTO is bare `z.number()`, only `requested_qty > 0` at submit; lots are automatic FIFO; `sr_type` is derived server-side (`logic/sr-type.helper.ts`) | [SR business rules](/en/inventory/store-requisition/02-business-rules) |
| `vendor-pricelist-management/requirements.md` §28 | Portal token has IP/session binding and revocation | Only expiry is real (`tb_shot_url.expired_at` = RFQ `end_date`, `UrlTokenGuard`); no IP/session/revoke | [Vendor portal flow](/en/inventory/vendor-pricelist/03-user-flow-vendor) |
| `vendor-pricelist-management/design.md` | Price-list status `under-review` / `approved` / `rejected` | `enum_pricelist_status` has `submitted` (migration `20260721105944`), no review/approve/reject states | [VPL data model](/en/inventory/vendor-pricelist/01-data-model) |
| `workflow-permissions-system.md` (role model) | Role types requester / purchaser / approver / reviewer, one workflow list | `enum_stage_role` = `create` / `approve` / `purchase` / `issue` / `view_only`; per-document-type workflow pages and endpoints; routing rules on `total_amount`; skeleton-only lock while documents are pending (`can_edit_stages`) | [Workflow](/en/inventory/system-config/workflow) |
| `app/system-administration/user-management/{DD,BR}-user-management.md` | HOD index and single-HOD rule; separate department-membership screen | Not implemented; a user's departments/locations/roles are edited in one request via `PATCH /api/config/:bu_code/users/:user_id` | [Access Control — User](/en/inventory/access-control/user) |
