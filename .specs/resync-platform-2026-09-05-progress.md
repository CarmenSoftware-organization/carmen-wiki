# Platform Re-sync 2026-09-05 — Progress Log

Spec: `.specs/2026-09-05-platform-resync-design.md`
Source HEAD: `157a65e` (2026-09-04)
Branch: `docs/resync-platform-2026-09-05`

## Ruling note (from controller pre-flight, binding on this map)

- **R1**: this book's page shape is a sibling landing file plus a folder of sub-pages
  (`en/platform/clusters.md` next to `en/platform/clusters/{data-model,ui-screens,permissions}.md`).
  There is **no `index.md` anywhere** in the book — every new module below follows this shape.
- The plan creates **16** new modules, not 17: `license-features` and
  `license-feature-groups` are two tabs (`FeatureCatalogPanel` / `GroupCatalogPanel`) of one
  screen, `LicenseCatalog.tsx` (confirmed by reading the component: it renders both panels
  behind a `TabStrip`, each backed by its own service). Documented as a single
  `license-catalog` module. The two original nav rows, permissions, and feature-flag keys
  are both preserved and noted in the row below — this is a documentation merge, not a
  product merge.
- `print-template-mapping` is being deleted in Task 11 (feature removed from the product,
  `de11377`, 2026-07-24). It has **no row** in this source map. Its route no longer exists in
  `src/App.tsx` (confirmed absent from the Step 2 regeneration below) — the SPA side of the
  removal is already done; only the wiki module remains to be deleted.
  **Stale e2e note**: `../carmen-platform-e2e/tests/print-template-mapping/` still exists on
  disk and is stale — do not treat it as coverage for anything; fixing/removing that e2e
  suite is out of scope for this plan.

## No static permission-key catalog

`../carmen-platform` has no static permission-key catalog file. `src/utils/permissions.ts`
defines only `PERMISSIONS.BROADCAST.SEND = 'broadcast.send'` (one constant, with a comment
directing future duplicated literals to be hoisted the same way) plus the pure
`checkPermission` / `checkPlatformAuthority` functions — every other permission key used in
the app is a bare string literal at its call site (nav item `permission:` field, or a
`<Can permission="...">` / `hasPermission('...')` call in a page component). Per-menu keys
live in `src/components/nav/platformNav.ts` (platform side) and
`src/components/nav/clusterAdminNav.ts` (cluster-admin side, which has **no permission
filtering at all** — see the `cluster-admin` row). The full permission catalog as humans can
browse it is served to `PermissionCatalog.tsx` (`/platform/category-permissions`) from the
backend at runtime; it is not enumerable from source alone.

The "Permission key" column below lists the nav-gating (list/read) key first; CRUD-level
keys found in the page components are noted in parentheses where they differ from the read
key. These are the keys later tasks should verify against; a module marked "list only"
had no separate create/update/delete literal found by grep and should be re-checked by
whichever task verifies that module's page.

## Source map

| Module | Routes | Components | Service | Permission key | Feature key | e2e suite |
|--------|--------|-----------|---------|----------------|-------------|-----------|
| `clusters` | `/clusters`, `/clusters/new`, `/clusters/:id/edit` | `ClusterManagement`, `ClusterEdit` | `clusterService.ts` | `cluster.read` (nav; also gates `business-units` and `tenant-migrations` nav rows) + `cluster.create`/`cluster.update`/`cluster.delete` | `clusters` | `clusters` |
| `business-units` | `/business-units`, `/business-units/new`, `/business-units/:id/edit` | `BusinessUnitManagement`, `BusinessUnitEdit` | `businessUnitService.ts` + `businessUnitLicenseService.ts` (Licenses tab, split out of Users per `#276`) | `cluster.read` (nav; BU shares the Clusters resource) | `business_units` | `business-units` |
| `users` | `/users`, `/users/new`, `/users/:id/edit` | `UserManagement`, `UserEdit` | `userService.ts` + `userRoleService.ts` | `user.read` (nav) + `user.create`/`user.update`/`user.delete` | `users` | `users` |
| `rbac` | `/platform/roles`, `/platform/roles/new`, `/platform/roles/:id/edit`, `/platform/category-permissions` | `RoleManagement`, `RoleEdit`, `PermissionCatalog` | `roleService.ts`, `permissionService.ts` | `platform_role.read` (nav, roles only) + `platform_role.create`/`update`/`delete`; **`/platform/category-permissions` has no nav entry and no permission gate** — reachable by any authenticated platform user (`PrivateRoute` only) | `platform_roles` (roles only; category-permissions has none) | `roles`, `permission-catalog` |
| `applications` | `/applications`, `/applications/new`, `/applications/:id/edit` | `ApplicationManagement`, `ApplicationEdit` | `applicationService.ts` | `application.read` (nav) + `application.create`/`update`/`delete` | `applications` | `applications` |
| `broadcasts` | `/broadcasts`, `/broadcasts/new`, `/broadcasts/:id/edit` | `BroadcastManagement`, `BroadcastCompose`, `BroadcastEdit` | `broadcastService.ts` | `broadcast.read` (nav) + `PERMISSIONS.BROADCAST.SEND` (`broadcast.send`, the one hoisted constant) / `broadcast.update` / `broadcast.delete` | `broadcasts` | `broadcast` |
| `news` | `/news`, `/news/new`, `/news/:id/edit` | `NewsManagement`, `NewsEdit` | `newsService.ts` | `news.read` (nav) + `news.create`/`update`/`delete` | `news` | `news` |
| `report-templates` | `/report-templates`, `/report-templates/new`, `/report-templates/:id/edit` | `ReportTemplateManagement`, `ReportTemplateEdit` | `reportTemplateService.ts` | `report_template.read` (nav) + `report_template.create`/`update`/`delete` | `report_templates` | `report-templates` |
| `licenses` | `/licenses`, `/licenses/:clusterId`, `/licenses/subscriptions/new`, `/licenses/subscriptions/:id/edit`, `/licenses/seats/new`, `/licenses/seats/:id/edit`, `/licenses/bu-quota/new`, `/licenses/bu-quota/:id/edit`, plus `/subscriptions*` legacy redirects (`Navigate` / `SubscriptionEditRedirect`) | `LicenseCenter`, `ClusterLicenseDetail`, `SubscriptionForm`, `LicensePurchaseForm` | `subscriptionService.ts`, `clusterLicenseService.ts`, `businessUnitLicenseService.ts`, `expiryThresholdService.ts` | `subscription.read` (nav) + `subscription.manage`/`license.manage` (CRUD) | `licenses` | **none — no e2e backing** |
| `license-catalog` | `/license-features`, `/license-feature-groups`, `/license-feature-groups/new`, `/license-feature-groups/:id/edit` | `LicenseCatalog` (single component, two tabs: `FeatureCatalogPanel`, `GroupCatalogPanel`), `LicenseFeatureGroupEdit` | `licenseFeatureService.ts`, `licenseFeatureGroupService.ts` (+ `subscriptionService.ts` inside `GroupCatalogPanel`) | Features tab: `license_feature.read` (nav) + `license_feature.manage`; Groups tab: `license_feature_group.read` (nav) + `license_feature_group.manage` — **two separate nav rows/permission pairs feed one documented module** | Features tab: `license_features`; Groups tab: `license_feature_groups` | **none — no e2e backing** |
| `cluster-admin` | `/cluster-admin`, `/cluster-admin/:clusterId/cluster`, `/cluster-admin/:clusterId/business-units`, `/cluster-admin/:clusterId/business-units/:buId/edit`, `/cluster-admin/:clusterId/users`, `/cluster-admin/:clusterId/licenses`, `/cluster-admin/:clusterId/profile` | `ClusterAdminEntry`, `ClusterProfile`, `ClusterAdminBusinessUnitList` (`BusinessUnitList.tsx`), `ClusterAdminBusinessUnitForm` (`BusinessUnitForm.tsx`), `ClusterAdminUsers` (`ClusterUsers.tsx`), `ClusterAdminLicenses`, `Profile` (shared with the platform-side profile page) | `clusterAdminService.ts` | **none** — `ClusterAdminRoute` applies no permission filtering; clearing the route guard is the whole check (second persona, distinct from platform-side RBAC) | `cluster_admin_cluster`, `cluster_admin_business_units`, `cluster_admin_licenses`, `cluster_admin_users` (from `clusterAdminNav.ts`, separate key namespace from the platform nav despite identical menu labels) | **none — no e2e backing** |
| `platform-config` | `/platform/configs` | `PlatformConfigManagement` | `platformConfigService.ts` | `platform_config.read` (nav) + `platform_config.manage` (all card saves) | `platform_config` | **none — no e2e backing** |
| `email-settings` | `/platform/email-settings` | `EmailSettingManagement` | `emailSettingService.ts` | `email_setting.read` (nav) + `email_setting.manage` | `email_settings` | **none — no e2e backing** |
| `user-platform` | `/platform/user-platform`, `/platform/user-platform/:userId` | `UserPlatformManagement`, `UserPlatformEdit` | `userPlatformService.ts` + `userRoleService.ts`, `roleService.ts`, `clusterService.ts`, `userService.ts` | `user_platform.read` (nav) + `user_platform.manage` | `user_platform` | `user-platform` |
| `super-admins` | `/platform/super-admins` | `SuperAdminManagement` | `superAdminService.ts` | none — gated by `superAdminOnly: true` in nav, not a `permission` string | `super_admins` | `super-admins` |
| `feature-flags` | `/platform/features` | `FeatureFlagManagement` | `featureFlagService.ts` | `feature_flag.manage` (nav; single key, no separate `.read`) | **none — deliberately ungated**: comment in `platformNav.ts` says a switch that could hide itself could never be restored from the UI | **none — no e2e backing** |
| `tenant-migrations` | `/tenant-migrations` | `TenantMigrationManagement` (+ `DeployConsole`, `FleetSync`) | `tenantMigrationService.ts` + `businessUnitService.ts` | `cluster.read` (nav; shares the Clusters resource, no dedicated `tenant_migration.*` key found) | `tenant_migrations` | **none — no e2e backing** |
| `tenant-imports` | `/tenant-imports` | `TenantImportWizard` | `preconfigImportService.ts` + `businessUnitService.ts` | `data_import.manage` (nav) | `tenant_imports` | **none — no e2e backing** |
| `usage-analytics` | `/analytics` | `UsageAnalytics` (+ `StatCards`, `TopList`, `UsageChart`) | `analyticsService.ts` | `activity_event.read` (nav) + `activity_event.detail` (drill-down) | `usage_analytics` | **none — no e2e backing** |
| `activity-events` | `/activity-events` | `ActivityEventManagement` (+ `EventDetailSheet`) | `analyticsService.ts` | `activity_event.detail` (nav) | `activity_events` | **none — no e2e backing** |
| `platform-migrations` | `/platform/migrations` | `PlatformMigrationManagement` (+ `OpRow`, `RunConsole`) | `platformMigrationService.ts`, `platformSeedService.ts` + `businessUnitService.ts` | none — gated by `superAdminOnly: true`, not a `permission` string | `platform_migrations` | **none — no e2e backing** |
| `database-pools` | `/platform/database-pools`, `/platform/database-pools/new`, `/platform/database-pools/:id/edit` | `DatabasePoolManagement`, `DatabasePoolEdit` | `databasePoolService.ts` | `database_pool.read` (nav) + `database_pool.manage` | `database_pools` | **none — no e2e backing** |
| `cronjobs` | `/cronjobs`, `/cronjobs/new`, `/cronjobs/:id/edit` | `CronJobManagement`, `CronJobEdit` (+ `CronJobFilterSheet`, `CronScheduleField`) | `cronjobService.ts` | `cronjob.read` (nav) + `cronjob.manage` | `cronjobs` | **none — no e2e backing** |
| `report-form-groups` | `/report-form-groups` | `ReportFormGroupManagement` (+ `GroupCard`) | `reportTemplateService.ts` (shared with `report-templates`, no dedicated service file) | `report_template.read` (nav; shares the Report Templates resource, no dedicated `report_form_group.*` key found) | `report_form_groups` | **none — no e2e backing** |
| `sql-workbench` | `/sql-workbench` | `SqlWorkbench` | `sqlQueryService.ts` | `sql_workbench.read` (nav) + `sql_workbench.manage` (Run/Save/Drop — three mutating actions share one permission string per a comment in `SqlWorkbench.tsx`) | `sql_workbench` | **none — no e2e backing** |
| `dashboard` | `/dashboard` | `Dashboard` | Aggregates `clusterService.ts`, `businessUnitService.ts`, `userService.ts`, `applicationService.ts`, `newsService.ts`, `reportTemplateService.ts` (summary counts only, no dedicated dashboard service) | none in nav — visible to anyone with platform authority | none | `dashboard` |
| `landing` | `/` | `Landing` | none (static; reads `AuthContext` only, redirects authenticated users) | none — public route | none | `landing` |
| `profile` | `/profile` (shared component also serves `/cluster-admin/:clusterId/profile`) | `Profile` | `api.ts` (generic client, no dedicated profile service) | none — acts on the caller's own account | none | `profile` |
| `changelog` | `/changelog` | `Changelog` | none — reads static `src/data/changelog.json`, not an API service | none — public route (not wrapped in `PrivateRoute`) | none | `changelog` |

### Modules with no e2e backing (15 of the 29 rows above)

`licenses`, `license-catalog`, `cluster-admin`, `platform-config`, `email-settings`,
`feature-flags`, `tenant-migrations`, `tenant-imports`, `usage-analytics`,
`activity-events`, `platform-migrations`, `database-pools`, `cronjobs`,
`report-form-groups`, `sql-workbench`. (`super-admins` and `user-platform` — both new
modules — DO have e2e backing; see the source-map table above.) Every claim written for
the 15 no-e2e modules in Phase 3 must be sourced from implementation alone and marked
"no e2e backing" in the Status table below, per the design doc's error-handling rule.

`../carmen-platform-e2e/tests/` also holds `auth` and `journeys`, which are cross-cutting
(login flow / multi-step journeys) and do not map one-to-one to any single wiki module.

## Status

| # | Module | Type | EN | TH | Claims fixed | e2e backing | Routes visually changed | Commit |
|---|--------|------|----|----|--------------|-------------|-------------------------|--------|
| 2 | `clusters` | Existing (verify) | 4 pages rewritten (landing + data-model + permissions + ui-screens) | 4 pages updated (landing fully mirrored; 3 sub-pages are pre-existing stubs, corrected in place per the standing TH-stub deferral) | Source drifted far beyond a normal verify: (1) `tb_cluster.max_license_bu` and `tb_business_unit.max_license_users` are both **removed** from the Prisma schema (`schema.prisma`; migration `20260821000000_drop_bu_max_license_users`) — BU quota now comes from a new `tb_cluster_license` ledger (winning-row semantics via `v_cluster_bu_quota`), seats from a new `tb_business_unit_license` ledger (summed via `v_business_unit_seat`); (2) `tb_cluster_user.parent_bu_id` is fully removed from the schema and the SPA (Users tab has no BU column/field, Add-User dialog has no BU select, bulk "Move to BU" no longer exists); (3) `ClusterEdit.tsx` was rewritten twice since 2026-07-29 — scrollspy (Overview/Details/Branding/Business Units/Users) → a persistent `ClusterPlate` header + 3-tab body (Licensing/Business Units/Users), commit `69027b9` (2026-08-23); old `ClusterEditNav`/`useScrollSpy`/`DetailsSection`/`BrandingSection` files still exist but now serve only the cluster-admin persona's `ClusterProfile.tsx`; (4) create form (`ClusterCreateForm`) now requires an opening BU-quota licence (`licensed_bus`/`license_end_date`) and shows a live `ClusterDraftPlate` preview; (5) new cross-cutting `activity_log.read` permission gates a "View History" action on both cluster screens; (6) `PrivateRoute` gained a `feature` prop (`clusters` feature flag, checked after permission) and a platform-authority/cluster-admin resolution branch that runs *before* the permission check; (7) Fleet Capacity band now reads a dedicated `GET /api-system/clusters/summary` endpoint and adds a clickable "Quota expiring" stat; (8) Business Units tab dropped its per-BU seat meter, added an "Over limit" rank badge (`businessUnitRank.ts`, matching `v_cluster_bu_quota`'s `is_hq DESC, created_at ASC, id ASC`); list columns/CSV export updated (`bu_cap`/`bu_cap_end_date` replace `max_license_bu`). Full citations in `task-2-report.md`. | `clusters` (7 specs, 904 lines) — **stale, not usable as current evidence**: page object (`ClusterEditPage.ts`, last touched 2026-08-22) asserts a page-level Edit button/read-only-mode toggle that predates even the July scrollspy revision, and `cluster-create.spec.ts`'s `fillForm()` never fills the now-required `licensed_bus`/`license_end_date` fields. Flagged for whoever owns e2e maintenance; out of scope to fix here. | `/clusters` (list: Fleet Capacity band, columns, row actions), `/clusters/new` (two-column draft layout), `/clusters/:id/edit` (plate + 3 tabs, replacing 5-section scrollspy) — all three UI-changed; screenshots deferred per plan | `f0ba1b5` |
| 3 | `business-units` | Existing (verify) | 4 pages rewritten (landing + data-model + ui-screens rewritten in full; tenant-migrations lightly edited) | 4 pages updated (landing fully mirrored; data-model/ui-screens are pre-existing stubs, corrected in place per the standing TH-stub deferral; tenant-migrations is a pre-existing full TH mirror — got the same edits as EN, not stub-reduced) | Drifted as far as `clusters` did, via the same rewrite wave: (1) `tb_business_unit.max_license_users` is **physically dropped** (migration `20260821000000_drop_bu_max_license_users`, same one Task 2 found for `max_license_bu`) — seats are now a per-BU `tb_business_unit_license` ledger, summed via view `v_business_unit_seat`; (2) `tb_business_unit.db_connection` is **also physically dropped** — replaced by `database_pool_id` (FK to new `tb_database_pool`) + `db_schema`; the BU no longer holds any DB credential fields, and `revealDbPassword`/`SAFE_DB_CONNECTION_KEYS` no longer exist anywhere in the SPA; (3) `code` is now **unique platform-wide**, not per-cluster (`business_unit_code_global_u`, migration `20260904000000_business_unit_code_global_unique` — the Prisma-level `@@unique([cluster_id, code, deleted_at])` is explicitly dead-on-active-rows per the schema's own doc-comment) — the last sync's page had this backwards; (4) confirmed all four brief items: `code` is backend-generated (`generateBusinessUnitCode()`, Crockford base32 8 chars, `micro-cluster/.../business-unit-code.helper.ts`, retried up to 5x on collision — PR #279), the Technical tab has a "Generate schema" randomizer (`generateSchemaName()`, `bu_` + 16 random chars — PR #280), Licenses is its own tab split out of Users (PR #276), and its card has a New Subscription button gated on `subscription.manage` (PR #275); (5) `BusinessUnitEdit.tsx` was rewritten from a one-document scroll into a 6-tab document (General/Location/Formats/Technical/Users/Licenses, commits `dde5787`/`fd087f1`/`c305b61`) — the same redesign pattern as `ClusterEdit.tsx`; (6) list page reintroduced a `BrandMark` avatar in the Name column, moved its Overview strip onto a dedicated `GET /api-system/business-units/summary` endpoint, dropped the "Max Licensed Users" CSV column, added a "View History" row action (`activity_log.read`), and — correcting a claim in the last sync's page — the empty-state Add Business Unit button **is** `<Can>`-gated in current source; (7) all three routes plus the nav entry gained `feature="business_units"`, and `tenant-migrations.md` gained `feature="tenant_migrations"` plus two small UI-copy fixes (Deploy-all button variant/label, on-screen Pending column removed). Full citations in `task-3-report.md`. | `business-units` (6 specs) — **stale, not usable as current evidence**: `business-unit-create.spec.ts` fills a `code` field on the create form (`fillBasicInfo({ code: buData.code, ... })`) and refers to a "Basic Information section", both of which predate not only the current 6-tab layout but PR #279's removal of the code field entirely; last touched 2026-06-11, before every rewrite this row documents. Flagged for whoever owns e2e maintenance; out of scope to fix here. | `/business-units` (list: BrandMark avatar, CSV columns, row actions), `/business-units/new` (no code field; only 4 of 6 tabs shown), `/business-units/:id/edit` (one-document scroll replaced by 6-tab document) — all three UI-changed; screenshots deferred per plan | `e63214c`, `d418e7f`, `2271cd8` |

**Cross-module note for Task 4 (`users`):** the Licenses content that moved out of the BU edit page's Users tab (§4.7 of `business-units/ui-screens.md`) belongs to `business-units` now, not `users` — confirmed `en/platform/users.md` and its sub-pages currently contain **no** license-related content to remove (grepped clean), so there is nothing to delete there today, but Task 4 must not re-introduce seat/license descriptions under `users` — the two pages would then both claim the same content.

| 4 | `users` | Existing (verify) | 4 pages rewritten (landing + data-model + ui-screens + lifecycle, all previously last touched 2026-07-29) | Landing fully re-mirrored; 3 pre-existing TH stubs (data-model, lifecycle, ui-screens) corrected in place per the standing TH-stub deferral | The brief's Step 2 (remove licensing content) was a no-op — confirmed clean, nothing to delete. But the module had drifted through an entire September design-consistency wave the last sync never saw: (1) list identity columns (avatar/username/name/email) collapsed into one cell and the Status column went asymmetric (`fd1f6ae`, #219); (2) the account form was grouped into three labelled sections with `firstname`/`lastname` now **required**, not optional (`d21c57c`, #220) — the last sync's field-order note ("Last before Middle") was also stale, current order is firstname→middlename→lastname; (3) in view mode there is **no longer a separate "User Details" card at all** — the whole card, including its `ReadOnlyField` read-only branches, is wrapped in `{editing && (...)}`, so view mode shows identity exclusively through the Hero; (4) **Change Password is now `<Can permission="user.update">`-gated** — every touched page previously stated it was ungated; (5) **audit precedence was stated backwards** on all four pages (`normalizeAudit()` tries the nested `audit.*` shape first, flat columns are the fallback — the reverse of what was written, the same class of error Task 3 found on `business-units`), plus the Updated-column suppression rule is `everEdited` (name present, or timestamp differs absent a name), not a plain `updated_at === created_at` check; (6) a new **View History** action (`activity_log.read`, platform-scoped) appeared on both the list row menu and the edit-page hero — the same cross-cutting Activity Trail feature Tasks 2/3 documented; (7) the Directory summary strip now reads a dedicated `GET /api-system/user/summary` endpoint instead of a client-side aggregate; (8) CSV export grew from 4 to 7 columns; (9) audit columns render as relative time + absolute tooltip, not a fixed `YYYY-MM-DD HH:mm:ss` string; (10) `tb_cluster_user.parent_bu_id` is **physically dropped** (migration `20260825000000_drop_cluster_user_parent_bu`) — never read by any service; (11) three new `tb_user` email-verification columns exist in the schema but are surfaced only on the User Platform screen (`UserPlatformEdit.tsx`), not this module's own UI — **flagged for whoever next verifies `rbac`/`user-platform`, since `rbac.md` doesn't mention them yet**; (12) the "Users" nav item's source moved from `Layout.tsx` to `platformNav.ts` (line 16) — the stale `Layout.tsx` citation was on all four pages. Full citations in `task-4-report.md`. | `users` (6 specs, last touched 2026-06-11) — **stale, not usable as current evidence**: `user-create.spec.ts`'s "minimum required fields" test fills only `username`/`email`, which the current form's `required` attributes and preflight validation would now reject (`firstname`/`lastname` are required too). Flagged for whoever owns e2e maintenance; out of scope to fix here. | `/users` (collapsed identity column, asymmetric Status, 4-item row menu, Directory strip source), `/users/new` (sectioned form, required firstname/lastname), `/users/:id/edit` (no more separate User Details card in view mode — Hero absorbs it; View History button) — all three UI-changed; screenshots deferred per plan | `7b6d8e2`, `ef909d3`, `92a8e17`, `9641b15`, `37c9e0e`, `83b0207` |

**Cross-module note for whoever creates the `user-platform`/`super-admins` modules:** Task 5 scoped its `rbac` verification to Roles + Permission Catalog only (per the design doc's plan to split User Platform and Super Admins into their own standalone Platform modules) and updated those two screens' sections in `rbac.md`/`ui-screens.md` at summary level only, not line-by-line. Both have drifted heavily since the module's 2026-07-29 baseline: `SuperAdminManagement` was rewritten a second time on 2026-09-02 (`#244`) from a `DataTable` into a card-based roster with a self-removal guard and a `UserPicker` typeahead (replacing the shadcn `Select`); `UserPlatformManagement`/`UserPlatformEdit` were rewritten the same day (`#250`/`#251`) into a "privilege registry" — filter-consistent summary sourced from the list response itself (no more N+1 sweep), inline `RoleChips`, per-assignment CSV export, a "Revoke all access" action, an Access Reach Band, an inactive-holder warning, an Email-Unverified badge, and a `Membership Card`. None of this is verified fact-by-fact yet; treat the current `rbac.md`/`ui-screens.md` §4/§5 as a directional summary only when scoping that future task.

| 5 | `rbac` | Existing (verify) | 4 pages rewritten in full for Roles + Permission Catalog (landing + data-model + permissions + ui-screens); User Platform/Super Admins sections updated at summary level only, per an explicit scope note added to `rbac.md` and `ui-screens.md` (see cross-module note above) | 4 pages fully mirrored (all four were pre-existing full TH mirrors, not stubs — same depth of edit as EN, not stub-reduced) | Drifted through two waves the 2026-07-29 baseline never saw: (1) **`role.*` → `platform_role.*` rename + `rbac.read` retirement + `/platform/permissions` → `/platform/category-permissions` route move**, all one commit (`8df0b10`, 2026-08-20) — every `role.*` key and the old route on all four pages was stale; (2) **Permission Catalog's SPA route carries no permission gate at all** (bare `<PrivateRoute>`, no `requiredPermission`/`feature`) but its `GET .../permissions` call is enforced server-side on `platform_role.read` — the prior "None (read-only screen)" framing conflated the two; (3) **`PermissionPicker` (accordion + checkboxes) was deleted outright** and replaced by `PermissionGrid` (resource-per-row, toggle buttons, withheld actions shown dimmed not omitted, ordered by `resourceRank()`/new `actionRank()`) — commit `42eeafe`, same day; (4) **`RoleEdit`'s Settings card now renders only while editing** — view mode shows name/description/status/audit via `RoleIdentityHero` instead, the same "hero absorbs the read-mode card" pattern Task 4 found in `users` (`#253`, 2026-09-02); (5) **`RolesAccessSummary`/new `RoleReachCell` now scale against catalog size, not the widest role** (`#252`, 2026-09-02), and surface a `deleted`-roles count that was on the wire but never rendered; (6) **`GET /api-system/platform/roles/:id` returns no audit block at all** — undocumented previously; `RoleEdit.tsx` falls back to a second list-endpoint query for the hero's audit line; (7) **audit rendering corrected** to the shared `AuditMeta` (relative time + tooltip, `everEdited` suppression) — the list's Updated column previously described a plain `updated_at===created_at` check, the same error class Task 4 found in `users`; (8) **login gained a `hasClusterAdmin` OR-branch** independent of `hasPermission`/`checkPermission` (unchanged, verified line-for-line); (9) **`DEV_MOCK_EFFECTIVE_PERMISSIONS` was deleted from source** (`19d90c4`, 2026-08-06) — edge case 9 described a mechanism that no longer exists; (10) **PR #281 (multi-BU seed selection) is NOT this module's tables** — traced to a BU-scoped tenant-app-role seed op, documented as a "do not confuse" gotcha; (11) confirmed `email_verified_at` (Task 4's flag) surfaces only on `UserPlatformEdit.tsx`. **Coordinator-caught mid-task:** `platformNav.ts`'s own `resourceRank()` doc comment names `rbac`/`license` as resources with no menu row — `rbac` is stale (retired by the rename; `platform_role` now has its own nav row) — re-verified from code (grepped every permission call site) and corrected to `activity_log`/`license` in both `rbac.md` and `permissions.md`. Full citations in `task-5-report.md`. | `roles`, `permission-catalog` (2 + 1 specs) — **stale, not usable as current evidence**: `permission-catalog.spec.ts` navigates to the dead `/platform/permissions` route; `role-crud.spec.ts` drives the permission picker as a checkbox, which no longer exists post-`PermissionGrid`; its own page object's doc comment ("Name cell is a button") also contradicts the current `<Link>` implementation. Flagged for whoever owns e2e maintenance; out of scope to fix here. | No routes visually re-verified with screenshots this task (not requested); `/platform/roles`, `/platform/roles/new`, `/platform/roles/:id/edit`, and `/platform/category-permissions` all have UI changes documented above | `33201f5`, `16f5fa9`, `24dc5ce`, `0ac85ba`, `ba579a4`, `3e309bc`, `84b1b96`, `a7dfefd`, `eb75c6f`, `61a64d7` |
| 6 | `applications` | Existing (verify) | 4 pages rewritten (landing + data-model + permissions + ui-screens) | 4 pages mirrored at full depth (all four were pre-existing full TH mirrors, not stubs — same depth of edit as EN, per the standing full-mirror precedent) | Drifted through two undocumented waves since 2026-07-29: (1) **Access-column redesign** (`89ba8a8`, 2026-09-02, `#254`) — `ApplicationReachCell` replaces the old "All APIs"/"N APIs" badge with a bar + `granted/catalogSize` fraction anchored to the live catalog (warning tone at full reach); the Status column was removed outright (folded into an exception-only Inactive badge next to Name); Device is now quiet text via a new shared `formatDevice()`, not a badge; (2) **the edit page adopted the same reach ruler** (`1c2894d`, 2026-09-02, `#255`, `utils/apiReach.ts`'s `reachOf()`), plus new authority-verb tinting (`isAuthorityAction()`/`AUTHORITY_VERBS` — a narrower set than "any write": delete/approve/submit/revoke and similar), stale-grant `+N` accounting for grants pointing at retired catalog keys, an "`N` modules never reached" line, and a dormant-grants note when `allow_all` hides but doesn't clear stored `api_names`; Device/Status in the Settings rail were also de-duplicated from badges to plain read-only text (the prior wiki text wrongly said "capitalised Badge"/"Status badge"); (3) **new cross-cutting View History** (`activity_log.read`, `PLATFORM_SCOPED_RECORD` sentinel) on both the list row menu and the edit hero — completely undocumented before this task, same feature pattern as clusters/business-units/users; (4) **Registry summary strip moved to a dedicated endpoint** (`GET /api-system/applications/summary`, `99a93c8`, 2026-08-24 — one of the same "5 pages" `rbac`'s equivalent fix came from) replacing a client-side `perpage:-1` sweep; its response carries a fleet-wide `deleted` count that is on the wire but not rendered; (5) **audit precedence corrected**: Created/Updated now read via the shared `auditColumns()`/`normalizeAudit()` (added `a85a166`, 2026-08-22, one of "5 main Management tables") — nested tried first, flat is the fallback, `updated` gated on `everEdited` — the prior text described a bespoke flattening mechanism with a fixed timestamp format and a plain `updated_at===created_at` check, both wrong; (6) **`feature="applications"` flag** added to all three routes and the nav entry (`8f95aae`), previously undocumented; (7) **nav-source citation fixed**: sidebar entry lives in `platformNav.ts` (line 40), not `Layout.tsx`, which no longer defines nav rows — same correction Tasks 4/5 made elsewhere; (8) **CSV export column count corrected** (9 columns incl. 4 audit columns added `a85a166`, prior text said 5); (9) **catalog size refreshed** to 900 keys / 148 modules (source HEAD `157a65e`, 2026-09-04), up from 788/125; (10) a pre-existing **internal inconsistency** (At-a-Glance said "5 `localStorage` keys" while its own table below listed 6) was corrected to 6. Two claims were re-verified and confirmed still correct, not changed: the "Export/Debug Sheet intentionally ungated" claim (the brief's explicit ask) and the schema fields for `tb_application`/`tb_application_api` (byte-identical to the last sync). Full citations in `task-6-report.md`. | `applications` (4 specs: create/delete/edit/list) — **stale, not usable as current evidence**: `application-delete.spec.ts` asserts a `role="alert"` banner reading "Failed to load application" after navigating to a deleted app's edit URL; current source instead renders a dedicated not-found `EmptyState` card ("Application not found..."), and the alert-role error path is reserved for a different (transient-fetch) failure that the not-found branch never triggers. The other three specs use generic page-object helpers with no column/badge assertions, so they don't directly contradict the redesign, but were last touched 2026-06-11 (before every change this row documents) — flagged as unverified-fresh. Flagged for whoever owns e2e maintenance; out of scope to fix here. | No routes visually re-verified with screenshots this task (not requested); `/applications` (Access column, Name-column Inactive badge, Device text, View History row action) and `/applications/:id/edit` (hero reach ruler + authority count, API access card authority tinting/stale-grant/untouched-module lines, Settings rail plain-text Device/Status, hero View History) both have UI changes documented above | `cdffa57`, `00e92ce`, `e508dcb`, `753dd8a`, `8bce209`, `f0386bb`, `807126f` |
| 7 | `broadcasts` | Existing (verify) | 4 pages rewritten in full (landing + data-model + permissions + ui-screens) | 4 pages mirrored at full depth (all four were pre-existing full TH mirrors, not stubs — same depth of edit as EN) | **The module's shape changed more than any prior task in this plan**: the 2026-06-10 baseline documented a single compose-only screen (fire-and-forget, no list/edit/delete); a 2026-08-10/11 "notification redesign" (`20260810000000_notification_redesign_additive`/`20260811000000_notification_redesign_drop_legacy` migrations, read in full) plus follow-on work through 2026-09-02 added two entirely new screens and a full sender-side lifecycle the last sync never saw: (1) **`BroadcastManagement` (list, `/broadcasts`) and `BroadcastEdit` (view/edit, `/broadcasts/:id/edit`) are brand new components/routes**, alongside the pre-existing `BroadcastCompose` — confirmed Compose and Edit are two separate files/components per the brief's explicit ask, not one screen in two modes; (2) **`category`/`type` varchar columns dropped from `tb_broadcast_notification`, replaced by `scope`/`doc_type`/`event` enums** — `event` is hardcoded `info` on every broadcast (no `severity` column exists anywhere); the sender's Info/Warning/Critical/Maintenance/Other… choice is purely decorative, stored in `metadata.severity`, and the compose UI says so directly ("recipients see a standard notification"); (3) **`end_at` flipped from "declared but dead" to mandatory** — required by both the SPA and the Zod request schema, and is the sole input to a newly-computed (never-stored) `status`; (4) **`doc_version` flipped from schema-only to a real optimistic lock** — `PATCH`/`DELETE` both check it, 409 on mismatch; (5) **`broadcast.read` flipped from a seeded-but-orphan key to the real gate** on the List route, nav entry, and Edit page's view mode; **`broadcast.update`/`.delete` are two more real keys** gating Edit/Expire-Now and Delete respectively — all six broadcast routes (2 send + 4 new admin) are guarded server-side via `PlatformPermissionGuard`, coarse enforcement unchanged since PR #239; (6) **content lock**: title/message/metadata editable only while a broadcast is still `scheduled` (400 `content_locked` otherwise) — an entirely new rule; (7) **`created_by_id` divergence fixed**: the pre-redesign wiki documented `system_all` sends leaving `created_by_id = null` (Swagger promised otherwise) — `BroadcastService.create()` now sets it consistently for every non-`users` audience; (8) **email fan-out on send removed entirely** — read `NotificationWriteService`/`BroadcastService`/`NotificationGateway` in full, none call `EmailService`; (9) **a new `ScheduleWorker` (30 s cron, `tb_notification` only) now delivers a live push for scheduled *targeted* (`system_users`) sends** — fixing the exact "no such worker exists" gap the last sync found — but **`system_all`/`bu` scheduled broadcasts still get no live push ever**, since the worker never queries `tb_broadcast_notification`; this is the one gap that persists structurally unchanged, just for a narrower reason; (10) **targeted (`system_users`) sends remain the sole fire-and-forget mode** — they still fork into `tb_notification` and are the one target mode the new List/Edit screens cannot see, search, edit, or delete (the Compose subtitle states this outright: "Broadcasts sent to specific users don't appear here"); (11) **BU-not-found on send corrected from a claimed 500 to the actual 404** (`COMMON_BUSINESS_UNIT_NOT_FOUND`, traced through the shared error catalog's `http_status: 404`); (12) confirmed the brief's two explicit checks: Compose/Edit are separate components (true), and the "Reset/tabs/fields ungated" `permissions.md:39` claim is correct as far as it goes but incomplete — the two system-audience tabs are conditionally rendered behind a raw `hasPermission()` check (not a `<Can>` wrapper, and still practically unreachable since the route requires the same key). Full citations in `task-7-report.md`. | `broadcast` (1 spec: `broadcast-compose.spec.ts`, 2 tests) — **not stale, unlike every other suite checked in this plan so far**: page title "Send Broadcast", the title input's placeholder, and the empty-submit validation messages all matched current `BroadcastCompose.tsx` exactly; it only covers Compose's smoke/validation path and has zero coverage of the new List/Edit screens. | No routes visually re-verified with screenshots this task (not requested); `/broadcasts` (new list screen entirely), `/broadcasts/new` (Related-BU metadata tag, mandatory expiry presets, back link now goes to the list), `/broadcasts/:id/edit` (new screen entirely) — all UI-changed; screenshots deferred per plan | `b7489df`, `52ecb92`, `03c2076`, `ed7aacd`, `8801e4a`, `a540c37` |
| 8 | `news` | Existing (verify) | 4 pages verified and surgically edited (landing + data-model + permissions + ui-screens) — the 2026-07-29 baseline was already unusually thorough, so this was targeted correction, not a full rewrite | 4 pages mirrored with the same surgical edits (all four were pre-existing full TH mirrors, not stubs) | The single biggest finding: **server-side permission enforcement on `/api/news` is asymmetric and changed mid-project.** Before `9b474a3fc` (2026-08-20, proven in `carmen-turborepo-backend-v2`), `news.controller.ts` had **zero** `@RequirePlatformPermission` decorators — only `KeycloakGuard` + `AppIdGuard` — so any authenticated, app-id-allowed caller could create/update/delete news regardless of RBAC keys; the wiki's pre-existing framing ("`news.delete` exists only as an in-page gate") was accurate for both reads *and* writes at sync time. That commit added `PlatformPermissionGuard` to the three **write** routes only; the four **read** routes (`findAll`/`findOne`/`tags`/`summary`) remain deliberately ungated on the user-permission axis forever, per the commit's own stated reason: the `mobile-app` application shares these routes for tenant-level users holding no platform role at all. This flips the "known gap" framing entirely and is now documented as two edge cases (14–15) plus rewritten §1/§2 in `permissions.md`. Also fixed: (1) **the empty-state Add News CTA is `<Can>`-gated, not ungated** — the brief's flagged pre-existing error, traced to `239b4a9` (2026-06-10, proven in `carmen-platform`), which predates the page's own 2026-07-29 sync date — a genuine stale-wiki bug the July 29 writer missed, not new drift; "Export is ungated" was independently re-verified and confirmed still correct; (2) **`NewsroomSummary` now reads a dedicated `GET /api/news/summary` endpoint** (added 2026-08-24, gateway commit `96389a3ee`) — the wiki described the pre-fix client-side `perpage:-1` sweep via a since-deleted `summarizeNews` helper; the REST table in `data-model.md` was missing this endpoint entirely; (3) **cross-cutting View History** (`activity_log.read`, `PLATFORM_SCOPED_RECORD`) was completely undocumented — added to all four pages, with a location correction caught before commit: on `NewsEdit` it sits in the page's top header row next to the back link, **not** inside `NewsMasthead`'s own `actions` slot (unlike some other modules' hero-embedded pattern); (4) **audit rendering corrected**: the list's Updated column and the edit page's History card both switched to the shared `AuditMeta` component on 2026-08-22 (relative time + absolute-time tooltip, `everEdited` suppression) — the wiki described a bespoke absolute-`YYYY-MM-DD HH:mm:ss` renderer, the same error class Tasks 4–6 found elsewhere; (5) stale citations fixed: nav source is `platformNav.ts` line 27, not `Layout.tsx`; `schema.prisma` line numbers for `tb_news`/`enum_news_status` shifted (812→884, 726→798) as the file grew. Two claims were independently re-verified and confirmed correct, not changed: "Export is ungated" and the server-side sort override to `updated_at DESC`. Full citations in `task-8-report.md`. | `news` (5 specs: create/delete/edit/filter/image-upload, last touched 2026-06-11) — **one confirmed-broken locator, rest apparently compatible but narrow**: `NewsEditPage`'s `backButton` locator (`[aria-label="Back to news"]`) matches nothing — current `BackLink` component renders no `aria-label` at all, only visible text ("News") plus an icon; the create/delete/edit/filter happy-path specs otherwise still reference existing selectors (title/content/status/search) but exercise none of bulk actions, tags, the thumbnail column, `NewsroomSummary`, View History, or the 2026-08-20 write-permission fix — a coverage gap, not by itself further staleness. Flagged for whoever owns e2e maintenance; out of scope to fix here. | No routes visually re-verified with screenshots this task (not requested); no structural UI change on `/news`, `/news/new`, or `/news/:id/edit` (the module's screens were already correctly documented as of 2026-07-29) — only gating/enforcement, endpoint, and audit-rendering corrections | `318f321`, `949d921`, `55b2629`, `a480f58`, `85ad01e` |

**Cross-module note for whoever executes Task 28 (`report-form-groups`):** the Task 1 source map above lists `report-form-groups` as a brand-new top-level module for Task 28 to create from scratch. It already isn't green-field: `en/platform/report-templates/form-groups.md` / `th/platform/report-templates/form-groups.md` (created `dd95e68`, 2026-07-29, in scope for and updated by Task 9) already document `ReportFormGroupManagement` in full — 8 sections, current as of this task — but at the sub-page URL `/en(th)/platform/report-templates/form-groups`, not the future top-level `/en(th)/platform/report-form-groups`. Task 9 did not relocate or delete this sub-page (breaking its working, accurate, actively-linked cross-references to create a page that doesn't exist yet seemed like the wrong call for a single-module verify task), and instead added one forward-looking sentence to `report-templates.md` §1 in both locales noting the planned move. **Task 28 needs to decide**: relocate/split this existing sub-page's content into the new top-level module (updating every cross-reference across all four `report-templates` pages, both locales, that currently link to the sub-page), or some other resolution — but the content-authoring work is largely already done, not something to write fresh.

| 9 | `report-templates` | Existing (verify) | 5 pages touched (landing + data-model + permissions + ui-screens + form-groups); `xml-spec.md` read but not edited — no drift found | 5 pages mirrored at matching depth (landing/permissions/data-model/form-groups are pre-existing full TH mirrors, edited at full depth; `ui-screens.md` is a pre-existing TH stub, corrected in place per the standing TH-stub deferral — not expanded to full mirror) | The 2026-07-29 baseline was, like `news`, unusually thorough — it had already captured essentially all ten of the brief's "known drift" items (all ten cited commits are dated 2026-07-21 through 2026-07-24, *before* the 2026-07-29 sync date), so this was a targeted correction pass. Fixed the brief's flagged pre-existing error and **two further downstream repetitions the brief didn't call out**: the empty-state Add Template button is `<Can permission="report_template.create">`-gated identically to the header button (`ReportTemplateManagement.tsx:529-536`, traced to `239b4a9`, 2026-06-10) — the wrong "ungated"/"visible but leads to Forbidden" framing appeared in *four* places (`permissions.md` §3 matrix, §7 table, §7 tester-consequences prose, and `ui-screens.md` §2.3), all fixed both locales. Caught a full undocumented drift wave from 2026-08-20–2026-09-02 the last sync never saw: (1) **cross-cutting View History** (`activity_log.read`, `PLATFORM_SCOPED_RECORD`) on list rows and the edit-page `PageHeader` actions row — same feature pattern as every other module in this plan; (2) **the "Metadata" card was deleted 2026-08-22** (`682652d`) in favor of a `PageHeader`-level `audit={normalizeAudit(...)}` line — the same "hero absorbs the read-mode card" pattern Tasks 4/5/8 found elsewhere; (3) **audit rendering corrected** to shared `auditColumns()`/`AuditMeta` (nested-then-flat precedence, `everEdited` suppression) — the wiki's own pre-existing text hadn't been backwards here, it just hadn't been updated to the new component at all; (4) **CSV export grew from 6 to 9 columns** (4 audit fields, always absolute-ISO); (5) **nav source moved to `platformNav.ts`** (not `Layout.tsx`) with a **third** filter condition (`feature` flag, `hide`/`inactive` states) the last sync's quoted snippet never had — Report Templates and Form Groups **share** `permission: 'report_template.read'` but carry **separate** `feature` keys (`report_templates` vs. `report_form_groups`), a nuance the brief explicitly asked to be verified and stated; (6) `doc_version` rollout **mis-dated 2026-07-16 → corrected to 2026-06-12** (migration `20260612000000_add_doc_version`, proven via commit date); (7) stale `schema.prisma` line citation fixed (734→806); (8) a landing-page-only internal inconsistency fixed — `report-templates.md` §4 still said `<AccessDenied>` even though the sibling `permissions.md` had already documented the `Forbidden` rename on the same 2026-07-29 sync date; (9) Form Groups' `GroupCard` gained a new compact per-row audit line 2026-08-22 (`f62a90e`), previously undocumented; (10) the Dialog XML tab's error aria-label changed "Invalid"→"Has errors" in a 2026-09-02 `TabStrip` refactor (`3b7cba0`) — a dropped line-citation from this specific edit was caught by the removed-line audit and fixed in a follow-up commit. **See the cross-module note above** for the one structural finding that isn't a simple drift item: a `report-form-groups` sub-page already exists under this module, pre-empting part of Task 28's planned scope. Full citations in `task-9-report.md`. | `report-templates` (2 specs: crud/list, last touched 2026-06-11) — **one confirmed-broken test**: `report-template-crud.spec.ts`'s create step (TC-RT-030001) never fills `template_type` (`ReportTemplateEditPage.fillRequired()` only sets `name`/`report_group`/`description`), but `template_type` became a required field with an inline validation error on `9cfbf72` (2026-07-21) — over a month after the suite's last touch; the test would now fail before ever reaching the "created successfully" toast it asserts. `report-template-list.spec.ts` (3 tests) is narrow but not contradicted by current source. Flagged for whoever owns e2e maintenance; out of scope to fix here. | No routes visually re-verified with screenshots this task (not requested); no structural UI change on `/report-templates` or `/report-templates/new` beyond what was already correctly documented as of 2026-07-29 — `/report-templates/:id/edit` lost its separate Metadata card in favor of a `PageHeader` audit line, and both list and edit pages gained a View History action | `95e2666`, `c621912`, `ae3fe5c`, `32f1169`, `0d9f9c5`, `10c2302`, `fe469eb` |
| 10 | Five standalone pages (`dashboard`, `landing`, `profile`, `changelog`, `sql-workbench`) | Existing (verify) | 5 pages rewritten in full | 5 pages mirrored at full depth (all five were pre-existing full TH mirrors, not stubs) | These five pages sit outside every module folder and had drifted through the same shell-level changes every module page in this plan cited stale (`Layout.tsx` no longer defines nav rows — it only calls `buildPlatformNav()` from `platformNav.ts`, which grew from a smaller list to 25 rows across 7 `groupKey` groups since 2026-07-29), plus three page-specific findings none of the module tasks could have caught: (1) **`PrivateRoute` gained a whole platform-authority/cluster-admin resolution branch** (`382c0b5`/`c4a1275`, 2026-08-05/06) that silently redirects a membership-only cluster admin from `/dashboard` (and `/profile`) to `/cluster-admin` — both pages' "authentication is the only gate, identical to Profile" framing was true as far as it went but omitted this shared mechanism entirely; documented in full on `dashboard.md` §5 and cross-referenced from `profile.md` §4; (2) **`Profile.tsx` is confirmed mounted at two routes sharing one component** (`/profile` via bare `PrivateRoute`, `/cluster-admin/:clusterId/profile` via `ClusterAdminRoute` + `isClusterAdminOf(clusterId)` membership check, no permission string) — the component's own comment states the param is the only difference; added as new §1.1 with a chrome-comparison table (guard, nav, brand identity, brand-mark destination, header extra); (3) **the SQL Workbench permission gap this page and the Inventory book's Query Dataset page both documented as open was closed 2026-08-20** (`525597688`, `carmen-turborepo-backend-v2`) — all four previously-unguarded routes (`GET db-objects`, `GET db-objects/definition`, `POST save`, `DELETE db-objects`) now carry `PlatformPermissionGuard`; Query Dataset itself was not updated for this fix and remains stale (flagged, out of scope — Inventory book). Also fixed: (4) Dashboard's Activity Stream `FetchErrorState` claim was backwards — `fetchActivity()` uses `Promise.allSettled` and never rejects, so a total failure across all six domains renders the same empty state as six genuinely-quiet domains, not the error/Retry path; (5) Dashboard's "What's Not on the Dashboard" list was stale by ~13 modules (License Management/Analytics/Scheduling groups plus half of Platform didn't exist at the last sync) — rewritten as a full `groupKey`-by-`groupKey` table; a removed-line audit then caught that the list's own "Broadcasts has no list endpoint" claim was itself stale (`broadcastService.getAll()` has existed since the 2026-08-10/11 notification redesign Task 7 documented) and fixed it in a follow-up commit; (6) Landing's "Confirmed Drift" section undercounted by an order of magnitude — the real sidebar has grown to 7 `groupKey` groups against Landing's static 3, so the section was rewritten with a full group-by-group comparison table; a self-caught error during construction misattributed a "deliberately ungated" citation to `sql-workbench.md` §4 (which doesn't carry that claim — the pattern belongs to Feature Flags' own nav entry) and was corrected in a follow-up commit before this row was written; (7) SQL Workbench's own screen was redesigned 2026-09-02 into a fixed three-pane workbench frame — the result table's client-side pagination (50/100/200/500 rows/page) was replaced by a virtual-scrolled grid, and a new draggable `PaneDivider` splits editor/results (both documented in §3); its sidebar entry also moved from the Platform group to a new Database group (`b902d6a`, 2026-08-31); (8) Changelog's release process was substantially rewritten (`scripts/release.mjs`, 2026-08-05) from a bare version-bump-and-build into a gated release script (branch/tree/upstream/tag guards, typecheck+lint+test gates, git commit + annotated tag, no implicit "patch" default) — §5 rewritten in full. `sql-workbench.md`'s single-page depth remains appropriate (one route, one screen, two permission keys) despite the redesign — not recommended for a folder split. Full citations, both gating sweeps, the TH heading diff, and every proven hash are in `task-10-report.md`. | **Correction to the Task 1 source map**: `landing` has **no e2e suite at all** — `../carmen-platform-e2e/tests/landing/` is an empty directory (no spec file, no git history), so the source map's "e2e suite: `landing`" cell was wrong from the start; sql-workbench has none, matching the map. Of the three real suites: `dashboard.spec.ts` (8 tests, last touched at the original `bb8f671` import) is mostly narrow smoke coverage compatible with current source, but its three conditional "click generic text, expect navigation" tests (Cluster/Business Unit/User) are plausibly broken by the Activity Stream redesign — its filter chips (plain, non-navigating `<button>`s labelled "Clusters" etc.) render before the timeline in DOM order, so a bare `text=Cluster` locator likely resolves to a chip instead of a navigating link; not run to confirm, flagged as a risk rather than a confirmed break. `profile.spec.ts` (15 tests) and `changelog.spec.ts` (3 tests) are both **notably not stale** — every locator (element IDs, button text, the `<h1>` title, the version-agnostic `v\d+\.\d+` regex) still matches current source exactly, the same "narrow suite still accurate" exception Task 7 found for `broadcast`. | No routes visually re-verified with screenshots this task (not requested); `/dashboard` (What's Not on the Dashboard list), `/` (Confirmed Drift table), and `/sql-workbench` (three-pane workbench frame, virtual-scroll result grid, draggable split, Database group sidebar entry) all have content/UI changes documented above; `/profile` and `/cluster-admin/:clusterId/profile` have no UI change, only a documentation gap closed | `42e9485`, `cc0550a`, `e865e37`, `1b3b9d4`, `9b8bd52`, `092f927` |
| 11 | `print-template-mapping` | **Deletion** — feature removed from the product | Deleted `print-template-mapping.md` + 3 sub-pages (`data-model`/`permissions`/`ui-screens`). Rewrote every inbound link found by a full `](...)`-pattern grep — 19 link occurrences across `platform.md`, `landing.md`, `report-templates.md`, and its `permissions`/`data-model`/`form-groups`/`ui-screens` sub-pages, plus `rbac/permissions.md` — not just the 8 line numbers the brief enumerated. Removed the dead `slug: print-template-mapping` row from `scripts/nav-overrides.yaml`. Added a new §6 "Notable product removals" to `changelog.md` (no prior entry existed, confirmed — `src/data/changelog.json` itself has never used the `Removed` category) | Same 4 files deleted; same 18 link occurrences rewritten on the TH mirrors (`report-templates/ui-screens.md` carried no TH reference to begin with — confirmed by grep, nothing to touch); same nav-overrides row (shared file, one edit for both locales); same new §6 added to TH `changelog.md` | Proved both citied removal commits with `git cat-file -e` before writing them: `de11377` (carmen-platform, 2026-07-24, "remove the print template mapping pages") and `c135bb21e` (carmen-turborepo-backend-v2, 2026-07-23, "delete the print-template-mapping module and its reports proxy"). The five "historical page only" sentences the brief flagged as a conflict (`report-templates.md` §5, `report-templates/permissions.md` §7 cross-links, `data-model.md` §7 cross-links, `form-groups.md` §6 related-modules, `rbac/permissions.md` §2) were rewritten to carry the succession fact directly in prose — `template_type` + Form Groups, and the one capability that did not carry over (per-BU default scoping) — rather than pointing at a page that no longer exists. `landing.md` §4's claim about the SPA's own hardcoded index was re-verified against current `Landing.tsx` (`pages.landing.itemPrintMapping` is still in the Content group) and kept, de-linked only, per the brief's explicit instruction to check before touching it; its §6 Related-Modules bullet (a bare cross-reference back to §4, no independent content) was dropped rather than rewritten. Two discrepancies found in the controller's own brief/prep: (1) `scripts/migrate_books/platform_pages.yaml`, named as a file to modify, has zero print-template-mapping references — grepped clean, left untouched; (2) the "Task 11 PREP" block this task was told to read in this log does not exist here — the closest equivalent is the "Ruling note" at the top of this file (lines 19–25), which was used instead and is consistent with everything found independently. Full detail in `task-11-report.md`. | `../carmen-platform-e2e/tests/print-template-mapping/` is stale per the brief and was left untouched — out of scope | No routes — deletion-only task, nothing to screenshot | `e1d1af1` |

**Cross-module note for whoever executes Task 21 (`tenant-migrations`):** the same situation Task 9 flagged for `report-form-groups` (above) recurs here. `en/platform/business-units/tenant-migrations.md` / `th/platform/business-units/tenant-migrations.md` (existing full pages, last touched by Task 3 on `e63214c`/`d418e7f`/`2271cd8`) already document `TenantMigrationManagement` (+ `DeployConsole`, `FleetSync`) in full — Overview, gating (`cluster.read` + `tenant_migrations` feature flag, super-admin-only actions), and the fleet-wide relationship to the per-BU `TenantMigrationCard` — at the sub-page URL `/en(th)/platform/business-units/tenant-migrations`, not the future top-level `/en(th)/platform/tenant-migrations` this plan's Task 1 source map calls for. Task 21 needs to decide, same as Task 28: relocate the existing sub-page's content into the new top-level module (updating cross-references in `business-units.md` and its sibling sub-pages, both locales), or some other resolution. Flagged during Task 12 scaffolding, not resolved there — the new `tenant-migrations.md`/`tenant-migrations/data-model.md` skeletons reference this note in their own `todo:` lists in `platform_pages.yaml`.

| 12 | 16 new modules (scaffold): `licenses`, `license-catalog`, `cluster-admin`, `platform-config`, `email-settings`, `user-platform`, `super-admins`, `feature-flags`, `tenant-migrations`, `tenant-imports`, `usage-analytics`, `activity-events`, `platform-migrations`, `database-pools`, `cronjobs`, `report-form-groups` | New — scaffold only, no prose | 35 skeleton pages written across the 16 modules per the brief's depth table (landing always; data-model/ui-screens/permissions per the depth rule) — none expanded beyond the brief's table; no module needed fewer pages either | 35 skeleton pages mirrored (same `path`s under `th/platform/`), with real Thai `title_th` values per module (technical terms with no natural Thai equivalent, e.g. "Cronjobs"/"SQL Workbench"-style, kept in English per the book's existing precedent) | N/A — this task writes no product-behavior prose; `at_a_glance`/`description`/`references` in `platform_pages.yaml` are sourced from the Task 1 source map and repo file listings, not invented | N/A — no content claims made yet; per-module e2e-backing notes are already recorded in the Task 1 source map above for Tasks 13–28 to consult | None — scaffold-only task, nothing to screenshot | `c352554`, `73526b8`, `56cfda2` |

**Findings from Task 12 (scaffolding), for Tasks 13–28 and the controller:**
- **Two pre-empted modules, not one.** Task 9 already flagged `report-form-groups` (Task 28) as pre-empted by `report-templates/form-groups.md`. Task 12 found a second, identical case: `tenant-migrations` (Task 21) is pre-empted by `business-units/tenant-migrations.md` — see the cross-module note above. Both existing sub-pages are current, accurate, and were edited by their parent module's verify task; both `Fill from source` `todo:` items in the new landing pages instead read "decide on relocation."
- **Step 1b pruning matched the brief exactly**: the seven stale blocks (`index.md`, `clusters/index.md`, `business-units/index.md`, `users/index.md`, `report-templates/index.md`, `profile/index.md`, `auth-roles/index.md`) were the only ones missing from disk; the re-run check printed nothing.
- **`report-templates/form-groups.md` was missing from the YAML's declarative record** despite existing on disk since Task 9 — added as an entry (path, titles, description, references) matching the file's actual frontmatter/content, no `todo` items since it needs no further scaffolding work.
- **No module needed more pages than the brief's table assumed.** Checked each module's permission-key count and route count against the depth rule; several modules (`platform-config`, `email-settings`, `database-pools`, `cronjobs`) have two permission keys (a `.read`/`.manage` pair) but the brief's table already omits a `permissions.md` page for these — consistent with the pattern of not giving a dedicated page to a simple read/manage pair. No deviation made from the brief's table.

| 13 | `licenses` | New — full write, no prior prose | 4 pages written in full (landing + data-model + ui-screens + permissions), replacing Task 12's three-section placeholder body on each | 4 pages mirrored in full Thai (Thai section headings throughout, matching the book's established mirror pattern — not the English-header stub shape some other modules' pre-existing TH sub-pages still carry) | Wrote this module from scratch (Task 12 left it a skeleton), so "claims fixed" here means corrections to the skeleton's own seeded description text, not drift since a prior sync: (1) **`permissions.md`'s pre-existing description named `license.manage` as a CRUD gate for this module — false.** A repo-wide grep finds `license.manage` in exactly one place, `PlatformConfigManagement.tsx:75` + `platformConfig/LicenseEnforcementCard.tsx:14,109`, gating an unrelated License Enforcement toggle on the **`platform-config`** module. This module's only two keys are `subscription.read`/`subscription.manage`; the description was rewritten and the correction stated explicitly in `permissions.md` §1 (both locales) so a future editor who re-adds the claim has something to check against first; (2) confirmed and documented the module's one genuinely distinctive gating shape — the three `:id/edit` purchase-form routes require `subscription.read`, not `subscription.manage` (only the sibling `new` routes need `.manage`) — proven three ways (route table in `App.tsx`, each component's own `canEdit`/`canEditFields` computation plus its `<Can>` wraps, and matching backend `@RequirePlatformPermission('subscription.manage')` on every write route with no permission decorator at all on any `GET`); (3) confirmed and wrote up the two-views-two-grains distinction the brief flagged: `v_cluster_bu_cap` (one row per cluster, winning-licence semantics, `cancelled_at IS NULL` added by `20260901020000_cluster_license_cancel`) vs. `v_cluster_bu_quota` (one row per business unit, ranks BUs `is_hq DESC, created_at ASC, id ASC`, borrows — never recomputes — the cap from the first view); (4) documented `LicenseKindConfig`'s full seat-vs-BU-quota difference table (owner, amount field, no-expiry toggle, note field, counting rule — sum vs. winner-take-all, usage reader, cancel availability, licence-number prefix, edit-path segment, threshold field) — seats have **no cancel endpoint at all** on either the frontend service or backend controller, confirmed at the schema level (no `cancelled_at`/`cancelled_by_id`/`cancel_reason` columns on `tb_business_unit_license`); (5) documented all three expiry-threshold fields (`subscription_days`/`bu_quota_days`/`seat_days`, default 30 each, merged per-field onto in-code defaults, delivered via a deliberately permission-free endpoint) and their distinct per-ledger `isExpiringSoon()` semantics, per the brief's explicit ask that this belongs in `data-model.md`; (6) confirmed this module has **no cross-cutting View History/Activity Trail feature** at all, unlike most other modules in this plan — a full grep of `src/pages/licenses/` and `LicenseCenter.tsx` for `activity_log.read`/`ActivityTrailSheet`/`PLATFORM_SCOPED_RECORD` returns nothing; (7) found and flagged as genuinely dead code `src/pages/licenses/subscriptionEdit/SeatsCard.tsx` — it exists in source with its own test file, but is imported by zero production page components (confirmed by a full-repo grep); a comment in its sibling test file claims it is "still used at License Center," which is stale relative to current source — not documented as a live screen, flagged instead in `ui-screens.md` §7 as a source-comment-vs-code discrepancy; (8) self-caught during construction: my first draft of the backend permission-line citations in `permissions.md` had several `platform_cluster-licenses.controller.ts` line numbers off by 1–6 lines (guessed from an earlier full-file read rather than re-grepped at citation time); caught by re-running `grep -n` against the live file before committing and corrected in place, before any commit landed with the wrong numbers. Full citations in this report. | **None — confirmed no e2e suite exists.** `../carmen-platform-e2e/tests/` has no `licenses` directory, matching the Task 1 source map's "no e2e backing" list; every claim in all four pages is sourced from `../carmen-platform` and `../carmen-turborepo-backend-v2` implementation directly, marked as such in each page's At-a-Glance callout | Not applicable — new module, no prior screenshots to compare and none requested this task | `22c0f6f`, `6705985`, `6087fa1`, `eef680b`, `11dcda7`, `e2253fd`, `c7b8d20` |
| 14 | `license-catalog` | New — full write, no prior prose | 3 pages written in full (landing + data-model + ui-screens), replacing Task 12's three-section placeholder body on each; no dedicated `permissions.md` (per the depth rule) — both permission pairs documented in full on the landing page's §4 instead | 3 pages mirrored in full Thai (Thai section headings throughout) | Wrote from scratch (Task 12 left a skeleton); confirmed all three of the brief's flagged tab behaviours directly from `LicenseCatalog.tsx` (tabs are routes not state — `navigate(TAB_PATH[next])` remounts and refetches; header title constant, only subtitle switches, verified the actual i18n strings; tab strip hidden via `tabs.length > 1` when only one tab passes both `hasPermission`+`flagOf` checks — a client re-check of what `PrivateRoute` already enforced in the same order, permission then feature flag). Two substantive findings beyond the brief: (1) **the source itself is stale, not just a prior sync** — `FeatureCatalogPanel.tsx`'s and `ModuleShelf.tsx`'s own doc-comments describe "76 rows"/"10 modules + 66 children," which predates the 2026-09-03 "license feature tree" n-tier restructuring (design doc read in full); counting the generator's own current output (`seed.license-feature.data.ts`) directly gives **89 rows / 11 root modules / 79 `active` / 10 `inactive`** (the ten newly-reserved `accounting.*` keys, seeded not-yet-sellable ahead of their real endpoints) — documented with the exact grep commands in `data-model.md` §3, both locales, as a "verify against generator output, not the in-source comment" warning; (2) **a genuine cross-module permission dependency**: the Bundles tab's Features-composition divisor and the whole feature picker on `LicenseFeatureGroupEdit` both load through `subscriptionService.getFeatureCatalog()` → `GET /api-system/platform/license-features` (no `/all`), which lives in `platform_subscriptions.controller.ts:198-200` and requires **`subscription.read`** — the `licenses` module's own key, not either of this module's two pairs; a session with only `license_feature_group.read`/`.manage` can fully reach and edit bundle metadata but the composition bar silently disappears and the feature picker fails outright — documented as a first-class finding on the landing page §4.3, cross-referenced from `ui-screens.md`. Also confirmed and documented: the server-side "child drags full ancestor chain" rule in `LicenseFeatureGroupService.setFeatures()` (expand-before-validate, not the reverse, per the service's own comment); delete-in-use is blocked server-side (409 via `ALREADY_EXISTS`) while the confirm dialog's stronger copy is only a warning, not the enforcement point; backend gating here is fully explicit (every route, reads included, carries `@RequirePlatformPermission`) — the opposite shape from `licenses`' scope-authorized `GET`s, stated as a named contrast, not a bare "ungated" claim; `FeatureSelectionCard` now has exactly one caller (`LicenseFeatureGroupEdit`) since a 2026-09 sales-flow phase moved subscriptions to whole-bundle selection via the `licenses` module's own `GroupSelectionCard`. Self-caught before the first commit: the first draft of every "Licenses" cross-link on the landing/data-model pages pointed at this module's own URL (`/en/platform/license-catalog`) instead of `/en/platform/licenses` — found by grep, fixed with `sed`, re-verified clean before committing. Full citations in `task-14-report.md`. | **None — confirmed no e2e suite exists.** `../carmen-platform-e2e/tests/` has no directory for this module; every claim in all three pages is sourced from `../carmen-platform` and `../carmen-turborepo-backend-v2` implementation directly, marked as such in each page's At-a-Glance callout | Not applicable — new module, no prior screenshots to compare and none requested this task | `12fc957`, `797a9de`, `91a3b7f` |
| 15 | `cluster-admin` | New — full write, no prior prose | 3 pages written in full (landing + ui-screens + permissions), replacing Task 12's three-section placeholder body on each | 3 pages mirrored in full Thai (Thai section headings throughout) | Wrote from scratch (Task 12 left a skeleton) documenting a **second console and persona**, not another platform screen: every one of the five per-cluster routes (`ClusterProfile`, `BusinessUnitList`, `BusinessUnitForm`, `ClusterUsers`, `ClusterAdminLicenses`) sits behind `ClusterAdminRoute`, and every page states the gate the same way per the brief's binding Step 1b ruling: **"no RBAC permission key — gated instead by cluster membership via `isClusterAdminOf`,"** checked before the feature flag, never "no permission check at all"/"unguarded" — the exact error three earlier modules in this plan made once each, most recently `licenses`. Verified the order directly from `ClusterAdminRoute.tsx:20-59` (authenticated → `adminScope` loaded → membership `<Forbidden>` → feature flag last, per the component's own comment) and traced the identical check server-side: `ClusterAdminAuthzService.isClusterAdmin()` (`../carmen-turborepo-backend-v2/apps/micro-cluster/src/common/cluster-admin-authz.service.ts:46-63`) is called independently before every write this module's screens make, and the one backend read this module's entry screen depends on (`GET api-system/me/admin-clusters`) is confirmed in the backend's own permission-coverage exception list as a deliberate self-scope design, not an oversight. Corrected every seeded skeleton sentence against source (Task 12's placeholders were route-name guesses only) and contrasted each of Business Units/Users/Licenses against its already-documented platform-side twin: `BusinessUnitList` has **no create action at all** (BU creation consumes platform-owned `max_license_bu` quota); `BusinessUnitForm` is a **5-tab, edit-only** document (Overview/People/Hotel/Company/Configuration) splitting Hotel from Company where the platform's 6-tab form combines them as one Location tab, with Configuration fully read-only and no config-table/database-pool sections at all; the People tab's `BusinessUnitUsersCard` is the *same component* the platform edit page renders but computes `canEdit` from route reachability alone (`!accessLost`) rather than `cluster.update`, so a cluster admin can add/edit/remove BU users with **no permission key of any kind** — a genuine write-gating asymmetry worth flagging for testers; `ClusterUsers` manages an entirely different data model (`tb_cluster_user` membership + invitations, never the platform `tb_user` record); `ClusterAdminLicenses` was confirmed **fully read-only** by grepping the page and its four children for every write-affordance pattern (only two Retry buttons found) and reuses the `licenses` module's own hooks/services. `licenses.md`/`licenses/permissions.md` (Task 13) already cross-link this module using the identical corrected gate phrasing, confirmed consistent rather than restated differently. One genuine product gap found and documented as an edge case, not asserted as a bug: `ClusterAdminLicenses` is the only one of the module's five per-cluster screens that does **not** implement the shared `ClusterAccessLost` mid-session-403 pattern its four siblings share. Full citations in `task-15-report.md`. | **None — confirmed no e2e suite exists.** `../carmen-platform-e2e/tests/` has no `cluster-admin` directory; every claim across all six files is sourced from `../carmen-platform` implementation directly (plus `../carmen-turborepo-backend-v2` for the backend mirror of the membership gate), marked as such in each page's At-a-Glance callout | Not applicable — new module, no prior screenshots to compare and none requested this task | `f904f45`, `fe58357`, `96d8c28`, `8915bc6`, `9d993d7`, `35543a2` |
| 16 | `platform-config` | New — full write, no prior prose | 2 pages written in full (landing + data-model), replacing Task 12's three-section placeholder body on each; no dedicated `permissions.md`/`ui-screens.md` (per the depth rule) — all permission and UI-behavior content documented on the landing page's §3/§4 instead | 2 pages mirrored in full Thai (Thai section headings throughout, code identifiers kept in backticks per book precedent) | Wrote from scratch (Task 12 left a skeleton). The brief's core ask — enumerate every config key with its type, default, and real-world effect — is answered in full on landing page §3.2 and data-model.md §3-4 for all ten `PLATFORM_CONFIG_REGISTRY` keys (eight rendered by this module's own nine cards, `invitation` splitting across two; two more, `email_routing`/`feature_flags`, confirmed to exist on the same table but owned by other modules' own screens/endpoints, §3.5). The brief's flagged cross-module fact was confirmed precisely: a repo-wide grep finds `license.manage` in exactly one place, this module (`PlatformConfigManagement.tsx:75` + backend `writeKeyDenial()`), gating the License Enforcement card via `canManage && hasPermission('license.manage')` — **both halves of the conjunction matter**: `platform_config.manage` alone hides the Edit button entirely (no edit-then-403 trap), while `license.manage` alone still 403s on save because the endpoint-level decorator checked first is `platform_config.manage`, not `license`. Corrected the `licenses` module's earlier-scaffolded misassignment is not this task's own finding (Task 13 already fixed it there); this page is now the definitive, cross-linked source for what the key controls. Beyond the brief: (1) **`notification_email` has no confirmed reader anywhere** — grepped the key and its field names across `carmen-turborepo-backend-v2`, `micro-notification`, `micro-report`, `micro-cronjobs`, and `micro-data`; found only the registry definition and a stale test-file env assignment (not treated as source); documented as "write-and-persist-only, inert until a consumer is confirmed" rather than asserting an unverified effect; (2) **`license`/`platform_migration` share the identical 60s-cache/`=== true`-parsing shape but fail in opposite directions for a stated reason** — `license` fails toward "don't block a paying customer," `platform_migration` fails toward "keep the migration door shut," both being the safe direction for what each switch itself guards, not a plain inconsistency; (3) **the `tb_platform_config` unique index does not actually constrain live rows** (`(key, deleted_at)`, every live row's `deleted_at IS NULL`, and Postgres treats each `NULL` as distinct) — the same dead-on-active-rows shape Task 2 found for `tb_business_unit.code`, defended against structurally in `PlatformConfigService.findAll()`/`findOne()`/`patch()` by always taking the most-recently-updated row rather than relying on the constraint; (4) **`doc_version` exists on the table but is not read or incremented by any write path** — confirmed by reading `writeValue()` in full, matching the frontend service's own comment; (5) **flagged forward, not fixed:** `email_routing` is edited from the Email Settings module's own `EmailRoutingCard.tsx:124` (calling this module's identical generic `platformConfigService.update()`), gated on the frontend by `email_setting.manage` (`EmailSettingManagement.tsx:48`) — but the endpoint it actually calls still requires `platform_config.manage` at the controller level, the same universal decorator documented in §4.2. A session holding only `email_setting.manage` would see a working Edit UI there and 403 on every save — the identical bug shape `license.manage` exists to warn about, on a screen this task did not touch; documented on this module's own pages for whoever writes/verifies `email-settings` next, not corrected there (out of scope). Also independently verified (not merely quoted from a comment) that `feature_flags`'s `GET` truly carries no `PlatformPermissionGuard` at all (`AppIdGuard` only) while its `PUT` requires `feature_flag.manage` — a different key entirely. Ran the plan's required "do not describe a gate by its absence" sweep against all four files: zero hits in either language. Full citations in `task-16-report.md`. | **None — confirmed no e2e suite exists.** `../carmen-platform-e2e/tests/` has no `platform-config`/`configs` directory; every claim in both pages is sourced from `../carmen-platform` and `../carmen-turborepo-backend-v2` implementation directly, marked as such in each page's At-a-Glance callout | Not applicable — new module, no prior screenshots to compare and none requested this task | `fc524e2`, `44f694a` |
| 17 | `email-settings` | New — full write, no prior prose | 2 pages written in full (landing + data-model), replacing Task 12's three-section placeholder body on each; no dedicated `permissions.md`/`ui-screens.md` (per the depth rule) — all permission and UI-behavior content documented on the landing page's §3/§4 instead | 2 pages mirrored in full Thai (Thai section headings throughout, code identifiers kept in backticks per book precedent) | Wrote from scratch (Task 12 left a skeleton). Documented all five `EMAIL_FLOWS` with real trigger/setting/sender per the brief's core ask (landing §3.2): `register` (`AuthService.signupRequest()`, micro-business — two messages, `sendSignupLinkEmail()`/`sendAccountExistsEmail()`, share the flow, gated by the `signup` config key), `verify_email` (`AuthService.resendVerificationEmail()`, the legacy pre-verification path, `email_verification` key), `forgot_password` (`AuthService.forgotPassword()` → fire-and-forget `sendPasswordResetEmail()`, `password_reset` key), `invitation` (`UserInvitationService`, micro-cluster — three call sites: the link itself, an account-created confirmation, and an address-conflict notice, `invitation` key), and `notification` — **confirmed dead**: a repo-wide grep of every call site of the `Notifications.platformEmailSend` RPC contract across `carmen-turborepo-backend-v2`, `micro-report`, `micro-cronjobs`, and `micro-data` finds exactly seven, none passing `flow: 'notification'`; the routing panel still renders a live, assignable lane for it. Explicitly distinguished this from the platform-config module's own separate `notification_email`-key-has-no-reader finding (two independent "nothing consumes this" results on two unrelated mechanisms that share the word "notification"). **Step 1b, traced end to end as required:** the parent gate on the Email Routing card's Edit button is `email_setting.manage` (`EmailSettingManagement.tsx:48,140-154`); the card itself has no permission check of its own; the endpoint it saves to, `PUT /api-system/platform/configs/email_routing`, requires `platform_config.manage` at the controller level (`platform_configs.controller.ts:236-238`), and `writeKeyDenial()` adds nothing further for this key (only `license`/`platform_migration` get a second gate) — the two keys do not agree, confirming rather than contradicting the platform-config module's own forward-flagged account. Went a step further than the brief's write-side framing: the **read** side is cross-gated too — `useEmailRouting()`'s `GET .../configs/email_routing` also requires `platform_config.read`, not `email_setting.read`, so a viewer holding only this module's own `.read` key sees the routing card permanently error, independent of whether they can manage anything. Beyond the brief, three "source comment is not source" findings of the class this plan keeps warning about: (1) the orphaned `enum_email_sender_purpose` Postgres/Prisma enum (`schema.prisma:1415-1425`, zero columns reference it) carries a doc-comment claiming a `no_reply` fallback that the 2026-08-08 redesign (migration `20260808130000_email_profile_master_and_routing`) replaced with an operator-configured `default` in `email_routing`; (2) `resolveSmtpConfig()` (micro-notification) has a doc-comment claiming an environment-variable fallback that does not exist anywhere in the file (only `process.env.SYSTEM_DATABASE_URL` is read) — the method itself is additionally dead code, zero callers found; (3) `emailSettingService.ts`'s `PERPAGE = 20` comment ("the purpose enum caps this list at 3 rows") is stale in the same way — no such cap exists post-redesign, so a 21st live profile would silently not appear anywhere in the UI. Also corrected a stale backend Swagger description: the gateway controller's 409 doc-text for profile create/update still says "an active profile already exists for this purpose," but the actual constraint (and the error code returned, `EMAIL_SENDER_PROFILE_NAME_EXISTS`) is keyed on `name`, since `purpose` was dropped as a column entirely. Confirmed a real behavioral contrast worth stating explicitly: `tb_email_sender_profile.doc_version` **is** an enforced optimistic lock (`COMMON_DOC_VERSION_REQUIRED` + a compound-where update), unlike the same-named column on the shared `tb_platform_config` table that `email_routing` itself lives on. Full citations, every hash proven, and the complete end-to-end Step 1b trace are in `task-17-report.md`. | **None — confirmed no e2e suite exists.** `../carmen-platform-e2e/tests/` has no `email-settings` directory, matching the Task 1 source map's "no e2e backing" list; every claim in both pages is sourced from `../carmen-platform` and `../carmen-turborepo-backend-v2` implementation directly, marked as such in each page's At-a-Glance callout | Not applicable — new module, no prior screenshots to compare and none requested this task | `608561c`, `79bd239` |
| 18 | `user-platform` | New — full write, no prior prose | 3 pages written in full (landing + ui-screens + permissions), replacing Task 12's three-section placeholder body on each | 3 pages mirrored in full Thai (Thai section headings throughout, code identifiers/routes/permission keys kept in English per book precedent) | Wrote from scratch (Task 12 left a skeleton). Corrected the one seeded skeleton error found: `ui-screens.md`'s At-a-Glance had misattributed "Revoke-all-access action" to `UserPlatformEdit` (the detail page) — it is a `UserPlatformManagement` (list) row-menu action (`UserPlatformManagement.tsx:394-402`); the detail page's own two write actions are Add Role and per-row Remove, both corrected in place with exact line citations. Confirmed the brief's route-shape ask precisely: `/platform/user-platform/:userId` has no `/edit` suffix and no read/edit-mode toggle at all — flagged prominently on all three pages since almost every other edit route in this book does have one. **Cross-module `email_verified_at` finding (Task 4/5's flag) resolved in full**: three `tb_user` columns (`email_verified_at`, `email_verification_token_hash`, `email_verification_expires_at`, migration `20260804000000_user_email_verification`) are read only by `UserPlatformEdit.tsx` (lines 43, 195, confirmed by a repo-wide grep with zero other hits); traced what sets `email_verified_at` through `../carmen-turborepo-backend-v2`'s `auth.service.ts` — `createVerifiedUser()` for invitation-based accounts (proof happens before the row exists), `verifyEmail(token)` consuming a `resendVerificationEmail()`-issued token pair otherwise, a login-blocking check independent of this module, and the deploy-time backfill that verified every pre-migration account so the new gate wouldn't lock out the platform. **Went beyond the brief and traced a new end-to-end permission gap, verified not to repeat the previous module's mistake of asserting an unreachable trap**: `user_platform.read` alone (the nav/route key for both routes) is not sufficient to see anything useful on the detail page — both `GET /api-system/user/:user_id` (account identity) and `GET .../roles` (role list) require `user.read`, a **Users**-module key the route guard never checks (`user-platform-roles.controller.ts:138-140`; `platform-user.controller.ts:254-256`). A session missing it gets a visible error banner (not silent), but everything below the banner still renders confident-looking empty states ("No platform privilege," "No roles assigned"), while the `user_platform.manage`-gated Add Role sheet renders and can still complete a write — verified directly from `AddRoleSheet`'s JSX (`<Can permission="user_platform.manage">` alone, no second condition) before writing the claim, specifically to rule out the class of mistake `email-settings`'s fix round corrected. A second, independent instance of the same shape exists on the list screen's Grant Access dialog (`UserPicker` search also needs `user.read`; fails visibly inline, not silently). Documented in full on `permissions.md` §3 (both locales) with a 7-row edge-case table and an explicit "the reverse combination is not a gap" statement. Also found and documented: (1) the `summary` aggregate's frontend "ships in a later backend deploy" framing is itself stale in the resolved-not-pending direction — `buildRegistrySummary()` is called unconditionally on every branch of `listPlatformUsers()` at this plan's pinned backend HEAD, including the zero-result fast path; (2) a documented, in-source concurrency caveat on the duplicate-grant check (`assignBulk()`'s own comment: the unique index is NULL-distinct on `deleted_at` in Postgres, so two concurrent identical grants are not blocked and can land as duplicate live rows) — added as an edge case, not flagged as an undocumented bug. Also fixed one stale cross-link found while researching: `en/platform/users/data-model.md`'s `email_verified_at` row linked "User Platform" to `/en/platform/rbac` (correct before this module existed, now a dead end) — corrected to `/en/platform/user-platform`. Full citations in `task-18-report.md`. | `user-platform` (2 specs, HEAD `a8e3b31`, 2026-08-25) — **has coverage, unlike most of this batch, but demonstrated stale against current source, not merely flagged as possibly stale**: both specs predate the 2026-09-02 privilege-registry rewrite (`fc690cb`/`f6e91c9`). Two concrete, verified mismatches: (1) the list page object's own doc-comment states "NO Add button... the inherited `addButton`/`clickAdd` must never be used," but current source has a `user_platform.manage`-gated Grant Access button that the suite's own super-admin session would see rendered (`checkPermission()`'s `is_super_admin` short-circuit); (2) the same page object's `openUser`/`openFirstNonLoginUser` click `row.locator('button').first()` on the stated assumption the username cell is "a BUTTON... not an `<a>`" — current source renders it as a react-router `<Link>` (`<a>` tag), and the only actual `<button>` in a data row is the row-actions kebab trigger (confirmed by reading `data-table.tsx`'s row/column construction) — clicking it today opens a dropdown, not the detail page, so both the navigation test and the entirety of `user-platform-config.spec.ts` (which reaches the detail page only through this helper) would fail at that step. A third mismatch: the config spec's page object expects "Add Role" to reveal an inline `div.rounded-md.border.p-3.space-y-3` panel; current source is a side `Sheet` with no matching element, so `assignFirstAvailableRole()` would time out. The list's own smoke test (heading/row-count/chrome) likely still passes; every other test in both files depends on one of these broken helpers. Flagged for whoever owns e2e maintenance; out of scope to fix here. | Not applicable — new module, no prior screenshots to compare and none requested this task | `efc3737`, `b007594` |
| 19 | `super-admins` | New — full write, no prior prose | 1 page written in full (landing only, per the brief's explicit Step 2 ruling), replacing Task 12's three-section placeholder body; no `data-model.md`/`ui-screens.md`/`permissions.md` — `tb_platform_super_admin` documented in full in landing §5, the gate matrix in §4, the roster/add/remove UI in §3 | 1 page mirrored in full Thai (Thai section headings throughout, code identifiers/routes/permission keys/file paths kept in English per book precedent) | Wrote from scratch (Task 12 left a skeleton). Led every section with the gate per the brief: **no RBAC permission key at all — the nav entry and route are both `superAdminOnly: true`**, never "no permission check at all" (the nav's own Thai comment at `platformNav.ts:44-45` — translated on the page — states the divider exists because these two rows "change what other people can access"). Documented the `tb_platform_super_admin` table in full per the brief's Step 2: **creation** — two paths, a `SUPER_ADMIN_USER_ID` seed script writing directly to the DB (the only way to bootstrap the first row, since the API itself requires an existing super admin) or `POST .../super-admins` by an existing super admin (409 on duplicate); **deactivation** — proven to be a pure soft delete via `deleted_at`, not an `is_active` flip: a repo-wide grep found exactly one `.update()` call against this table in the whole backend, and it touches only `deleted_at`, while `is_active` is written exactly twice total, both `true`, both at creation. **Two genuinely new findings beyond the brief, both directly relevant to the privilege-escalation surface the brief flagged**: (1) **no server-side self-removal guard, and no last-remaining-admin guard anywhere** — `remove(id)` never compares the row's `user_id` to the caller's, and none of `add`/`remove`/`list` check a minimum headcount; the SPA's "cannot remove self" UI is confirmed client-side-only by reading `remove()`'s full body; (2) **the audit trail can never show who granted or revoked a privilege, only when** — `created_by_id`/`deleted_by_id` are never written by either creation path or by `remove()`, traced through the gateway's `@EnrichAuditUsers()` mechanism to confirm the frontend's own type comment ("nests `created_at` here as `audit.created.at`," `types/index.ts:985`) already anticipates this — a name was never captured, not merely never rendered. Documented "effect on a session already open" precisely per the brief: server-side, every consumer of `is_super_admin` (the guard, `EffectivePermissionsService.resolve()`, and four independent re-implementations of the identical check in `auth.service.ts`/`tenant.service.ts`/the gateway's `permission.service.ts`/`cluster-admin-authz.service.ts`) queries live with no cache, so removal takes effect on the very next request; the SPA's own `effectivePermissions` is fetched only at login/mount (never polled), so an already-open tab keeps showing super-admin UI until reload/re-login, though its next privileged API call still 403s live — stated on the page as an inference from reading both sides, not as an observed runtime result. Full citations in `task-19-report.md`. | `super-admins` (1 spec, HEAD `bb8f671`, 2026-06-11) — **fails at the shared test fixture itself, before either test body runs, not merely in individual assertions**: `goto()` waits for a native `<select aria-label="Select user to add as super admin">` that no longer exists in the initial DOM at all post-`#244` (2026-09-02) — the picker is now a `UserPicker` `<input>` inside a `Dialog` that is closed by default. Two further confirmed mismatches even if the fixture were fixed: `adminRows`/`removeButtons` assume `<div>` rows and an aria-label reading "Remove `<name>` as super admin," where current markup is `<ul>/<li>` and the current i18n key reads `'Remove super admin {{name}}'` (reversed word order); `addButton` looks for a button whose accessible name is exactly "Add," but no button in current source reads anything but "Add Super Admin"/"Adding...". The toast copy (`addSuccess`/`removeSuccess`) does still match verbatim — flagged as a coincidence, not evidence of partial suite health. Net: not usable as current evidence for any behavior on this page. | Not applicable — new module, no prior screenshots to compare and none requested this task | `4e0aae8` |
| 20 | `feature-flags` | New — full write, no prior prose | 2 pages written in full (landing + data-model), replacing Task 12's two-section placeholder body on each | 2 pages mirrored in full Thai (Thai section headings throughout, code identifiers/routes/permission keys/file paths kept in English per book precedent) | Wrote from scratch (Task 12 left a skeleton). Documented the mechanism every other module's page states a feature key for: three flag states from `buildPlatformNav()`'s actual filter (`hide` removes the row, `inactive` keeps its position and marks it `comingSoon`, verified against the filter's own logic rather than its adjacent comment), and the sidebar's consecutive-`groupKey`-run contiguity rule cross-checked against `Sidebar.tsx`'s own `navGroups` computation, not just quoted from `platformNav.ts`. Listed the full 28-key `FEATURE_CATALOG` inventory (24 Platform-console keys + 4 separately-namespaced `cluster_admin_*` keys from `clusterAdminNav.ts`) and cross-checked the count against the union of every `feature:` value actually used in both nav files (identical, 28). Led with the brief's central framing: this page's own nav row deliberately carries no `feature` key — "no feature key, deliberately — gated by `feature_flag.manage`," never "unguarded" — because a switch that could hide itself could never be restored from the UI (`platformNav.ts:47-48`); verified this is the *only* row of 26 in `ALL_PLATFORM_NAV_ITEMS` with a permission but no feature property. **A security fact reported from three independent sources, not inferred from one**: the permission catalog's own seeded description states `feature_flag.manage` is "Frontend visibility only — it does NOT block the corresponding backend endpoints"; the page's own i18n subtitle says the same to every admin who opens it; and `ComingSoon.tsx`'s own design comment confirms the `inactive`-state substitution carries no HTTP status code "because the server refused nothing — the UI did." Confirmed `feature_flag.manage` is seeded on exactly one of the four platform role bundles (`Platform Admin`), and confirmed the `GET`/`PUT` pair's actual guards directly from `feature_flags.controller.ts` (`GET`: `KeycloakGuard`+`AppIdGuard` only, no RBAC; `PUT`: adds `PlatformPermissionGuard`+`RequirePlatformPermission('feature_flag.manage')`), plus the super-admin short-circuit in `PlatformPermissionGuard` that bypasses it. On the data-model page: documented `feature_flags` as the one registry key that is a Zod `record` rather than an `object`, and traced why that shape makes it structurally impossible to `PATCH` (an empty precomputed `fields` list causes `rejectBadPatchShape()` to reject every non-empty payload) — verified against `platform-config.schema.ts`'s actual builder code, not asserted from its comment alone. Deliberately did not re-derive `tb_platform_config`'s general write mechanics (validation order, non-enforced `doc_version`, the duplicate-live-row race) since `platform-config/data-model.md` already documents them in full — cross-linked instead, per the pattern `email-settings/data-model.md` already established for the same shared table. Full citations in `task-20-report.md`. | **None** — `../carmen-platform-e2e/tests/` has no `feature-flags` directory; confirmed by listing the tree directly, not inferred from the Task 1 source map alone. | Not applicable — no UI redesign to compare against a prior sync; screenshots not requested this task | `ec63c8e`, `cda95a8` |
| 21 | `tenant-migrations` | **Relocation + re-verify** — pre-empted by `business-units/tenant-migrations.md` (Task 1/12's flagged case, the `tenant-migrations` twin of Task 9's `report-form-groups` finding) | 2 pages written in full (landing + data-model) by migrating the sub-page's content forward, not rewriting from a blank skeleton; every claim re-verified against current source rather than carried over as-is | 2 pages mirrored in full Thai (Thai section headings throughout, code identifiers/routes/permission keys/file paths kept in English per book precedent) | Per the brief's binding Step 1b ruling, migrated `business-units/tenant-migrations.md` (EN+TH, last touched by Task 3) into the new top-level module and reduced the sub-page to a short pointer keeping only the embedded per-BU `TenantMigrationCard` story (also documented in `business-units/ui-screens.md` §4.5). Re-verification found the moved content basically sound but surfaced four things worth flagging: (1) **a stale frontend comment** — `tenantMigrationService.ts`, unchanged since `8fc1124` (2026-06-29), still says the backend resolves the tenant DB from `tb_business_unit.db_connection`; `af2437074` (2026-08-13, `../carmen-turborepo-backend-v2`) moved that to `db_schema` + `tb_database_pool` months ago, confirmed by direct read of `resolveConnection()` — the comment was never updated; (2) **a dead error-status mapping** — the gateway's `deployStream` pre-stream error mapper (`resolvePreStreamErrorStatus()`, added `4ca923229` for the old `db_connection`-era message text) no longer matches any message `resolveConnection` actually throws post-`af2437074` ("is not linked to a database pool" / "has no database schema configured" / "has been deleted" / "is inactive"), so a connection-resolve failure on `/deploy/stream` now falls through to HTTP 500 instead of the documented 422 — the operator-visible message text is unaffected since the frontend renders it regardless of status code, but the API's own documented contract is wrong; confirmed the non-streaming `/deploy`/`/status` endpoints are unaffected since they map status via `StdResponse.fromResult()`'s generic `ErrorCode`, not this regex; (3) **corrected the old sub-page's own framing** that only the fleet table gates the read-only Check by super-admin "unlike the BU edit page" — direct read of `TenantMigrationCard.tsx`'s `actionsDisabled` shows its Check-status button is gated by `isSuperAdmin` identically to the fleet table; the real, verified difference is that the card also pre-disables on `!hasDbConnection`, which the fleet table does not check before letting an operator attempt Check/Apply on any row; (4) confirmed by independent grep (not by trusting the old page's claim) that `POST :bu_id/resolve` has no frontend caller anywhere in `carmen-platform`, and additionally found the non-streaming `tenantMigrationService.deploy()` is equally uncalled — the SPA runs every apply through the streaming variant only. Documented on the data-model page, per the brief's "cover the migration states" ask, that there is no persisted migration-status entity anywhere in the platform schema: `up_to_date`/`has_pending` are regex-classified live from `prisma migrate status` text output, a failed `migrate deploy` leaves Prisma's own `_prisma_migrations` bookkeeping (inside the tenant schema, never read directly by this codebase) in whatever state Prisma itself leaves it, and the in-memory `runningBuIds`/`isBatchRunning` locks do not survive a process restart. Also documented, independently verified from `config.env.ts`, that `TENANT_MIGRATION_API_ENABLED` defaults to `false` (off) unless explicitly set — not previously stated on the sub-page — and distinguished it from the similarly-named but separately-migrated `platform_migration.api_enabled` switch on the unrelated Platform Migrations module. Repointed every inbound link found by a full-book grep (`business-units.md`, `rbac/permissions.md`, `sql-workbench.md`, both locales) at the new module; left `.specs/platform-coverage-checklist.md`'s own stale mention alone since that file already disclaims itself as provisional pending a dedicated future rewrite task. Full citations in `task-21-report.md`. | **None — confirmed no e2e suite exists**, per the Task 1 source map's no-e2e list; every claim is sourced from `../carmen-platform` and `../carmen-turborepo-backend-v2` implementation directly, marked as such on both pages. | Not applicable — no UI redesign to compare against a prior sync; this task moved and re-verified documentation, not screenshots | `592650d` |
| 22 | `tenant-imports` | New — full write, no prior prose | 2 pages written in full (landing + ui-screens), replacing Task 12's unverified route-name-only skeleton on each | 2 pages mirrored in full Thai (Thai section headings throughout, code identifiers/paths/permission keys/commit hashes/table technical columns kept in English per book precedent) | Read the whole `src/pages/tenantImport/` directory (`TenantImportWizard.tsx` plus `WorkbookDropzone`/`FileCheckPanel`/`StepRail`/`StepPanel`/`CompanyProfilePanel`) and the full backend importer (`preconfig-import.service.ts`, `preconfig-catalog.ts`, `preconfig-workbook.ts`, `preconfig-lookup.ts`, `preconfig-related.ts`, `preconfig-types.ts`) plus the gateway proxy. Documented the twelve-step catalog, the check→preview→import three-tier validation model, and precisely which of `clear_existing` (browser-only precondition) versus `accept_lookup_creation` (server-enforced per row, independent of the client) is enforced where — the brief's "say which side enforces it" ask. Following Task 21's cross-check-a-commit-against-a-regex habit, found the **same class of live defect** on this module's own stream endpoint: `import/stream`'s hand-rolled `resolvePreStreamErrorStatus()` (added `e865dcaab`, 2026-08-03 — **before** the `af2437074` database-pool refactor of 2026-08-13) still matches the pre-refactor connection-failure wording ("no database connection configured" / "unsupported database provider"); `preconfig-import.service.ts`'s own `resolveConnection()` already calls the post-refactor `resolveConnectionForBusinessUnit()` and propagates one of the four new messages ("is not linked to a database pool" / "has no database schema configured" / "has been deleted" / "is inactive"), none of which match — confirmed no commit since (`cc0c3b4e6`, `b188a4d80`, `d9cc3ecde`, `2f134a614`, all same-day) touched the function, so a connection-resolve failure on `import/stream` currently returns 500 instead of the 422 the identical failure correctly returns from `preview` against the same business unit. Also newly found and verified independently (not in the Task 1 source map): Company Profile's "Apply to BU" write (`businessUnitService.update()`, the same `PUT /api-system/business-units/:id` the ordinary Business Units edit page uses) is gated on that backend route only by `AppIdGuard('businessUnit.update')` — an `x-app-id` allowlist check (confirmed by reading `app-id.guard.ts`), not a platform-RBAC permission check — so a `data_import.manage`-only session, blocked by the frontend from ever opening `/business-units/:id/edit` (which requires `cluster.update`), is not blocked by either the frontend or the backend from writing the same business-unit fields through this wizard's Company Profile step. Documented the streaming importer's batch-then-per-row-retry recovery (a failed 200-row batch is deliberately non-atomic) and the `tb_activity` audit row every run — completed, cancelled, or failed — leaves on the **tenant** database. Full citations, every hash verified with `git cat-file -e`, in `task-22-report.md`. | **None — confirmed no e2e suite exists** (`../carmen-platform-e2e/tests/` has no `tenant-imports`/`preconfig-import` directory), matching the Task 1 source map's no-e2e list; every claim is sourced from `../carmen-platform` and `../carmen-turborepo-backend-v2` implementation directly, marked as such on both pages. | Not applicable — new module, no prior UI sync to compare against; screenshots deferred per plan | `94d16ca` |
| 23 | `usage-analytics` | New — full write, no prior prose | 1 page written in full (single-page module, no sub-pages per the depth rule), replacing Task 12's unverified route-name-only skeleton | 1 page mirrored in full Thai (Thai section headings throughout, code identifiers/paths/permission keys/SQL/commit hashes kept in English per book precedent) | Read `UsageAnalytics.tsx` (full, 251 lines) and every file in `src/pages/usageAnalytics/` (`StatCards`, `TopList`, `UsageChart`), plus `analyticsService.ts`, `utils/analyticsRange.ts`, `utils/csvExport.ts`, `hooks/useAnalyticsFilterOptions.ts`, `components/analytics/DateRangeFilter.tsx`, `components/Can.tsx`, and `components/PrivateRoute.tsx` (both full). Backend: `platform-analytics.controller.ts`/`.service.ts`, `analytics-range.ts`, `analytics-scope.ts` (all full), `auth/guards/platform-permission.guard.ts` (full), `common/guard/app-id.guard.ts` (full, independently re-read rather than trusted from Task 22's report), and `micro-business/src/log/activity-event/{activity-event.service.ts (full, 348 lines), activity-event.types.ts, activity-event.query.ts}` (all full) for the exact SQL behind every metric. Documented, with file:line and the exact SQL, what each of the five StatCards / the daily series / Top Pages / Top Elements counts, its source table (`tb_activity_event`), and its window (`server_ts` half-open `[from, to)`, 90-day cap, `Asia/Bangkok` daily bucketing via a constant duplicated — not derived — between frontend and backend) — including the caveats that daily sessions/users are distinct only within that day (so they do not sum to the summary total), only `sessions`/`users` are charted (`clicks`/`page_views` reach only the CSV export), Top Elements is never drill-down-able for anyone, and the four aggregate queries run outside a shared transaction by the service's own admission. Documented the brief's named gating fact precisely: `activity_event.read` opens this dashboard, `activity_event.detail` opens the sibling `/activity-events` explorer **and** separately gates this page's own Top-Pages drill-down via `<Can>` (same list, `onSelect` present or absent) — confirmed both are decorated with `@RequirePlatformPermission` (no gate-by-absence), both routes stack `KeycloakGuard`+`AppIdGuard`+`PlatformPermissionGuard`, and `resolveAllowedClusterIds()` scopes every one of the four `getOverview()` queries (not just raw rows) using whichever key actually gates that endpoint. Traced role-bundle assignment (`seed.platform-role-permission.data.ts:11-52`, full): Platform Admin holds both keys via `activity_event.*`; Support Manager holds `.read` only (line 40) — can see the dashboard, cannot drill down or open `/activity-events`; Support Staff and Security Officer hold neither. Caught and fixed several of its own imprecise line citations during a self-verification pass (re-grepped every cited file:line against source rather than trusting the first read) before committing. Full citations, every hash verified with `git cat-file -e`, in `task-23-report.md`. | **None — confirmed no e2e suite exists** (`../carmen-platform-e2e/tests/` has no `usage-analytics`/`analytics`/`activity-events` directory; the only trace is a stray manual-run screenshot artifact, not a test), matching the Task 1 source map's no-e2e list; every claim is sourced from `../carmen-platform` and `../carmen-turborepo-backend-v2` implementation directly, marked as such on the page. | Not applicable — new module, no prior UI sync to compare against; screenshots deferred per plan | `4f70389` |
| 24 | `activity-events` | New — full write, replacing Task 12's conflated skeleton | 2 pages written in full (landing + data-model, per the depth rule — this module owns `tb_activity_event`'s field-level documentation) | 2 pages mirrored in full Thai (Thai section headings throughout; code identifiers/paths/permission keys/SQL/hashes kept in English per book precedent) | **Corrected the skeleton's core error first (Step 1b)**: Task 12's seed described this module as backing the `activity_log.read` "View History" audit trail; independently confirmed it is unrelated — `activity_event`/`tb_activity_event` (UI telemetry) vs. `activity_log`/`tb_activity` (change-history audit), verified by reading both Prisma models side by side (`schema.prisma:907-929` vs. `:1489-1509`) and the permission catalog's own adjacent rows and comment (`seed.platform-permission.data.ts:93-101`) — then independently corroborated by a *third*, unrelated source: `LogEventsModule` (the generic audit-log writer, `apps/micro-business/src/app.module.ts:211-222`) explicitly excludes `tb_activity`, `tb_activity_event`, and `tb_activity_event_daily` from its own model-tracking list, keeping the two systems from feeding into each other in code that has nothing to do with either page's permission strings. Read `ActivityEventManagement.tsx` (full, 434 lines) and `EventDetailSheet.tsx` (full, 78 lines) for every filter, column (cross-checked the 4-column sort whitelist in `activity-event.service.ts:25-30` against which UI columns actually expose sorting — `client_ts` is whitelisted server-side but has no reachable UI column), the sort-tiebreak-on-`id` mechanism (`activity-event.service.ts:284`), and the CSV export's narrower field set vs. the detail sheet's full set. Traced the full write path end to end — a path Task 23 did not need and did not cover: `../carmen-inventory-frontend-react/lib/analytics.ts` (full, 223 lines, the click/page-view batcher; confirmed by grep that `carmen-platform` itself never calls the ingestion endpoint) → `analytics-events.controller.ts`/`.service.ts` (`POST api/analytics-events`, `KeycloakGuard`+`AppIdGuard('analyticsEvent.create')`) → `ActivityEventService.createBatch()` (`activity-event.service.ts:82-112`, `createMany({skipDuplicates:true})` keyed on client-generated `event_id`) → `extract_client_context.ts` (full, 62 lines: `user_agent` capped 512 chars, `domain` is `Origin`'s hostname only, `app_id` re-validated as UUID) → `activity-event.dto.ts` (full: per-field length caps, 1–100 event batch cap). Built the data-model page's field table entirely from this path plus `schema.prisma:1489-1509`, marking `user_id`/`app_id`/`domain`/`user_agent` as server-stamped (never in the client payload — confirmed absent from both the frontend `AnalyticsEvent` interface and the backend `ActivityEventSchema`) and `user_name`/`user_email`/`app_name` as read-time-only enrichment from `findEvents()` (`activity-event.service.ts:246-347`), not stored columns. **Retention traced to its actual enforcement point, not assumed from the schema comment**: read `../micro-cronjobs`'s `activity_retention.go` (full, 65 lines) and confirmed it is wired to a **seeded, active** cron job (`migrations/20260730093731_seed_activity_retention_job.up.sql`: 365 days, batched delete, `0 4 * * *`) via the `job.JobType` dispatch switch (`executor.go:68-84`) — not merely present in source but actually scheduled. Also read the rollup job (`activity_rollup.go`, full, 80 lines; seeded active at `30 3 * * *`, 2 days back) and, going beyond what either sibling page needed, searched `../carmen-turborepo-backend-v2`, `../micro-cronjobs`, `../micro-report`, and `../micro-data` for any reader of `tb_activity_event_daily` and found none — stated as a finding from the repositories available, not proof no consumer exists anywhere; also found and correctly characterized the one `DELETE` against that table as a one-time, guarded migration-time reset (`20260731000000_activity_event_client_context/migration.sql`) tied to a dimension-key change, not an ongoing retention mechanism, so it is not conflated with the real retention job. Cross-checked the permission/role-bundle facts already established by Task 23 (`activity_event.detail` nav/route/endpoint gating, `seed.platform-role-permission.data.ts:11-52` role assignment) by independently re-reading the same source rather than only citing the sibling page — **agree with Task 23 on every shared fact** (the two-key seam, cluster-scoping mechanism, role bundle table); no discrepancy found. Full citations, every hash verified with `git cat-file -e`, in `task-24-report.md`. | **None — confirmed no e2e suite exists** (`../carmen-platform-e2e/tests/` (HEAD `a8e3b31`) has no `activity-events` directory), matching the Task 1 source map's no-e2e list; every claim on both pages is sourced from `../carmen-platform`, `../carmen-turborepo-backend-v2`, `../carmen-inventory-frontend-react`, and `../micro-cronjobs` implementation directly, marked as such on the pages. | Not applicable — new module, no prior UI sync to compare against; screenshots deferred per plan | `b534859` |
| 25 | `platform-migrations` | New — full write, replacing Task 12's unverified route-name-only skeleton | 1 page written in full (single-page module, no sub-pages per the depth rule) | 1 page mirrored in full Thai (Thai headings, code identifiers/citations kept in backticks) | Confirmed and documented the brief's Step 1b finding by reading `PlatformMigrationGuard.canActivate()` directly (`platform-migration.guard.ts:118-140`, `carmen-turborepo-backend-v2`): the switch (`platform_migration.api_enabled`) is checked first and gates both authorities equally; only then does a matching `x-deploy-token` header (tagging `deployActor: 'ci:deploy-token'`) get checked **before** falling through to a super-admin session (`deployActor: 'super-admin:<user_id>'`) — `PLATFORM_DEPLOY_TOKEN` defaults to `''`, so the token path is inert until explicitly configured. Read all 8 seed scripts and all 6 check scripts in full from `packages/prisma-shared-schema-platform/prisma/` and stated a specific re-run verdict for each with its source line: all 8 seeds are upsert/skip-based and safe to re-run (most additive-only — never revoke a stale link), `seed-report-template-upload` uniquely depends on a live external system and credentials **hardcoded as literals in the script itself**; 5 of 6 checks are confirmed genuinely read-only by reading the whole file (no write call); the 6th, `check-seat-pool-view`, writes real rows for one arbitrary active BU inside a transaction that always rolls back (net zero, but holds a real row lock) and is filtered out of the visible console by `HIDDEN_OPS` while remaining reachable via direct API call. Traced `seed-role-permission`'s multi-BU picker (PR #281, commit `157a65e` itself) to `tb_application_role`/`tb_application_role_tb_permission`, confirming by reading the write path directly that it is a per-tenant application-role system distinct from the platform RBAC tables `rbac.md` documents — the BU picker itself runs each selected BU sequentially (not in parallel) via the same single-BU script. Also found and cited the `PlatformDbLock` concurrency lock's own doc-comment scope limitation (one process, not cross-replica) as an edge case, and confirmed `db:seed.platform-super-admin` is deliberately excluded from the console's catalog by reading the excluded script itself, not just trusting the catalog's comment. | **None** — `../carmen-platform-e2e/tests/` has no `platform-migrations` directory | Not requested; no screenshots taken this task | `224f94e` |
| 26 | `database-pools` | New — full write, replacing Task 12's unverified route-name-only skeletons | 3 pages written in full (landing + data-model + ui-screens, per the depth rule) | 3 pages mirrored in full Thai (Thai section headings throughout, code identifiers/routes/permission keys/file paths/commit hashes kept in English per book precedent) | Documented the full `tb_database_pool` field table (`schema.prisma:1382-1411`), including `note` — a real column (`:1398`) that `business-units/data-model.md` §2.4 does not list, consistent with that page's own declared depth-limited scope, not an error there; flagged explicitly on `database-pools/data-model.md` §5's reconciliation section rather than left implicit. Re-verified (not re-derived) the connection-resolution path both `business-units` and `tenant-migrations` already document — `database_pool_id`+`db_schema` replacing the physically-dropped `db_connection`, migrations `20260813000000_database_pool_additive` (commit `343b8c16b`) and `20260813010000_database_pool_drop_db_connection` (commit `7f825bb20`) — and found **no discrepancy** with either sibling page; read the one-off `migrate.database-pool.ts` backfill script in full (437 lines) for the migration's own mechanics (password-conflict abort, missing-schema skip, `--verify` mode, `note` provenance). Checked for a hardcoded default credential/key across this module's own frontend, backend, and backfill script per the brief's explicit ask and **found none** — `SECRET_ENCRYPTION_KEY` is fail-closed with no code-level fallback (`crypto.util.ts`), unlike the sibling `platform-migrations` module's `PLATFORM_DEPLOY_TOKEN`, which does default to an empty string; no credential value reproduced anywhere in the pages, this report, or the commit messages. New findings beyond the brief: (1) all three frontend routes (`/`, `/new`, `/:id/edit`) are gated on `database_pool.read` alone (`App.tsx:507-527`) — combined with `DatabasePoolEdit.tsx` starting a new pool unconditionally in edit mode, a read-only session can open and fill in the create form, though it cannot submit through any path (`<Can>`-gated Save button plus an independent `hasPermission()` check inside `handleSubmit`, mirroring `BusinessUnitEdit.tsx`'s own defense-in-depth) — phrased to name what decides, not just what's absent; (2) unlike `business-units`' own `PUT` endpoint, every one of this module's five backend operations correctly stacks both an `AppIdGuard` and a `PlatformPermissionGuard`, so no equivalent RBAC gap exists here; (3) the list page's Active/Inactive filter chips are both selectable but the server-side filter only applies when exactly one is selected; (4) `perpage_database_pool` is singular, unlike its four sibling `localStorage` keys. | **None — confirmed no e2e suite exists** (`../carmen-platform-e2e/tests/` has no `database-pools` directory), matching the Task 1 source map's no-e2e list; every claim on all three pages is sourced from `../carmen-platform` and `../carmen-turborepo-backend-v2` implementation directly, marked as such. | Not applicable — new module, no prior UI sync to compare against; screenshots deferred per plan | `b4f5906`, `e95d2be` |
| 27 | `cronjobs` | New — full write, replacing Task 12's unverified route-name-only skeletons | 3 pages written in full (landing + data-model + ui-screens, per the depth rule) | 3 pages mirrored in full Thai (Thai section headings throughout, code identifiers/routes/permission keys/file paths/SQL/commit hashes kept in English per book precedent) | Corrected the skeleton's `data-model.md` source pointer: `"CRONJOBS"."Cronjob"` is not a Prisma model at all (grepped the platform schema — no match) — it is owned entirely by `../micro-cronjobs`'s own hand-written SQL migrations (all ten `.up.sql` files read), stated plainly on the page rather than silently fixed. Read all six executors in full (`report.go` 422 lines, `notification.go` 102, `cleanup.go` 40, `dashboard.go` 98, `activity_rollup.go` 80, `activity_retention.go` 64) plus `executor.go`'s dispatch switch, `scheduler.go` (345 lines — polling/reconciliation/retry/timezone), `cronjob_handler.go` (451 lines — the REST surface including the still-defended-but-dead `FOREIGN_OWNED_JOB` path), and `cronjob_repo.go` (207 lines — `UpdateLastRun`'s never-bumps-`doc_version` guarantee). Documented the brief's central ask — where a failed run becomes visible — as its own section on both the landing page (§3.5) and a full accounting on `data-model.md` (§5): the DB row's `last_error`/`retry_count`, the list page's Last-Run warning icon (the only place a human reads the raw error text) and page-scoped "With Errors" stat, the edit page's explicit **absence** of any run-outcome field (its History card shows only config-edit audit, not execution), the OpenTelemetry span recorded on every failure (SigNoz-only, out of console reach), and the independent "Active in Scheduler" live count that a mismatch against the DB-derived Running/Stopped figures itself signals a stuck scheduler. Found and documented that `dashboard_refresh`'s per-MV errors never reach `last_error` at all (logged as a warning only) and that `cleanup` has zero implementation (`// TODO`, always reports success) — both read directly from the executor source, not inferred from absence. **Agreement with `activity-events`' account (Task 24)**: independently re-read both seed migrations and the executor dispatch table for the rollup (03:30, `activity_rollup`, `days_back:2`) and retention (04:00, `activity_retention`, `retention_days:365`/`batch_size:10000`) jobs and reached the identical schedule/config conclusion — no discrepancy; added only that both seeded rows are administrable from this console like any other, a structural fact `activity-events` had no reason to state. Verified the nav-group-isolation comment (`platformNav.ts:34-35`, "scheduled work, not system configuration") structurally rather than quoting it as fact: `navGroup.scheduling` contains exactly one row, genuinely isolated between the Analytics and Platform groups in the nav array — the comment holds up. New findings beyond the brief: (1) `notify_day_offset` (added by migration `20260903100000`) is fully supported end-to-end by the backend and executor but has **no field anywhere in the console's create/edit form** — every job authored here is frozen at the column's default (same-day notify); (2) the frontend still carries a `FOREIGN_OWNED_JOB` 409 handling branch that the current gateway's `update()`/`remove()`/`control()` can no longer produce (each method's own comment says "No ownership check") — dead code kept for backward compatibility, not a live gate; (3) only the Platform Admin role holds any `cronjob.*` permission (`seed.platform-role-permission.data.ts:11-15`) — Support Manager/Staff/Security Officer hold neither key. | **None — confirmed no e2e suite exists** (`../carmen-platform-e2e/tests/` has no `cronjobs` directory), matching the Task 1 source map's no-e2e list; every claim on all three pages is sourced from `../carmen-platform`, `../carmen-turborepo-backend-v2`, and `../micro-cronjobs` implementation directly, marked as such. | Not applicable — new module, no prior UI sync to compare against; screenshots deferred per plan | `ec2a345` |
| 28 | `report-form-groups` | **Migration** — content already existed under the wrong parent (`report-templates/form-groups`), previously verified against source and passed review in the `report-templates` task; the last of the plan's 16 new modules | 1 page written at the new top-level path, migrated from the sub-page rather than written green-field (single-page module, no sub-pages) | 1 page mirrored in full Thai, migrated from its own sub-page mirror — **not** derived from the EN migration: per the brief's own flag, the TH sub-page (17.9 KB) was larger than the EN one (11.3 KB). Line-by-line comparison found both were exactly 78 lines and section-for-section identical in content — the byte gap is fully explained by Thai UTF-8 multi-byte encoding of an already-faithful translation, not by any real informational surplus in either direction; nothing was added to EN or dropped from TH. | Re-verified rather than trusted, against `../carmen-platform` (`ReportFormGroupManagement.tsx`, `reportFormGroups/GroupCard.tsx`, `constants/reportGroups.ts`, `services/reportTemplateService.ts`, `utils/docVersion.ts`, `utils/audit.ts`, `components/AuditMeta.tsx`, `components/nav/platformNav.ts`, `App.tsx`, and the `i18n/en.ts`/`th.ts` catalogs) and the backend migration (`20260723120000_print_form_default/migration.sql`). Found and fixed one real citation error carried over from the prior review: the confirm-dialog text was quoted with single quotes around a placeholder invented as `{old name}` — the actual i18n literals (`setDefaultConfirm`/`setDefaultReplaces`) use double quotes and the same `{{name}}` key twice (the second fill is the *current* default's name, not the target's) — fixed to quote the literal exactly, with the disambiguation stated unquoted rather than folded into the quote. Corrected the `App.tsx` route citation from `:323-329` to `:322-329` (the `<Route` opening tag itself starts at 322). Re-confirmed rather than re-asserted that `latestActor()`'s "Created"/"Updated" verb is an i18n **key**, not a hardcoded string — source's own comment on that function describes a prior bug where it was one, producing mixed-language Thai lines; the existing page's rendering description was already correct. New, previously undocumented detail added from direct source reading: the paged fetch's `MAX_PAGES = 50` runaway guard; `setGroupDefault`'s defensive `if (current.id === target.id) return` self-select guard, confirmed unreachable from this screen's own call site since `requestDefault` already excludes the target's id when computing `current`; the kebab menu's disabled Deactivate item getting a literal `" (default)"` suffix appended, not just a disabled state; and a genuinely new edge case found by reading the grouping and `GroupCard` filter logic side by side — a fixed report-group card whose rows are all hidden by "Active only" still renders an (ambiguously worded) empty card, while a legacy-code card in the identical situation disappears entirely, because only the legacy branch's visibility test is `rows.length > 0` computed after the same filter. Reconstructed and cited the full print-template-mapping succession commit sequence — `cd4fc5d` (group default exposed + `IA` added, 2026-07-23), `bf7a28a` (page added), `ea699bc` (`GroupCard` added), `aa454d3` (`FORM_REPORT_GROUPS` extracted to a shared constant), `2a73a4d` (Add pre-fill), `b707b0a` (pagination fix), all 2026-07-24 — that the prior sub-page had not enumerated; this agrees with, and adds granularity to, the succession note already carried on `report-templates.md` §1/§5 and `changelog.md` §6, re-read (not merely cited) to confirm agreement. Reduced the old sub-page to a short pointer keeping only what is genuinely report-templates-specific — the shared `tb_report_template` columns (`template_type`/`report_group`/`is_default`) and the Add/Edit hand-off to `ReportTemplateEdit.tsx` — and rewrote every sentence on `report-templates.md`/`changelog.md`/`landing.md`/`rbac/permissions.md`/`report-templates/{permissions,data-model}.md`/`sql-workbench.md` (both locales) whose link or framing depended on the sub-page holding the full account, following the `Sub-pages: 5` / "Pages in This Module" pointer-annotation pattern Task 21 established (count unchanged; the file still exists, now as a pointer) rather than deleting the entry. | **None — confirmed no e2e suite exists** (`../carmen-platform-e2e/tests/` has no `report-form-groups`/`form-groups` directory, HEAD `a8e3b31` 2026-08-25; `tests/report-templates/`'s two specs, read directly, do not cover this screen either) | Not applicable — migration/re-verification task, no prior UI sync to compare against; screenshots deferred per plan | `133e1a0` |
