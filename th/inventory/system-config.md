---
title: ระบบและการตั้งค่า (System Configuration)
description: การตั้งค่าการไหลของเอกสารและช่วงงวดสินค้าคงคลัง — workflow, inventory period, running code, email profile/template เป็นหน้าจอจริง; dimension และ menu เป็น schema เท่านั้น; UI ของ query-dataset อยู่ฝั่ง Platform ตรวจสอบซ้ำ 2026-09-22
published: true
date: '2026-09-23T01:30:00.000Z'
tags: system-config, configuration, carmen-software
editor: markdown
dateCreated: 2026-05-16T08:00:00.000Z
---

# ระบบและการตั้งค่า (System Configuration)

> **At a Glance**
> **วัตถุประสงค์โมดูล:** กลไกสำหรับการไหลของเอกสารและช่วงงวดสินค้าคงคลัง — เวิร์กโฟลว์การอนุมัติ ช่วงงวดสินค้าคงคลัง (บัญชี) การกำหนดเลขที่เอกสาร sender profile และ message template ของอีเมลขาออก บวกแนวคิดหลายตัวที่ provision ไว้ใน schema แต่ยังไม่ implement (มิติ, menu registry) &nbsp;·&nbsp; **กลุ่มเป้าหมาย:** Sysadmin, Workflow Administrator, Finance (ปิดงวด) &nbsp;·&nbsp; **เอนทิตี/ตารางหลัก:** `tb_workflow`, `tb_inventory_period` (เปลี่ยนชื่อจาก `tb_period` 2026-09-16), `tb_dimension`, `tb_config_running_code`, `tb_application_config`, `tb_menu`, `tb_business_unit` (ฟิลด์ config), `tb_notification_template`, `tb_activity` &nbsp;·&nbsp; **หน้าย่อย:** 14 &nbsp;·&nbsp; **ตรวจสอบซ้ำ 2026-09-22: 9 จาก 14 หน้าย่อยอธิบายหน้าจอ Sysadmin ที่มีจริงและเข้าถึงได้ (workflow, period → ตอนนี้คือ *Inventory Period*, running-code, config-email → ตอนนี้คือหน้าจอ *Email Profile* + *Email Template*, document, dashboard-dataset, company-profile, notification-template, activity-log); dimension และ menu ยังเป็นตาราง schema ที่ dead; application-config ถูก consume เป็นราย key (ตอนนี้ผ่านสามหน้าจอที่ gate ด้วย licence); UI ของ query-dataset คือ SQL Workbench ของ Platform SPA และ key permission/licence `system_admin.query_dataset` ของมันถูกลบออกจากแคตตาล็อกเมื่อ 2026-09-21**

![ระบบและการตั้งค่า (System Configuration) screen](/screenshots/system-config/index.png)

## 1. ภาพรวม

ระบบและการตั้งค่าเป็นโมดูลร่มของ **กลไกการไหลของเอกสารและช่วงงวดสินค้าคงคลัง** ที่ทุกโมดูลธุรกรรมต้องพึ่งพา เวิร์กโฟลว์กำหนดเส้นทางการอนุมัติแบบหลายขั้น พร้อมการกระทำ ผู้รับ และการแสดงผลของฟิลด์ในแต่ละขั้น — ตั้งแต่ 2026-09-16 หน้าจอถูกแยกเป็นหนึ่งหน้าต่อประเภทเอกสาร (`/system-admin/workflow/purchase-request`, `/purchase-order`, `/store-requisition`) แต่ละหน้ามี endpoint และ permission key ของตัวเอง ช่วงงวดสินค้าคงคลัง (โมดูลที่เดิมชื่อ *Period* เปลี่ยนชื่อเมื่อ 2026-09-16 — ตาราง `tb_inventory_period`, route `/system-admin/inventory-period`, endpoint `api/:bu_code/inventory-periods`, permission `system_admin.inventory_period`) กำหนดปฏิทินบัญชีและควบคุมว่าวันที่ใดของการ post จะถูกอนุญาต (ผ่านหน้าจอ CRUD ธรรมดา — ดู [system-config/period](/th/inventory/system-config/period)) Running code ขับเคลื่อนการกำหนดเลขที่เอกสาร (ตอนนี้ผ่าน editor แบบทีละส่วนที่มีโครงสร้างพร้อม preview สด — ดู [system-config/running-code](/th/inventory/system-config/running-code)) Application config คือตาราง key-value ที่ใช้จริง ถูก consume เป็นราย key โดยฟีเจอร์เฉพาะ: key `email_profiles` และ `email_templates` ตอนนี้มีหน้าจอของตัวเอง (`/system-admin/email-profile`, `/system-admin/email-template`) และ licence feature ของตัวเอง (`configuration.email_profile`, `configuration.email_template` แยกออกจาก `configuration.app_config` เมื่อ 2026-09-20) และ key `interface_<category>_<brand>` อยู่เบื้องหลังหน้าจอ `/system-admin/interface` (licence เท่านั้น ไม่มี permission key) — แต่ยังไม่มี editor key-value ทั่วไป มิติและ menu registry ยังคงเป็น **สิ่งที่ provision ไว้ใน schema แต่ยังไม่ implement** — ไม่มี CRUD service หรือ frontend route สำหรับทั้งสองที่ HEAD (`config_dimension-comments` เป็น controller เดียวที่เกี่ยวกับ dimension; `tb_menu` ไม่มีการอ้างอิงจากโค้ด non-schema เลย)

ทั้งหกเอนทิตีในที่นี้อยู่ระหว่าง [master-data](/th/inventory/master-data) (แคตตาล็อกแบบสถิตย์ — หน่วยนับ ผู้ขาย สกุลเงิน) และเลเยอร์ runtime [access-control](/th/inventory/access-control) (ผู้ใช้ บทบาท สิทธิ์) ในขณะที่ master data ตอบคำถามว่าธุรกรรมกำลังอ้างอิง *อะไร* การตั้งค่าระบบตอบคำถามว่าควรไหล *อย่างไร* ควร post *เมื่อไหร่* และควรพ่วง *มิติพิเศษอะไร* — ข้อสุดท้าย ("มิติพิเศษ") ยังคงเป็นเจตนาการออกแบบเท่านั้นในวันนี้ รายการส่วนใหญ่เป็นเจ้าของและแก้ไขโดย Sysadmin; บางส่วน — โดยเฉพาะขั้นเวิร์กโฟลว์ — มีการปรับแต่งรายวันโดย Workflow Administrator ที่ได้รับมอบหมาย

ทั้งหกเอนทิตีอยู่ใน **tenant** schema ไม่มีเอนทิตีใดที่มี counterpart ในระดับแพลตฟอร์ม — เพราะอธิบายการไหลของเอกสารต่อ property ดังนั้นแต่ละ tenant ได้สำเนาของตัวเอง

## 2. กลุ่มเป้าหมาย

Sysadmin การนิยามเวิร์กโฟลว์อาจมอบหมายให้ persona Workflow Administrator (โดยทั่วไปคือ Finance Manager หรือ Procurement Manager) เพื่อบำรุงรักษาขั้น / ผู้อนุมัติรายวัน Finance เป็นเจ้าของการปิดงวด

## 3. รายการเอนทิตี

| เอนทิตี | วัตถุประสงค์ | จัดการโดย | การ implement |
| ------ | ------- | ---------- | --------------- |
| [workflow](/th/inventory/system-config/workflow) | เวิร์กโฟลว์การอนุมัติแบบหลายขั้น พร้อมการกระทำ ผู้รับ และการแสดงผลของฟิลด์ในแต่ละขั้น | Sysadmin / Workflow Admin | หน้าจอจริง |
| [period](/th/inventory/system-config/period) | **Inventory Period** (เปลี่ยนชื่อ 2026-09-16): ช่วงงวดบัญชี (open/closed/locked) และ snapshot สต๊อกต่องวด — `tb_inventory_period*`, `/system-admin/inventory-period`, `api/:bu_code/inventory-periods` | Sysadmin / Finance | หน้าจอจริง — CRUD ธรรมดา ไม่มี action Close/Lock/Reopen เฉพาะ; URL เดิม `/system-admin/period` redirect, API path เดิม `/periods` คงไว้เป็น alias ที่ซ่อน |
| [running-code](/th/inventory/system-config/running-code) | รูปแบบเลขที่เอกสารต่อประเภทเอกสาร | Sysadmin | หน้าจอจริง — part editor แบบมีโครงสร้าง + preview สดตั้งแต่ 2026-08-27 (JSON ดิบคงไว้เป็น fallback "Advanced" ที่พับอยู่) |
| [config-email](/th/inventory/system-config/config-email) | อีเมลขาออก: **sender profile** SMTP ที่มีชื่อ (key `email_profiles`, `/system-admin/email-profile`) + **คลังข้อความ** ต่อประเภทเอกสาร (key `email_templates`, `/system-admin/email-template`); หน้าจอ `report_email` เดี่ยวแบบ legacy ไม่มี route แล้ว | Sysadmin | หน้าจอจริงตั้งแต่ 2026-09-08 / 2026-09-16; แต่ละตัวมี licence feature ของตัวเอง; app-config controller ยังคง **ไม่มี RBAC guard** |
| [document](/th/inventory/system-config/document) | Registry การจัดเก็บไฟล์ scope ตาม tenant — upload, list, download และ delete สำหรับเอกสารที่แนบกับ record ธุรกรรม | Sysadmin | หน้าจอจริง; backed ด้วย `tb_file_tag` + MinIO (ไม่ใช่ `tb_attachment`) |
| [dashboard-dataset](/th/inventory/system-config/dashboard-dataset) | แคตตาล็อก read-only ของ data feed ที่ลงทะเบียนไว้ในโค้ดสำหรับ widget บนแดชบอร์ด | Sysadmin | หน้าจอจริง |
| [company-profile](/th/inventory/system-config/company-profile) | สองหน้าจอ (Company Profile + Default Setting) แก้ไขกลุ่มฟิลด์ที่แยกกันของแถว `tb_business_unit` ปัจจุบัน — identity/ที่อยู่/format และ config PR/SI/PO + การเลือก print-form | Sysadmin | หน้าจอจริง; `/system-admin/business-setting` เป็น redirect ที่ตายแล้วไปยัง Company Profile |
| [notification-template](/th/inventory/system-config/notification-template) | Template ข้อความในแอปที่ใช้ซ้ำได้ ถูกเลือกต่อ workflow stage/action/recipient | Sysadmin / Workflow Admin | หน้าจอจริง; ตั้งแต่ 2026-09-16 หน้าจอสร้าง **template channel `app` เท่านั้น** (รายการกรอง `type:app` control Channel/Subject ถูกเอาออก) และ workflow editor เสนอเฉพาะ channel `app` — ตรงกับ dispatcher ที่ไม่เคยส่งอย่างอื่น; แถว legacy `email`/`sms`/`line` ยังอยู่ใน DB |
| [activity-log](/th/inventory/system-config/activity-log) | UI แบบ list/grid เหนือ audit log ของ tenant (`tb_activity`) พร้อม filter action/entity-type/actor, export, print | Sysadmin / Auditor | หน้าจอจริง; เป็น activity UI *เดียว* ในผลิตภัณฑ์ — โมเดลข้อมูลอยู่ที่ [reporting-audit/activity](/th/inventory/reporting-audit/activity) |
| [application-config](/th/inventory/system-config/application-config) | การตั้งค่า key-value ระดับ tenant + การ override preference ต่อผู้ใช้ | ไม่มีหน้าจอ admin ทั่วไป | ตารางจริง consume เป็นราย key: `email_profiles` / `email_templates` / `interface_*` แต่ละตัวมีหน้าจอเฉพาะและ licence feature (แยกเมื่อ 2026-09-20); `report_email`, `gl_setting`, `*_approval_flow`, `*_print_config`, การตั้งค่า signature ถูก consume โดยฟีเจอร์ของตัวเอง; `GET /app-config` (รายการ) ตอนนี้ **ไม่รวม** สามกลุ่ม key ที่แยกตาม licence |
| [query-dataset](/th/inventory/system-config/query-dataset) | SQL Workbench — เขียน tenant view, stored procedure และ function เป็นแหล่งข้อมูลใช้ซ้ำ | ผู้ดูแล Platform SPA (สิทธิ์ platform `sql_workbench.*`) | Backend service จริง; UI คือ SQL Workbench ของ Platform SPA ไม่ใช่ผลิตภัณฑ์นี้; key permission/licence `system_admin.query_dataset` ถูกลบเมื่อ 2026-09-21 |
| [dimension](/th/inventory/system-config/dimension) | Custom field ที่ผู้ใช้นิยามได้ พร้อม matrix การแสดงผลต่อสถานที่ | ไม่มีใคร — ไม่มี CRUD path | Schema เท่านั้น; ไม่พบ service, controller หรือ route |
| [menu](/th/inventory/system-config/menu) | Registry ของการนำทางที่แสดงผลโดย app shell | ไม่มีใคร — ตารางไม่ได้ใช้ | Schema เท่านั้น; ไม่มีการอ้างอิงจากโค้ด non-schema เลย; navigation คือค่าคงที่ใน frontend |
| [doc-version](/th/inventory/system-config/doc-version) | ตัวกัน concurrency ด้วย `doc_version` — client ต้องส่ง version ปัจจุบันตอนบันทึก ไม่งั้นได้ 409 | Engineering | กลไกข้ามโมดูล ไม่ใช่หน้าจอ |

## 4. การพึ่งพาข้ามโมดูล

**การผูก workflow แคบกว่าที่เวอร์ชันก่อนหน้าของรายการนี้เคยระบุไว้** `enum_workflow_type` มีสี่ค่าที่ HEAD — `purchase_request`, `store_requisition`, `purchase_order` และ `gl_jv` (เพิ่มเมื่อ 2026-09-09 พร้อมโมดูล GL journal-voucher, `schema.prisma:276-281`; GL เป็นโมดูลบัญชีที่อยู่นอกเล่มนี้และหน้าจอเวิร์กโฟลว์ของ inventory ไม่มีหน้าสำหรับมัน) GRN, inventory-adjustment, physical-count, spot-check และ vendor-pricelist ยังคง **ไม่มี enum member `workflow_type`** และไม่สามารถผูก row `tb_workflow` ได้ ภาษา "การอนุมัติแบบไม่บังคับ" สำหรับโมดูลเหล่านั้นด้านล่างถูกแก้ไขแล้ว เช่นเดียวกัน claim การติด tag ของ [system-config/dimension](/th/inventory/system-config/dimension) ถูกแก้ไขทั่วทั้งโมดูล — คอลัมน์ `dimension` JSONB มีอยู่บนตารางเหล่านี้แต่ไม่พบโค้ดใดอ่านหรือเขียนมันเลย (ดู Implementation status ของหน้านั้น)

- [purchase-request](/th/inventory/purchase-request) ต้องการ [system-config/workflow](/th/inventory/system-config/workflow) (เส้นทางอนุมัติ PR — จริง, `workflow_type = purchase_request`), [system-config/running-code](/th/inventory/system-config/running-code) (เลขที่ PR) คอลัมน์ `dimension` มีอยู่ ไม่ได้ใช้
- [purchase-order](/th/inventory/purchase-order) ต้องการ [system-config/workflow](/th/inventory/system-config/workflow) (เส้นทางอนุมัติ PO — จริง, `workflow_type = purchase_order` แม้ `tb_purchase_order.workflow_id` จะเป็นฟิลด์ UUID อิสระ ไม่ใช่ relation ของ Prisma ที่ประกาศไว้), [system-config/running-code](/th/inventory/system-config/running-code) (เลขที่ PO) คอลัมน์ `dimension` มีอยู่ ไม่ได้ใช้
- [good-receive-note](/th/inventory/good-receive-note) ต้องการ [system-config/period](/th/inventory/system-config/period) (การ์ดวันที่ posting), [system-config/running-code](/th/inventory/system-config/running-code) (เลขที่ GRN) **ไม่มีการผูก workflow** — `goods_received_note` ไม่ใช่ member ของ `enum_workflow_type`
- [store-requisition](/th/inventory/store-requisition) ต้องการ [system-config/workflow](/th/inventory/system-config/workflow) (เส้นทางอนุมัติ SR — จริง, `workflow_type = store_requisition`), [system-config/running-code](/th/inventory/system-config/running-code) (เลขที่ SR)
- [inventory-adjustment](/th/inventory/inventory-adjustment) ต้องการ [system-config/period](/th/inventory/system-config/period) (การ์ดวันที่ posting), [system-config/running-code](/th/inventory/system-config/running-code) (เลขที่ IA / SI / SO) **ไม่มีการผูก workflow**
- [inventory](/th/inventory/inventory) ต้องการ [system-config/period](/th/inventory/system-config/period) (ขอบเขตของงวดบนทุกการเคลื่อนไหว) คอลัมน์ `dimension` มีอยู่ ไม่ได้ใช้
- [costing](/th/inventory/costing) ต้องการ [system-config/period](/th/inventory/system-config/period) (เครื่องยนต์ปิด cost เขียน `tb_inventory_period_snapshot` เดิมชื่อ `tb_period_snapshot`)
- [physical-count](/th/inventory/physical-count) ต้องการ [system-config/period](/th/inventory/system-config/period) (เอกสารนับถูกแช่แข็งกับงวด), [system-config/running-code](/th/inventory/system-config/running-code) (เลขที่เอกสารนับ) **ไม่มีการผูก workflow**
- [spot-check](/th/inventory/spot-check) ต้องการ [system-config/running-code](/th/inventory/system-config/running-code) (เลขที่เอกสาร) **ไม่มีการผูก workflow**
- [vendor-pricelist](/th/inventory/vendor-pricelist) ต้องการ [system-config/running-code](/th/inventory/system-config/running-code) (เลขที่อ้างอิง pricelist) **ไม่มีการผูก workflow**
- [system-config/application-config](/th/inventory/system-config/application-config) คือตารางจริง แต่ถูก consume เป็นราย key โดยฟีเจอร์เฉพาะ (SMTP sender profile, คลังข้อความอีเมล, config ของ interface, การตั้งค่า signature, config ของ print/approval-flow) — ไม่ใช่เลเยอร์ feature-flag ทั่วไปที่ทุกโมดูลอ่าน [system-config/menu](/th/inventory/system-config/menu) ไม่มีผู้บริโภคที่ยืนยันได้เลย; navigation คือค่าคงที่ใน frontend แทน

## 5. แหล่งข้อมูลอ้างอิง

- **Prisma tenant:** `../carmen-turborepo-backend-v2/packages/prisma-shared-schema-tenant/prisma/schema.prisma`
- **carmen/docs:** `../carmen/docs/workflow-permissions-system.md` (ประเภทของ role ในเวิร์กโฟลว์และ matrix ของสิทธิ์ที่บริโภคโดย [system-config/workflow](/th/inventory/system-config/workflow)) freeze ตั้งแต่ 2026-04-27 — implementation มีน้ำหนักกว่า
- **Seed data:** `../carmen-turborepo-backend-v2/packages/prisma-shared-schema-tenant/prisma/seed-data-a01/` — `tb_workflow.json`, `tb_config_running_code.json`, `tb_application_config.json`; `packages/prisma-shared-schema-tenant/src/seed-data/{running-code,email-templates}.ts`
- **Design spec:** `.specs/2026-05-16-master-config-design.md`
- **Plan:** `.specs/2026-05-16-master-config-plan.md`

## 6. การเปลี่ยนแปลงตั้งแต่ 2026-07-29 (ตรวจสอบ 2026-09-22)

| การเปลี่ยนแปลง | วันที่ | แหล่ง | หน้า wiki |
|---|---|---|---|
| โมดูล Period เปลี่ยนชื่อเป็น **Inventory Period**: ตาราง `tb_period*` → `tb_inventory_period*`, HTTP path `api/:bu_code/periods` → `api/:bu_code/inventory-periods` (path เดิมคงไว้เป็น alias ที่ซ่อน), key permission/licence/`api_name` `system_admin.period` → `system_admin.inventory_period`, FE route `/system-admin/period` → `/system-admin/inventory-period` (คง redirect) | 2026-09-16 | BE `3933277dc`, `a92151730`, `c8de2fa76`; tenant migration `20260916141000_rename_tb_period_to_tb_inventory_period`; platform migration `20260916140000_rename_period_to_inventory_period`, `20260916150000_fix_license_group_item_inventory_period_key`; FE `06c68975`, `1caecb42` | [period](/th/inventory/system-config/period) |
| ข้อมูล licence ย้ายจาก `GET /api/user/profile` ไป `GET /api/license` (`state`, `end_date`, `features[]`, `hidden_features[]`, `expired_features[]` ต่อ BU + union); `403 LICENSE_REQUIRED` / `LICENSE_EXPIRED` ตอนนี้มี `bu_codes`/`bu_names` | 2026-09-09 / 2026-09-21 | BE `dc5623a05`, `11df68225`, `c7a0d9168`; FE `bebd5f75` (`hooks/use-license.ts`, `types/license.ts`, `components/license-expired-banner.tsx`) | [application-config](/th/inventory/system-config/application-config) |
| การแยก licence ของ app-config: `LICENSE_ROUTE_OVERRIDES` map `app-config/email_profiles` → `configuration.email_profile`, `app-config/email_templates` → `configuration.email_template`, `app-config/interface_*` → `interface`; รายการ `GET /app-config` ตัด key เหล่านั้นออก | 2026-09-20 | BE `84667a405`, `9c52288c0`, `5ff86e322`; `packages/prisma-shared-schema-platform/prisma/permission.route-map.ts:257-268` | [application-config](/th/inventory/system-config/application-config), [config-email](/th/inventory/system-config/config-email) |
| Email sender profile เป็น master list + คลังข้อความต่อเอกสาร; endpoint lookup ที่ไม่มี secret `GET /api/:bu_code/email-senders` และ `/email-messages`; `enum_activity_action` += `email_sent` | 2026-08-08 → 2026-09-20 | BE `f5c3e5c5b`, `f00088307`, `6a859f9e2`; FE `67b8a8c1`, `2b80f83c`, `e98ef3ee` | [config-email](/th/inventory/system-config/config-email) |
| Workflow: หนึ่งหน้าต่อประเภทเอกสาร ไม่มีรายการรวม; เอกสารที่กำลังดำเนินอยู่ล็อกเฉพาะรายการ stage/routing (ฟอร์มยังแก้ได้); `GET workflows/:id/edit-availability`, `GET workflows/assignees/:user_id` + `POST .../handover`, `GET workflows/:id/products/:product_id/locations` | 2026-09-01 → 2026-09-16 | BE `b1bcb1e98`, `003e12fb8`, `4fecf678f`, `00ffaaeba`, `383fc4ca4`; FE `8cac942d`, `f3e13d30` | [workflow](/th/inventory/system-config/workflow) |
| Notification template: channel `app` เท่านั้น + `{{variable}}` จริงแปดตัวพร้อม chip แทรกและ preview; nav entry ย้ายไปใต้กลุ่ม Workflows | 2026-09-16 | FE `541690f5`, `00f2196d` | [notification-template](/th/inventory/system-config/notification-template) |
| Running code: editor แบบทีละส่วนพร้อม preview สด; เพิ่ม preset `GL-JV` (ไม่มี segment prefix แบบ literal) | 2026-08-27 / 2026-09-15 | FE `704f1f9f`; BE `27f65cba1` | [running-code](/th/inventory/system-config/running-code) |
| status bar ที่ footer แสดงงวดสินค้าคงคลังปัจจุบัน (จาก `/user/profile`) และ build ของ backend (`GET /version` → `{ version, commit }`) | 2026-09-18 | BE `94b44828d` (`apps/backend-gateway/src/app.controller.ts:71-75`); FE `ad213a11` (`components/footer/status-bar.tsx`, `hooks/use-backend-version.ts`) | [period](/th/inventory/system-config/period) |
| `system_admin.query_dataset` ถูกลบออกจากแคตตาล็อก permission และ licence — SQL Workbench เป็นของ Platform เท่านั้น | 2026-09-21 | BE `7bebddaa7`; FE `02bfff7a` | [query-dataset](/th/inventory/system-config/query-dataset) |
| ghost permission key ของ frontend (`system_configuration.*`) ถูกแทนด้วย resource `tb_permission` จริง (`system_admin.business_unit`, `system_admin.activity_log`, `system_admin.user_activity`, `system_admin.document`, …) | 2026-09-21 | FE `b9e2de5f`, `91b2b274` (`constant/permissions.ts`, `constant/module-list.ts`) | [activity-log](/th/inventory/system-config/activity-log), [company-profile](/th/inventory/system-config/company-profile) |
| `enum_workflow_type` += `gl_jv` | 2026-09-09 | BE `0d119b990` | [workflow](/th/inventory/system-config/workflow) |

ไม่ครอบคลุมโดยหน้าใดในโมดูลนี้ (นอกขอบเขตของเล่ม Inventory บันทึกไว้เพื่อความครบถ้วน): registry cron-job ฝั่ง Platform (`platform/cronjobs/*` ใน Bruno, job ของ `micro-cronjobs` `activity_rollup`, `activity_retention`, `gl_run_due`, คอลัมน์ `notifyAt` + `notifyDayOffset`, `docVersion` + audit, middleware internal-token — 2026-07-30 → 2026-09-21) และหน้าจอ `/system-admin/interface` (config ของ interface accounting/POS/PMS ที่เก็บเป็น key app-config `interface_*` licence เท่านั้น)
