---
title: ใบขอซื้อ (Purchase Request) — Test Scenarios — Approver
description: Test case ของ Approver (happy path, permission, validation, edge case) สำหรับโมดูล purchase-request
published: true
date: '2026-09-23T01:30:00.000Z'
tags: purchase-request, test-scenarios, approver, inventory, carmen-software
editor: markdown
dateCreated: 2026-05-15T09:00:00.000Z
---

# ใบขอซื้อ (Purchase Request) — Test Scenarios — Approver

> **At a Glance**
> **Persona:** Approver (Department Head, Budget Controller, Finance Officer / Manager) &nbsp;·&nbsp; **โมดูล:** [purchase-request](/th/inventory/purchase-request) &nbsp;·&nbsp; **Scenario:** ~33
> **หมวด:** Happy Path &nbsp;·&nbsp; Permission &nbsp;·&nbsp; Validation &nbsp;·&nbsp; Edge Case
> **E2E coverage:** map ไปยัง `tests/303-pr-approver-journey.spec.ts`, `tests/311-pr-returned-flow.spec.ts` และ `tests/301-pr.spec.ts` (fixture hodTest / fcTest / gmTest) ใน `../carmen-inventory-frontend-e2e/`

> **Executable coverage (2026-09-22)** Spec `../carmen-inventory-frontend-e2e/tests/303-pr-approver-journey.spec.ts` (62 test) + `tests/201-my-approvals.spec.ts` (21) + `tests/311-pr-returned-flow.spec.ts` (21) + block approver ของ `tests/301-pr.spec.ts` (197) case ที่ catalogue ไว้แต่ยังไม่ automate: `docs/test-cases/gaps/303-pr-approver-journey-gap.md` (37 case), `gaps/201-my-approvals-gap.md` (34), `gaps/311-pr-returned-flow-gap.md` (30); story `docs/user-stories/303-pr-approver-journey.md`, `201-my-approvals.md` ไม่มีหน้า catalogue `docs/test-cases/303-*.md` แถวด้านล่างถูกแก้ตรงที่ HEAD ขัดแย้ง: send-back คง `pr_status = in_progress`; `approved_qty` ถูกเช็คแค่ `≥ 0` (และเฉพาะโดย `POST …/verify`); ไม่มี panel Budget Impact, ไม่มี soft commitment และไม่มี void ธุรการ

หน้านี้จับ test scenario ที่ persona Approver ขับโดยตรงในโมดูล `purchase-request` Approver ครอบคลุมสาม decision-maker กลางใน chain อนุมัติ — **Department Head** (Stage 1), **Budget Controller** (Stage 2) และ **Finance Officer / Manager** (Stage 3) — ทั้งหมด share UI review-and-decide เดียวกัน Scenario ถูก group เป็นสี่ happy-path action ที่อธิบายใน [03-user-flow-approver.md](./03-user-flow-approver.md) Section 2 (approve, send-back, reject, split-reject), ขอบเขต RBAC ที่บังคับโดย `PR_AUTH_002`–`PR_AUTH_006` ใน [02-business-rules.md](./02-business-rules.md) Section 4, กฎ validation ที่ fire บน approval action (โดยเฉพาะ `PR_VAL_013` บน `approved_qty` และ `PR_VAL_016` บน optimistic concurrency) และชุดเล็กของ boundary / concurrency case ที่มาจาก state machine หลาย stage Handoff ข้าม persona ที่ pivot off Approver (`X-PR-02`, `X-PR-03`, `X-PR-05`, `X-PR-06`, `X-PR-07`) อยู่ในภาพรวมหลัก ไม่ใช่ที่นี่

## 1. Happy Path

| # | Scenario | Pre-condition | Steps | คาดหวัง |
| - | -------- | ------------- | ----- | -------- |
| APP-HP-01 | Approve PR บรรทัดเดียวที่ Stage 1 (Department Head) | PR `pr_status = in_progress`, `workflow_current_stage` ที่ stage `approve` แรก; HOD `hod@blueledgers.com` ล็อกอินอยู่และอยู่ใน `tb_purchase_request.user_action.execute[]`; บรรทัดเดียว | 1. จาก **My Approval** (`/procurement/approval`) หรือแท็บ **My Pending** ของ list PR เปิด PR 2. Review header และบรรทัดใน grid **Items** 3. ไม่แก้ `approved_qty` 4. คลิก **Approve** (footer หรือ bulk toolbar ใน Edit mode) 5. comment optional, ยืนยันใน dialog | `PR_POST_004` fire: `workflow_previous_stage` ← Stage 1, `workflow_current_stage` ← Stage 2, `last_action = approved`, `last_action_by_*` = HOD; `stages_status` สำหรับ Stage 1 ถูก mark; `pr_status` ยังคง `in_progress`; `user_action.execute[]` คำนวณใหม่สำหรับ Stage 2; ผู้อนุมัติ Stage 2 ได้รับแจ้ง; row หายจากคิวของ HOD |
| APP-HP-02 | Approve PR หลายบรรทัดผ่าน stage อนุมัติทั้งสาม | PR `in_progress` ที่มีสองบรรทัด; chain สาม stage ตั้งค่า (`Department Head → Budget Controller → Finance`); fixture `hod@`, `bc@`, `fc@blueledgers.com` | 1. HOD เปิด PR → Approve 2. ผู้ใช้ Stage 2 เปิด PR, Approve 3. ผู้ใช้ Stage 3 เปิด PR, Approve | หลัง approve กลางแต่ละครั้ง `PR_POST_004` เลื่อน `workflow_current_stage`; ตอน approve สุดท้าย `PR_POST_005` พลิก `pr_status` จาก `in_progress` เป็น `approved` (`purchase-request.service.ts:1913`); Requestor ได้รับแจ้ง; PR เข้าเกณฑ์ Convert-to-PO |
| APP-HP-03 | Send-back จาก Stage 1 ด้วยเหตุผล mandatory | PR `in_progress` ที่ Stage 1; HOD ล็อกอินอยู่ | 1. เปิด PR 2. คลิก **Send Back** 3. Dialog ถาม stage เป้าหมาย (จาก `GET …/:pr_id/previous-stages`) และเหตุผล mandatory — เลือก stage create, พิมพ์ `"Please update justification and resubmit"` 4. ยืนยัน | `PR_POST_003` fire: `workflow_current_stage` rollback ไป stage `create`; `last_action = reviewed`; **`pr_status` คงอยู่ที่ `in_progress`** (`purchase-request.service.ts:2052`); Requestor กลับเข้า `user_action.execute[]` และแก้ไขได้; comment `type = system` immutable จับเหตุผล (`PR_POST_008`) |
| APP-HP-04 | Reject ระดับ header ยุติ chain | PR `in_progress` ที่ stage อนุมัติใด ๆ; Approver ปัจจุบันมีสิทธิ์; ข้อความเหตุผลพร้อม | 1. เปิด PR 2. คลิก **Reject** 3. Dialog เหตุผล mandatory — พิมพ์ `"Duplicate of PR-202605-0099"` 4. ยืนยัน | `PR_AUTH_004` + `PR_POST_006` ใช้: `pr_status` พลิกจาก `in_progress` เป็น `voided` (terminal, `purchase-request.service.ts:2201`); `workflow_history` append; `stages_status` / `history` ของทุกบรรทัดอัปเดต; comment `type = system` เขียน; ไม่มี stage ถัดไปทำงาน; Requestor ได้รับแจ้ง |
| APP-HP-05 | Split — ย้ายบางบรรทัดไป PR ใหม่ เก็บที่เหลือ | PR `in_progress` ที่ Stage 1 พร้อมอย่างน้อย 3 บรรทัด (`L1`, `L2`, `L3`); HOD ล็อกอินอยู่ | 1. เปิด PR → **Edit** 2. เลือกเฉพาะ `L2` 3. Bulk **Split** → ยืนยัน | `POST …/:id/split` สร้าง PR ใหม่ที่พก `L2` (สถานะ `draft` ถ้าต้นทางเป็น `draft` ไม่งั้น mirror — `purchase-request.service.ts:1557`); `L1` และ `L3` คงอยู่บนต้นฉบับซึ่งยังคง `in_progress` ที่ Stage 1; roll-up ของ header คำนวณใหม่บนทั้งสองเอกสาร |
| APP-HP-06 | Approve ด้วยการปรับ qty (`approved_qty < requested_qty`) | PR `in_progress` ที่ Stage 1; บรรทัด `L1` มี `requested_qty = 100`, `requested_unit_id = PCS` | 1. เปิด PR → **Edit** 2. บน `L1`, เปลี่ยน `approved_qty` จาก `100` เป็น `60`; `approved_unit_id` ยังคง `PCS` 3. Save 4. คลิก **Approve** | บรรทัด persist ด้วย `approved_qty = 60`; header คำนวณใหม่ตาม `PR_CALC_001`..`PR_CALC_006`; `PR_POST_004` เลื่อนไป Stage 2 ด้วยยอดใหม่; ยอดใหม่ feed routing ของ stage ถัดไปตาม `PR_AUTH_005` (server ไม่เปรียบเทียบกับ `requested_qty` — ดู APP-VAL-05) |
| APP-HP-07 | Approval ของ stage สุดท้ายพลิก `pr_status` เป็น `approved` | PR `in_progress` ที่ stage สุดท้ายของ chain ที่ตั้ง; ผู้ใช้ของ stage นั้นล็อกอินอยู่ | 1. เปิด PR จากคิว 2. Review header และบรรทัด 3. คลิก **Approve** 4. Comment optional, ยืนยัน | `PR_POST_005` fire: `pr_status` transition `in_progress → approved`; `workflow_history` บันทึก final approve; Requestor ได้รับแจ้ง; PR เลือกได้ใน wizard Convert-to-PO แล้ว (`GET …/purchase-requests/for-po`) |
| APP-HP-08 | Delegated approval — delegate ทำแทน Approver ที่ไม่อยู่ **(ยังไม่ยืนยัน — ไม่พบโค้ด delegation)** | *ไม่เกี่ยวข้อง — ดู คาดหวัง* | *ไม่เกี่ยวข้อง — ดู คาดหวัง* | เอกสารรุ่นก่อนหน้าระบุ delegation window ที่ live ตาม `PR_AUTH_006` โดยมีผู้ใช้ delegate ทำแทน HOD และ `last_action_by_id` / audit comment จับแหล่งที่มา delegation การค้นหาทั่ว repo ทั้ง workflow admin ฝั่ง frontend (รวม panel routing rule ที่ยืนยัน `PR_AUTH_005`) และ backend workflow orchestrator ไม่พบกลไก delegation, reassignment, proxy หรือ substitute-approver เลย scenario นี้ยังทดสอบไม่ได้ในปัจจุบัน ถือเป็น design intent ที่ยังไม่ยืนยัน ไม่ใช่ test case ที่ verified |

## 2. Permission / Authorization

| # | Scenario | พฤติกรรมที่คาด (allow/deny + เหตุผล) |
| - | -------- | --------------------------------------- |
| APP-PERM-01 | Approver เปิด PR ในคิวของตน (ผู้ใช้ปัจจุบันอยู่ใน `user_action.execute[]` สำหรับ `workflow_current_stage`) | **Allow** read และปุ่ม action (Approve / Send Back / Reject) `PR_AUTH_002` ผ่านสำหรับ stage ปัจจุบัน |
| APP-PERM-02 | Approver เปิด PR ที่ stage ต่างกัน (เช่น HOD เปิด PR ที่อยู่ stage Budget Controller ปัจจุบัน) | **Deny action, read-only.** ปุ่ม action (Approve / Send Back / Reject / toolbar Split) ถูก disable พร้อมคำอธิบาย inline; `PR_AUTH_002` คำนวณ `user_action.execute[]` ใหม่ทุกการ transition stage และ HOD ไม่อยู่ใน list นั้นแล้ว Visibility list อาจยังให้แถวปรากฏใต้ **All Documents** ขึ้นกับ grant |
| APP-PERM-03 | Approver คลิก **Approve** ที่ stage ปัจจุบัน | **Allow.** `PR_AUTH_002` ผ่านสำหรับ stage ปัจจุบัน; `PR_AUTH_003` ให้สิทธิ์ approve / send-back / reject เป็นสิทธิ์ระดับบรรทัด; `PR_POST_004` (หรือ `PR_POST_005` ถ้าสุดท้าย) commit transition |
| APP-PERM-04 | Approver คลิก **Approve** บน PR ที่ already advanced past stage ของพวกเขา (race หลัง approver concurrent อื่นลงมือ) | **Deny.** `PR_AUTH_002` ฝั่ง server reject เพราะ `user_action.execute[]` ถูกคำนวณใหม่สำหรับ stage ใหม่และผู้ใช้ที่เรียกไม่อยู่ใน list นั้นแล้ว; UI แสดงข้อความ "PR has moved to a later stage — please refresh" และ disable action bar ตอน refresh |
| APP-PERM-05 | Department Head approve PR จากแผนกที่ตนไม่รับผิดชอบ | **Deny.** Routing default chain Stage 1 scope `user_action.execute[]` ไปยังแผนกของ requestor (หรือ HOD ที่ตั้งสำหรับแผนกใน `tb_workflow`) HOD แผนกอื่นไม่อยู่ใน list; `PR_AUTH_002` บล็อก action |
| APP-PERM-06 | Approver พยายาม approve PR ที่ตน submit เอง (segregation of duties) | **Deny.** Role stage `create` และ role stage `approve` ปลายน้ำ disjoint ตามนิยาม workflow แม้ผู้ใช้เดียวกันปรากฏในทั้งสอง stage โดย misconfig `user_action.execute[]` ถูกคำนวณใหม่ต่อ stage และเจ้าของ create-stage ถูก filter ออกก่อน action รัน (ดู [02-business-rules.md](./02-business-rules.md) Section 4, chain default) |
| APP-PERM-07 | ผู้ใช้ Delegate ลงมือกับ PR ที่ delegate ใน window delegation **(ยังไม่ยืนยัน — ไม่พบโค้ด delegation)** | **ยังไม่ยืนยันว่ามีอยู่จริง** ไม่พบกลไก delegation, reassignment, proxy หรือ substitute-approver ใน workflow admin ฝั่ง frontend หรือ backend workflow orchestrator `user_action.execute[]` ถูกคำนวณใหม่จาก `assigned_users` แบบ static ของ stage และ (แยกต่างหาก) `routing_rules` (amount / department / category — แม้ `category` จะไม่เคย match จริง เพราะไม่มี mapper ใดส่ง field `category` ระดับเอกสาร, `workflows.navagation.service.ts` ~บรรทัด 455-456) — ไม่มีตัวใดรองรับการส่งต่อ stage ให้ผู้ใช้อื่นชั่วคราว |
| APP-PERM-08 | Approver เปิด URL PR ใด ๆ ด้วย fixture `noAuth` / session หมดอายุ | **Deny — redirect ไป login.** ไม่มี auth context, `user_action.execute[]` ไม่สามารถ evaluate กับผู้ใช้ปัจจุบัน; `PR_AUTH_002` ผ่านไม่ได้ Covered โดย fixture `noAuthTest` ใน `301-pr.spec.ts` |

## 3. Validation / Error

| # | Scenario | Trigger | Error ที่คาด |
| - | -------- | ------- | -------------- |
| APP-VAL-01 | Approver ลงมือกับ PR ที่ตนไม่มีสิทธิ์ | Approver ที่ **ไม่อยู่** ใน `user_action.execute[]` สำหรับ `workflow_current_stage` เรียก endpoint Approve โดยตรง (bypass ปุ่ม UI ที่ disabled) | `PR_AUTH_002` — reject ด้วย `"You are not authorised to act on this PR at the current stage"` List ถูกคำนวณใหม่ทุกการ transition stage; client ที่ stale ตกในเส้นทางนี้ |
| APP-VAL-02 | Approver คลิก **Send Back** โดยไม่ให้เหตุผล | เปิด dialog send-back, ทิ้ง textarea เหตุผลว่าง, คลิก Confirm | Reject — เหตุผล mandatory ตาม contract dialog; ปุ่ม **Confirm** ก็ disabled ใน UI เมื่อเหตุผลว่าง แต่การเรียก API ตรงถูก reject ที่ server ด้วย `"A reason is required to send back this PR"` Comment `type = system` ที่ `PR_POST_003` เขียนต้องมีข้อความเหตุผล; ถ้าไม่มี write fail |
| APP-VAL-03 | Approver คลิก **Reject** โดยไม่ให้เหตุผล | เปิด dialog reject, ทิ้งเหตุผลว่าง, คลิก Confirm | Reject — `PR_AUTH_004` + `PR_POST_006` ต้องการ system comment immutable พร้อมเหตุผล reject (`PR_POST_008`); server reject การเรียกเหตุผลว่างด้วย `"A reason is required to reject this PR"` |
| APP-VAL-04 | Approver ตั้ง `approved_qty = 0` และคลิก Approve | บนบรรทัด, ใส่ `0` ใน input approved-qty | **ยอมรับ** — schema ฝั่ง client เป็น `min(0)` (`pr-form-schema.ts:67`) และ server reject เฉพาะค่าติดลบ (`verify-approve.ts:172-181`); บรรทัดพก `approved_qty = 0` (ในทางปฏิบัติคือไม่มีอะไรจะสั่ง) และ PR เดินหน้า เอกสารรุ่นก่อนอ้างกฎเข้ม `> 0` — ไม่มี code path |
| APP-VAL-05 | Approver ตั้ง `approved_qty > requested_qty` | บนบรรทัดที่ `requested_qty = 100`, ใส่ `120` | **ยอมรับ** — ทั้ง client และ server ไม่เปรียบเทียบ `approved_qty` กับ `requested_qty` (`PR_VAL_013` เพดานยังไม่ยืนยัน) บันทึกเป็น product decision ที่ต้องตัดสิน ไม่ใช่ความคาดหวังของ test |
| APP-VAL-06 | Approver post `approved_qty` ติดลบ หรือหน่วยอนุมัติที่ไม่ใช่ order unit ของสินค้า | API call ตรงด้วย `approved_qty = -1` หรือ `approved_unit_id` นอก order unit ของสินค้า | `POST …/verify` (`verify_state = approve`, `stage_role = approve`) list `"Detail line N: approved_qty cannot be negative"` / ปัญหา order-unit (`PR_APPROVED_UNIT_CHECK`) **ข้อควรระวัง:** `PATCH …/approve` เองไม่รันการเช็คเหล่านี้ (`purchase-request.logic.ts:420` เป็นผู้เรียกเดียว) — client ที่ข้าม verify persist ค่าเหล่านี้ได้ |
| APP-VAL-07 | Approver พยายาม approve PR ที่ `workflow_id` required ไม่ active อีกต่อไป (scope เปลี่ยนหรือแถวถูก soft-delete กลางบิน) | แถว workflow ใน `tb_workflow` ถูก deactivate หรือ scope flip จาก `purchase-request` หลัง PR submit; Approver คลิก Approve | Reject — `PR_VAL_004` ถูก re-evaluate บน action; `"A valid PR workflow must be selected"` PR ค้างอย่างมีประสิทธิภาพจนกว่า admin re-activate workflow valid หรือ void PR (`PR_AUTH_007`) |
| APP-VAL-08 | Approver แก้ `approved_qty` ตาม `doc_version` ที่ stale (มี concurrent edit อื่นเกิด) | สอง Approver *(เอกสารรุ่นก่อนหน้าระบุอาจเป็น "Approver และ delegate" — ยังไม่ยืนยัน ดู APP-HP-08)* load PR เดียวกัน; A save edit `approved_qty`; B (ยังถือ `doc_version` เก่า) คลิก Approve | `PR_VAL_016` — reject ด้วย `"Document was modified by another user; reload and retry"` Write สำเร็จ bump `doc_version` ขึ้น 1 |
| APP-VAL-09 | Split-reject โดยไม่มีเหตุผลต่อบรรทัดบนบรรทัดที่ reject | Mark `L2` เป็น **reject** แต่ทิ้งเหตุผลต่อบรรทัดว่าง; คลิก Approve ที่ header | Reject — การ reject ระดับบรรทัดต้องการเหตุผลตาม `PR_AUTH_003` และ contract write comment ต่อบรรทัด (`PR_POST_008` ต้องการ payload ของ comment) UI ก็ disable ปุ่ม Approve จนกว่าทุกบรรทัด reject จะมีเหตุผลไม่ว่าง |
| APP-VAL-10 | Approver พยายามลงมือหลัง PR ถูก reject โดยผู้ใช้ stage อื่น | `pr_status` ของ PR เพิ่งพลิกเป็น `voided` โดย Reject ของเพื่อนร่วม stage; Approver load หน้าก่อนหน้านั้นและคลิก Approve | Reject — `purchase-request.logic.ts:702/842` ต้องการ `pr_status = in_progress` สำหรับ approve / reject; lock `doc_version` (`PR_VAL_016`) ก็สะดุดด้วย *(void ธุรการเป็น trigger — ยังไม่ยืนยัน `PR_AUTH_007`)* |

## 4. Edge Case

| # | Scenario | เงื่อนไข | คาดหวัง |
| - | -------- | --------- | -------- |
| APP-EDGE-01 | `approved_qty = 0` บนบรรทัดหนึ่ง บรรทัดอื่นไม่เปลี่ยน | Approver ตั้ง `approved_qty = 0` บน `L2` แล้ว approve | PR เดินหน้าโดย `L2` persist ที่ `approved_qty = 0`; ไม่มี reject ต่อบรรทัดถูก materialize อัตโนมัติ (เอกสารรุ่นก่อนอธิบาย toolbar ที่เขียน `null` + flag reject — ไม่มี code path) หากต้องการตัด `L2` ออกจริง ให้ reject มันหรือย้ายออกด้วย **Split** |
| APP-EDGE-02 | Boundary `approved_qty > requested_qty` | `requested_qty = 12.00000`, Approver ใส่ `approved_qty = 12.00001` | ยอมรับ — ไม่มีการเปรียบเทียบเพดาน (`PR_VAL_013`); ค่า persist ที่ทศนิยม 5 ตำแหน่ง |
| APP-EDGE-03 | Approve พร้อมกันโดยผู้ใช้สองคนของ stage เดียวกัน | Stage 1 ตั้งค่าให้มีผู้ใช้สองคนใน `assigned_users` (เอกสารรุ่นก่อนหน้าระบุว่าเป็น "สอง delegate" — ยังไม่ยืนยัน ดู APP-HP-08); ทั้งคู่เปิด PR ภายในไม่กี่วินาทีและทั้งคู่คลิก Approve | Approve แรกชนะและ bump `doc_version` + เลื่อน `workflow_current_stage`; Approve ที่สองถูก reject — โดย `PR_VAL_016` ถ้า client ที่สองยังถือ `doc_version` เก่า หรือโดย `PR_AUTH_002` ถ้า client refresh แล้ว (PR advanced past Stage 1 แล้วและผู้อนุมัติคนที่สองไม่อยู่ใน `user_action.execute[]`) Last-write-wins **ไม่ใช่** นโยบาย |
| APP-EDGE-04 | Window delegation หมดกลางการตัดสินใจ **(ยังไม่ยืนยัน — ไม่พบโค้ด delegation)** | *ไม่เกี่ยวข้อง — ดู คาดหวัง* | เอกสารรุ่นก่อนหน้าระบุ delegation window แบบจำกัดเวลาที่อาจหมดอายุระหว่าง page-load กับ Approve การค้นหาทั่ว repo ทั้ง workflow admin ฝั่ง frontend และ backend workflow orchestrator ไม่พบกลไก delegation เลย จึงไม่มี window แบบนั้น (และไม่มี expiry check) ให้ทดสอบ scenario นี้ยังทดสอบไม่ได้ในปัจจุบัน ถือเป็น design intent ที่ยังไม่ยืนยัน |
| APP-EDGE-05 | Boundary threshold — ยอด header ลงที่ threshold ของ routing พอดี | `tb_workflow.data.routing_rules` มี `total_amount gt 100,000.00000` เป้าหมาย Stage 4; Approver ปรับ `approved_qty` ให้ยอดใหม่เท่ากับ `100,000.00000 ฿` พอดี | ด้วย operator `gt` ค่าขอบเขตพอดี**ไม่** match ดังนั้น PR route ไป stage ปกติถัดไป; `100,000.00001 ฿` จะ match ด้วย `gte` ขอบเขตจะ match (operator ตั้งค่าได้ — `eq`/`gt`/`lt`/`gte`/`lte`/`between` จาก panel Routing — test จึงต้องอ่าน rule ก่อน) |
| APP-EDGE-06 | Send-back ได้รับโดย Requestor ขณะ Approver ที่สอง load PR | Approver A คลิก Send Back ที่ `T`; Approver B (parallel reviewer / observer) อยู่บนหน้า detail อยู่แล้วและคลิก Approve ที่ `T+2s` | Reject สำหรับ B `PR_AUTH_002` re-evaluate บน action และพบ `workflow_current_stage` rolled back ไป stage `create`; B ไม่อยู่ใน `user_action.execute[]` ของ stage นั้น UI prompt B ให้ refresh |
| APP-EDGE-07 | ความแม่นยำทศนิยมสูงสุดในการปรับ `approved_qty` | บรรทัดมี `requested_qty = 1.99999`; Approver ใส่ `approved_qty = 1.99998` | ยอมรับ — `PR_VAL_013` ผ่าน (`> 0` และ `≤ requested_qty`); ค่า persist ตรงที่ 5 ทศนิยมบน `Decimal(15, 5)`; roll-up header ถัดไป round ตามนโยบายใน [02-business-rules.md](./02-business-rules.md) Section 3 |

## 5. แหล่งอ้างอิง

- ภาพรวมหลัก: [04-test-scenarios.md](./04-test-scenarios.md) — handoff ข้าม persona ที่แตะ Approver: `X-PR-02` (send-back loop), `X-PR-03` (split-reject), `X-PR-05` (threshold escalation), `X-PR-06` (เส้นทาง reject), `X-PR-07` (Purchaser bounce-back), `X-PR-10` (returned-PR round trip)
- User flow: [03-user-flow-approver.md](./03-user-flow-approver.md) — แหล่ง happy-path สำหรับ Section 1 ด้านบน; อธิบาย chain สาม stage (Department Head → Budget Controller → Finance) และ action ตัดสินใจสี่ตัว
- กฎทางธุรกิจที่ verify: [02-business-rules.md](./02-business-rules.md) Section 4 (`PR_AUTH_001`–`PR_AUTH_008` — โดยเฉพาะ `PR_AUTH_002` stage-membership, `PR_AUTH_003` action ระดับบรรทัด, `PR_AUTH_004` reject header, `PR_AUTH_005` threshold routing ยืนยันแล้ว; `PR_AUTH_007` void ยังไม่ยืนยัน), Section 2 (`PR_VAL_013` บน `approved_qty` — `≥ 0` เท่านั้น, `PR_VAL_016` บน optimistic concurrency, `PR_VAL_017` verify) และ Section 5 (`PR_POST_003`–`PR_POST_006`, `PR_POST_008` system comment immutable) `PR_AUTH_006` (delegation) ยังไม่ยืนยัน — ดู APP-HP-08, APP-PERM-07 และ APP-EDGE-04 ด้านบน
- E2E: `../carmen-inventory-frontend-e2e/tests/303-pr-approver-journey.spec.ts` — spec persona-journey (HOD หลัก, Finance Controller สำหรับ scope ที่ต่าง) `TC-PR-060101`..`TC-PR-060104` (dashboard My Approvals), `TC-PR-060201`..`TC-PR-060205` (view approver ใน PR list), `TC-PR-060301`..`TC-PR-060304` (PR detail read-only), `TC-PR-060401`..`TC-PR-060412` (edit mode + bulk action: approved-qty แก้ได้, vendor / unit-price read-only, bulk Approve / Reject / Send-for-Review / Split, cancel-edit ทิ้ง), `TC-PR-060501` (FC scope หลายแผนก), `TC-PR-060901` (golden full flow) Loop send-back Returned-PR อยู่ใน `../carmen-inventory-frontend-e2e/tests/311-pr-returned-flow.spec.ts` Coverage permission ต่อ action × ต่อ role อยู่ใน `../carmen-inventory-frontend-e2e/tests/301-pr.spec.ts` (fixture `hodTest`, `fcTest`, `gmTest`)
- ที่มา: `../carmen/docs/purchase-request-management/testing.md` (level testing — pattern `approve-pr.spec.ts`, `reject-pr.spec.ts`), `../carmen/docs/purchase-request-management/troubleshooting.md` (Section 2.1 — ปัญหากระบวนการอนุมัติ: stage ค้าง, ปุ่มหาย, ประวัติหาย; Section 2.2 — ปัญหา workflow transition: ไม่สามารถย้าย stage ถัดไป, return อัตโนมัติ, ผิด stage)
