---
title: ใบเบิกของสโตร์ (Store Requisition) — Business Rules
description: กฎ validation, calculation, authorization, posting และข้ามโมดูลของ store-requisition
published: true
date: '2026-09-23T01:30:00.000Z'
tags: store-requisition, business-rules, inventory, carmen-software
editor: markdown
dateCreated: 2026-05-15T13:30:00.000Z
---

# ใบเบิกของสโตร์ (Store Requisition) — Business Rules

> **At a Glance**
> **กลุ่มของกฎ:** `SR_VAL_*` validation &nbsp;·&nbsp; `SR_AUTH_*` permission &nbsp;·&nbsp; `SR_CALC_*` calc &nbsp;·&nbsp; `SR_POST_*` posting &nbsp;·&nbsp; `SR_XMOD_*` cross-module
> **จำนวนกฎ:** ประมาณ 60 กฎ
> **กลุ่มผู้ใช้:** ผู้เขียน test + นักพัฒนา — ทุก rule ID ถูกอ้างจากหน้า `04-test-scenarios*`
> **วงจรชีวิตสถานะ:** หัวข้อ 5.1 (ในที่ที่มี) บรรจุ callout ความไม่ตรงกันระหว่าง Live UI กับ BRD
> **ตรวจสอบซ้ำ 2026-09-22** เทียบกับ backend HEAD `cd2e07f60` / frontend HEAD `8cf47f87` การเปลี่ยนแปลงหลัก: invariant ปริมาณสามค่า **ไม่ถูกบังคับใช้** ที่ใดเลย (`SR_VAL_008`/`SR_VAL_010` ทำเครื่องหมายใหม่), `sr_type` ถูก derive (`SR_VAL_003` เขียนใหม่), open-period gate **มีจริง** (`SR_VAL_014` ยืนยันแล้ว, เพิ่ม `SR_VAL_016`), submit ตรวจสอบว่าสินค้าเปิดใช้ที่ปลายทาง (`SR_VAL_015`), การลบจำกัดเฉพาะเจ้าของและมีแบบ batch (`SR_AUTH_004`, `SR_POST_011`) และ workflow verb ทุกตัวเป็น `PATCH`

## 1. ภาพรวม

หน้านี้รวบรวมกฎทางธุรกิจเชิงปฏิบัติการที่กำกับเอกสาร Store Requisition (SR) ตลอดวงจรชีวิต: input validation ตอน create / edit / submit / approve / issue, ชุดกฎการคำนวณเล็ก ๆ (quantity invariants และการ feed unit cost จากต้นทาง; SR เป็น **เอกสารเชิงปริมาณ** ไม่ใช่เอกสารเชิงราคา — ดู [store-requisition/01-data-model](/th/inventory/store-requisition/01-data-model) § 5 ข้อ 3 และ 4), การกำหนดสิทธิ์ตาม role และสถานะเอกสาร, ผลกระทบของการ posting ในแต่ละ transition ของ `enum_doc_status` และกฎข้ามโมดูลกับ [inventory](/th/inventory/inventory), [costing](/th/inventory/costing), [recipe](/th/inventory/recipe) และ [good-receive-note](/th/inventory/good-receive-note) SR คือ system of record สำหรับ **การเคลื่อนย้ายสต๊อกภายในระหว่างสถานที่**: จนกว่าจะ commit (`in_progress → completed`) ไม่มีการลด inventory ที่ต้นทางและไม่มี expense / inventory entry ที่ปลายทาง; เมื่อ commit แล้ว on-hand ของสถานที่ต้นทางลดลงตาม `issued_qty` ต่อบรรทัด, ปลายทางได้รับปริมาณเดียวกัน (`sr_type = transfer`, on-hand เพิ่ม) หรือมูลค่าลงเป็น expense ทันทีโดยไม่มีการเปลี่ยน on-hand ปลายทาง (`sr_type = issue`) ผ่าน `tb_inventory_transaction` ที่ลิงก์ การ post journal-entry / GL จาก event นี้ยังไม่ยืนยันในซอร์สปัจจุบัน — ดู `SR_POST_007`

จุดทางโครงสร้างสองจุดที่กระทบทุกกฎด้านล่างนี้และคุ้มค่าที่จะเน้นไว้ก่อน **ประการแรก** ปริมาณสามค่าบน `tb_store_requisition_detail` — `requested_qty`, `approved_qty`, `issued_qty` — ร่วมกันบรรจุเรื่องราวทั้งหมดข้ามวงจรชีวิต พร้อมลำดับที่ตั้งใจไว้ `0 ≤ issued_qty ≤ approved_qty ≤ requested_qty` **แก้ไข 2026-09-22:** ลำดับนั้น *ไม่* ถูกบังคับใช้โดยโค้ด — มีเพียง `requested_qty > 0` ที่ถูกตรวจ (schema ก่อน submit); `approved_qty` และ `issued_qty` ถูก persist ตรง ๆ จาก payload (ดู `SR_VAL_008`) **ประการที่สอง** ต่างจาก GRN ที่ `approved` / `rejected` / `committed` เป็นสถานะส่วนหัวแยก SR ยุบทั้งการอนุมัติและ fulfillment ภายใต้สถานะ `in_progress` เดียว — ฟิลด์ `workflow_current_stage` คือสิ่งที่แยก "รอผู้อนุมัติ" กับ "รอ store keeper" ดังนั้นกฎที่อ้าง "ช่วงอนุมัติ" หรือ "ช่วง issue" gate บน `(doc_status = 'in_progress', workflow_current_stage = <approval-stage-slug>)` แทนที่จะอยู่บนสถานะระดับบนเพียงอย่างเดียว

## 2. กฎ Validation

Rule ID ใช้ `SR_VAL_NNN` กฎส่วนหัว (001–005) ทำงานทุก save และตอน submit; กฎบรรทัด (006–010) ทำงานต่อบรรทัดตอน save, submit และทุก action approval / issue; กฎ aggregate / at-commit (011–014) ทำงานเฉพาะที่ transition `in_progress → completed` (posting event ครั้งเดียว)

| Rule ID | เงื่อนไข | บังคับใช้เมื่อ | Error / พฤติกรรม |
| ------- | -------- | -------------- | ---------------- |
| `SR_VAL_001` | `tb_store_requisition.sr_no` เป็น non-null และ unique เทียบกับ SR ที่ยังไม่ถูก soft-delete (`@@unique([sr_no, deleted_at])`) **แก้ไข 2026-09-22:** คอลัมน์เป็น `NOT NULL` แต่ draft ถูกสร้างด้วย placeholder `draft-<6 hex>` (`StoreRequisitionService.create()`); เลขที่ running-code ถูกออกตอน submit เท่านั้น (`generateSRNo()`, type `STORE-REQUISITION`, ส่วนวันที่จาก `sr_date` ที่ freeze แล้ว) และไม่ถูกออกใหม่เมื่อ resubmit หลัง send-back | Submit (ออกเลขที่); DB (uniqueness) | ความล้มเหลวของ running-code ปรากฏเป็น 500 (`Failed to get running code pattern for SR` / `Failed to generate SR number`); เลขซ้ำเป็น DB unique-constraint error ไม่ใช่ข้อความที่อยู่ใน catalogue |
| `SR_VAL_002` | `from_location_id` และ `to_location_id` ถูกบังคับโดย create/update DTO และทั้งคู่ต้อง resolve เป็นแถว `tb_location` ที่ยังไม่ถูก soft-delete (`resolveAndValidateSrType()` → `SR_LOCATION_NOT_FOUND`) ฟอร์ม frontend บังคับทั้งคู่ (`sr-form-schema.ts`) **ยังไม่ยืนยัน:** การตรวจ `from ≠ to` — `resolveAndValidateSrType()` ค้นหาสองแถวแยกกันและไม่พบการตรวจความเท่ากัน; lookup ปลายทางของฟอร์มตัดต้นทางที่เลือกออก ดังนั้นกรณีนี้ถูกป้องกันฝั่ง client เท่านั้น | Create, update | `SR_LOCATION_NOT_FOUND` (404) เมื่อ id ใดไม่รู้จัก |
| `SR_VAL_003` | **เขียนใหม่ 2026-09-22 — `sr_type` ถูก derive ไม่ใช่ validate** `deriveSrType()` (`logic/sr-type.helper.ts`) ตั้ง `sr_type = issue` เมื่อปลายทางเป็น `direct` และ `transfer` เมื่อเป็น `inventory` หรือ `consignment`; client ไม่เคยส่งฟิลด์นี้ การปฏิเสธเดียวคือ **ต้นทาง** แบบ `direct` | Create (`resolveAndValidateSrType()` ภายใน `create()`) | 400 `from_location must be inventory or consignment, not direct` ไม่มี error "ปลายทางไม่เข้ากัน" ให้ทดสอบ |
| `SR_VAL_004` | `sr_date` เป็น non-null และไม่อยู่ในอนาคตเกิน tolerance ของ tenant; `expected_date` (ถ้าตั้ง) อยู่ในวันเดียวกันหรือหลัง `sr_date` | Save, submit | Reject ด้วย "Requisition date is required; expected date must be on or after requisition date." |
| `SR_VAL_005` | `department_id` ถูกบังคับโดย create DTO (ตั้งแต่ `1dbaf76b1`, 2026-09-17 — create ที่ไม่มีค่านี้ได้ 400); `requestor_id`, `department_id` และ `workflow_id` ถูกบังคับโดย `ValidateSRBeforeSubmitSchema` หาก draft ยังไม่มีแผนก `submit()` จะ derive จากสมาชิกภาพ `tb_department_user` ของผู้ขอ (`resolveRequesterDepartment()`): ไม่มี → `SR_DEPARTMENT_NOT_FOUND`, หลายรายการ → `SR_DEPARTMENT_AMBIGUOUS` (ทั้งคู่ 422) ไม่พบการตรวจว่าผู้ขอ *เป็นสมาชิก* ของแผนกที่ client ส่งมา | Create (DTO), submit | 400 `Store requisition is not ready to submit — <path>: <issue>` (ความล้มเหลวของ Zod ถูกคืนเป็น `VALIDATION_FAILURE` ไม่ใช่ throw) หรือสอง catalogue code ข้างต้น |
| `SR_VAL_006` | ทุกแถว `tb_store_requisition_detail` มี `product_id` non-null อ้างอิง `tb_product` ที่ active และยังไม่ถูก soft-delete ซึ่งสถานะอนุญาตให้ issue (ไม่ discontinued / inactive ที่สถานที่ต้นทาง) | Save line, submit | Reject บรรทัดด้วย "Product is required and must be active for issue from the source location." |
| `SR_VAL_007` | triple `(store_requisition_id, product_id, dimension)` unique ข้ามแถว detail ที่ยังไม่ถูก soft-delete บน SR เดียวกัน (ตาม index `SRT1_*`) สินค้าเดียวกันบน SR เดียวกันที่ `dimension` ต่างกัน (split cost-centre allocation) อนุญาตเป็นสองแถว; product+dimension เหมือนกันเป็น duplicate | Save line, submit | Reject บรรทัดด้วย "This product is already on the SR with the same cost-dimension allocation; edit the existing line or use a different dimension to split." |
| `SR_VAL_008` | **แก้ไข 2026-09-22 — มีเพียงข้อแรกที่เป็นจริง** `requested_qty > 0` ถูกบังคับใช้ตอน submit เท่านั้น (`ValidateSRBeforeSubmitSchema`: `z.number().positive()` ต่อบรรทัด บวก `.min(1)` บรรทัด); detail DTO สำหรับ create/update (`StoreRequisitionDetailCreateSchema`) รับ `z.number()` ใดก็ได้ ดังนั้นบรรทัด `0` บันทึกบน draft ได้ `approved_qty` / `issued_qty` มี type เป็น `z.number()` เปล่า ๆ ใน `store-requisition-detail.dto.ts`; `saveStageRoleDetails()` เขียน `detail[qtyField]` ตรง ๆ และ `approve()` spread payload เข้า update ดังนั้นค่าติดลบ ศูนย์ หรือเกินเพดานถูกเก็บโดยไม่มีการเตือน frontend จำกัดเพียง `requested_qty ≥ 0` (`sr-form-schema.ts`) | Submit (requested เท่านั้น) | 400 `Store requisition is not ready to submit — store_requisition_detail.<n>.requested_qty: …` **ไม่มีข้อความสำหรับเพดาน approved/issued** |
| `SR_VAL_009` | **ยังไม่ยืนยัน — ไม่พบ code path (2026-09-22)** `submit()` ไม่อ่าน on-hand เลย; `grep -n "on_hand\|getOnHandQty\|inventory_status" store-requisition.service.ts store-requisition.logic.ts` คืนเพียง read model ของ stock-movements ไม่มี soft/hard switch ของ tenant และไม่มีแนวคิด reservation ผู้ขอเห็น on-hand ผ่าน dialog Inventory Information ต่อแถวเท่านั้น (`components/share/inventory-dialog.tsx`) ซึ่งเป็นข้อมูลประกอบ | — | ไม่มี การขอเกินถูกจับที่ขั้นสุดท้ายเท่านั้น (`SR_VAL_013`) |
| `SR_VAL_010` | **ไม่ถูกบังคับใช้ (แก้ไข 2026-09-22)** ทั้งเพดาน `approved_qty ≤ requested_qty` และ `reject_message` ที่บังคับไม่มีอยู่ในโค้ด: `approve()`/`reject()` เก็บ `stage_message || ''` และ dialog Approve / Reject / Issue ใน footer ถูก render ด้วย `showMessage={false}` (`sr-form-dialogs.tsx`) — ช่องเหตุผลปรากฏเฉพาะใน dialog Send Back และ action bulk-Reject เหนือตารางรายการ และแม้ที่นั่นก็เป็น optional (`Enter reason (optional)…`) การ reject ต่อบรรทัด **ไม่** ตั้ง `approved_qty` เป็นศูนย์อัตโนมัติ | — | ไม่มี |
| `SR_VAL_011` | **ยังไม่ยืนยัน — ไม่ถูกบังคับใช้** `executeTransferOnComplete()` กรองบรรทัดเหลือ `issued_qty > 0` และ return เฉย ๆ เมื่อไม่เหลือบรรทัดใด; ส่วนหัวยังพลิกเป็น `completed` ไม่มี guard "อย่างน้อยหนึ่งบรรทัดที่อนุมัติ" ใน `approve()` | — | ไม่มี — SR ที่ทุกบรรทัด `issued_qty = 0` complete โดยไม่มี inventory movement |
| `SR_VAL_012` | ตอน commit ทุกบรรทัดที่ `issued_qty > 0` และ `product.product_type = 'inventory'` ต้องมี `inventory_transaction_id` บรรจุค่า และ `tb_inventory_transaction_detail` ที่ลิงก์บรรจุ `lot_no` และ `cost_per_unit` **แก้ไขในรอบนี้:** การเลือก lot เป็น FIFO ที่คำนวณโดยระบบ (`getAvailableFifoLots` / `consumeFifoLots` ใน `inventory-transaction.service.ts`) — ไม่มี UI เลือก lot ใน SR frontend ดังนั้น "lot ที่ store-keeper เลือก" ไม่ใช่เส้นทางจริง ให้ถือว่าการ assign lot เป็นแบบอัตโนมัติ | Commit | ยังไม่ยืนยันว่ามี validation message เฉพาะที่ฝั่ง client — ไม่มีฟอร์มเลือก lot ให้ล้มเหลว |
| `SR_VAL_013` | ที่ขั้นสุดท้ายต้นทางต้องมีสต๊อกครบทุก `issued_qty`: `InventoryTransactionService.createFifoConsumption()` / `createAverageConsumption()` อ่านยอดคงเหลือ live ของสถานที่ (`getLocationBalance`) และ `throw new Error('Insufficient stock. Requested: <qty>, Available: <balance>')` ภายใน `$transaction` ทำให้ทุกบรรทัด rollback นี่เป็น guard **หลัง approve**: การ update ส่วนหัวถูก commit ไปแล้วใน `StoreRequisitionService.approve()` ก่อนที่ `executeTransferOnComplete()` จะ run ดังนั้นความล้มเหลวที่สังเกตได้คือ 500 บน approve call โดยส่วนหัวเป็น `completed` ไปแล้ว (ดูหมายเหตุ `SR_POST_006`) | ขั้นสุดท้าย (`executeTransferOnComplete`) | `Insufficient stock. Requested: …, Available: …` (ข้อความ error ดิบ; ไม่อยู่ใน catalogue) |
| `SR_VAL_014` | **ยืนยันแล้ว 2026-09-22 (เดิมยังไม่ยืนยัน)** `logic/sr-date.helper.ts` (`cd2e07f60`, 2026-09-18) gate ทั้งสองวันที่เทียบกับ `tb_inventory_period`: ตอน submit `resolveSubmitSrDate()` เก็บวันนี้เมื่อวันนี้อยู่ใน open period มิฉะนั้นเรียกร้อง `sr_date_pattern` (`SR_DATE_PATTERN_REQUIRED`) และปฏิเสธเมื่อไม่มี period ใดเปิดเลย (`SR_NO_OPEN_PERIOD`); ที่ขั้น issue `resolveIssueDate()` บังคับก่อนว่า `sr_date` ของใบเบิกเองต้องอยู่ใน open period (`SR_DATE_OUTSIDE_OPEN_PERIOD`) แล้วใช้กฎ today/pattern เดียวกัน (`SR_ISSUE_DATE_PATTERN_REQUIRED`) โดยปฏิเสธ `today` ที่อยู่นอกทุก open period (`SR_ISSUE_DATE_TODAY_OUTSIDE_PERIOD`) วันที่ issue ที่ resolve แล้วเดินทางไป `executeTransfer()` เป็น `doc_date` และกำหนด period ของ movement | Submit; ทุก approve call ที่ทำด้วย `stage_role = issue` (ไม่ใช่เฉพาะครั้งสุดท้าย) | ทั้งห้า code เป็น 422 พร้อมข้อความ EN/TH ใน `packages/error-catalog/src/catalog.ts` (`SR` module ids 7–11) |
| `SR_VAL_015` | สินค้าของทุกบรรทัดที่ไม่ถูกลบต้องถูกเปิดใช้ที่ปลายทาง: `submit()` โหลด `tb_product_location` ของ `to_location_id` และปฏิเสธหาก `product_id` ของบรรทัดใดไม่มีอยู่ที่นั่น | Submit | 400 `The following products are not allowed in the destination location: <names>` (`VALIDATION_FAILURE`) |
| `SR_VAL_016` | ค่า transport ของ date-pattern: `sr_date_pattern` (submit body) และ `issue_date_pattern` (approve body) รับเฉพาะ `open-period` หรือ `today` (`SR_DATE_PATTERNS`, `sr-date.helper.ts`); ไม่เคยถูกเก็บ frontend ตอบสอง 422 code ด้วย dialog สองปุ่มแล้ว retry (`use-sr-form-actions.ts` `datePatternField()`, `sr-date-pattern-dialog.tsx`) | Submit, approve (ขั้น issue) | ถูกละเลยเมื่อวันนี้อยู่ใน open period |

## 3. กฎการคำนวณ

SR เป็น **เอกสารเชิงปริมาณ** ไม่ใช่เอกสารเชิงราคา ดังนั้น surface การคำนวณจึงเล็ก ปริมาณทั้งหมดถูกเก็บเป็น `Decimal(20, 5)` ที่ระดับแถว; การปัดเศษเพื่อแสดงผลใช้ half-up 3 ทศนิยมสำหรับปริมาณ **ไม่มีคอลัมน์เงินบน `tb_store_requisition` หรือ `tb_store_requisition_detail`** — unit cost และ line total ถูกอ่านจาก inventory transaction ที่ลิงก์ตอนแสดงผล (ดู [store-requisition/01-data-model](/th/inventory/store-requisition/01-data-model) § 5 ข้อ 3 และ 4)

Rule ID ใช้ `SR_CALC_NNN`

| Rule ID | สูตร |
| ------- | ---- |
| `SR_CALC_001` (quantity invariant) | `0 ≤ issued_qty ≤ approved_qty ≤ requested_qty` บนทุกแถว detail ที่ active (`deleted_at IS NULL`) **เป็นเจตนาที่บันทึกไว้เท่านั้น** — ไม่ถูกบังคับใช้ตอนเขียน (ดู `SR_VAL_008`); ผู้อ่านที่คำนวณ variance ควรป้องกันแถวที่ละเมิดลำดับนี้ |
| `SR_CALC_002` (variance — requested vs issued) | `variance_qty = requested_qty − issued_qty` ต่อบรรทัด; variance บวกคือ fulfillment ขาด (approver trim, source stock-out หรือ partial issue) variance ไม่ writeback ลงบรรทัด — คำนวณสำหรับ variance dashboard ([inventory](/th/inventory/inventory) / outlet variance reporting) |
| `SR_CALC_003` (variance — approved vs issued) | `fulfilment_gap = approved_qty − issued_qty` ต่อบรรทัด; gap บวกคือ shortfall ตอน issue (store keeper ไม่สามารถปล่อยจำนวนที่อนุมัติเต็ม) นี่คือ metric ที่ store keeper ปิด (fulfillment เต็มเมื่อ `fulfilment_gap = 0`) |
| `SR_CALC_004` (unit cost feed จากต้นทาง) | `unit_cost = tb_inventory_transaction_cost_layer.cost_per_unit` ตอน issue เลือกตามวิธี costing ของสถานที่ต้นทาง (FIFO หรือ moving-average ตาม [costing](/th/inventory/costing)) SR **ไม่** คำนวณหรือเก็บ unit cost เอง — cost เป็นเจ้าของโดยโมดูล costing ฝั่ง inventory-transaction; บรรทัด SR เข้าถึงผ่าน `inventory_transaction_id` |
| `SR_CALC_005` (line total — แสดงผลเท่านั้น) | `line_total = qty × unit_cost` — ตั้งแต่ 2026-09-14 ฟอร์มดึงตัวเลขจริงจาก `GET /api/{bu}/cost/products/{product_id}/location/{from_location_id}/qty/{qty}` (`sr-item-cost-sync.tsx`) และเขียน `cost_per_unit` / `total_cost` เข้า form state โดยไม่ทำให้ฟอร์ม dirty; หลัง complete แท็บ Stock Movement แสดง `cost_per_unit` / `total_cost` ที่ post แล้วจาก `tb_inventory_transaction_detail` **ไม่ persist บน `tb_store_requisition_detail`** |
| `SR_CALC_006` (header total — แสดงผลเท่านั้น) | `header_total = Σ (issued_qty × unit_cost)` ข้ามบรรทัดที่ issue และ active — คำนวณเพื่อแสดงผล แต่ **ไม่ persist บน `tb_store_requisition`** |
| `SR_CALC_007` (โหมดปัดเศษ) | Half-up 3 ทศนิยมสำหรับปริมาณ (เก็บ `Decimal(20, 5)`) สำหรับฟิลด์เงินที่แสดงผลเท่านั้น half-up ถึงความแม่นยำของสกุลเงินของสถานที่ต้นทาง (โดยทั่วไป 2 dp) inventory transaction รับผิดชอบการเก็บค่าเงินที่ปัดเศษแล้ว |

### 3.1 ตัวอย่างการคำนวณ (1 SR, 3 บรรทัด, ผลลัพธ์ผสม)

เอาท์เลต *Main Kitchen* (`tb_location.location_type = 'direct'`) ตั้ง SR กับ *Central Store* (`tb_location.location_type = 'inventory'`); `sr_type = issue` สามบรรทัด

- **บรรทัด 1** (ข้าว): `requested_qty = 25.000`, on-hand ต้นทาง = 100.000
  - ผู้อนุมัติอนุมัติเต็ม: `approved_qty = 25.000`
  - Store keeper issue เต็ม: `issued_qty = 25.000`
  - Cost-layer ต้นทาง (moving-average): `cost_per_unit = ฿42.50` ต่อ kg
  - Line variance: `requested − issued = 0`; fulfilment gap = 0
  - Total ที่แสดง: `25.000 × ฿42.50 = ฿1,062.50`
- **บรรทัด 2** (แป้ง): `requested_qty = 15.000`, on-hand ต้นทาง = 12.000
  - ผู้อนุมัติตัดลงตามความพร้อมต้นทาง: `approved_qty = 12.000`; `approved_message = "trimmed to source on-hand"`
  - Store keeper issue ตามที่อนุมัติ: `issued_qty = 12.000`
  - Cost-layer ต้นทาง (FIFO): `cost_per_unit = ฿28.00` ต่อ kg
  - Line variance: `requested − issued = 3.000` (ขาด 3; เอาท์เลตขอ 15 ได้ 12)
  - Total ที่แสดง: `12.000 × ฿28.00 = ฿336.00`
- **บรรทัด 3** (น้ำตาล): `requested_qty = 10.000`, on-hand ต้นทางตอน submit = 10.000 แต่ตอน issue = 6.000 (SR อื่นใช้ไป 4)
  - ผู้อนุมัติอนุมัติเต็ม: `approved_qty = 10.000`
  - Store keeper เจอ stock-out ตอน issue (`SR_VAL_013`): ปล่อยได้แค่ 6; บันทึก `issued_qty = 6.000`; เขียน system comment "issued 6 of 10; 4 short due to concurrent consumption"
  - Cost-layer ต้นทาง (moving-average): `cost_per_unit = ฿31.50` ต่อ kg
  - Line variance: `requested − issued = 4.000`; fulfilment gap = 4.000
  - Total ที่แสดง: `6.000 × ฿31.50 = ฿189.00`

Roll-up ส่วนหัว (แสดงผลเท่านั้น):
- `Σ line_total = 1,062.50 + 336.00 + 189.00 = ฿1,587.50` — นี่คือต้นทุนที่ย้ายออกจาก inventory ต้นทางและตกที่ค่าใช้จ่าย cost-centre ของ Main Kitchen (`sr_type = issue` ในที่นี้) หรือเข้าบัญชี inventory ของ Main Kitchen (ถ้าเป็น `sr_type = transfer`)
- inventory transaction สามใบถูกเขียน ทั้งหมด stamp `inventory_doc_type = store_requisition`, ทั้งหมดเป็นแถว `outQty` ที่ต้นทาง สำหรับ `sr_type = transfer` (ไม่ใช่กรณีนี้) แถว `inQty` คู่กันสามแถวจะลงที่ปลายทางด้วย

## 4. กฎ Authorization

Rule ID ใช้ `SR_AUTH_NNN` Authorization บังคับโดย RBAC ที่ชั้น API บวกการ gate ขั้น workflow ผ่าน `tb_store_requisition.user_action.execute` ชื่อ role สะท้อนตาราง RBAC ของ carmen/docs (Requester / Approver / Fulfiller / Receiver / Inventory Controller / Finance Manager / System Administrator) แต่ **รอบนี้พบว่าโมดูล implement เพียงสาม stage-facing actor ในโค้ด: Requester (ขั้น `create`) และผู้ถือขั้น `approve` หรือ `issue`** (`enum_stage_role`) — ทั้งสองอย่างหลังกระทำผ่าน approve action ทั่วไปเดียวกัน (`useApproveStoreRequisition` และ `useIssueStoreRequisition` ทั้งคู่เรียก `PATCH .../approve`; ต่างกันแค่ tag `stage_role` ใน request body) ไม่พบการตรวจ segregation-of-duties ใด ๆ (Requester ≠ Approver, Approver ≠ Fulfiller) ใน `store-requisition.service.ts` หรือ `store-requisition.logic.ts` — ดู `SR_AUTH_011`/`SR_AUTH_012` ด้านล่าง

| Rule ID | ผู้กระทำ | สิทธิ์ | ข้อจำกัด |
| ------- | -------- | ------ | --------- |
| `SR_AUTH_001` | Requester (Outlet Manager) | สร้าง SR (`doc_status = draft`) | Requester ต้องเป็นสมาชิก `department_id`; `from_location_id` และ `to_location_id` ต้องเป็นสถานที่ที่ requester อนุญาตให้ทำธุรกรรมระหว่างกันได้ |
| `SR_AUTH_002` | Requester | แก้ SR, เพิ่ม / แก้ / ลบบรรทัด | ขณะ `doc_status = draft` และยังไม่ submit เท่านั้น เมื่อถึง `in_progress` แล้ว เอกสารไม่อยู่ในมือ requester อีกต่อไป |
| `SR_AUTH_003` | Requester | Submit (`draft → in_progress`) | ผ่าน validation ในส่วนที่ 2 ตั้งแต่ `SR_VAL_001`–`SR_VAL_009` ตอน submit workflow engine จัดเส้นทางเอกสารไปยังขั้นอนุมัติแรกและบรรจุ `user_action.execute` จาก permitted users ของขั้นนั้น |
| `SR_AUTH_004` | Requester (เจ้าของเอกสาร) | ลบ draft ของตน (เดี่ยวหรือหลายรายการ) | Soft-delete (`StoreRequisitionService.delete()`) จำกัดเฉพาะ `doc_status = draft` (`Only draft store requisitions can be deleted`) **และตั้งแต่ `215927c0a` (2026-07-29) จำกัดเฉพาะเจ้าของเอกสาร** (`isDocumentOwner()`) โดย platform super-admin ข้ามได้ — มิฉะนั้น `SR_DELETE_FORBIDDEN` (403) `DELETE /api/{bu}/store-requisitions/batch` (`{ ids: [] }`, `5278bdb46`) validate ทุก id ก่อนและไม่ลบเลยหากมี id ใดเป็น `not_found` / `not_draft` / `not_owner` โดยคืนรายการที่ถูกบล็อก (`SR_BATCH_DELETE_BLOCKED`, 400) ไม่มีการถอนแบบ `in_progress → cancelled` — `cancelled` ไม่เคยถูก assign (ดู [01-data-model.md](/th/inventory/store-requisition/01-data-model) §5 ข้อ 11) ปุ่มลบในหน้า list ถูก gate ด้วย permission ฝั่ง client (`65751027`) |
| `SR_AUTH_005` | Approver (Department Head) | อนุมัติ / ตัด / reject บรรทัด, ส่งกลับเพื่อแก้ไข | ขณะ `doc_status = in_progress` และ `workflow_current_stage` เป็นขั้นที่ approver อยู่ใน `user_action.execute` เท่านั้น อาจตั้ง `approved_qty` (รับตัวเลขใดก็ได้ — `SR_VAL_010` ไม่ถูกบังคับใช้), ทำเครื่องหมายบรรทัดเป็น `reject` (รวมอยู่ใน `PATCH .../approve` call เดียวกันกับการตัดสินใจของบรรทัดอื่น; ข้อความเป็น optional) หรือส่งเอกสารทั้งใบกลับ (`PATCH .../review` ต้องระบุขั้นปลายทาง `des_stage` ข้อความเป็น optional) ข้อกล่าวอ้าง "Approver ต้องไม่ใช่ผู้ใช้คนเดียวกับ requester" (`SR_AUTH_011`) ยังไม่ยืนยัน — ดูด้านล่าง |
| `SR_AUTH_006` | Approver | การตัดสินใจแบบผสมต่อบรรทัดใน `/approve` call เดียว | ยืนยันแล้วใน `computeSrAction()` ของ `sr-form-schema.ts`: หนึ่ง submission อาจผสม `approve` และ `reject` ต่อบรรทัด (ทั้งคู่ route ผ่าน `/approve`) ตราบใดที่**ไม่มี**บรรทัดใดตั้งเป็น `review` — ถ้ามีบรรทัดใดเป็น `review` ทั้ง submission จะกลายเป็น send-back (`/review`) และการเลือกของบรรทัดอื่นจะไม่ถูก apply ใน call นั้น นี่แคบกว่า "split & reject ที่ระดับ SR" — approve/reject ผสมกันได้ แต่ review ผสมกับอย่างใดอย่างหนึ่งไม่ได้ใน submission เดียวกัน |
| `SR_AUTH_007` | Fulfiller / ผู้ถือขั้นที่ tag `issue` | บันทึก `issued_qty`, เดินไปยัง `completed` | ขณะ `doc_status = in_progress` และ `workflow_current_stage` เป็นขั้นที่ tag `enum_stage_role.issue` ที่ user อยู่ใน `user_action.execute` เท่านั้น `issued_qty ≤ approved_qty` ต่อบรรทัด (`SR_VAL_008`) ไม่มี endpoint "commit" แยก — การบันทึก `issued_qty` และการปิดเอกสารกระทำผ่าน `PATCH .../approve` call เดียวกันที่ Approver ใช้ (`stage_role: "issue"`, `issue_date_pattern` เป็น optional); เอกสารกลายเป็น `completed` เมื่อ `workflow_next_stage` ที่ได้จาก call นั้นเป็น `'-'` และส่วนหัวได้รับ `issue_at` / `issue_by_id` การบันทึกที่ขั้น issue (`PUT .../store-requisitions/:id` พร้อม `stage_role: "issue"`, `issue_at` เป็น optional) ก็ stamp สองคอลัมน์นี้โดยไม่ complete การเลือก lot เป็น FIFO ที่คำนวณโดยระบบ ไม่ใช่ action ของ Fulfiller (ดู [01-data-model.md](/th/inventory/store-requisition/01-data-model) §5 ข้อ 12) ข้อกล่าวอ้าง "Fulfiller ต้องไม่ใช่ผู้ใช้คนเดียวกับ approver" (`SR_AUTH_012`) ยังไม่ยืนยัน — ดูด้านล่าง |
| `SR_AUTH_008` | "Receiver" (ผู้แทนเอาท์เลตปลายทาง) | — | **ยังไม่ยืนยันว่าเป็น role แยก** ไม่พบ route, permission key, comment-escalation flag หรือกลไกความคลาดเคลื่อนเฉพาะของ receiver ใน frontend หรือ backend ตาราง comment (`tb_store_requisition_comment` / `tb_store_requisition_detail_comment`) เป็นแบบทั่วไปและใช้ได้กับผู้ใช้ที่มีสิทธิ์อ่าน SR คนใดก็ได้ — ไม่ใช่ surface การเขียนที่สงวนไว้เฉพาะ receiver ดู [03-user-flow-receiver.md](/th/inventory/store-requisition/03-user-flow-receiver) สำหรับการแก้ไขฉบับเต็ม |
| `SR_AUTH_009` | Inventory Controller | Variance review, void เชิงบริหาร | **ยังไม่ยืนยันบางส่วน** สิทธิ์อ่านทุกสถานะเป็นไปได้ในฐานะ RBAC read grant ทั่วไป แต่ไม่พบ permission key เฉพาะ `inventory-controller` หรือ route variance-dashboard "Void" สำหรับโมดูลนี้คือ whole-document `/reject` action เดียวกับที่ผู้ถือขั้นปัจจุบันคนใดก็เรียกได้ (ดู `SR_AUTH_013`) ไม่ใช่เส้นทาง admin ที่สงวนเฉพาะ Inventory Controller |
| `SR_AUTH_010` | "Finance Manager" | ตรวจสอบการ map cost-centre / journal-entry, period close | **ยังไม่ยืนยันว่าเป็น role แยก** และกลไก journal-entry / GL-posting ที่ role นี้ควรตรวจสอบก็ไม่พบในโค้ด (`journal`, `ledger` — ศูนย์รายการทั่ว repo สำหรับโมดูลนี้) `enum_stage_role` ไม่มีสมาชิก `finance` |
| `SR_AUTH_011` | Segregation of duties — "Requester ≠ Approver" | — | **ยังไม่ยืนยัน** การค้นหาทั่ว repo ใน `store-requisition.service.ts` และ `store-requisition.logic.ts` สำหรับการ cross-check `requestor_id` ที่ approve action ไม่พบเลย อย่านำเสนอสิ่งนี้ว่าเป็น control ที่บังคับใช้จริง จนกว่าจะพบการตรวจสอบ |
| `SR_AUTH_012` | Segregation of duties — "Approver ≠ Fulfiller" | — | **ยังไม่ยืนยัน** ด้วยเหตุผลเดียวกับ `SR_AUTH_011` — ไม่พบโค้ด cross-check และไม่มี call "commit" แยกให้ตรวจ (ดู `SR_AUTH_007`) ข้อกล่าวอ้าง "tenant config อาจผ่อนคลายสำหรับ SR มูลค่าต่ำ" ก็ยังไม่ยืนยันเช่นกัน — ไม่พบคำว่า `threshold` ที่ใดในโมดูลนี้ |
| `SR_AUTH_013` | ผู้ถือขั้น workflow ปัจจุบัน | Whole-document reject (`in_progress → voided`) | **แก้ไขในรอบนี้** `StoreRequisitionService.reject()` ตั้ง `doc_status = voided` แบบไม่มีเงื่อนไขสำหรับ whole-document reject call ใด ๆ — ไม่ได้จำกัดเฉพาะ Inventory Controller / System Administrator และเป็นชุด actor เดียวกับ `SR_AUTH_005`/`SR_AUTH_007` (ผู้อยู่ใน `user_action.execute` สำหรับขั้นปัจจุบัน) SR ที่ `completed` ไม่สามารถ void ได้ (service method `reject` ต้องการ `doc_status = in_progress`) — การแก้ไขหลัง commit ใช้ `[inventory-adjustment](/th/inventory/inventory-adjustment)` `cancelled` ประกาศไว้ใน enum แต่ไม่มี SR mutation ปัจจุบันใดเข้าถึงได้ |
| `SR_AUTH_014` | Authorization derive จาก workflow | Action ที่ gate ตามขั้น | ชุดผู้ใช้ใน `tb_store_requisition.user_action.execute` ที่ `workflow_current_stage` ปัจจุบันคือชุดเดียวที่อนุญาตให้เดินเอกสารต่อ; การพยายาม action อื่นถูก reject ด้วย `"You are not assigned to act on this requisition at the current stage."` |

## 5. กฎ Posting

ค่าสถานะคือสมาชิก literal ของ `enum_doc_status` ที่ใช้ร่วมตามที่ระบุใน [store-requisition/01-data-model](/th/inventory/store-requisition/01-data-model) § 4: **`draft`**, **`in_progress`**, **`completed`**, **`cancelled`**, **`voided`** วงจรชีวิตจริงจึงเป็น `draft → in_progress → completed` โดยมี `voided` เป็นเส้นทางยกเลิกที่จุดสิ้นสุดเดียวที่ยืนยันได้จาก `in_progress` (whole-document reject action — `SR_AUTH_013`) `cancelled` ประกาศไว้ใน enum แต่ **ไม่มี SR service method ใดในปัจจุบันเข้าถึงได้** — ดู [01-data-model.md](/th/inventory/store-requisition/01-data-model) §5 ข้อ 11 Posting event ครั้งเดียวคือ final workflow-stage advance (`/approve` call ทั่วไปเดียวกันที่ทุกขั้นใช้); ไม่มีอะไร post ที่ `draft` หรือที่ขั้นใด ๆ ของ `in_progress` ที่ไม่ใช่ขั้นสุดท้าย ไม่มีค่า `submitted`, `approved`, `partially_approved`, `fulfilled` หรือ `rejected` ที่ระดับ Prisma — สิ่งเหล่านั้นคือ label ของขั้น workflow ภายใต้ `in_progress` ไม่ใช่สถานะส่วนหัว (โมเดล 6 สถานะแบบเก่าของ carmen/docs ต่างออกไป — ดู [store-requisition/01-data-model](/th/inventory/store-requisition/01-data-model) § 5 ข้อ 1)

Rule ID ใช้ `SR_POST_NNN`

| Rule ID | Transition / Event | ผลกระทบ |
| ------- | ------------------ | ------- |
| `SR_POST_001` | Create (→ `draft`) | Insert `tb_store_requisition` ด้วย `doc_status = draft`, `doc_version = 0`, `sr_no` กำหนดตามนโยบายเลขของ tenant Append `workflow_history`: `{ stage: 'draft', action: 'created', by, at }` ไม่กระทบ inventory, GL, reservation |
| `SR_POST_002` | Submit (`draft → in_progress` หรือ resubmit จาก `in_progress` + `last_action = reviewed`) | Guard: `SR_VAL_005` (derive แผนกหากขาด), `ValidateSRBeforeSubmitSchema`, `SR_VAL_015` (สินค้าเปิดใช้ที่ปลายทาง), `SR_VAL_014` (วันที่) ในการ submit ครั้งแรก: `sr_date` ถูก freeze โดย `resolveSubmitSrDate()`, `sr_no` ถูกออกโดย `generateSRNo()`; ในการ resubmit ทั้งสองค่าคงเดิม ตั้ง `doc_status = in_progress`, คอลัมน์ workflow ของส่วนหัวจาก `buildSubmitWorkflow()`, `doc_version + 1` และบน **ทุก** บรรทัด `approved_qty := requested_qty`, `last_action = submitted`, append `stages_status` / `history` / `current_stage_status` ไม่มี reservation ใด ๆ และไม่มี inventory write มีการส่ง submit notification (`sendSubmitNotification`) |
| `SR_POST_003` | Approve บรรทัด (ภายใน `in_progress`) | ใน approve action ต่อบรรทัด: ตั้ง `approved_qty`, `approved_by_id`, `approved_by_name`, `approved_date_at`, `approved_message` Append `history` ต่อบรรทัด เมื่อบรรทัดทั้งหมดบนขั้นปัจจุบันถูก action แล้ว เดิน `workflow_current_stage` ไปขั้นถัดไป (ระดับอนุมัติถัดไป หรือขั้น fulfilment ถ้าอนุมัติครบ); refresh `user_action.execute` `doc_status` ส่วนหัวยังคงเป็น `in_progress` ตลอด |
| `SR_POST_004` | Reject บรรทัด (ภายใน `/approve` call) / send back (`/review`) | Reject ต่อบรรทัด (รวมอยู่ใน `/approve` call เดียวกันกับการตัดสินใจ approve ของบรรทัดอื่น): ตั้ง `approved_qty = 0`, `reject_by_id`, `reject_by_name`, `reject_date_at`, `reject_message` Send-back (`/review`, ผสมกับ approve/reject ใน call เดียวกันไม่ได้ — `SR_AUTH_006`): ตั้ง `review_by_id`, `review_by_name`, `review_date_at`, `review_message`; workflow ส่ง SR กลับไปขั้นก่อนหน้า (โดยทั่วไปคือ requester) และเอกสารยังคงเป็น `in_progress` **แก้ไขในรอบนี้:** ไม่มีเส้นทาง "ทุกบรรทัดถูก reject → auto-cancel" ที่ยืนยันได้ — ดู `SR_POST_009` |
| `SR_POST_005` | Final stage advance (`in_progress → completed`) — **posting event** | `PATCH .../approve` call ทั่วไปเดียวกันที่ทุกขั้นใช้; เอกสารกลายเป็น `completed` เมื่อ `workflow_next_stage` ที่ได้เป็น `'-'` (`StoreRequisitionLogic.approve()` / `isFinalApproval`) เมื่อขั้นที่กระทำคือ `issue` `resolveIssueDate()` run ก่อน (`SR_VAL_014`) และวันที่ที่ resolve แล้วถูก stamp เป็น `issue_at` พร้อม `issue_by_id = actor` ตั้ง `doc_status = completed`, `doc_version + 1`, append `workflow_history` จากนั้น cross-module fan-out fire: ดู `SR_POST_006` |
| `SR_POST_006` | Final-stage advance — ฝั่ง inventory (cross-ref [inventory](/th/inventory/inventory)) | ยืนยันแล้วใน `executeTransferOnComplete()` (`store-requisition.logic.ts`): สำหรับแต่ละบรรทัดที่ `issued_qty > 0`, insert แถว `tb_inventory_transaction` (`inventory_doc_type = store_requisition`, `inventory_doc_no = <sr id>`, `doc_date = issue_at`) ผ่าน `InventoryTransactionService.executeTransfer()` — period ถูก resolve จาก `doc_date` (`resolveDocumentPeriod`) ไม่ใช่จาก "ตอนนี้" สังเกตลำดับ: การ update ส่วนหัวใน `StoreRequisitionService.approve()` commit *ก่อน* fan-out นี้ ดังนั้นการ throw `Insufficient stock` ตรงนี้ทิ้ง SR ไว้ในสถานะ `completed` โดยไม่มี movement (`SR_VAL_013`) `executeTransfer()` fan out ไปยัง children `tb_inventory_transaction_detail` ที่บรรจุ `lot_no`, `expiry_date`, `cost_per_unit` Stamp id ที่ insert บน `tb_store_requisition_detail.inventory_transaction_id` **สำหรับ `sr_type = issue` เข้าปลายทาง `direct`**: ต้นทาง deplete และ transfer-in ถูก expense ทันที ดังนั้น on-hand ปลายทางคงที่ 0 (เป็นการบริโภค ไม่ใช่การย้ายสต๊อกที่ถือครอง — ดู inline comment ของ `executeTransfer`) **สำหรับ `sr_type = transfer`**: on-hand ต้นทางลด, on-hand ปลายทางเพิ่มในปริมาณเดียวกัน การ consume cost-layer ที่ต้นทางใช้ FIFO หรือ moving-average ตามวิธีคำนวณของ tenant (`getCalculationMethod`) การเลือก lot เป็น FIFO ที่คำนวณโดยระบบ (`getAvailableFifoLots` / `consumeFifoLots`) ไม่ใช่การเลือกด้วยมือ |
| `SR_POST_007` | การ post GL / journal | **ยังไม่ต่อสาย (ตรวจซ้ำ 2026-09-22)** ตอนนี้มี GL core แล้ว (`apps/micro-business/src/gl/{gl-posting,gl-jv,gl-period,…}`, migrations `20260909170000_gl_core_master`, `20260914030000_gl_core_jv_ledger`) แต่ `grep -rn "store_requisition\|inventory_doc_type" apps/micro-business/src/gl/` ได้ศูนย์ผลลัพธ์ — ไม่มีสิ่งใดใน commit path ของ SR ที่สร้าง journal ให้ถือผลทาง GL ของ SR เป็น design intent เท่านั้น |
| `SR_POST_008` | Final-stage advance — feed variance | `requested_qty − issued_qty` และ `approved_qty − issued_qty` ต่อบรรทัดคำนวณได้จากสามคอลัมน์ปริมาณที่เก็บไว้ ไม่พบ route variance-dashboard เฉพาะหรือ scheduled job ในรอบนี้; ให้ถือว่า "feed outlet variance reporting" เป็น metric ที่คำนวณได้ ไม่ใช่หน้าจอที่ยืนยันแล้วว่าใช้งานจริง |
| `SR_POST_009` | ~~Cancel~~ — **ไม่มี code path ใดเข้าถึงได้ในซอร์สปัจจุบัน** | เวอร์ชันก่อนหน้าของหน้านี้บรรยาย `draft/in_progress → cancelled` ว่า trigger โดย requester ถอน, เส้นทางอัตโนมัติ "ทุกบรรทัดถูก reject" หรือ "reject SR" โดยผู้อนุมัติที่ได้รับอนุญาต ไม่พบสิ่งเหล่านี้เลย: `cancelled` ไม่เคยถูก assign โดย method ใดใน `store-requisition.service.ts` กลไกถอนก่อน submit เดียวที่ยืนยันได้คือ soft-delete แบบ `draft` เท่านั้น (`SR_POST_011`); กลไก "reject SR ทั้งฉบับ" เดียวที่ยืนยันได้ตั้งเป็น `voided` ไม่ใช่ `cancelled` (`SR_POST_010`) |
| `SR_POST_010` | Whole-document reject (`in_progress → voided`) | `StoreRequisitionService.reject()`: ตั้ง `doc_status = voided` แบบไม่มีเงื่อนไข, `last_action = rejected` บนทุกบรรทัดใน payload **แก้ไขในรอบนี้:** นี่คือ reject action ทั่วไปเดียวกันที่ผู้ถือขั้น workflow ปัจจุบันคนใดก็ใช้ได้ — ไม่ได้จำกัดเฉพาะ Inventory Controller / System Administrator และไม่มี endpoint "administrative void" แยก ต้องการ `doc_status = in_progress` (SR ที่ `completed` ไม่สามารถ reject/void ได้) — การแก้ไขหลัง commit ใช้ `[inventory-adjustment](/th/inventory/inventory-adjustment)` ไม่กระทบ inventory หรือ GL (เอกสารไม่เคย post) จุดสิ้นสุด |
| `SR_POST_011` | Soft delete (เดี่ยวหรือ batch) | `deleted_at = now()`, `deleted_by_id = user` บนส่วนหัวและบรรทัดที่ยังไม่ถูกลบ (บรรทัดที่ key ซ้ำได้ timestamp เหลื่อมกัน, `9f5ec1bd3`) จำกัดเฉพาะ `doc_status = draft` และเจ้าของ / platform super-admin (`SR_AUTH_004`) batch endpoint เป็นแบบ all-or-nothing และรายงาน `{ id, reason, message }` ต่อรายการที่ถูกบล็อก แถวยังคงในฐานข้อมูล; index `@@unique([sr_no, deleted_at])` ให้ SR ใหม่ reuse `sr_no` เดียวกันได้ นี่คือกลไกจริงเบื้องหลัง "requester ถอน draft ของตน" — ไม่มี action ถอนหลัง submit |
| `SR_POST_012` | Short fulfillment ตอน stock-out ที่ issue | เมื่อ on-hand live ที่ขั้นสุดท้ายน้อยกว่า `approved_qty` ผู้กระทำอาจลด `issued_qty` เป็น `min(approved_qty, on_hand)` และบันทึกเป็น partial SR ยังคงย้ายไป `completed` พร้อม partial ที่บันทึกไว้ — ถ้อยคำ system-comment เฉพาะต่อบรรทัดและทางเลือก "ยกเลิกบรรทัด" ที่เวอร์ชันก่อนหน้าของหน้านี้บรรยายไว้ไม่ได้ถูกยืนยันโดยตรงกับ frontend/backend และควรถือว่าเป็นตัวอย่างประกอบ ไม่ใช่ข้อความที่ยืนยันแล้ว |
| `SR_POST_013` | "Receiver discrepancy flag" | **ยังไม่ยืนยันว่าเป็นกลไกแยก** ไม่พบ comment type, discrepancy flag หรือ escalation route เฉพาะของ receiver ผู้ใช้ที่มีสิทธิ์อ่าน SR ที่ `completed` คนใดก็สามารถเพิ่ม `user` comment แบบทั่วไปผ่านตาราง comment ที่มีอยู่ได้ — นั่นคือ surface การ annotate หลัง commit เดียวที่ยืนยันได้ ดู [03-user-flow-receiver.md](/th/inventory/store-requisition/03-user-flow-receiver) |
| `SR_POST_014` | Read model — แท็บ Stock Movement (`GET /api/{bu}/store-requisitions/:id/stock-movements`, `c5aeca45b`, 2026-08-27) | ไม่ใช่กฎ posting แต่เป็นผลที่สังเกตได้ของ `SR_POST_006`: หนึ่งแถวต่อ `tb_inventory_transaction_detail` (location, product, `lot_no` จาก `current_lot_no`/`from_lot_no`, `qty_in` / `qty_out`, `cost_per_unit`, `total_cost`) บวกยอดรวม `summary` จนกว่า `doc_status = completed` และมี transaction อยู่ `is_posted = false` และแถวเป็น preview ที่สร้างจากบรรทัด SR (`source = store_requisition_detail`, lot `-`, cost `0`) frontend render ใน `sr-stock-table.tsx` (`useSrStockMovements`) |

State diagram (ยืนยันแล้วกับ `store-requisition.service.ts` ในรอบนี้):

```
[*] → draft → in_progress → completed
  |               |
  (soft-delete,   voided (whole-document reject,
   draft only)     any current-stage actor)
```

`cancelled` ประกาศไว้ใน enum แต่ไม่มี service method ใดในปัจจุบันเข้าถึงได้ — ตัดออกจาก diagram `completed` และ `voided` เป็นจุดสิ้นสุด `draft` ยอมรับ soft-delete (เจ้าของเท่านั้น) การ send-back ทิ้งเอกสารไว้ที่ `in_progress` พร้อม `last_action = reviewed`; `submit()` รับสถานะนั้นเป็น resubmit (คง `sr_no` และ `sr_date` ไว้)

### 5.1 วงจรชีวิตสถานะ — การ Map ระหว่าง Live UI กับ BRD

`enum_doc_status` ของ Prisma ที่ระบุข้างบนคือสิ่งที่ live UI ใช้ BRD (`BR-store-requisitions v1.5.0`) และ `SR-User-Experience.md` อธิบายวงจรชีวิตหกสถานะ ตารางด้านล่าง map ทุกสถานะที่สังเกตได้ใน live UI ไปยังคู่ใน BRD เพื่อให้ผู้ทดสอบและนักพัฒนาสามารถ reconcile ทั้งสองได้โดยไม่กำกวม Source: `Test_case/System_Process/tx-03-sr.md` (capture date 2026-04-27)

> Diff legend: ✅ match · 🟡 renamed · 🔵 BRD only

| Live UI status | BRD equivalent | Diff | หมายเหตุ |
|---|---|---|---|
| `draft` | `Draft` | ✅ match | สถานะเริ่มต้นที่แก้ไขได้; requester ป้อนข้อมูลบรรทัด |
| `in_progress` | `Submitted`, `UnderReview`, `Approved`, `PartiallyApproved`, `InProcess`, `Fulfilled` | 🟡 renamed | โมเดล BRD 6 สถานะยุบเป็นสถานะ Prisma เดียว; sub-stage จริงติดตามผ่าน `workflow_current_stage` Stage flow ตาม `BR-SR-014`: Draft → Submit → Approve → Issue → Complete |
| `completed` | `Completed` / `Fulfilled` | 🟡 renamed | Posting event ครั้งเดียว (`in_progress → completed`) On-hand ต้นทางลด; cost-layer consume; inventory transactions เขียน |
| `voided` | `Rejected` / `Reject` / `Voided` / `Void` | 🟡 renamed | **แก้ไขในรอบนี้:** ผลลัพธ์ `Rejected`/`Reject` และ `Voided`/`Void` ของ BRD ทั้งคู่ map ลงบนค่า `voided` ค่าเดียวของ Prisma — `StoreRequisitionService.reject()` ตั้ง `voided` โดยตรง และไม่มี action "Void" ที่สงวนเฉพาะ admin หรือผลลัพธ์ `cancelled` แยกในโค้ดปัจจุบัน (ดู [01-data-model.md](/th/inventory/store-requisition/01-data-model) §5 ข้อ 11) |
| — | `PartiallyApproved` | 🔵 BRD only | ไม่ใช่สถานะ Prisma แยก; แทนด้วยผลการอนุมัติต่อบรรทัดที่ผสมขณะ `doc_status = in_progress` |
| — | `cancelled` (ค่า enum ของ Prisma) | 🔵 enum only | ประกาศไว้ใน `enum_doc_status` แต่ไม่ถูก assign โดย SR service method ใดในปัจจุบัน — ตายในโค้ดปัจจุบัน ไม่ใช่ความต่างระหว่าง BRD กับ live ในความหมายปกติ |

> ⚠️ **ความไม่ตรงกัน — ลักษณะ 3 variant ไม่สะท้อนใน `enum_doc_status`:** Live UI รองรับ destination variant สามแบบ (INV → INV / INV → DIR / INV → CONS) ที่ให้ผลด้าน inventory ต่างกัน (on-hand ปลายทางเพิ่มสำหรับ INV → INV; คงเป็น 0 สำหรับอีกสองแบบ — ผลด้าน GL ยังไม่ยืนยัน ดู `SR_POST_007`) แต่ทั้งสามใช้เส้นสถานะ `draft → in_progress → completed` เหมือนกัน enum `sr_type` (`issue` / `transfer`) แยก variant สองแบบใน live (DIR และ CONS ทั้งคู่เป็น `sr_type = issue`; INV → INV เป็น `sr_type = transfer`) BRD `BR-store-requisitions v1.5.0` และ `Test_case/System_Process/tx-03-sr.md` อธิบายสาม variant; ระบบ live ยุบ DIR และ CONS ภายใต้ค่า `sr_type = issue` เดียวโดยแยกด้วย `tb_location.location_type` ของปลายทางเท่านั้น Source: `Test_case/System_Process/tx-03-sr.md` (capture date 2026-04-27)

> ⚠️ **ความไม่ตรงกัน — view Stock Transfer vs transaction code TRF แยก:** BRD `BR-period-end v2.0.0` ระบุ `TRF` เป็น transaction code คู่กันกับ `SR` ใน Stage 1 ของ period-close validation gate การ implement (`BR-stock-transfers.md`) ยืนยันว่า Stock Transfers **ไม่ใช่** เอนทิตีแยก — เป็น SR ที่มีปลายทาง INVENTORY (`sr_type = transfer`) สำหรับ period-close Stage 1 validation, SR records ที่มี INV → INV destinations satisfy ทั้ง bucket `SR` และ `TRF` หน้า Stock Transfer เป็น view กรองอ่านอย่างเดียวของ SR records ไม่ใช่เอกสารแยก Source: `Test_case/System_Process/tx-03-sr.md` (capture date 2026-04-27)

> ℹ️ **หมายเหตุ — ไม่มีกลไก completion แยกต่อ variant (แก้ไขรอบนี้):** เอกสาร BRD อธิบาย `sr_type = transfer` ว่าเป็น "Issue = Complete auto-collapse" และ `sr_type = issue` ว่าต้องมี action Complete แยกอย่างชัดแจ้ง source ปัจจุบันไม่พบความแตกต่างเช่นนั้น: `sr_type` **ทั้งสองค่า** complete ผ่านการเรียก `/approve` เดียวกันเมื่อยิงที่ขั้น workflow สุดท้าย (`workflow_next_stage === '-'`, `StoreRequisitionLogic.approve()`); ไม่มี action "Complete" แยกสำหรับ variant ใด และไม่มีขั้นยืนยัน receiver สำหรับ variant ใดทั้งสิ้น ความแตกต่างเชิงพฤติกรรมเดียวระหว่าง variant อยู่ภายใน `executeTransfer()` (on-hand ปลายทางเพิ่มสำหรับ `transfer`; transfer-in ถูกลง expense ทันทีสำหรับ `issue` เข้าปลายทาง `direct`)

## 6. กฎข้ามโมดูล

Rule ID ใช้ `SR_XMOD_NNN`

| Rule ID | โมดูลที่เกี่ยวข้อง | กฎ |
| ------- | ------------------ | -- |
| `SR_XMOD_001` | [inventory](/th/inventory/inventory) | On-hand inventory ลดที่ต้นทาง **เฉพาะตอน SR commit** (`SR_POST_006`) — ไม่ใช่ตอน submit ไม่ใช่ตอน approve การลดผ่าน insert ลง `tb_inventory_transaction` / `tb_inventory_transaction_detail` เข้าถึงจากฝั่ง SR ผ่าน `tb_store_requisition_detail.inventory_transaction_id` สำหรับ `sr_type = transfer` on-hand ปลายทางเพิ่มใน atomic transaction เดียวกัน; สำหรับ `sr_type = issue` ไม่มีการเพิ่ม on-hand ปลายทาง (มูลค่าลงเป็นค่าใช้จ่ายการบริโภค ไม่ใช่สต๊อกที่ถือ) |
| `SR_XMOD_002` | [inventory](/th/inventory/inventory) | เลข lot, วันหมดอายุ และปริมาณต่อ lot สำหรับบรรทัดที่ issue อยู่บน `tb_inventory_transaction_detail` (และ `tb_inventory_transaction_cost_layer.lot_no`) **ไม่ใช่** บนบรรทัด SR บรรทัด SR เข้าถึงข้อมูล lot ผ่านลิงก์ `inventory_transaction_id` **แก้ไขในรอบนี้:** การเลือก lot เป็น FIFO ที่คำนวณโดยระบบ (`getAvailableFifoLots` / `consumeFifoLots`) ที่ final workflow-stage advance — ไม่มี UI เลือก lot ใน SR frontend ดังนั้น fulfiller ไม่ได้เลือก lot ด้วยมือ (PRD `BR-Lot-01` บรรยายการเลือกด้วยมือ; ยังไม่ยืนยันในโค้ดปัจจุบัน) |
| `SR_XMOD_003` | [inventory](/th/inventory/inventory) | ค่า `enum_inventory_doc_type` ที่ stamp บน inventory transaction ที่ลิงก์คือ `store_requisition`; query inventory ปลายน้ำที่ filter ตามประเภทเอกสารดึง movements ที่ขับโดย SR ผ่าน stamp นี้ stamp เดียวกันคือสิ่งที่ `[inventory-adjustment](/th/inventory/inventory-adjustment)` อ่านตอน reverse error หลัง commit |
| `SR_XMOD_004` | [costing](/th/inventory/costing) | Valuation ตอน commit ตามวิธี costing ของ **สถานที่ต้นทาง** (FIFO หรือ moving-average ตาม config ต่อ-สถานที่ของ tenant) — ไม่ใช่ของปลายทาง `SR_CALC_004` อ่าน `cost_per_unit` จากตาราง cost-layer ของต้นทางตอน issue FIFO consumption เลือก layer ที่ active เก่าสุด; moving-average ใช้ weighted-average cost ปัจจุบันของสถานที่ โมดูล costing เป็นเจ้าของการคำนวณ; โมดูล SR รับผิดชอบเฉพาะการ trigger การ consume |
| `SR_XMOD_005` | [costing](/th/inventory/costing) | ที่ปลายทาง value-receipt accounting ขึ้นกับ `sr_type`: `transfer` ลงในบัญชี inventory ปลายทางที่ `cost_per_unit` เดียวกัน (cost ย้ายระหว่างบัญชี inventory แบบครบ); `issue` ลงเป็น expense บน cost-centre ปลายทางที่ `cost_per_unit` เดียวกัน (cost ย้ายจาก inventory ไป expense) Cost-per-unit ที่ปลายทางหลัง `transfer` เข้าสู่ cost layers ของปลายทางตามวิธี costing ของตน — โดยทั่วไป append เป็น FIFO layer ใหม่หรือถูก absorb เข้า moving-average |
| `SR_XMOD_006` | [recipe](/th/inventory/recipe) | **ยังไม่ยืนยัน — ไม่พบ code path (2026-09-22)** `grep -rln "tb_store_requisition\|store_requisition" apps/micro-business/src/` นอกโฟลเดอร์ SR, stock-replenishment, workflow และ my-pending ไม่คืนสิ่งใดที่เกี่ยวกับ recipe; ไม่มี service ใดเขียน `info.recipe_id` ให้ถือ "recipe สร้าง SR อัตโนมัติ" เป็น design intent การสร้าง SR อัตโนมัติเดียวที่ยืนยันได้คือ stock replenishment (`SR_XMOD_011`) |
| `SR_XMOD_007` | [good-receive-note](/th/inventory/good-receive-note) | การโอนระหว่างคลังอาจจับคู่ SR-OUT ที่คลังต้นทางกับ GRN-IN ที่คลังปลายทาง — SR บันทึก issue จากมุมมองต้นทาง GRN บันทึกการรับจากมุมมองปลายทาง ทั้งคู่อ้างอิงตระกูล `tb_inventory_transaction` เดียวกันแต่จากฝั่งตรงข้าม; การ reconcile ระหว่างทั้งสองเป็นความรับผิดชอบของ finance / inventory-controller `transfer` SR ขั้นตอนเดียว (`sr_type = transfer`) บันทึกทั้งสองขาบนเอกสารเดียวและไม่ต้องการ GRN คู่; รูปแบบ paired-GRN สงวนสำหรับกรณีที่ปลายทางต้องการยืนยันการรับด้วยเอกสารของตน (โดยทั่วไปเมื่อปลายทางเป็นนิติบุคคลต่างกันหรือ remote facility) |
| `SR_XMOD_008` | Approval workflow | Approval routing — จำนวนขั้นและ role ที่แต่ละขั้น — เป็นเจ้าของโดย `tb_workflow` (อ้างจากส่วนหัว SR ผ่าน `workflow_id` ด้วย Prisma `@relation` ที่ระบุชัด) โมดูล SR รับผิดชอบการปรึกษา workflow ที่แต่ละ transition และบรรจุ `user_action.execute` จากขั้นปัจจุบัน; ไม่รับผิดชอบกฎ routing เอง **แก้ไขในรอบนี้:** stage routing ที่ขับด้วยมูลค่ามีจริง ไม่ใช่ยังไม่ยืนยัน `tb_workflow.data.routing_rules` สามารถ route ตาม `total_amount` ได้ — คำนวณโดย `sr-workflow.mapper.ts` เป็นผลรวม (`approved_qty` หรือ `requested_qty`) `× current_average_cost` ต่อบรรทัด เนื่องจากบรรทัด detail ของ SR ไม่มี column ราคาของตัวเอง — หรือตาม `department` / `category` ประเมินโดย `evaluateCondition`/`findNextStep` ใน `workflows.navagation.service.ts` ที่ใช้ร่วมกัน ทุกครั้งที่ submit/approve นี่คือกลไกทั่วไปแบบเดียวกับที่ documented ไว้สำหรับ PR และ PO (แท็บ **Routing** ของ `/system-admin/workflow`) ไม่ใช่เฉพาะของ SR ข้อค้นพบ "ผลศูนย์รายการ" ก่อนหน้าค้นหาเฉพาะคำตรงตัว `threshold`; คำศัพท์ที่ใช้งานจริงคือ `routing_rules` / `total_amount` กฎที่อิง `category` จะไม่ match เลยอย่างเงียบ ๆ (ไม่มี mapper ใดส่ง field `category` ระดับเอกสาร — source comment, `workflows.navagation.service.ts` ~บรรทัด 455-456) **การ delegate การอนุมัติยังไม่ยืนยัน:** การค้นหาทั่ว repo สำหรับ `delegat` ต่อโมดูลนี้และ workflow orchestrator ได้ผลศูนย์รายการ ให้ถือว่าเป็น design intent ไม่ใช่พฤติกรรมที่ยืนยันแล้ว |
| `SR_XMOD_009` | [inventory-adjustment](/th/inventory/inventory-adjustment) | การแก้ไขหลัง commit — เลือก lot ผิด, นับ issue ผิด, ปลายทางรับขาด — ไหลผ่าน inventory-adjustment ไม่ใช่ผ่านการแก้ SR Adjustment มี back-reference ไปยัง `tb_store_requisition.id` ต้นกำเนิดสำหรับ audit SR เองยังคงเป็น `completed` |
| `SR_XMOD_010` | Vendor performance / outlet variance | Feedback variance (`SR_POST_008`) feed รายงาน outlet-level food-cost variance และ supply-discipline การขอเกินเรื้อรัง (gap `requested_qty − approved_qty` ใหญ่), shortfall เรื้อรังตอน issue (`approved_qty − issued_qty`) และรูปแบบการขอตามฤดูกาล surface จาก join ข้ามสามคอลัมน์ปริมาณ ชุด metric เป็นเจ้าของโดยชั้น reporting; โมดูล SR รับผิดชอบเฉพาะการ persist คอลัมน์ที่ feed มัน |
| `SR_XMOD_011` | [stock-replenishment](/th/inventory/store-requisition/stock-replenishment) | `POST /api/{bu}/stock-replenishment/sr` (`57d5715a0`, 2026-08-26) เปลี่ยนแถวที่ต่ำกว่า par เป็น draft SR ผ่าน `StoreRequisitionLogic.create()` ปกติ — หนึ่งเอกสารต่อคู่ `(from_location, location_id)`, ส่วนหัวจากแผนกของผู้เรียกและ workflow ที่เลือก, `requested_qty = request_qty` ต่อสินค้า จากนั้น draft เดินตาม `SR_POST_001` เป็นต้นไปโดยไม่เปลี่ยนแปลง |

## 7. แหล่งอ้างอิง

- `../carmen/docs/store-requisitions/SR-Technical-Specification.md` — Functional requirements `SR_CRT_001`–`SR_CRT_010` (creation), `SR_APR_001`–`SR_APR_008` (approval), `SR_FUL_001`–`SR_FUL_008` (fulfilment), `SR_RPT_001`–`SR_RPT_005` (reporting) Validation rules feed `SR_VAL_*`; การคำนวณ cost feed `SR_CALC_*` (พร้อมข้อสังเกตว่า SR เป็นเอกสารเชิงปริมาณ — ดู Section 3 intro) หมายเหตุ: enum `RequisitionItem.approvalStatus` และฟิลด์ `Requisition.totalAmount` ของ Tech Spec ต่างจาก Prisma; กฎข้างบนใช้ schema canonical
- `../carmen/docs/store-requisitions/SR-Overview.md` — §Business Rules (Creation Rules, Approval Rules, Movement Rules, Financial Rules) map ข้างบนไปยัง `SR_VAL_*`, `SR_AUTH_*`, `SR_POST_*`, `SR_XMOD_*` หมายเหตุ: ส่วนหัว 5 สถานะของ Overview (`Draft / In Process / Complete / Reject / Void`) ยุบเป็น Prisma 5-state `enum_doc_status` (`draft / in_progress / completed / cancelled / voided`)
- `../carmen/docs/store-requisitions/SR-User-Experience.md` — User journeys (Create / Approve / Process), state diagram (6 สถานะ legacy — ไม่ canonical), คำอธิบาย persona Logic branch ของการอนุมัติและรูปแบบ partial-approval / send-back feed `SR_AUTH_005`–`SR_AUTH_006` และ `SR_POST_003`–`SR_POST_004`
- `../carmen/docs/store-requisitions/Store Requisitions.md` — Use cases UC-64 (Approve), UC-65 (Deny), UC-66 (Modify), UC-67 (Monitor), UC-68 (Create and Manage), UC-69 (Approve and Record Stock as Issued) — รวมกันนิยามรูปแบบ interaction ของผู้อนุมัติและ store-keeper ที่ใช้ในตาราง authorization และ posting ข้างบน
- Sibling: `en/store-requisition/01-data-model.md` — โมเดล Prisma canonical, ค่า enum (โดยเฉพาะ `enum_doc_status` 5 ค่าที่ใช้ร่วม และ `enum_sr_type` 2 ค่า) และ catalogue จุดที่ต่างที่ Section 1, Section 3 และ Section 6 พึ่งพา
- Backend rule implementation: `../carmen-turborepo-backend-v2/apps/micro-business/src/inventory/store-requisition/` — `store-requisition.service.ts` (`create`, `submit`, `approve`, `reject`, `delete`, `deleteBatch`, `getStockMovements`, `saveStageRoleDetails`, `generateSRNo`), `logic/store-requisition.logic.ts` (`approve`, `executeTransferOnComplete`, `validateBeforeSubmit`), `logic/sr-date.helper.ts`, `logic/sr-department.helper.ts`, `logic/sr-type.helper.ts`, `dto/store-requisition.dto.ts`, `dto/store-requisition-detail.dto.ts`; gateway `apps/backend-gateway/src/application/store-requisitions/store-requisitions.controller.ts` (workflow verb ทั้งหมดเป็น `PATCH`; SR ไม่มี endpoint `verify` ต่างจาก PR/PO/GRN); error codes `SR_*` ใน `packages/error-catalog/src/catalog.ts` ไม่มี segregation-of-duties guard, ไม่มีการตรวจเพดานปริมาณ และไม่มี journal-entry posting
- Frontend: `../carmen-inventory-frontend-react/routes/store-operation/store-requisition/` — `use-sr.ts` (`useSrAction` → `httpClient.patch`), `use-sr-form-actions.ts` (date-pattern retry), `sr-form-schema.ts` (`computeSrAction`, ขอบเขต qty), `sr-form-dialogs.tsx` (flag `showMessage`), `sr-item-cost-sync.tsx`, `sr-stock-table.tsx`
- Bruno: `../carmen-turborepo-backend-bruno/collections/carmen-inventory/inventory/store-requisition/` — `PATCH-{submit,approve,reject,review}-*`, `DELETE-delete-batch-*`, `GET-get-stock-movements-*`
