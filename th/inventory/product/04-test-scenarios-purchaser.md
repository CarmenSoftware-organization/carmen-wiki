---
title: สินค้า (Product) — Test Scenarios — Purchaser
description: test case ของ Purchaser (happy-path lookup, RBAC scope, validation ด้าน read, comment / feedback, edge case) สำหรับโมดูลสินค้า
published: true
date: 2026-05-17T12:00:00.000Z
tags: product, test-scenarios, purchaser, carmen-software
editor: markdown
dateCreated: 2026-05-15T15:30:00.000Z
---

# สินค้า (Product) — Test Scenarios — Purchaser

> **At a Glance**
> **Persona:** Purchaser (ผู้บริโภคแคตตาล็อก read-only) &nbsp;·&nbsp; **โมดูล:** [[product]] &nbsp;·&nbsp; **scenario:** ~33
> **หมวด:** Happy Path &nbsp;·&nbsp; Permission &nbsp;·&nbsp; Validation &nbsp;·&nbsp; Edge Case
> **การครอบคลุม E2E:** ทางอ้อม — exercise ผ่าน spec โมดูลต้นน้ำ (`300-pr.spec.ts`, `400-po.spec.ts`) ใน `../carmen-inventory-frontend-e2e/`

หน้านี้บันทึก test scenario ที่ persona Purchaser ขับเคลื่อนในโมดูล `product` พวกเขาเป็น **ผู้บริโภค read-only** ของแคตตาล็อก — พวกเขาค้นหา filter ดู และเลือกสินค้าสำหรับการจัดทำ PR / PO; อ้างอิง standard cost, ต้นทุนรับล่าสุด (derive), การแปลงหน่วย และ vendor mapping; และ post comment สำหรับ entry ที่เก่าหรือคำขอสินค้าใหม่ เนื่องจาก persona เป็น lookup-only scenario รวมไปที่ **พฤติกรรม search / picker** (filter, scope, sort), **RBAC ด้าน read** (อะไรที่พวกเขาเห็นและไม่เห็นได้), **กฎ validation ที่พวกเขาพบในฐานะผู้บริโภค** (ความพยายามที่ถูกปฏิเสธในการเลือกสินค้า inactive ความพยายามที่ถูกปฏิเสธในการใช้หน่วยที่ไม่ตั้งค่า) และ **เส้นทาง comment / feedback** ที่ route ความกังวลของพวกเขากลับไปยัง Product Administrator ไม่มี scenario CRUD สำหรับ persona นี้; งานธุรกรรมของพวกเขา (การจัดทำ PR และ PO) อยู่ใน [[purchase-request]] และ [[purchase-order]] handoff ข้าม persona ที่ pivot จาก Purchaser (Scenario 1, 8, 10, 16 ใน parent overview) อยู่ใน [04-test-scenarios.md](./04-test-scenarios.md) ไม่ใช่ที่นี่

## 1. Happy Path

| # | Scenario | Pre-condition | ขั้นตอน | คาดหวัง |
| - | -------- | ------------- | ----- | -------- |
| PR-HP-01 | ค้นหาและเลือกสินค้าบนบรรทัด PR ด้วย code | Purchaser `purchase@blueledgers.com` login; จัดทำ PR ใหม่; สินค้า `COF-001` (Arabica Beans Premium) มีอยู่ active พร้อม order-unit conversion อย่างน้อยหนึ่ง (`1 CASE = 5 KG`) และ vendor mapping อย่างน้อยหนึ่ง | 1. จาก line entry ของ [[purchase-request]] → คลิกฟิลด์สินค้า → picker เปิด 2. พิมพ์ `COF-001` ใน search 3. Picker แสดงแถวพร้อม code, name, หน่วยฐาน (KG), standard cost, ต้นทุนรับล่าสุด (derive ตาม `PRD_CALC_008`), จำนวน vendor 4. คลิกแถว | บรรทัด PR populate ด้วย `product_id = COF-001.id`, หน่วยฐาน `KG`, หน่วยสั่งซื้อ default `CASE` (จาก conversion `is_default = true`) Map ไปยัง parent Scenario 1 Picker ปิด; บรรทัด PR ดำเนินต่อด้วย qty / unit-price entry |
| PR-HP-02 | ค้นหาและเลือกด้วย barcode | Pre-condition เดียวกัน; สินค้ามี `barcode = '8851234567890'` | 1. เปิด picker 2. พิมพ์ `885123` ใน search 3. Picker match กับ `barcode` | เหมือน PR-HP-01 — picker resolve และบรรทัด populate การค้นหา Barcode เป็นเส้นทาง non-default บน PR แต่มีสำหรับ cross-checking |
| PR-HP-03 | filter ตามการจำแนก | จัดทำ PR; ต้องการหาสินค้ากาแฟทั้งหมด | 1. เปิด picker 2. ใช้ filter การจำแนก: `category = Beverages → sub-category = Hot → item-group = Coffee Beans` | Picker แสดงสินค้า live ทั้งหมดใต้ item-group นั้น (โดยทั่วไป 5–15 แถว) Sort ตาม code หรือตามต้นทุนรับล่าสุดตามต้องการ ตาม `PRD_AUTH_005` Purchaser มี scope การอ่านข้ามสินค้า live ทั้งหมด |
| PR-HP-04 | filter ตาม vendor scope บนบรรทัด PO | จัดทำ PO ต่อ vendor "Coffee Imports Co." | 1. จาก line entry ของ [[purchase-order]] → product picker 2. Picker scope ไปยังสินค้าที่ `tb_product_tb_vendor` mapping กับ vendor นี้ตาม `PRD_AUTH_006` | Picker แสดงเฉพาะสินค้าที่ map กับ Coffee Imports Co. — โดยทั่วไป subset ที่เล็ก ถ้าสินค้าที่ Purchaser ต้องการไม่อยู่ใน list พวกเขา post comment ขอ vendor mapping (decision branch จัดการโดย parent Scenario 10 / 16) |
| PR-HP-05 | ตรวจสอบรายละเอียดสินค้าก่อนการเลือก | ต้องการตรวจสอบ standard cost และต้นทุนรับล่าสุดก่อนเพิ่ม item มูลค่าสูงไปยัง PR | 1. เปิด picker → search → หาสินค้า 2. คลิก "View details" แทน "Select" 3. มุมมองรายละเอียดแสดง: เส้นทางการจำแนก, tax profile ที่สืบทอด, ค่าความคลาดเคลื่อน, การแปลงหน่วยที่นิยามทั้งหมด, vendor mapping, นโยบายสต๊อกต่อ location (เพื่อแจ้งให้ทราบ) และ tab "Latest Purchase" พร้อม 5 GRN รับล่าสุด (date / vendor / unit cost) | ทุกค่า render ถูกต้องตาม `PRD_CALC_001` (เส้นทางการจำแนก), `PRD_CALC_002` (tax profile), `PRD_CALC_003` (ค่าความคลาดเคลื่อน), `PRD_CALC_008` (ต้นทุนรับล่าสุด — derive จาก `tb_inventory_transaction_cost_layer`) Purchaser ปิดรายละเอียดและกลับไป picker เพื่อเลือก |
| PR-HP-06 | Override หน่วยสั่งซื้อ default บนบรรทัด PR | สินค้ามี order-unit conversion `1 CASE = 5 KG` (default) และ `1 BAG = 10 KG`; Purchaser ต้องการสั่งใน BAG | 1. เลือกสินค้าบนบรรทัด PR; บรรทัด default `CASE` 2. บนบรรทัด เปลี่ยน unit dropdown เป็น `BAG` 3. กรอก qty `2 BAG` | บรรทัดเก็บ `qty = 2` ในหน่วย `BAG`; ระบบคำนวณ base-unit equivalent `2 × 10 = 20 KG` ตาม `PRD_CALC_005` ทั้งสองค่าแสดงบนบรรทัดสำหรับการอ้างอิง |
| PR-HP-07 | Post comment สำหรับ standard-cost เก่า | พบสินค้า `COF-001` ที่ `standard_cost = ฿450`; ใบเสนอราคา vendor ปัจจุบันคือ `฿520`; tolerance deviation คือ 10% (ดังนั้น 10% × 450 = ฿45, ใบเสนอราคา vendor สูงกว่า standard ฿70 — เกิน tolerance) | 1. เปิดรายละเอียดสินค้า → tab Comments 2. comment ใหม่: "Standard cost ล้าสมัย — vendor Coffee Imports quote ฿520 วันนี้; standard คือ ฿450" แนบ screenshot ใบเสนอราคา vendor 3. Save | แถว `tb_product_comment` insert ด้วย `user_id = purchase@blueledgers.com`, `type = user`, message, attachment Product Administrator หยิบจากคิว comment (ตาม parent Scenario 8 / 17) Purchaser อาจดำเนินต่อกับบรรทัด PR (deviation จะ flag ที่ submit สำหรับการอนุมัติเกินเกณฑ์) หรือถือบรรทัดรอ Product Administrator update |
| PR-HP-08 | Browse แคตตาล็อกสำหรับการวางแผน procurement | การ refresh เมนูรายไตรมาส; Purchaser ต้องการยืนยันว่า item "Hot Beverages" ทั้งหมดมี vendor mapping active อย่างน้อยหนึ่ง | 1. Products → list view → filter ตามการจำแนก "Hot Beverages" 2. Sort ตามคอลัมน์ "Vendor Count" ขึ้น 3. ระบุสินค้าที่จำนวน vendor = 0 | Browse read-only ตาม `PRD_AUTH_005` สินค้าที่มี vendor mapping เป็นศูนย์ปรากฏ; Purchaser export list และ route ไปยัง Product Administrator สำหรับการตั้งค่า vendor-mapping (โดยทั่วไปผ่าน comment บนสินค้าที่ได้รับผลกระทบแต่ละตัวหรือช่อง bulk request) |
| PR-HP-09 | การเปรียบเทียบต้นทุนรับล่าสุด vs standard-cost | จัดทำ PR; Purchaser ต้องการ benchmark กับการรับจริงล่าสุด | 1. เลือกสินค้า; ขยาย tab Latest Purchase บนรายละเอียด 2. ดู 5 GRN รับล่าสุดพร้อม date / vendor / unit cost | ตาม `PRD_CALC_008`: ต้นทุนรับล่าสุด derive จาก `tb_inventory_transaction_cost_layer` (inbound ล่าสุด) แสดงข้างเคียง `standard_cost` สำหรับการเปรียบเทียบ Purchaser ใช้ทั้งสอง — `standard_cost` เป็น budget reference; ต้นทุนรับล่าสุดเป็น market reference (ตาม [[product/03-user-flow-purchaser]] decision branch) |

## 2. Permission / Authorization

| # | Scenario | พฤติกรรมที่คาดหวัง (allow/deny + เหตุผล) |
| - | -------- | --------------------------------------- |
| PR-PERM-01 | Purchaser ค้นหา / filter / ดูแคตตาล็อก | **Allow** ตาม `PRD_AUTH_005` scope read-only ข้ามสินค้า live ทั้งหมด Picker filter ไปที่ `product_status_type = active` ตาม default; toggle มีสำหรับมุมมอง inactive (read-only) |
| PR-PERM-02 | Purchaser ดู join `tb_product_tb_vendor` และ pricelist | **Allow** ตาม `PRD_AUTH_006` read-only ใช้สำหรับการ filter vendor-scope บน PO และสำหรับ default unit-price ขับเคลื่อนด้วย pricelist |
| PR-PERM-03 | Purchaser พยายามสร้างสินค้าใหม่ | **Deny — ต้องการ Product Administrator** ตาม `PRD_AUTH_001` create authority อยู่บน Product Administrator Picker ไม่มี affordance "create new" สำหรับ Purchaser การส่ง API ตรง return `"Product creation requires the Product Administrator role."` `403 Forbidden` เส้นทางของ Purchaser คือ post comment คำขอสินค้าใหม่ตาม parent Scenario 8 |
| PR-PERM-04 | Purchaser พยายามแก้ฟิลด์ `tb_product` (เช่น update standard cost จาก supplier feed velocity สูง) | **Deny** เหมือนข้างบน — write authority เป็นของ Product Administrator API ตรง `PATCH /product/<id>` return `403 Forbidden` |
| PR-PERM-05 | Purchaser พยายามเพิ่มแถว `tb_unit_conversion` | **Deny — ต้องการ Product Administrator** Conversion-factor authority เป็น Product Administrator ตาม `PRD_AUTH_002` เส้นทางของ Purchaser คือ post comment ขอให้เพิ่ม conversion ตาม parent Scenario 10 |
| PR-PERM-06 | Purchaser พยายามแก้นโยบายตัวเลข `tb_product_location` | **Deny** ตาม `PRD_AUTH_004` / `INV_AUTH_004` การแก้นโยบายเป็นอำนาจ Inventory Controller เส้นทางของ Purchaser คือ escalate ผ่านช่อง feedback ของ Store Keeper (หายาก — โดยทั่วไปไม่ใช่ความกังวลของ Purchaser) |
| PR-PERM-07 | Purchaser post comment บนสินค้า | **Allow** ตาม `PRD_XMOD_011` role ทั้งหมดสามารถ post comment Comment route ไปยัง Product Administrator สำหรับ review และตอบ |
| PR-PERM-08 | Purchaser ดู activity log บนสินค้า | **Allow read-only** ตาม `PRD_XMOD_011` เห็นการสร้าง การแก้ การเปลี่ยนสถานะ soft-delete (เมื่อใช้ได้) บน product master |
| PR-PERM-09 | Purchaser ดูสินค้าที่ soft-deleted | **Allow read** ถ้า filter "show inactive / deleted" toggle — แต่ **ไม่สามารถเลือก** สำหรับบรรทัดใหม่ตาม `PRD_XMOD_001` ใช้สำหรับการอ้างอิงประวัติ / research เท่านั้น |
| PR-PERM-10 | Purchaser export แคตตาล็อก | **อนุญาตที่ระดับ role** สำหรับมุมมองที่ไม่ละเอียดอ่อน (รายการสินค้าพื้นฐาน การจำแนก) การ export ฟิลด์ที่ละเอียดอ่อน (`standard_cost`, vendor mapping) อาจถูกจำกัดให้ Product Administrator / Auditor ขึ้นอยู่กับการตั้งค่า tenant |

## 3. Validation / Error

| # | Scenario | Trigger | Error ที่คาดหวัง |
| - | -------- | ------- | -------------- |
| PR-VAL-01 | เลือกสินค้า inactive บนบรรทัด PR ใหม่ (`PRD_XMOD_001`) | Purchaser toggle filter "show inactive" และพยายามเลือกสินค้า inactive | **ปฏิเสธที่ line save** — `"Product <code> is inactive or deleted and cannot be added to new transactions."` `400 Bad Request` Picker ปกติ grey out แถว inactive เมื่อ toggle เปิด; การส่ง API ตรงตรวจสอบใหม่ Map ไปยังการจัดการสินค้า inactive |
| PR-VAL-02 | เลือกหน่วยที่ไม่ตั้งค่าสำหรับสินค้า (`PRD_XMOD_006`) | สินค้ามี order-unit conversion `1 CASE = 5 KG` เท่านั้น; Purchaser พยายามกรอก qty ใน `BAG` ผ่าน API | **ปฏิเสธที่ line save** — `"No conversion factor defined for unit BAG → KG on product COF-001."` `400 Bad Request` Picker ปกติแสดงเฉพาะหน่วยที่ตั้งค่า; นี่คือการตรวจสอบ backend ป้องกัน การ resolve: post comment ขอ conversion ตาม parent Scenario 10 |
| PR-VAL-03 | เลือกสินค้าที่ไม่ map กับ vendor ที่เลือกของ PO | PO scope ไปยัง vendor V1; สินค้า P1 ไม่มีแถว `tb_product_tb_vendor` สำหรับ V1 | **ปฏิเสธที่ line save** — `"Product P1 is not mapped to vendor V1."` `400 Bad Request` (ธรรมเนียม; ไม่ใช่กฎ schema แข็ง — บังคับใช้ที่ application) การ resolve: เปลี่ยน vendor ของ PO หรือ post comment ขอ mapping ตาม parent Scenario 16 |
| PR-VAL-04 | Submit บรรทัด PR ด้วย unit-price เกิน `price_deviation_limit` | สินค้ามี `price_deviation_limit = 10`, `standard_cost = ฿100`; Purchaser กรอก unit-price = `฿120` (สูงกว่า standard 20% เกิน 10%) | **Soft-block** — บรรทัดยอมรับแต่ flag สำหรับการอนุมัติเกินเกณฑ์ตามกฎ validation ของ [[purchase-request]] (กฎด้าน product ตาม `PRD_CALC_003` ป้อน gating) flag มองเห็นได้บน workflow การอนุมัติของ PR ตาม `PRD_XMOD_007` tolerance deviation คือ gate |
| PR-VAL-05 | Comment พร้อม attachment ผิดรูปแบบ | Purchaser post comment พร้อม metadata attachment ขาด `fileToken` | **ปฏิเสธที่ submit** — รูปทรง attachment ของ comment บังคับใช้ตามธรรมเนียม tenant-comment; attachment ผิดรูปแบบ return `400 Bad Request` |

## 4. Edge Cases

| # | Scenario | เงื่อนไข | คาดหวัง |
| - | -------- | --------- | -------- |
| PR-EDGE-01 | Picker แสดงสินค้าแต่ vendor scope ว่าง | สินค้า active และมี `tb_product_tb_vendor` mapping กับ vendor V1 และ V2 เท่านั้น PO scope ไปยัง V3 | Picker filter ตาม `PRD_AUTH_006`; สินค้าไม่ปรากฏ Purchaser เห็นสถานะ empty-result พร้อม prompt "สินค้าไม่ map กับ V3 — ขอ mapping ผ่าน comment" เส้นทางต่อ: สลับ vendor ของ PO หรือขอ mapping ตาม Scenario 16 |
| PR-EDGE-02 | Standard cost เพิ่งถูก update กลาง session | Purchaser เปิด picker; พร้อมกัน Product Administrator commit การเปลี่ยน standard-cost บนสินค้าที่ Purchaser กำลังดู | Picker อ่านเมื่อต้องการ; การเปิด picker ครั้งต่อไปแสดง `standard_cost` ใหม่ มุมมองรายละเอียดปัจจุบันอาจแสดง stale data จนกว่า refresh ไม่มี conflict — ความ stale ด้านการอ่านยอมรับได้ |
| PR-EDGE-03 | filter การจำแนกหลายระดับลึก | filter ตาม category = Beverages (depth 1); 5 sub-category, 20 item-group, 200 สินค้าใต้ | Picker แสดงทั้ง 200 สินค้า การจัดหน้าเริ่มทำงานตาม `TR-API-004` ประสิทธิภาพ: indexed query บนเส้นทาง item-group; การตอบสนองใต้วินาทีที่คาดหวัง |
| PR-EDGE-04 | Precision ทศนิยมบน unit-price | standard cost ของสินค้าคือ `฿11.33333` (5dp จากการ derive ledger weighted-average); Purchaser กรอก unit-price `฿11.33` | การตรวจสอบ deviation ที่ line submit ตาม `PRD_CALC_003`: `(11.33 vs 11.33333)` → ภายใน 0.003% deviation → ดีภายใน tolerance ปกติ ไม่ flag แสดงปัดเศษเป็น 2dp |
| PR-EDGE-05 | สินค้าใหม่เพิ่งสร้างและยังไม่อยู่บน picker cache | Product Administrator สร้างสินค้าใหม่ที่ T; Purchaser เปิด picker ที่ T+50ms — frontend cache อาจยังไม่มีแถวใหม่ | Frontend picker โดยทั่วไป debounce / cache ผลลัพธ์ 30s–5min ขึ้นอยู่กับการตั้งค่า tenant Purchaser อาจต้อง refresh / search ชัดเจนเพื่อเห็นสินค้าใหม่ ไม่มี state corruption — สินค้าอยู่ใน DB; picker แค่ตามหลัง |
| PR-EDGE-06 | ต้นทุนรับล่าสุดเป็น null (สินค้าไม่เคยรับ) | สินค้า P1 active แต่ไม่มี GRN ที่เคย post สำหรับมัน | ตาม `PRD_CALC_008`: ต้นทุนรับล่าสุดคือ `null` (ไม่มี cost-layer inbound ให้ derive จาก) มุมมองรายละเอียดแสดง "—" หรือ "ไม่มีประวัติการซื้อ" Standard cost คือค่าอ้างอิงเดียว |
| PR-EDGE-07 | สินค้าเดียวกัน lot ต่างมีต้นทุนต่าง | สินค้าคิดต้นทุน FIFO (ตาม business-unit `calculation_method = fifo`); มี `LOT-1` ที่ ฿10 และ `LOT-2` ที่ ฿14 บนมือ | ต้นทุนรับล่าสุดตาม `PRD_CALC_008` return inbound ล่าสุด เช่น `LOT-2` ที่ `฿14` FIFO-vs-WA-cost-pick ไม่เกี่ยวบนด้าน read (Purchaser เห็น last-receiving โดยไม่คำนึงถึงวิธี) |
| PR-EDGE-08 | สินค้าย้ายไปยัง item-group ต่างกลาง PR-composition | Purchaser มี PR draft กับบรรทัดสินค้า P1 populate (snapshot tax-profile และค่า deviation); Product Administrator re-classify P1 เป็น item-group ต่างกับ tax profile สืบทอดต่าง | ตาม `PRD_LIFE_010`: การเปลี่ยน classification เป็น prospective บรรทัด PR ที่มีอยู่ของ Purchaser เก็บ snapshot บรรทัด PR ใหม่สำหรับ P1 หลังการ re-classification อ่าน tax profile ที่มีผลใหม่ |
| PR-EDGE-09 | vendor mapping ถูกลบขณะ Purchaser อยู่กลาง PO | Purchaser เปิด picker บรรทัด PO scope ไปยัง V1; สินค้า P1 เพิ่งถูกยกเลิก mapping จาก V1 (แถว `tb_product_tb_vendor` ถูก soft-delete) | Picker re-fetch return ชุด update ยกเว้น P1 ถ้า Purchaser populate บรรทัดแล้วและ save application revalidate ตาม PR-VAL-03 และปฏิเสธด้วย `"Product P1 is not mapped to vendor V1."` Edge case; หายาก |

## 5. แหล่งอ้างอิง

- Parent overview: [04-test-scenarios.md](./04-test-scenarios.md) — handoff ข้าม persona ที่ pivot จาก Purchaser: Scenario 1 (สินค้าใหม่ถึงผู้บริโภค), Scenario 8 (comment แคตตาล็อกเก่า), Scenario 10 (conversion ขาด), Scenario 16 (การ update vendor mapping)
- User flow: [03-user-flow-purchaser.md](./03-user-flow-purchaser.md) — แหล่ง happy-path สำหรับ Section 1 ด้านบน; อธิบาย primary flow 6 ขั้นตอน (lookup สินค้าสำหรับบรรทัด PR) flow comment และ flow catalogue-browse Decision branch (filter ตาม inactive การเลือกหน่วยสั่งซื้อ standard vs last-receiving vendor scope บน PO) แจ้ง edge case Section 4
- กฎทางธุรกิจที่ verify: [02-business-rules.md](./02-business-rules.md) — calculation `PRD_CALC_001` / `PRD_CALC_002` / `PRD_CALC_003` / `PRD_CALC_005` / `PRD_CALC_008` อ้างใน Section 1; authorization `PRD_AUTH_005` (Purchaser read-only), `PRD_AUTH_006` (อ่าน vendor-mapping), `PRD_AUTH_009` (filter picker); cross-module `PRD_XMOD_001` (สินค้า inactive ถูกปฏิเสธบนบรรทัดใหม่), `PRD_XMOD_006` (จำเป็นต้องมี order-unit conversion), `PRD_XMOD_007` (tolerance deviation gate การอนุมัติ PR / GRN) กฎ validation ตาม `PR_VAL_*` บน [[purchase-request]] คือคู่ขนานด้านบรรทัด
- spec E2E: ไม่มี spec ด้าน product ของ Purchaser ตรง; การครอบคลุมเป็นทางอ้อมผ่าน spec [[purchase-request]] และ [[purchase-order]] (ซึ่ง exercise product picker บนบรรทัด PR / PO) scenario ส่วนใหญ่ด้านบนเป็น manual / planned Happy-path fixture ตรงกับ `purchase@blueledgers.com`
- Cross-link: [[purchase-request]] / [[purchase-order]] — โมดูลหลัก; product picker ฝัง; tolerance deviation gate การ routing การอนุมัติ
- Cross-link: [[vendor-pricelist]] — pricelist อ้างอิงสินค้าและให้ unit price default บนบรรทัด PR / PO
- Cross-link: [[good-receive-note]] — GRN post ป้อน `tb_inventory_transaction_cost_layer.cost_per_unit` ซึ่งเป็นสิ่งที่ `PRD_CALC_008` อ่านกลับเพื่อแสดงต้นทุนรับล่าสุด
