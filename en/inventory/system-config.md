---
title: System Configuration
description: Document-flow and inventory-period configuration — workflow, inventory period, running codes, email profiles/templates are real screens; dimension and menu are schema-only; query-dataset UI is Platform-side. Re-verified 2026-09-22.
published: true
date: '2026-09-22T18:00:00.000Z'
tags: system-config, configuration, carmen-software
editor: markdown
dateCreated: 2026-05-16T08:00:00.000Z
---

# System Configuration

> **At a Glance**
> **Module purpose:** Document-flow and inventory-period machinery — approval workflows, inventory (accounting) periods, document numbering, outbound-email sender profiles and message templates, plus several schema-provisioned-but-unimplemented concepts (dimensions, menu registry) &nbsp;·&nbsp; **Audience:** Sysadmin, Workflow Administrator, Finance (period close) &nbsp;·&nbsp; **Key entities/tables:** `tb_workflow`, `tb_inventory_period` (renamed from `tb_period` 2026-09-16), `tb_dimension`, `tb_config_running_code`, `tb_application_config`, `tb_menu`, `tb_business_unit` (config fields), `tb_notification_template`, `tb_activity` &nbsp;·&nbsp; **Sub-pages:** 14 &nbsp;·&nbsp; **Re-verified 2026-09-22: 9 of 14 sub-pages describe a real, reachable Sysadmin screen (workflow, period → now *Inventory Period*, running-code, config-email → now the *Email Profile* + *Email Template* screens, document, dashboard-dataset, company-profile, notification-template, activity-log); dimension and menu are still dead schema tables; application-config is consumed key-by-key (now through three licence-gated screens); query-dataset's UI is the Platform SPA's SQL Workbench and its `system_admin.query_dataset` permission/licence key was removed from the catalog on 2026-09-21.**

![System Configuration screen](/screenshots/system-config/index.png)

## 1. Overview

System Configuration is the umbrella for the **document-flow and inventory-period machinery** that every transactional module depends on. Workflows define multi-stage approval routing with per-stage actions, recipients, and field visibility — since 2026-09-16 the screen is split into one page per document type (`/system-admin/workflow/purchase-request`, `/purchase-order`, `/store-requisition`), each backed by its own endpoint and permission key. Inventory periods (the module formerly named *Period*, renamed on 2026-09-16 — table `tb_inventory_period`, route `/system-admin/inventory-period`, endpoint `api/:bu_code/inventory-periods`, permission `system_admin.inventory_period`) define the accounting calendar and gate which dated postings are allowed (via a plain CRUD screen — see [system-config/period](/en/inventory/system-config/period)). Running codes drive document numbering (now via a structured part-by-part editor with a live preview — see [system-config/running-code](/en/inventory/system-config/running-code)). Application config is a real, actively-used key-value table, consumed key-by-key by specific features: the `email_profiles` and `email_templates` keys now have their own screens (`/system-admin/email-profile`, `/system-admin/email-template`) and their own licence features (`configuration.email_profile`, `configuration.email_template`, split out of `configuration.app_config` on 2026-09-20), and the `interface_<category>_<brand>` keys back the `/system-admin/interface` screen (licence-only, no permission key) — but there is still no general key-value editor. Dimensions and the menu registry remain **schema-provisioned but unimplemented** — no CRUD service or frontend route exists for either at HEAD (`config_dimension-comments` is the only dimension-adjacent controller; `tb_menu` has zero non-schema references).

The six entities here sit between [master-data](/en/inventory/master-data) (the static catalogues — units, vendors, currencies) and the runtime [access-control](/en/inventory/access-control) layer (users, roles, permissions). Where master data answers *what* a transaction is referencing, system configuration answers *how* it should flow, *when* it should post, and *what extra dimensions* it should carry — the last of these ("extra dimensions") remains design intent only today. Most rows are owned and edited by Sysadmin; a few — workflow stages in particular — see day-to-day adjustment from a designated Workflow Administrator.

All six entities live in the **tenant** schema. None of them have a platform counterpart — they describe per-property document flow, so each tenant gets its own copy.

## 2. Audience

Sysadmin. Workflow definition may be delegated to a Workflow Administrator persona (typically the Finance Manager or Procurement Manager) for day-to-day stage / approver maintenance. Finance owns period close.

## 3. Entity List

| Entity | Purpose | Managed by | Implementation |
| ------ | ------- | ---------- | --------------- |
| [workflow](/en/inventory/system-config/workflow) | Multi-stage approval workflows with per-stage actions, recipients, field visibility | Sysadmin / Workflow Admin | Real screen |
| [period](/en/inventory/system-config/period) | **Inventory Period** (renamed 2026-09-16): accounting periods (open/closed/locked) and per-period inventory snapshots — `tb_inventory_period*`, `/system-admin/inventory-period`, `api/:bu_code/inventory-periods` | Sysadmin / Finance | Real screen — plain CRUD, no dedicated Close/Lock/Reopen actions; old `/system-admin/period` URL redirects, old `/periods` API path kept as a hidden alias |
| [running-code](/en/inventory/system-config/running-code) | Document-number patterns per document type | Sysadmin | Real screen — structured part editor + live preview since 2026-08-27 (raw JSON kept as a collapsed "Advanced" fallback) |
| [config-email](/en/inventory/system-config/config-email) | Outbound email: named SMTP **sender profiles** (`email_profiles` key, `/system-admin/email-profile`) + per-document-type **message library** (`email_templates` key, `/system-admin/email-template`); the legacy single-`report_email` screen is no longer routed | Sysadmin | Real screens since 2026-09-08 / 2026-09-16; each has its own licence feature; the app-config controller still has **no RBAC guard** |
| [document](/en/inventory/system-config/document) | Tenant-scoped file-storage registry — upload, list, download, and delete for documents attached to transactional records | Sysadmin | Real screen; backed by `tb_file_tag` + MinIO (not `tb_attachment`) |
| [dashboard-dataset](/en/inventory/system-config/dashboard-dataset) | Read-only catalog of code-registered data feeds available to dashboard widgets | Sysadmin | Real screen |
| [company-profile](/en/inventory/system-config/company-profile) | Two screens (Company Profile + Default Setting) editing disjoint field groups of the current `tb_business_unit` row — identity/address/formats, and PR/SI/PO config + print-form selection | Sysadmin | Real screens; `/system-admin/business-setting` is a dead redirect to Company Profile |
| [notification-template](/en/inventory/system-config/notification-template) | Reusable in-app message templates picked per workflow stage/action/recipient | Sysadmin / Workflow Admin | Real screen; since 2026-09-16 the screen creates **`app`-channel templates only** (list filtered `type:app`, Channel/Subject controls removed) and the workflow editor offers only the `app` channel — matching the dispatcher, which never delivered anything else; legacy `email`/`sms`/`line` rows stay in the DB |
| [activity-log](/en/inventory/system-config/activity-log) | List/grid UI over the tenant audit log (`tb_activity`) with action/entity-type/actor filters, export, print | Sysadmin / Auditor | Real screen; the *only* activity UI in the product — data model lives at [reporting-audit/activity](/en/inventory/reporting-audit/activity) |
| [application-config](/en/inventory/system-config/application-config) | Tenant-wide key-value settings + per-user preference overrides | No general admin screen | Real table, consumed key-by-key: `email_profiles` / `email_templates` / `interface_*` each have a dedicated screen and licence feature (2026-09-20 split); `report_email`, `gl_setting`, `*_approval_flow`, `*_print_config`, signature settings are consumed by their own features; `GET /app-config` (list) now **excludes** the three licence-split key groups |
| [query-dataset](/en/inventory/system-config/query-dataset) | SQL Workbench — author tenant views, stored procedures, and functions as reusable data sources | Platform SPA operators (`sql_workbench.*` platform permissions) | Real backend service; UI is the Platform SPA's SQL Workbench, not this product; `system_admin.query_dataset` permission/licence key removed 2026-09-21 |
| [dimension](/en/inventory/system-config/dimension) | User-defined custom fields with per-place display matrix | Nobody — no CRUD path | Schema only; no service, controller, or route found |
| [menu](/en/inventory/system-config/menu) | Navigation registry rendered by the app shell | Nobody — table unused | Schema only; zero non-schema code references; nav is a static frontend constant |
| [doc-version](/en/inventory/system-config/doc-version) | Optimistic-concurrency `doc_version` guard — clients echo the version on save or get a 409 | Engineering | Cross-cutting mechanism, not a screen |

## 4. Cross-Module Dependencies

**Workflow attachment is narrower than a prior version of this list claimed.** `enum_workflow_type` has four values at HEAD — `purchase_request`, `store_requisition`, `purchase_order`, and `gl_jv` (added 2026-09-09 with the GL journal-voucher module, `schema.prisma:276-281`; GL is an accounting module outside this book and the inventory workflow screen has no page for it). GRN, inventory-adjustment, physical-count, spot-check, and vendor-pricelist still have **no `workflow_type` enum member** and cannot attach a `tb_workflow` row; any "optional approval" language for those modules below has been corrected. Likewise, [system-config/dimension](/en/inventory/system-config/dimension)'s tagging claims have been corrected module-wide — the `dimension` JSONB column exists on these tables but no code was found that reads or writes it anywhere (see that page's Implementation status).

- [purchase-request](/en/inventory/purchase-request) requires [system-config/workflow](/en/inventory/system-config/workflow) (PR approval routing — real, `workflow_type = purchase_request`), [system-config/running-code](/en/inventory/system-config/running-code) (PR number). `dimension` column present, unused.
- [purchase-order](/en/inventory/purchase-order) requires [system-config/workflow](/en/inventory/system-config/workflow) (PO approval routing — real, `workflow_type = purchase_order`, though `tb_purchase_order.workflow_id` is a loose UUID field, not a declared Prisma relation), [system-config/running-code](/en/inventory/system-config/running-code) (PO number). `dimension` column present, unused.
- [good-receive-note](/en/inventory/good-receive-note) requires [system-config/period](/en/inventory/system-config/period) (posting-date guard), [system-config/running-code](/en/inventory/system-config/running-code) (GRN number). **No workflow attachment** — `goods_received_note` is not a member of `enum_workflow_type`.
- [store-requisition](/en/inventory/store-requisition) requires [system-config/workflow](/en/inventory/system-config/workflow) (SR approval routing — real, `workflow_type = store_requisition`), [system-config/running-code](/en/inventory/system-config/running-code) (SR number).
- [inventory-adjustment](/en/inventory/inventory-adjustment) requires [system-config/period](/en/inventory/system-config/period) (posting-date guard), [system-config/running-code](/en/inventory/system-config/running-code) (IA / SI / SO numbers). **No workflow attachment.**
- [inventory](/en/inventory/inventory) requires [system-config/period](/en/inventory/system-config/period) (period boundaries on every movement). `dimension` column present, unused.
- [costing](/en/inventory/costing) requires [system-config/period](/en/inventory/system-config/period) (the cost-close engine writes `tb_inventory_period_snapshot`, formerly `tb_period_snapshot`).
- [physical-count](/en/inventory/physical-count) requires [system-config/period](/en/inventory/system-config/period) (count documents are frozen against a period), [system-config/running-code](/en/inventory/system-config/running-code) (count document number). **No workflow attachment.**
- [spot-check](/en/inventory/spot-check) requires [system-config/running-code](/en/inventory/system-config/running-code) (document number). **No workflow attachment.**
- [vendor-pricelist](/en/inventory/vendor-pricelist) requires [system-config/running-code](/en/inventory/system-config/running-code) (pricelist reference). **No workflow attachment.**
- [system-config/application-config](/en/inventory/system-config/application-config) is a real table, but consumed key-by-key by specific features (SMTP sender profiles, email message library, interface configs, signature settings, print/approval-flow configs) — not a general feature-flag layer read by every module. [system-config/menu](/en/inventory/system-config/menu) has no confirmed consumer anywhere; navigation is a static frontend constant instead.

## 5. References

- **Prisma tenant:** `../carmen-turborepo-backend-v2/packages/prisma-shared-schema-tenant/prisma/schema.prisma`.
- **carmen/docs:** `../carmen/docs/workflow-permissions-system.md` (workflow role types and the permission matrix consumed by [system-config/workflow](/en/inventory/system-config/workflow)). Frozen since 2026-04-27 — implementation beats it.
- **Seed data:** `../carmen-turborepo-backend-v2/packages/prisma-shared-schema-tenant/prisma/seed-data-a01/` — `tb_workflow.json`, `tb_config_running_code.json`, `tb_application_config.json`; `packages/prisma-shared-schema-tenant/src/seed-data/{running-code,email-templates}.ts`.
- **Design spec:** `.specs/2026-05-16-master-config-design.md`.
- **Plan:** `.specs/2026-05-16-master-config-plan.md`.

## 6. Changes since 2026-07-29 (verified 2026-09-22)

| Change | Date | Source | Wiki page |
|---|---|---|---|
| Period module renamed to **Inventory Period**: tables `tb_period*` → `tb_inventory_period*`, HTTP path `api/:bu_code/periods` → `api/:bu_code/inventory-periods` (old path kept as a hidden alias), permission/licence/`api_name` key `system_admin.period` → `system_admin.inventory_period`, FE route `/system-admin/period` → `/system-admin/inventory-period` (redirect kept) | 2026-09-16 | BE `3933277dc`, `a92151730`, `c8de2fa76`; tenant migration `20260916141000_rename_tb_period_to_tb_inventory_period`; platform migrations `20260916140000_rename_period_to_inventory_period`, `20260916150000_fix_license_group_item_inventory_period_key`; FE `06c68975`, `1caecb42` | [period](/en/inventory/system-config/period) |
| Licence data moved off `GET /api/user/profile` onto `GET /api/license` (`state`, `end_date`, `features[]`, `hidden_features[]`, `expired_features[]`, per-BU + union); `403 LICENSE_REQUIRED` / `LICENSE_EXPIRED` now carry `bu_codes`/`bu_names` | 2026-09-09 / 2026-09-21 | BE `dc5623a05`, `11df68225`, `c7a0d9168`; FE `bebd5f75` (`hooks/use-license.ts`, `types/license.ts`, `components/license-expired-banner.tsx`) | [application-config](/en/inventory/system-config/application-config) |
| App-config licence split: `LICENSE_ROUTE_OVERRIDES` maps `app-config/email_profiles` → `configuration.email_profile`, `app-config/email_templates` → `configuration.email_template`, `app-config/interface_*` → `interface`; `GET /app-config` list drops those keys | 2026-09-20 | BE `84667a405`, `9c52288c0`, `5ff86e322`; `packages/prisma-shared-schema-platform/prisma/permission.route-map.ts:257-268` | [application-config](/en/inventory/system-config/application-config), [config-email](/en/inventory/system-config/config-email) |
| Email sender profiles as a master list + per-document message library; secret-free lookup endpoints `GET /api/:bu_code/email-senders` and `/email-messages`; `enum_activity_action` += `email_sent` | 2026-08-08 → 2026-09-20 | BE `f5c3e5c5b`, `f00088307`, `6a859f9e2`; FE `67b8a8c1`, `2b80f83c`, `e98ef3ee` | [config-email](/en/inventory/system-config/config-email) |
| Workflow: one page per document type, no combined list; in-flight documents lock only the stage list/routing (form stays editable); `GET workflows/:id/edit-availability`, `GET workflows/assignees/:user_id` + `POST .../handover`, `GET workflows/:id/products/:product_id/locations` | 2026-09-01 → 2026-09-16 | BE `b1bcb1e98`, `003e12fb8`, `4fecf678f`, `00ffaaeba`, `383fc4ca4`; FE `8cac942d`, `f3e13d30` | [workflow](/en/inventory/system-config/workflow) |
| Notification template: `app` channel only + eight real `{{variables}}` with insert chips and preview; nav entry moved under the Workflows group | 2026-09-16 | FE `541690f5`, `00f2196d` | [notification-template](/en/inventory/system-config/notification-template) |
| Running code: part-by-part editor with live preview; `GL-JV` preset added (no literal prefix segment) | 2026-08-27 / 2026-09-15 | FE `704f1f9f`; BE `27f65cba1` | [running-code](/en/inventory/system-config/running-code) |
| Footer status bar shows the current inventory period (from `/user/profile`) and the backend build (`GET /version` → `{ version, commit }`) | 2026-09-18 | BE `94b44828d` (`apps/backend-gateway/src/app.controller.ts:71-75`); FE `ad213a11` (`components/footer/status-bar.tsx`, `hooks/use-backend-version.ts`) | [period](/en/inventory/system-config/period) |
| `system_admin.query_dataset` removed from the permission and licence catalogs — SQL Workbench is Platform-only | 2026-09-21 | BE `7bebddaa7`; FE `02bfff7a` | [query-dataset](/en/inventory/system-config/query-dataset) |
| Frontend ghost permission keys (`system_configuration.*`) replaced by the real `tb_permission` resources (`system_admin.business_unit`, `system_admin.activity_log`, `system_admin.user_activity`, `system_admin.document`, …) | 2026-09-21 | FE `b9e2de5f`, `91b2b274` (`constant/permissions.ts`, `constant/module-list.ts`) | [activity-log](/en/inventory/system-config/activity-log), [company-profile](/en/inventory/system-config/company-profile) |
| `enum_workflow_type` += `gl_jv` | 2026-09-09 | BE `0d119b990` | [workflow](/en/inventory/system-config/workflow) |

Not covered by any page in this module (out of scope for the Inventory book, noted for completeness): the Platform-side cron-job registry (`platform/cronjobs/*` in Bruno, `micro-cronjobs` jobs `activity_rollup`, `activity_retention`, `gl_run_due`, `notifyAt` + `notifyDayOffset` columns, `docVersion` + audit, internal-token middleware — 2026-07-30 → 2026-09-21) and the `/system-admin/interface` screen (accounting/POS/PMS interface configs stored as `interface_*` app-config keys, licence-only).
