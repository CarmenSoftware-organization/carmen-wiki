---
title: Store Requisition — Business Rules
description: Validation, calculation, authorization, posting, and cross-module rules for store-requisition.
published: true
date: '2026-09-22T18:00:00.000Z'
tags: store-requisition, business-rules, inventory, carmen-software
editor: markdown
dateCreated: 2026-05-15T13:30:00.000Z
---

# Store Requisition — Business Rules

> **At a Glance**
> **Rule families:** `SR_VAL_*` validation &nbsp;·&nbsp; `SR_AUTH_*` permission &nbsp;·&nbsp; `SR_CALC_*` calc &nbsp;·&nbsp; `SR_POST_*` posting &nbsp;·&nbsp; `SR_XMOD_*` cross-module
> **Rule count:** approximately 60 rules
> **Audience:** Test author + developer — every rule ID is anchored from `04-test-scenarios*` pages
> **Status lifecycle:** Section 5.1 (where present) carries the Live UI vs BRD discrepancy callouts
> **Re-verified 2026-09-22** against backend HEAD `cd2e07f60` / frontend HEAD `8cf47f87`. Headline changes: the three-quantity invariant is **not enforced** anywhere (`SR_VAL_008`/`SR_VAL_010` re-marked), `sr_type` is derived (`SR_VAL_003` rewritten), the open-period gate is **real** (`SR_VAL_014` confirmed, `SR_VAL_016` added), submit checks product-to-destination enablement (`SR_VAL_015`), delete is owner-gated with a batch variant (`SR_AUTH_004`, `SR_POST_011`), and every workflow verb is `PATCH`.

## 1. Overview

This page captures the operational business rules that govern a Store Requisition (SR) document through its lifecycle: input validation at create / edit / submit / approve / issue time, the small set of calculation rules (quantity invariants and the source-costed unit cost feed; the SR is a **quantity document**, not a price document — see [store-requisition/01-data-model](/en/inventory/store-requisition/01-data-model) § 5 items 3 and 4), authorization gates by role and document status, posting effects on each transition of `enum_doc_status`, and cross-module rules with [inventory](/en/inventory/inventory), [costing](/en/inventory/costing), [recipe](/en/inventory/recipe), and [good-receive-note](/en/inventory/good-receive-note). The SR is the system of record for **internal stock movement between locations**: until it commits (`in_progress → completed`), no inventory is decremented at source and no expense / inventory entry is raised at destination; once committed, the source location's on-hand falls by `issued_qty` per line, and the destination either receives the same quantity (`sr_type = transfer`, on-hand increments) or the value lands as an immediate expense with no destination on-hand change (`sr_type = issue`), through the linked `tb_inventory_transaction`. GL/journal-entry posting from this event is unconfirmed in current source — see `SR_POST_007`.

Two structural points colour every rule below and are worth restating up front. **First**, the three quantities on `tb_store_requisition_detail` — `requested_qty`, `approved_qty`, `issued_qty` — together carry the full story across the lifecycle, with the intended ordering `0 ≤ issued_qty ≤ approved_qty ≤ requested_qty`. **Corrected 2026-09-22:** that ordering is *not* enforced by code — only `requested_qty > 0` is checked (pre-submit schema); `approved_qty` and `issued_qty` are persisted verbatim from the payload (see `SR_VAL_008`). **Second**, unlike GRN where `approved` / `rejected` / `committed` are separate header statuses, the SR collapses both approval and fulfilment under the single `in_progress` status — the `workflow_current_stage` field is what distinguishes "awaiting approver" from "awaiting store keeper". So rules referencing "the approval phase" or "the issue phase" gate on `(doc_status = 'in_progress', workflow_current_stage = <approval-stage-slug>)` rather than on a top-level status alone.

## 2. Validation Rules

Rule IDs follow `SR_VAL_NNN`. Header rules (001–005) run on every save and on submit; line rules (006–010) run per line on save, on submit, and on each approval / issue action; aggregate / at-commit rules (011–014) run only at the `in_progress → completed` transition (the single posting event).

| Rule ID | Condition | When enforced | Error / behaviour |
| ------- | --------- | ------------- | ----------------- |
| `SR_VAL_001` | `tb_store_requisition.sr_no` is non-null and unique against non-soft-deleted SRs (`@@unique([sr_no, deleted_at])`). **Corrected 2026-09-22:** the column is `NOT NULL`, but a draft is created with the placeholder `draft-<6 hex>` (`StoreRequisitionService.create()`); the running-code number is minted only at submit (`generateSRNo()`, type `STORE-REQUISITION`, date part from the frozen `sr_date`) and is not re-minted on a resubmit after send-back. | Submit (number minted); DB (uniqueness) | Running-code failures surface as a 500 (`Failed to get running code pattern for SR` / `Failed to generate SR number`); a duplicate is a DB unique-constraint error, not a catalogued message. |
| `SR_VAL_002` | `from_location_id` and `to_location_id` are required by the create/update DTO and both must resolve to non-soft-deleted `tb_location` rows (`resolveAndValidateSrType()` → `SR_LOCATION_NOT_FOUND`). The frontend form requires both (`sr-form-schema.ts`). **Unconfirmed:** a `from ≠ to` check — `resolveAndValidateSrType()` looks the two rows up independently and no equality check was found; the form's destination lookup excludes the chosen source, so the case is only client-prevented. | Create, update | `SR_LOCATION_NOT_FOUND` (404) when either id is unknown. |
| `SR_VAL_003` | **Rewritten 2026-09-22 — `sr_type` is derived, not validated.** `deriveSrType()` (`logic/sr-type.helper.ts`) sets `sr_type = issue` when the destination is `direct` and `transfer` when it is `inventory` or `consignment`; the client never sends the field. The one rejection is a `direct` **source**. | Create (`resolveAndValidateSrType()` inside `create()`) | 400 `from_location must be inventory or consignment, not direct`. There is no "incompatible destination" error to test. |
| `SR_VAL_004` | `sr_date` is non-null and not in the future beyond tenant tolerance; `expected_date` (if set) is on or after `sr_date`. | Save, submit | Reject with "Requisition date is required; expected date must be on or after requisition date." |
| `SR_VAL_005` | `department_id` is required by the create DTO (since `1dbaf76b1`, 2026-09-17 — a create without it is a 400); `requestor_id`, `department_id` and `workflow_id` are required by `ValidateSRBeforeSubmitSchema`. If a draft still lacks a department, `submit()` derives it from the requester's `tb_department_user` memberships (`resolveRequesterDepartment()`): none → `SR_DEPARTMENT_NOT_FOUND`, several → `SR_DEPARTMENT_AMBIGUOUS` (both 422). No check that the requester *belongs* to a client-supplied department was found. | Create (DTO), submit | 400 `Store requisition is not ready to submit — <path>: <issue>` (Zod failures are returned as `VALIDATION_FAILURE`, not thrown), or the two catalogue codes above. |
| `SR_VAL_006` | Each `tb_store_requisition_detail` row has non-null `product_id` referencing an active, non-soft-deleted `tb_product` whose status permits issue (not discontinued / inactive at the source location). | Save line, submit | Reject the line with "Product is required and must be active for issue from the source location." |
| `SR_VAL_007` | The `(store_requisition_id, product_id, dimension)` triple is unique across non-soft-deleted detail rows on the same SR (per the `SRT1_*` index). The same product on the same SR with a different `dimension` (split cost-centre allocation) is permitted as two rows; identical product+dimension is a duplicate. | Save line, submit | Reject the line with "This product is already on the SR with the same cost-dimension allocation; edit the existing line or use a different dimension to split." |
| `SR_VAL_008` | **Corrected 2026-09-22 — only the first clause is real.** `requested_qty > 0` is enforced only at submit (`ValidateSRBeforeSubmitSchema`: `z.number().positive()` per line, plus `.min(1)` lines); the create/update detail DTO (`StoreRequisitionDetailCreateSchema`) accepts any `z.number()`, so a `0` line can be saved on a draft. `approved_qty` / `issued_qty` are typed as bare `z.number()` in `store-requisition-detail.dto.ts`; `saveStageRoleDetails()` writes `detail[qtyField]` as-is and `approve()` spreads the payload into the update, so a negative, zero, or over-cap value is stored without complaint. The frontend bounds only `requested_qty ≥ 0` (`sr-form-schema.ts`). | Submit (requested only) | 400 `Store requisition is not ready to submit — store_requisition_detail.<n>.requested_qty: …`. **No message exists for the approved/issued caps.** |
| `SR_VAL_009` | **Unconfirmed — no code path found (2026-09-22).** `submit()` performs no on-hand read; `grep -n "on_hand\|getOnHandQty\|inventory_status" store-requisition.service.ts store-requisition.logic.ts` returns only the stock-movements read model. There is no soft/hard tenant switch and no reservation concept. The requester sees on-hand only through the per-row Inventory Information dialog (`components/share/inventory-dialog.tsx`), which is informational. | — | None. Over-requests are caught only at the final stage (`SR_VAL_013`). |
| `SR_VAL_010` | **Not enforced (corrected 2026-09-22).** Neither the cap `approved_qty ≤ requested_qty` nor a mandatory `reject_message` exists in code: `approve()`/`reject()` store `stage_message || ''`, and the footer Approve / Reject / Issue dialogs are rendered with `showMessage={false}` (`sr-form-dialogs.tsx`) — a reason field appears only in the Send Back dialog and the bulk-Reject action above the item table, and even there it is optional (`Enter reason (optional)…`). A per-line reject does **not** zero `approved_qty` automatically. | — | None. |
| `SR_VAL_011` | **Unconfirmed — not enforced.** `executeTransferOnComplete()` filters lines to `issued_qty > 0` and simply returns when none remain; the header still flips to `completed`. There is no "at least one approved line" guard in `approve()`. | — | None — an SR with every line at `issued_qty = 0` completes with no inventory movement. |
| `SR_VAL_012` | At commit, for every line with `issued_qty > 0` and `product.product_type = 'inventory'`, the row's `inventory_transaction_id` is populated and the linked `tb_inventory_transaction_detail` carries `lot_no` and `cost_per_unit`. **Corrected this pass:** lot assignment is system-computed FIFO (`getAvailableFifoLots` / `consumeFifoLots` in `inventory-transaction.service.ts`) — there is no lot-selection UI in the SR frontend, so "store-keeper-selected" lots are not a real path; treat lot assignment as automatic. | Commit | Unconfirmed as a distinct client-facing validation message — no lot-selection form exists to fail against. |
| `SR_VAL_013` | At the final stage the source must hold every `issued_qty`: `InventoryTransactionService.createFifoConsumption()` / `createAverageConsumption()` read the live location balance (`getLocationBalance`) and `throw new Error('Insufficient stock. Requested: <qty>, Available: <balance>')` inside the `$transaction`, rolling back all lines. This is a **post-approve** guard: the header update has already been committed in `StoreRequisitionService.approve()` before `executeTransferOnComplete()` runs, so the observable failure is a 500 on the approve call with the header already `completed` (see `SR_POST_006` note). | Final stage (`executeTransferOnComplete`) | `Insufficient stock. Requested: …, Available: …` (raw error text; not catalogued). |
| `SR_VAL_014` | **Confirmed 2026-09-22 (was unconfirmed).** `logic/sr-date.helper.ts` (`cd2e07f60`, 2026-09-18) gates both dates against `tb_inventory_period`: at submit `resolveSubmitSrDate()` keeps today when today is inside an open period, else demands `sr_date_pattern` (`SR_DATE_PATTERN_REQUIRED`) and refuses when no period is open at all (`SR_NO_OPEN_PERIOD`); at the issue stage `resolveIssueDate()` first requires the requisition's own `sr_date` to lie in an open period (`SR_DATE_OUTSIDE_OPEN_PERIOD`), then applies the same today/pattern rule (`SR_ISSUE_DATE_PATTERN_REQUIRED`), refusing `today` outside every open period (`SR_ISSUE_DATE_TODAY_OUTSIDE_PERIOD`). The resolved issue date travels to `executeTransfer()` as `doc_date` and fixes the movement's period. | Submit; every approve call made with `stage_role = issue` (not only the final one) | All five codes are 422 with EN/TH messages in `packages/error-catalog/src/catalog.ts` (`SR` module ids 7–11). |
| `SR_VAL_015` | Every non-deleted line's product must be enabled at the destination: `submit()` loads `tb_product_location` for `to_location_id` and rejects if any line's `product_id` is missing there. | Submit | 400 `The following products are not allowed in the destination location: <names>` (`VALIDATION_FAILURE`). |
| `SR_VAL_016` | Date-pattern transport values: `sr_date_pattern` (submit body) and `issue_date_pattern` (approve body) accept only `open-period` or `today` (`SR_DATE_PATTERNS`, `sr-date.helper.ts`); they are never stored. The frontend answers the two 422 codes with a two-button dialog and retries (`use-sr-form-actions.ts` `datePatternField()`, `sr-date-pattern-dialog.tsx`). | Submit, approve (issue stage) | Ignored when today is inside an open period. |

## 3. Calculation Rules

The SR is a **quantity document**, not a price document, so the calculation surface is small. All quantities are stored as `Decimal(20, 5)` at the row level; display rounding is half-up to 3 decimals for quantities. There are **no monetary columns on `tb_store_requisition` or `tb_store_requisition_detail`** — unit cost and line total are read from the linked inventory transaction at display time (see [store-requisition/01-data-model](/en/inventory/store-requisition/01-data-model) § 5 items 3 and 4).

Rule IDs follow `SR_CALC_NNN`.

| Rule ID | Formula |
| ------- | ------- |
| `SR_CALC_001` (quantity invariant) | `0 ≤ issued_qty ≤ approved_qty ≤ requested_qty` on every active (`deleted_at IS NULL`) detail row. **Documented intent only** — not enforced at write time (see `SR_VAL_008`); readers computing variance should guard against rows that violate it. |
| `SR_CALC_002` (variance — requested vs issued) | `variance_qty = requested_qty − issued_qty` per line; positive variance is a short-fulfilment (approver trim, source stock-out, or partial issue). The variance does not write back to the line — it is computed for the variance dashboard ([inventory](/en/inventory/inventory) / outlet variance reporting). |
| `SR_CALC_003` (variance — approved vs issued) | `fulfilment_gap = approved_qty − issued_qty` per line; positive gap is an at-issue shortfall (the store keeper could not release the full approved amount). This is the metric the store keeper closes out (full fulfilment when `fulfilment_gap = 0`). |
| `SR_CALC_004` (unit cost feed from source) | `unit_cost = tb_inventory_transaction_cost_layer.cost_per_unit` at the moment of issue, picked per the source location's costing method (FIFO or moving-average per [costing](/en/inventory/costing)). The SR does **not** compute or store the unit cost itself — the cost is owned by the costing module on the inventory-transaction side; the SR line reaches it via `inventory_transaction_id`. |
| `SR_CALC_005` (line total — display only) | `line_total = qty × unit_cost` — since 2026-09-14 the form fetches the real figure from `GET /api/{bu}/cost/products/{product_id}/location/{from_location_id}/qty/{qty}` (`sr-item-cost-sync.tsx`) and writes `cost_per_unit` / `total_cost` into the form state without dirtying it; after completion the Stock Movement tab shows the posted `cost_per_unit` / `total_cost` from `tb_inventory_transaction_detail`. **Not persisted on `tb_store_requisition_detail`**. |
| `SR_CALC_006` (header total — display only) | `header_total = Σ (issued_qty × unit_cost)` across active issued lines — computed for display, but **not persisted on `tb_store_requisition`**. |
| `SR_CALC_007` (rounding mode) | Half-up to 3 decimals for quantities (`Decimal(20, 5)` storage). For the display-only monetary fields, half-up to the source location's currency precision (typically 2 dp). The inventory transaction is responsible for storing the rounded monetary values. |

### 3.1 Worked example (1 SR, 3 lines, mixed outcomes)

Outlet *Main Kitchen* (`tb_location.location_type = 'direct'`) raises an SR against *Central Store* (`tb_location.location_type = 'inventory'`); `sr_type = issue`. Three lines.

- **Line 1** (rice): `requested_qty = 25.000`, source on-hand = 100.000.
  - Approver approves in full: `approved_qty = 25.000`.
  - Store keeper issues in full: `issued_qty = 25.000`.
  - Source cost-layer (moving-average): `cost_per_unit = ฿42.50` per kg.
  - Line variance: `requested − issued = 0`; fulfilment gap = 0.
  - Display total: `25.000 × ฿42.50 = ฿1,062.50`.
- **Line 2** (flour): `requested_qty = 15.000`, source on-hand = 12.000.
  - Approver trims to source availability: `approved_qty = 12.000`; `approved_message = "trimmed to source on-hand"`.
  - Store keeper issues what was approved: `issued_qty = 12.000`.
  - Source cost-layer (FIFO): `cost_per_unit = ฿28.00` per kg.
  - Line variance: `requested − issued = 3.000` (short by 3; outlet asked for 15, got 12).
  - Display total: `12.000 × ฿28.00 = ฿336.00`.
- **Line 3** (sugar): `requested_qty = 10.000`, source on-hand at submit = 10.000 but at issue = 6.000 (another SR consumed 4).
  - Approver approves in full: `approved_qty = 10.000`.
  - Store keeper hits at-issue stock-out (`SR_VAL_013`): can only release 6; records `issued_qty = 6.000`; writes a system comment "issued 6 of 10; 4 short due to concurrent consumption".
  - Source cost-layer (moving-average): `cost_per_unit = ฿31.50` per kg.
  - Line variance: `requested − issued = 4.000`; fulfilment gap = 4.000.
  - Display total: `6.000 × ฿31.50 = ฿189.00`.

Header roll-up (display only):
- `Σ line_total = 1,062.50 + 336.00 + 189.00 = ฿1,587.50` — this is the cost moved out of source inventory and either onto Main Kitchen's cost-centre expense (`sr_type = issue`, as here) or into Main Kitchen's inventory account (had this been `sr_type = transfer`).
- Three inventory transactions are written, all stamped `inventory_doc_type = store_requisition`, all `outQty` rows at source. For `sr_type = transfer` (not this case), three paired `inQty` rows would land at the destination too.

## 4. Authorization Rules

Rule IDs follow `SR_AUTH_NNN`. Authorization is enforced by RBAC at the API layer plus workflow-stage gating via `tb_store_requisition.user_action.execute`. Role names mirror the carmen/docs RBAC table (Requester / Approver / Fulfiller / Receiver / Inventory Controller / Finance Manager / System Administrator), but **this pass found the module implements only three stage-facing actors in code: the Requester (`create` stage), and whoever holds the `approve` or `issue` stage** (`enum_stage_role`) — both of the latter act through the identical generic approve action (`useApproveStoreRequisition` and `useIssueStoreRequisition` both call `PATCH .../approve`; the only difference is the `stage_role` tag in the request body). No segregation-of-duties check (Requester ≠ Approver, Approver ≠ Fulfiller) was found anywhere in `store-requisition.service.ts` or `store-requisition.logic.ts` — see `SR_AUTH_011`/`SR_AUTH_012` below.

| Rule ID | Subject | Right | Constraint |
| ------- | ------- | ----- | ---------- |
| `SR_AUTH_001` | Requester (Outlet Manager) | Create SR (`doc_status = draft`) | Requester must be a member of `department_id`; `from_location_id` and `to_location_id` must be locations the requester is permitted to act between. |
| `SR_AUTH_002` | Requester | Edit SR, add / edit / delete lines | Only while `doc_status = draft` and the document has not been submitted. Once at `in_progress`, the document is no longer in the requester's hands. |
| `SR_AUTH_003` | Requester | Submit (`draft → in_progress`) | Passes Section 2 validation through `SR_VAL_001`–`SR_VAL_009`. On submit, the workflow engine routes the document to the first approval stage and populates `user_action.execute` from that stage's permitted users. |
| `SR_AUTH_004` | Requester (document owner) | Delete own draft(s) | Soft-delete (`StoreRequisitionService.delete()`) is restricted to `doc_status = draft` (`Only draft store requisitions can be deleted`) **and, since `215927c0a` (2026-07-29), to the document owner** (`isDocumentOwner()`), with a platform super-admin bypass — otherwise `SR_DELETE_FORBIDDEN` (403). `DELETE /api/{bu}/store-requisitions/batch` (`{ ids: [] }`, `5278bdb46`) validates every id first and deletes nothing if any is `not_found` / `not_draft` / `not_owner`, returning the blocked list (`SR_BATCH_DELETE_BLOCKED`, 400). No `in_progress → cancelled` withdrawal exists — `cancelled` is never assigned (see [01-data-model.md](/en/inventory/store-requisition/01-data-model) §5 item 11). The list's delete buttons are permission-gated client-side (`65751027`). |
| `SR_AUTH_005` | Approver (Department Head) | Approve / trim / reject line, send back for correction | Only while `doc_status = in_progress` and `workflow_current_stage` is a stage where the approver is in `user_action.execute`. May set `approved_qty` (any number is accepted — `SR_VAL_010` is not enforced), mark lines `reject` (bundled into the same `PATCH .../approve` call as other lines' decisions; the message is optional), or send the whole document back (`PATCH .../review`, requires a destination stage `des_stage`, message optional). The "Approver MUST NOT be the same user as the requester" claim (`SR_AUTH_011`) is unconfirmed — see below. |
| `SR_AUTH_006` | Approver | Mixed per-line decisions within one `/approve` call | Confirmed in `sr-form-schema.ts`'s `computeSrAction()`: a submission may mix `approve` and `reject` per line (both route through `/approve`) as long as **no** line is set to `review` — if any line is `review`, the whole submission becomes a send-back (`/review`) and the other lines' selections are not applied in that call. This is narrower than "split & reject at the SR level" — approve/reject can mix, but review cannot mix with either in the same submission. |
| `SR_AUTH_007` | Fulfiller / whoever holds the `issue`-tagged stage | Record `issued_qty`, advance to `completed` | Only while `doc_status = in_progress` and `workflow_current_stage` is a stage tagged `enum_stage_role.issue` where the user is in `user_action.execute`. `issued_qty ≤ approved_qty` per line (`SR_VAL_008`). There is no distinct "commit" endpoint — recording `issued_qty` and finishing the document both go through the same `PATCH .../approve` call as the Approver uses (`stage_role: "issue"`, optional `issue_date_pattern`); the document becomes `completed` when that call's resulting `workflow_next_stage === '-'`, and the header receives `issue_at` / `issue_by_id`. An issue-stage save (`PUT .../store-requisitions/:id` with `stage_role: "issue"`, optional `issue_at`) also stamps the two columns without completing. Lot selection is system-computed FIFO, not a Fulfiller action (see [01-data-model.md](/en/inventory/store-requisition/01-data-model) §5 item 12). The "Fulfiller MUST NOT be the same user as the approver" claim (`SR_AUTH_012`) is unconfirmed — see below. |
| `SR_AUTH_008` | "Receiver" (destination outlet representative) | — | **Unconfirmed as a distinct role.** No receiver-specific route, permission key, comment-escalation flag, or discrepancy mechanism was found in the frontend or backend. The comment tables (`tb_store_requisition_comment` / `tb_store_requisition_detail_comment`) are generic and available to any user with read access to the SR — not a receiver-exclusive write surface. See [03-user-flow-receiver.md](/en/inventory/store-requisition/03-user-flow-receiver) for the full correction. |
| `SR_AUTH_009` | Inventory Controller | Variance review, admin void | **Partially unconfirmed.** Read access across all statuses is plausible as a generic RBAC read grant, but no `inventory-controller`-specific permission key or variance-dashboard route was found. "Void" for this module is the same whole-document `/reject` action any current-stage actor can invoke (see `SR_AUTH_013`), not an Inventory-Controller-exclusive admin path. |
| `SR_AUTH_010` | "Finance Manager" | Cost-centre / journal-entry verification, period close | **Unconfirmed as a distinct role**, and the underlying journal-entry / GL-posting mechanism it would verify was not found in code (`journal`, `ledger` — zero hits repo-wide against this module). `enum_stage_role` has no `finance` member. |
| `SR_AUTH_011` | Segregation of duties — "Requester ≠ Approver" | — | **Unconfirmed.** A repo-wide search of `store-requisition.service.ts` and `store-requisition.logic.ts` for a `requestor_id` cross-check at the approve action found none. Do not present this as an enforced control until a check is found. |
| `SR_AUTH_012` | Segregation of duties — "Approver ≠ Fulfiller" | — | **Unconfirmed**, for the same reason as `SR_AUTH_011` — no cross-check code was found, and there is no separate "commit" call to check against (see `SR_AUTH_007`). The "tenant config may relax this for low-value SRs" claim is also unconfirmed — no `threshold` hits were found anywhere in this module. |
| `SR_AUTH_013` | Whoever holds the current workflow stage | Whole-document reject (`in_progress → voided`) | **Corrected this pass.** `StoreRequisitionService.reject()` sets `doc_status = voided` unconditionally for any whole-document reject call — it is not restricted to Inventory Controller / System Administrator, and it is the same actor set as `SR_AUTH_005`/`SR_AUTH_007` (whoever is in `user_action.execute` for the current stage). A `completed` SR cannot be voided (the `reject` service method requires `doc_status = in_progress`) — corrections after commit go through `[inventory-adjustment](/en/inventory/inventory-adjustment)`. `cancelled` is enum-defined but not reachable by any current SR mutation. |
| `SR_AUTH_014` | Workflow-derived authorization | Stage-gated action | The set of users in `tb_store_requisition.user_action.execute` at the current `workflow_current_stage` is the only set permitted to advance the document; all other action attempts are rejected with `"You are not assigned to act on this requisition at the current stage."`. |

## 5. Posting Rules

Status values are the literal members of the shared `enum_doc_status` documented in [store-requisition/01-data-model](/en/inventory/store-requisition/01-data-model) § 4: **`draft`**, **`in_progress`**, **`completed`**, **`cancelled`**, **`voided`**. The full lifecycle is therefore `draft → in_progress → completed`, with `voided` as the one confirmed terminal cancellation path from `in_progress` (the whole-document reject action — `SR_AUTH_013`). `cancelled` is enum-defined but **not reachable by any current SR service method** — see [01-data-model.md](/en/inventory/store-requisition/01-data-model) §5 item 11. The single posting event is the final workflow-stage advance (the same generic `/approve` call every stage uses); nothing posts at `draft` or at any non-final `in_progress` stage. There is no `submitted`, `approved`, `partially_approved`, `fulfilled`, or `rejected` value at the Prisma level — those are workflow-stage labels under `in_progress`, not header statuses (legacy carmen/docs 6-state model is divergent — see [store-requisition/01-data-model](/en/inventory/store-requisition/01-data-model) § 5 item 1).

Rule IDs follow `SR_POST_NNN`.

| Rule ID | Transition / Event | Effects |
| ------- | ------------------ | ------- |
| `SR_POST_001` | Create (→ `draft`) | Insert `tb_store_requisition` with `doc_status = draft`, `doc_version = 0`, `sr_no` assigned per tenant numbering policy. Append to `workflow_history`: `{ stage: 'draft', action: 'created', by, at }`. No inventory, no GL, no reservation. |
| `SR_POST_002` | Submit (`draft → in_progress`, or resubmit from `in_progress` + `last_action = reviewed`) | Guards: `SR_VAL_005` (department derived if missing), `ValidateSRBeforeSubmitSchema`, `SR_VAL_015` (products enabled at destination), `SR_VAL_014` (date). On a first submit: `sr_date` frozen by `resolveSubmitSrDate()`, `sr_no` minted by `generateSRNo()`; on a resubmit both are kept. Set `doc_status = in_progress`, workflow header columns from `buildSubmitWorkflow()`, `doc_version + 1`, and on **every** line `approved_qty := requested_qty`, `last_action = submitted`, `stages_status` / `history` / `current_stage_status` appended. No reservation of any kind and no inventory write. A submit notification is dispatched (`sendSubmitNotification`). |
| `SR_POST_003` | Approve line (within `in_progress`) | On the per-line approve action: set `approved_qty`, `approved_by_id`, `approved_by_name`, `approved_date_at`, `approved_message`. Append per-line `history`. When all lines on the current stage have been actioned, advance `workflow_current_stage` to the next stage (next approval level, or fulfilment stage if approvals are complete); refresh `user_action.execute`. Header `doc_status` remains `in_progress` throughout. |
| `SR_POST_004` | Reject line (within an `/approve` call) / send back (`/review`) | Per-line reject (bundled into the same `/approve` call as other lines' approve decisions): set `approved_qty = 0`, `reject_by_id`, `reject_by_name`, `reject_date_at`, `reject_message`. Send-back (`/review`, cannot be mixed with approve/reject in the same call — `SR_AUTH_006`): set `review_by_id`, `review_by_name`, `review_date_at`, `review_message`; the workflow returns the SR to an earlier stage (typically the requester) and the document remains `in_progress`. **Corrected this pass:** there is no confirmed "all lines rejected → automatic cancel" path — see `SR_POST_009`. |
| `SR_POST_005` | Final stage advance (`in_progress → completed`) — **the posting event** | The same generic `PATCH .../approve` call every stage uses; the document becomes `completed` when the resulting `workflow_next_stage === '-'` (`StoreRequisitionLogic.approve()` / `isFinalApproval`). When the acting stage is `issue`, `resolveIssueDate()` runs first (`SR_VAL_014`) and the resolved date is stamped as `issue_at` with `issue_by_id = actor`. Set `doc_status = completed`, `doc_version + 1`, append `workflow_history`. Then the cross-module fan-out fires: see `SR_POST_006`. |
| `SR_POST_006` | Final-stage advance — inventory side (cross-ref [inventory](/en/inventory/inventory)) | Confirmed in `executeTransferOnComplete()` (`store-requisition.logic.ts`): for each line with `issued_qty > 0`, insert a `tb_inventory_transaction` row (`inventory_doc_type = store_requisition`, `inventory_doc_no = <sr id>`, `doc_date = issue_at`) via `InventoryTransactionService.executeTransfer()` — the period is resolved from `doc_date` (`resolveDocumentPeriod`) rather than from "now". Note the ordering: the header update in `StoreRequisitionService.approve()` commits *before* this fan-out, so an `Insufficient stock` throw here leaves the SR `completed` with no movement (`SR_VAL_013`). `executeTransfer()` which fans out to `tb_inventory_transaction_detail` child(ren) carrying `lot_no`, `expiry_date`, `cost_per_unit`. Stamp the inserted id on `tb_store_requisition_detail.inventory_transaction_id`. **For `sr_type = issue` into a `direct` destination**: source depletes and the transfer-in is expensed immediately, so destination on-hand stays 0 (a consumption, not a stock-holding move — see `executeTransfer`'s inline comment). **For `sr_type = transfer`**: source on-hand decrements, destination on-hand increments by the same quantity. Cost-layer consumption at source uses FIFO or moving-average per the tenant's calculation method (`getCalculationMethod`). Lot assignment is system-computed FIFO (`getAvailableFifoLots` / `consumeFifoLots`), not a manual selection. |
| `SR_POST_007` | GL / journal posting | **Not wired (re-checked 2026-09-22).** A GL core now exists (`apps/micro-business/src/gl/{gl-posting,gl-jv,gl-period,…}`, migrations `20260909170000_gl_core_master`, `20260914030000_gl_core_jv_ledger`), but `grep -rn "store_requisition\|inventory_doc_type" apps/micro-business/src/gl/` returns zero hits — nothing in the SR commit path creates a journal. Treat GL effects of an SR as design intent only. |
| `SR_POST_008` | Final-stage advance — variance feed | Per-line `requested_qty − issued_qty` and `approved_qty − issued_qty` are computable from the three stored quantity columns. No dedicated variance-dashboard route or scheduled job was found in this pass; treat "feeds outlet variance reporting" as a computable metric, not a confirmed live screen. |
| `SR_POST_009` | ~~Cancel~~ — **not reachable in current source** | Prior versions of this page described `draft/in_progress → cancelled` triggered by requester withdrawal, an "all lines rejected" automatic path, or an authorised approver's "reject SR." None of these were found: `cancelled` is never assigned by any method in `store-requisition.service.ts`. The one confirmed pre-submit withdrawal mechanism is the `draft`-only soft-delete (`SR_POST_011`); the one confirmed "reject the whole SR" mechanism sets `voided`, not `cancelled` (`SR_POST_010`). |
| `SR_POST_010` | Whole-document reject (`in_progress → voided`) | `StoreRequisitionService.reject()`: set `doc_status = voided` unconditionally, `last_action = rejected` on every line in the payload. **Corrected this pass:** this is the same generic reject action available to whoever holds the current workflow stage — it is not restricted to Inventory Controller / System Administrator, and there is no separate "administrative void" endpoint. Requires `doc_status = in_progress` (a `completed` SR cannot be rejected/voided); post-commit corrections use `[inventory-adjustment](/en/inventory/inventory-adjustment)`. No inventory or GL impact (the document never posted). Terminal. |
| `SR_POST_011` | Soft delete (single or batch) | `deleted_at = now()`, `deleted_by_id = user` on the header and its non-deleted lines (duplicate-key lines get staggered timestamps, `9f5ec1bd3`). Restricted to `doc_status = draft` and to the owner / platform super-admin (`SR_AUTH_004`). The batch endpoint is all-or-nothing and reports `{ id, reason, message }` per blocked item. Row remains in the database; the `@@unique([sr_no, deleted_at])` index lets a new SR reuse the same `sr_no`. This is the actual mechanism behind "requester withdraws own draft" — there is no post-submit withdrawal action. |
| `SR_POST_012` | At-issue stock-out short fulfilment | When live on-hand at the final stage is less than `approved_qty`, the actor may reduce `issued_qty` to `min(approved_qty, on_hand)` and record a partial. The SR still moves to `completed` with the partial recorded — the specific per-line system-comment wording and the "cancel the line" alternative described in earlier versions of this page were not directly confirmed against the frontend/backend and should be treated as illustrative, not verified copy. |
| `SR_POST_014` | Read model — Stock Movement tab (`GET /api/{bu}/store-requisitions/:id/stock-movements`, `c5aeca45b`, 2026-08-27) | Not a posting rule but the observable result of `SR_POST_006`: one row per `tb_inventory_transaction_detail` (location, product, `lot_no` from `current_lot_no`/`from_lot_no`, `qty_in` / `qty_out`, `cost_per_unit`, `total_cost`) plus `summary` totals. Until `doc_status = completed` and a transaction exists, `is_posted = false` and the rows are a preview built from the SR lines (`source = store_requisition_detail`, lot `-`, cost `0`). The frontend renders it in `sr-stock-table.tsx` (`useSrStockMovements`). |
| `SR_POST_013` | "Receiver discrepancy flag" | **Unconfirmed as a distinct mechanism.** No receiver-specific comment type, discrepancy flag, or escalation route was found. Any user with read access to a `completed` SR can append a generic `user` comment via the existing comment tables — that is the only confirmed post-commit annotation surface. See [03-user-flow-receiver.md](/en/inventory/store-requisition/03-user-flow-receiver). |

State diagram (confirmed against `store-requisition.service.ts` this pass):

```
[*] → draft → in_progress → completed
  |               |
  (soft-delete,   voided (whole-document reject,
   draft only)     any current-stage actor)
```

`cancelled` is enum-defined but not reachable by any current service method — omitted from the diagram. `completed` and `voided` are terminal. `draft` accepts soft-delete (owner only). A send-back leaves the document `in_progress` with `last_action = reviewed`; `submit()` accepts that state as a resubmit (keeping `sr_no` and `sr_date`).

### 5.1 Status Lifecycle — Live UI vs BRD Mapping

The Prisma enum `enum_doc_status` documented above is what the live UI uses. The BRD (`BR-store-requisitions v1.5.0`) and `SR-User-Experience.md` describe a 6-state lifecycle. The table below maps every observable live-UI status to its BRD equivalent so testers and developers can reconcile the two without ambiguity. Source: `Test_case/System_Process/tx-03-sr.md` (capture date 2026-04-27).

> Diff legend: ✅ match · 🟡 renamed · 🔵 BRD only.

| Live UI status | BRD equivalent | Diff | Notes |
|---|---|---|---|
| `draft` | `Draft` | ✅ match | Initial editable state; requester entering line data. |
| `in_progress` | `Submitted`, `UnderReview`, `Approved`, `PartiallyApproved`, `InProcess`, `Fulfilled` | 🟡 renamed | BRD 6-state model collapses into a single Prisma status; actual sub-stage is tracked via `workflow_current_stage`. Stage flow per `BR-SR-014`: Draft → Submit → Approve → Issue → Complete. |
| `completed` | `Completed` / `Fulfilled` | 🟡 renamed | The single posting event (final workflow-stage advance). Source on-hand decremented; cost-layer consumed; inventory transactions written. |
| `voided` | `Rejected` / `Reject` / `Voided` / `Void` | 🟡 renamed | **Corrected this pass:** the BRD's `Rejected`/`Reject` and `Voided`/`Void` outcomes both map onto Prisma's single `voided` value — `StoreRequisitionService.reject()` sets `voided` directly, and there is no distinct admin-only "Void" action or separate `cancelled` outcome in current code (see [01-data-model.md](/en/inventory/store-requisition/01-data-model) §5 item 11). |
| — | `PartiallyApproved` | 🔵 BRD only | Not a separate Prisma status; represented by mixed per-line approval outcomes while `doc_status = in_progress`. |
| — | `cancelled` (Prisma enum value) | 🔵 enum only | Defined in `enum_doc_status` but not assigned by any current SR service method — dead in current code, not a BRD-vs-live divergence in the usual sense. |

> ⚠️ **Discrepancy — 3-variant nature not reflected in `enum_doc_status`:** The live UI supports three destination variants (INV → INV / INV → DIR / INV → CONS) that produce different inventory effects (destination on-hand increments for INV → INV; stays 0 for the other two — GL effects are unconfirmed, see `SR_POST_007`), but all three share the same `draft → in_progress → completed` status path. The `sr_type` enum (`issue` / `transfer`) distinguishes the two live variants (DIR and CONS are both `sr_type = issue`; INV → INV is `sr_type = transfer`). BRD `BR-store-requisitions v1.5.0` and `Test_case/System_Process/tx-03-sr.md` describe three variants; the live system collapses DIR and CONS under a single `sr_type = issue` value, differentiating them only by the destination `tb_location.location_type`. Source: `Test_case/System_Process/tx-03-sr.md` (capture date 2026-04-27).

> ⚠️ **Discrepancy — Stock Transfer view vs separate TRF transaction code:** BRD `BR-period-end v2.0.0` lists `TRF` as a peer transaction code alongside `SR` in Stage 1 of the period-close validation gate. The implementation (`BR-stock-transfers.md`) confirms that Stock Transfers are **not** a separate entity — they are SRs with INVENTORY destinations (`sr_type = transfer`). For period-close Stage 1 validation, SR records with INV → INV destinations satisfy both the `SR` and `TRF` buckets. The Stock Transfer page is a read-only filtered view of SR records, not a separate document. Source: `Test_case/System_Process/tx-03-sr.md` (capture date 2026-04-27).

> ℹ️ **Note — no per-variant completion mechanism (corrected this pass):** BRD material described `sr_type = transfer` as "Issue = Complete auto-collapse" and `sr_type = issue` as requiring an explicit separate Complete action. Current source shows no such distinction: **both** `sr_type` values complete through the identical `/approve` call when it fires at the final workflow stage (`workflow_next_stage === '-'`, `StoreRequisitionLogic.approve()`); there is no separate "Complete" action for either variant, and no receiver confirmation step exists for any variant. The only behavioral difference between variants is inside `executeTransfer()` (destination on-hand increments for `transfer`; the transfer-in is expensed immediately for `issue` into a `direct` destination).

## 6. Cross-Module Rules

Rule IDs follow `SR_XMOD_NNN`.

| Rule ID | Related module | Rule |
| ------- | -------------- | ---- |
| `SR_XMOD_001` | [inventory](/en/inventory/inventory) | Inventory on-hand is decremented at the source **only at SR commit** (`SR_POST_006`) — not at submit, not at approve. The decrement is via insert into `tb_inventory_transaction` / `tb_inventory_transaction_detail`, reached from the SR side through `tb_store_requisition_detail.inventory_transaction_id`. For `sr_type = transfer`, the destination's on-hand is incremented in the same atomic transaction; for `sr_type = issue`, no destination on-hand increment occurs (the value lands as a consumption expense, not as held stock). |
| `SR_XMOD_002` | [inventory](/en/inventory/inventory) | Lot number, expiry date, and per-lot quantity for issued lines live on `tb_inventory_transaction_detail` (and `tb_inventory_transaction_cost_layer.lot_no`), **not** on the SR line. The SR line reaches lot data through the `inventory_transaction_id` link. **Corrected this pass:** lot selection is system-computed FIFO (`getAvailableFifoLots` / `consumeFifoLots`) at the final workflow-stage advance — no lot-selection UI exists in the SR frontend, so the fulfiller does not manually choose lots (PRD `BR-Lot-01` describes manual selection; not confirmed in current code). |
| `SR_XMOD_003` | [inventory](/en/inventory/inventory) | The `enum_inventory_doc_type` value stamped on the linked inventory transaction is `store_requisition`; downstream inventory queries filtering by document type pick the SR-driven movements via this stamp. The same stamp is what `[inventory-adjustment](/en/inventory/inventory-adjustment)` reads when reversing a post-commit error. |
| `SR_XMOD_004` | [costing](/en/inventory/costing) | Valuation at commit is per the **source location's** costing method (FIFO or moving-average per the tenant's per-location config) — not the destination's. `SR_CALC_004` reads `cost_per_unit` from the source's cost-layer table at the moment of issue. FIFO consumption picks the oldest active layer; moving-average uses the location's current weighted-average cost. The costing module owns the calculation; the SR module is responsible only for triggering the consumption. |
| `SR_XMOD_005` | [costing](/en/inventory/costing) | At the destination, the value-receipt accounting depends on `sr_type`: `transfer` lands in the destination's inventory account at the same `cost_per_unit` (cost moves intact between inventory accounts); `issue` lands as an expense on the destination's cost-centre at the same `cost_per_unit` (cost moves from inventory to expense). The cost-per-unit at the destination after a `transfer` enters the destination's cost layers per its own costing method — typically appended as a new FIFO layer or absorbed into moving-average. |
| `SR_XMOD_006` | [recipe](/en/inventory/recipe) | **Unconfirmed — no code path found (2026-09-22).** `grep -rln "tb_store_requisition\|store_requisition" apps/micro-business/src/` outside the SR, stock-replenishment, workflow and my-pending folders returns nothing recipe-related; no service writes `info.recipe_id`. Treat "recipe auto-generates an SR" as design intent. The only confirmed automatic SR creation is stock replenishment (`SR_XMOD_011`). |
| `SR_XMOD_007` | [good-receive-note](/en/inventory/good-receive-note) | Inter-warehouse transfers may pair an SR-OUT at the source warehouse with a GRN-IN at the destination warehouse — the SR records the issue from source perspective, the GRN records the receipt from destination perspective. Both reference the same `tb_inventory_transaction` family but from opposite sides; reconciliation between the two is a finance / inventory-controller responsibility. Single-step `transfer` SRs (`sr_type = transfer`) record both legs on one document and do not require a paired GRN; the paired-GRN pattern is reserved for cases where the destination needs to acknowledge receipt with its own document (typically when the destination is a different legal entity or a remote facility). |
| `SR_XMOD_008` | Approval workflow | The approval routing — number of stages and the role at each stage — is owned by `tb_workflow` (referenced from the SR header via `workflow_id` with an explicit Prisma `@relation`). The SR module is responsible for consulting the workflow on each transition and populating `user_action.execute` from the current stage; it is not responsible for the routing rules themselves. **Corrected this pass:** value-based stage routing is real, not unconfirmed. `tb_workflow.data.routing_rules` can route on `total_amount` — computed by `sr-workflow.mapper.ts` as Σ (`approved_qty` or `requested_qty`) `× current_average_cost` per line, since SR detail rows carry no price column of their own — or on `department` / `category`, evaluated by `evaluateCondition`/`findNextStep` in the shared `workflows.navagation.service.ts` on every submit/approve. This is the same generic mechanism documented for PR and PO (the **Routing** tab of `/system-admin/workflow`), not SR-specific. The earlier "zero hits" finding searched only the literal word `threshold`; the live vocabulary is `routing_rules` / `total_amount`. A `category`-based rule would silently never match (no mapper emits a document-level `category` field — source comment, `workflows.navagation.service.ts` ~L455-456). **Approval delegation remains unconfirmed:** a repo-wide search for `delegat` against this module and the workflow orchestrator returned zero hits — treat delegation as design intent, not verified behavior. |
| `SR_XMOD_009` | [inventory-adjustment](/en/inventory/inventory-adjustment) | Post-commit corrections — wrong lot picked, miscounted issue, destination short-receipt — flow through inventory-adjustment, not through SR editing. The adjustment carries a back-reference to the originating `tb_store_requisition.id` for audit. The SR itself stays `completed`. |
| `SR_XMOD_011` | [stock-replenishment](/en/inventory/store-requisition/stock-replenishment) | `POST /api/{bu}/stock-replenishment/sr` (`57d5715a0`, 2026-08-26) turns below-par rows into SR drafts through the ordinary `StoreRequisitionLogic.create()` — one document per `(from_location, location_id)` pair, header from the caller's department and the chosen workflow, `requested_qty = request_qty` per product. The drafts then follow `SR_POST_001` onward unchanged. |
| `SR_XMOD_010` | Vendor performance / outlet variance | Variance feedback (`SR_POST_008`) feeds outlet-level food-cost variance and supply-discipline reporting. Chronic over-requesting (large `requested_qty − approved_qty` gaps), chronic at-issue shortfall (`approved_qty − issued_qty`), and seasonal request patterns are surfaced from joins across the three quantity columns. The metric set is owned by the reporting layer; the SR module is responsible only for persisting the columns that feed it. |

## 7. References

- `../carmen/docs/store-requisitions/SR-Technical-Specification.md` — Functional requirements `SR_CRT_001`–`SR_CRT_010` (creation), `SR_APR_001`–`SR_APR_008` (approval), `SR_FUL_001`–`SR_FUL_008` (fulfilment), `SR_RPT_001`–`SR_RPT_005` (reporting). Validation rules feed `SR_VAL_*`; cost calculations feed `SR_CALC_*` (with the caveat that the SR is a quantity document — see Section 3 intro). Note: the Tech Spec's `RequisitionItem.approvalStatus` enum and `Requisition.totalAmount` field are divergent from Prisma; the rules above use the canonical schema.
- `../carmen/docs/store-requisitions/SR-Overview.md` — §Business Rules (Creation Rules, Approval Rules, Movement Rules, Financial Rules) mapped above onto `SR_VAL_*`, `SR_AUTH_*`, `SR_POST_*`, `SR_XMOD_*`. Note: the Overview's 5-state header (`Draft / In Process / Complete / Reject / Void`) collapses into the Prisma 5-state `enum_doc_status` (`draft / in_progress / completed / cancelled / voided`).
- `../carmen/docs/store-requisitions/SR-User-Experience.md` — User journeys (Create / Approve / Process), state diagram (6-state legacy — not canonical), persona descriptions. Approval branch logic and partial-approval / send-back patterns feed `SR_AUTH_005`–`SR_AUTH_006` and `SR_POST_003`–`SR_POST_004`.
- `../carmen/docs/store-requisitions/Store Requisitions.md` — Use cases UC-64 (Approve), UC-65 (Deny), UC-66 (Modify), UC-67 (Monitor), UC-68 (Create and Manage), UC-69 (Approve and Record Stock as Issued) — together they define the approver and store-keeper interaction patterns used in the authorization and posting tables above.
- Sibling: `en/store-requisition/01-data-model.md` — canonical Prisma model, enum values (in particular the shared 5-value `enum_doc_status` and the two-value `enum_sr_type`), and the divergence catalogue that Section 1, Section 3, and Section 6 rely on.
- Backend rule implementation: `../carmen-turborepo-backend-v2/apps/micro-business/src/inventory/store-requisition/` — `store-requisition.service.ts` (`create`, `submit`, `approve`, `reject`, `delete`, `deleteBatch`, `getStockMovements`, `saveStageRoleDetails`, `generateSRNo`), `logic/store-requisition.logic.ts` (`approve`, `executeTransferOnComplete`, `validateBeforeSubmit`), `logic/sr-date.helper.ts`, `logic/sr-department.helper.ts`, `logic/sr-type.helper.ts`, `dto/store-requisition.dto.ts`, `dto/store-requisition-detail.dto.ts`; gateway `apps/backend-gateway/src/application/store-requisitions/store-requisitions.controller.ts` (all workflow verbs are `PATCH`; no `verify` endpoint exists for SR, unlike PR/PO/GRN); error codes `SR_*` in `packages/error-catalog/src/catalog.ts`. No segregation-of-duties guard, no quantity-cap check, and no journal-entry posting exist.
- Frontend: `../carmen-inventory-frontend-react/routes/store-operation/store-requisition/` — `use-sr.ts` (`useSrAction` → `httpClient.patch`), `use-sr-form-actions.ts` (date-pattern retry), `sr-form-schema.ts` (`computeSrAction`, qty bounds), `sr-form-dialogs.tsx` (`showMessage` flags), `sr-item-cost-sync.tsx`, `sr-stock-table.tsx`.
- Bruno: `../carmen-turborepo-backend-bruno/collections/carmen-inventory/inventory/store-requisition/` — `PATCH-{submit,approve,reject,review}-*`, `DELETE-delete-batch-*`, `GET-get-stock-movements-*`.
